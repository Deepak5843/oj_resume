import { o as __toESM, r as __exportAll } from "./chunk-olfrzTEO.js";
import { t as require_react } from "./react.js";
import "./shim-BYs2syrJ.js";
import { t as useControlled } from "./useControlled-Cq9vWdQs.js";
import { a as DROPDOWN_COLLISION_AVOIDANCE, c as ownerVisuallyHidden, d as NOOP, h as useRefWithInit, l as EMPTY_ARRAY, m as warn, n as BASE_UI_SWIPE_IGNORE_SELECTOR, o as LEGACY_SWIPE_IGNORE_SELECTOR, p as useMergedRefs, s as POPUP_COLLISION_AVOIDANCE, t as useRenderElement, u as EMPTY_OBJECT } from "./useRenderElement-D2X3HgaD.js";
import { n as useStableCallback, t as useIsoLayoutEffect } from "./useIsoLayoutEffect-DJOn7DQY.js";
import { n as useCompositeListItem, t as IndexGuessBehavior } from "./useCompositeListItem-i0UIjAeB.js";
import { t as require_jsx_runtime } from "./react_jsx-runtime.js";
import { t as CompositeList } from "./CompositeList-D0B6i0da.js";
import { n as useDirection, t as DirectionContext } from "./DirectionContext-CHpXJmUx.js";
import { a as CollapsibleRootContext, i as triggerOpenStateMapping, n as useCollapsiblePanel, o as useCollapsibleRootContext, r as collapsibleOpenStateMapping, s as useCollapsibleRoot, t as index_parts_exports } from "./accordion-BEGgvBe0.js";
import { i as mergePropsN, n as mergeClassNames, r as mergeProps, t as makeEventPreventable } from "./merge-props-AhmUGPk1.js";
import { C as listNavigation, D as scrub, M as triggerHover, N as triggerPress, P as windowResize, S as linkPress, T as outsidePress, _ as inputClear, a as clearPress, b as itemPress, c as decrementPress, d as escapeKey, f as focusOut, g as inputChange, h as inputBlur, i as chipRemovePress, k as swipe, m as incrementPress, n as createGenericEventDetails, o as closePress, r as cancelOpen, s as closeWatcher, t as createChangeEventDetails, v as inputPaste, w as none, x as keyboard, y as inputPress } from "./createBaseUIEventDetails-Ce1GvOf7.js";
import { b as SafeReact, d as isElement, f as isHTMLElement, l as getWindow, t as getComputedStyle$1 } from "./floating-ui.utils.dom-BaWpVxHO.js";
import { n as useId, t as useBaseUiId } from "./useBaseUiId-CIYtyRS4.js";
import { t as require_react_dom } from "./react-dom.js";
import { t as useOnMount } from "./useOnMount-DhYjSa9m.js";
import { n as useAnimationFrame } from "./useAnimationFrame-BS1scPxn.js";
import { a as transitionStatusMapping, i as TransitionStatusDataAttributes, n as useTransitionStatus, r as useAnimationsFinished, t as useOpenChangeComplete } from "./useOpenChangeComplete-3zMWC2uZ.js";
import { a as error, n as useFocusableWhenDisabled, t as useButton } from "./useButton-pHivfkP1.js";
import { n as isFirefox, r as isIOS, s as isWebKit, t as isAndroid } from "./detectBrowser-CIPzhxg1.js";
import { i as getTarget, n as contains, r as getFloatingFocusElement, t as activeElement, u as matchesFocusVisible } from "./element-DHmJ0Fe8.js";
import { B as getTabbableAfterElement, C as createSelectorMemoized, D as useFloatingNodeId, E as FloatingTree, F as FocusGuard, G as isTabbable, I as disableFocusInside, J as getNodeChildren, L as enableFocusInside, O as useFloatingParentNodeId, R as getNextTabbable, S as createSelector, T as FloatingNode, U as isOutsideEvent, _ as ReactStore, a as triggerOpenStateMapping$1, h as useTriggerRegistration, i as pressableTriggerOpenStateMapping, j as FloatingPortal, k as useFloatingTree, o as useInteractions, r as popupStateMapping, t as CommonPopupDataAttributes, u as getEmptyRootContext, v as Store, w as useDismiss, y as useStore, z as getPreviousTabbable } from "./popupStateMapping-ZthMM50s.js";
import { o as stopEvent } from "./event-CEx8tjzh.js";
import { t as inertValue, y as rectToClientRect } from "./inertValue-BPx0tsRN.js";
import { t as ownerDocument } from "./owner-Bk5VKRYX.js";
import { f as SHIFT, s as COMPOSITE_KEYS } from "./composite-DakarhXC.js";
import { n as useTimeout, t as Timeout } from "./useTimeout-BgYonD-d.js";
import { a as FloatingFocusManager, i as useClick, n as useOpenInteractionType, s as useScrollLock, t as InternalBackdrop } from "./InternalBackdrop-DHo0L8zl.js";
import { a as safePolygon, c as applySafePolygonPointerEventsMutation, d as useFloatingRootContext, f as platform, i as adaptiveOrigin, l as clearSafePolygonPointerEventsMutation, n as getDisabledMountTransitionStyles, o as useHoverReferenceInteraction, r as useAnchorPositioning, s as useHoverFloatingInteraction, u as useHoverInteractionSharedState } from "./usePopupViewport-B1m2wQqn.js";
import { t as index_parts_exports$27 } from "./tooltip-o2C53BeD.js";
import { t as useValueAsRef } from "./useValueAsRef-KGpJcnei.js";
import { n as visuallyHiddenInput, t as visuallyHidden } from "./visuallyHidden-B-CKKDaj.js";
import "./with-selector-C_SF9k7z.js";
import { i as useForcedRerendering, n as CompositeRoot, r as ACTIVE_COMPOSITE_ITEM, t as index_parts_exports$24 } from "./tabs-ZmZhRTEd.js";
import { D as useTypeahead, E as getPseudoElementBounds, O as useListNavigation, o as MenubarContext, s as useMenubarContext, t as index_parts_exports$12 } from "./menu-CuCJBGn5.js";
import { t as useValueChanged } from "./useValueChanged-BzojcSgH.js";
import { a as DialogTitle, c as useDialogPortalContext, i as DialogTrigger, l as DialogDescription, m as useDialogRootContext, n as createDialogHandle, o as DialogPortal, r as DialogViewport, t as DialogHandle, u as DialogClose } from "./DialogHandle-trFSxxG-.js";
import { t as index_parts_exports$1 } from "./alert-dialog-B5eaVjAC.js";
import { t as useOnFirstRender } from "./useOnFirstRender-oqcv_dpi.js";
import { a as FormContext, c as FieldRootContext, d as DEFAULT_VALIDITY_STATE, f as fieldValidityMapping, i as useField, l as useFieldRootContext, n as LabelableContext, o as useFormContext, r as useLabelableContext, s as getCombinedFieldValidityData, t as useLabelableId, u as DEFAULT_FIELD_STATE_ATTRIBUTES } from "./useLabelableId-dYA4XBSk.js";
import { a as formatNumberValue, c as useRegisteredLabelId, d as resolveAriaLabelledBy, i as formatNumberMaxPrecision, l as areArraysEqual, n as valueToPercent, o as getFormatter, r as formatNumber, s as useLabel, t as index_parts_exports$22, u as getDefaultLabelId } from "./slider-9bomX-jj.js";
import { t as Separator } from "./Separator-ChlLz53r.js";
import { t as index_parts_exports$3 } from "./avatar-DWahBSST.js";
import { t as Button } from "./button-DUiXXE3X.js";
import { n as useAriaLabelledBy, t as index_parts_exports$23 } from "./switch-BVKD4my5.js";
import "./@base-ui_react_separator.js";
import { n as useToolbarRootContext, t as ToolbarRootContext } from "./ToolbarRootContext-BxHSA3s9.js";
import { t as CompositeItem } from "./CompositeItem-CRpJlQuM.js";
import { t as getCssDimensions } from "./getCssDimensions-C-i0J0Gi.js";
import { t as index_parts_exports$7 } from "./context-menu-CQxRzp3r.js";
import { n as useCSPContext, t as CSPContext } from "./CSPContext-_4jpwTEf.js";
import { n as DialogRoot, t as index_parts_exports$8 } from "./dialog-D12Vfozs.js";
import { t as clamp } from "./clamp-D2d4A5vr.js";
import { n as FieldControl, t as Input } from "./input-CV945RHu.js";
import { t as index_parts_exports$16 } from "./popover-acbPW4xq.js";
import { t as index_parts_exports$17 } from "./preview-card-CqDUdtHF.js";
import { t as FloatingPortalLite } from "./FloatingPortalLite-D6W4E_7-.js";
import { n as styleDisableScrollbar, t as index_parts_exports$20 } from "./scroll-area-c2Zxtq5M.js";
import { n as ToggleGroupContext, t as Toggle } from "./toggle-17lBy-OB.js";
import { useRender } from "./@base-ui_react_use-render.js";
//#region node_modules/@base-ui/react/esm/combobox/root/ComboboxRootContext.js
var import_react = /* @__PURE__ */ __toESM(require_react(), 1);
var ComboboxRootContext = /* @__PURE__ */ import_react.createContext(void 0);
ComboboxRootContext.displayName = "ComboboxRootContext";
var ComboboxFloatingContext = /* @__PURE__ */ import_react.createContext(void 0);
ComboboxFloatingContext.displayName = "ComboboxFloatingContext";
var ComboboxDerivedItemsContext = /* @__PURE__ */ import_react.createContext(void 0);
ComboboxDerivedItemsContext.displayName = "ComboboxDerivedItemsContext";
var ComboboxInputValueContext = /* @__PURE__ */ import_react.createContext("");
ComboboxInputValueContext.displayName = "ComboboxInputValueContext";
function useComboboxRootContext() {
	const context = import_react.useContext(ComboboxRootContext);
	if (!context) throw new Error("Base UI: ComboboxRootContext is missing. Combobox parts must be placed within <Combobox.Root>.");
	return context;
}
function useComboboxFloatingContext() {
	const context = import_react.useContext(ComboboxFloatingContext);
	if (!context) throw new Error("Base UI: ComboboxFloatingContext is missing. Combobox parts must be placed within <Combobox.Root>.");
	return context;
}
function useComboboxDerivedItemsContext() {
	const context = import_react.useContext(ComboboxDerivedItemsContext);
	if (!context) throw new Error("Base UI: ComboboxItemsContext is missing. Combobox parts must be placed within <Combobox.Root>.");
	return context;
}
function useComboboxInputValueContext() {
	return import_react.useContext(ComboboxInputValueContext);
}
//#endregion
//#region node_modules/@base-ui/react/esm/utils/itemEquality.js
var defaultItemEquality = (itemValue, selectedValue) => Object.is(itemValue, selectedValue);
function compareItemEquality(itemValue, selectedValue, comparer) {
	if (itemValue == null || selectedValue == null) return Object.is(itemValue, selectedValue);
	return comparer(itemValue, selectedValue);
}
function selectedValueIncludes(selectedValues, itemValue, comparer) {
	if (!selectedValues || selectedValues.length === 0) return false;
	return selectedValues.some((selectedValue) => {
		if (selectedValue === void 0) return false;
		return compareItemEquality(itemValue, selectedValue, comparer);
	});
}
function findItemIndex(itemValues, selectedValue, comparer) {
	if (!itemValues || itemValues.length === 0) return -1;
	return itemValues.findIndex((itemValue) => {
		if (itemValue === void 0) return false;
		return compareItemEquality(itemValue, selectedValue, comparer);
	});
}
function removeItem(selectedValues, itemValue, comparer) {
	return selectedValues.filter((selectedValue) => !compareItemEquality(itemValue, selectedValue, comparer));
}
//#endregion
//#region node_modules/@base-ui/react/esm/utils/serializeValue.js
function serializeValue(value) {
	if (value == null) return "";
	if (typeof value === "string") return value;
	try {
		return JSON.stringify(value);
	} catch {
		return String(value);
	}
}
//#endregion
//#region node_modules/@base-ui/react/esm/utils/resolveValueLabel.js
var import_jsx_runtime = require_jsx_runtime();
function isGroupedItems(items) {
	return items != null && items.length > 0 && typeof items[0] === "object" && items[0] != null && "items" in items[0];
}
/**
* Checks if the items array contains an item with a null value that has a non-null label.
*/
function hasNullItemLabel(items) {
	if (!Array.isArray(items)) return items != null && "null" in items;
	const arrayItems = items;
	if (isGroupedItems(arrayItems)) {
		for (const group of arrayItems) for (const item of group.items) if (item && item.value == null && item.label != null) return true;
		return false;
	}
	for (const item of arrayItems) if (item && item.value == null && item.label != null) return true;
	return false;
}
function stringifyAsLabel(item, itemToStringLabel) {
	if (itemToStringLabel && item != null) return itemToStringLabel(item) ?? "";
	if (item && typeof item === "object") {
		if ("label" in item && item.label != null) return String(item.label);
		if ("value" in item) return String(item.value);
	}
	return serializeValue(item);
}
function stringifyAsValue(item, itemToStringValue) {
	if (itemToStringValue && item != null) return itemToStringValue(item) ?? "";
	if (item && typeof item === "object" && "value" in item && "label" in item) return serializeValue(item.value);
	return serializeValue(item);
}
function resolveSelectedLabel(value, items, itemToStringLabel) {
	function fallback() {
		return stringifyAsLabel(value, itemToStringLabel);
	}
	if (itemToStringLabel && value != null) return itemToStringLabel(value);
	if (value && typeof value === "object" && "label" in value && value.label != null) return value.label;
	if (items && !Array.isArray(items)) return items[value] ?? fallback();
	if (Array.isArray(items)) {
		const arrayItems = items;
		const flatItems = isGroupedItems(arrayItems) ? arrayItems.flatMap((group) => group.items) : arrayItems;
		if (value == null || typeof value !== "object") {
			const match = flatItems.find((item) => item.value === value);
			if (match && match.label != null) return match.label;
			return fallback();
		}
		if ("value" in value) {
			const match = flatItems.find((item) => item && item.value === value.value);
			if (match && match.label != null) return match.label;
		}
	}
	return fallback();
}
function resolveMultipleLabels(values, items, itemToStringLabel) {
	return values.reduce((acc, value, index) => {
		if (index > 0) acc.push(", ");
		acc.push(/* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_react.Fragment, { children: resolveSelectedLabel(value, items, itemToStringLabel) }, index));
		return acc;
	}, []);
}
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/store.js
var selectors$2 = {
	id: createSelector((state) => state.id),
	labelId: createSelector((state) => state.labelId),
	query: createSelector((state) => state.query),
	items: createSelector((state) => state.items),
	selectedValue: createSelector((state) => state.selectedValue),
	hasSelectionChips: createSelector((state) => {
		const selectedValue = state.selectedValue;
		return Array.isArray(selectedValue) && selectedValue.length > 0;
	}),
	hasSelectedValue: createSelector((state) => {
		const { selectedValue, selectionMode } = state;
		if (selectedValue == null) return false;
		if (selectionMode === "multiple" && Array.isArray(selectedValue)) return selectedValue.length > 0;
		return true;
	}),
	hasNullItemLabel: createSelector((state, enabled) => {
		return enabled ? hasNullItemLabel(state.items) : false;
	}),
	open: createSelector((state) => state.open),
	mounted: createSelector((state) => state.mounted),
	forceMounted: createSelector((state) => state.forceMounted),
	inline: createSelector((state) => state.inline),
	activeIndex: createSelector((state) => state.activeIndex),
	selectedIndex: createSelector((state) => state.selectedIndex),
	isActive: createSelector((state, index) => state.activeIndex === index),
	isSelected: createSelector((state, itemValue) => {
		const comparer = state.isItemEqualToValue;
		const selectedValue = state.selectedValue;
		if (Array.isArray(selectedValue)) return selectedValue.some((selectedItem) => compareItemEquality(itemValue, selectedItem, comparer));
		return compareItemEquality(itemValue, selectedValue, comparer);
	}),
	transitionStatus: createSelector((state) => state.transitionStatus),
	popupProps: createSelector((state) => state.popupProps),
	inputProps: createSelector((state) => state.inputProps),
	triggerProps: createSelector((state) => state.triggerProps),
	getItemProps: createSelector((state) => state.getItemProps),
	positionerElement: createSelector((state) => state.positionerElement),
	listElement: createSelector((state) => state.listElement),
	triggerElement: createSelector((state) => state.triggerElement),
	inputElement: createSelector((state) => state.inputElement),
	inputGroupElement: createSelector((state) => state.inputGroupElement),
	popupSide: createSelector((state) => state.popupSide),
	openMethod: createSelector((state) => state.openMethod),
	inputInsidePopup: createSelector((state) => state.inputInsidePopup),
	selectionMode: createSelector((state) => state.selectionMode),
	listRef: createSelector((state) => state.listRef),
	labelsRef: createSelector((state) => state.labelsRef),
	popupRef: createSelector((state) => state.popupRef),
	emptyRef: createSelector((state) => state.emptyRef),
	inputRef: createSelector((state) => state.inputRef),
	keyboardActiveRef: createSelector((state) => state.keyboardActiveRef),
	chipsContainerRef: createSelector((state) => state.chipsContainerRef),
	clearRef: createSelector((state) => state.clearRef),
	valuesRef: createSelector((state) => state.valuesRef),
	allValuesRef: createSelector((state) => state.allValuesRef),
	name: createSelector((state) => state.name),
	disabled: createSelector((state) => state.disabled),
	readOnly: createSelector((state) => state.readOnly),
	required: createSelector((state) => state.required),
	grid: createSelector((state) => state.grid),
	isGrouped: createSelector((state) => state.isGrouped),
	virtualized: createSelector((state) => state.virtualized),
	onOpenChangeComplete: createSelector((state) => state.onOpenChangeComplete),
	openOnInputClick: createSelector((state) => state.openOnInputClick),
	itemToStringLabel: createSelector((state) => state.itemToStringLabel),
	isItemEqualToValue: createSelector((state) => state.isItemEqualToValue),
	modal: createSelector((state) => state.modal),
	autoHighlight: createSelector((state) => state.autoHighlight),
	submitOnItemClick: createSelector((state) => state.submitOnItemClick)
};
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/root/utils/index.js
/**
* Enhanced filter using Intl.Collator for more robust string matching.
* Uses the provided `itemToStringLabel` function if available, otherwise falls back to:
* • When `item` is an object with a `value` property, that property is used.
* • When `item` is a primitive (e.g. `string`), it is used directly.
*/
function createCollatorItemFilter(collatorFilter, itemToStringLabel) {
	return (item, query) => {
		if (item == null) return false;
		const itemString = stringifyAsLabel(item, itemToStringLabel);
		return collatorFilter.contains(itemString, query);
	};
}
/**
* Enhanced filter for single selection mode using Intl.Collator that shows all items
* when query is empty or matches the current selection, making it easier to browse options.
*/
function createSingleSelectionCollatorFilter(collatorFilter, itemToStringLabel, selectedValue) {
	return (item, query) => {
		if (item == null) return false;
		if (!query) return true;
		const itemString = stringifyAsLabel(item, itemToStringLabel);
		const selectedString = selectedValue != null ? stringifyAsLabel(selectedValue, itemToStringLabel) : "";
		if (selectedString && collatorFilter.contains(selectedString, query) && selectedString.length === query.length) return true;
		return collatorFilter.contains(itemString, query);
	};
}
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/root/utils/useFilter.js
var filterCache = /* @__PURE__ */ new Map();
function stringifyLocale(locale) {
	if (Array.isArray(locale)) return locale.map((value) => stringifyLocale(value)).join(",");
	if (locale == null) return "";
	return String(locale);
}
function getFilter(options = {}) {
	const mergedOptions = {
		usage: "search",
		sensitivity: "base",
		ignorePunctuation: true,
		...options
	};
	const cacheKey = `${stringifyLocale(options.locale)}|${JSON.stringify(mergedOptions)}`;
	const cachedFilter = filterCache.get(cacheKey);
	if (cachedFilter) return cachedFilter;
	const collator = new Intl.Collator(options.locale, mergedOptions);
	const filter = {
		contains(item, query, itemToString) {
			if (!query) return true;
			const itemString = stringifyAsLabel(item, itemToString);
			for (let i = 0; i <= itemString.length - query.length; i += 1) if (collator.compare(itemString.slice(i, i + query.length), query) === 0) return true;
			return false;
		},
		startsWith(item, query, itemToString) {
			if (!query) return true;
			const itemString = stringifyAsLabel(item, itemToString);
			return collator.compare(itemString.slice(0, query.length), query) === 0;
		},
		endsWith(item, query, itemToString) {
			if (!query) return true;
			const itemString = stringifyAsLabel(item, itemToString);
			const queryLength = query.length;
			return itemString.length >= queryLength && collator.compare(itemString.slice(itemString.length - queryLength), query) === 0;
		}
	};
	filterCache.set(cacheKey, filter);
	return filter;
}
/**
* Matches items against a query using `Intl.Collator` for robust string matching.
*/
var useCoreFilter = getFilter;
/**
* Matches items against a query using `Intl.Collator` for robust string matching.
*/
function useComboboxFilter(options = {}) {
	const { multiple = false, value, ...collatorOptions } = options;
	const coreFilter = getFilter(collatorOptions);
	const contains = import_react.useCallback((item, query, itemToString) => {
		if (multiple) return createCollatorItemFilter(coreFilter, itemToString)(item, query);
		return createSingleSelectionCollatorFilter(coreFilter, itemToString, value)(item, query);
	}, [
		coreFilter,
		value,
		multiple
	]);
	return import_react.useMemo(() => ({
		contains,
		startsWith: coreFilter.startsWith,
		endsWith: coreFilter.endsWith
	}), [contains, coreFilter]);
}
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/root/utils/constants.js
var NO_ACTIVE_VALUE = Symbol("none");
var INITIAL_LAST_HIGHLIGHT = {
	value: NO_ACTIVE_VALUE,
	index: -1
};
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/root/AriaCombobox.js
/**
* @internal
*/
function AriaCombobox(props) {
	const { id: idProp, onOpenChangeComplete: onOpenChangeCompleteProp, defaultSelectedValue = null, selectedValue: selectedValueProp, onSelectedValueChange, defaultInputValue: defaultInputValueProp, inputValue: inputValueProp, selectionMode = "none", onItemHighlighted: onItemHighlightedProp, name: nameProp, disabled: disabledProp = false, readOnly = false, required = false, inputRef: inputRefProp, grid = false, items, filteredItems: filteredItemsProp, filter: filterProp, openOnInputClick = true, autoHighlight = false, keepHighlight = false, highlightItemOnHover = true, loopFocus = true, itemToStringLabel, itemToStringValue, isItemEqualToValue = defaultItemEquality, virtualized = false, inline: inlineProp = false, fillInputOnItemPress = true, modal = false, limit = -1, autoComplete = "list", formAutoComplete, locale, submitOnItemClick = false } = props;
	const { clearErrors } = useFormContext();
	const { setDirty, validityData, shouldValidateOnChange, setFilled, name: fieldName, disabled: fieldDisabled, setTouched, setFocused, validationMode, validation } = useFieldRootContext();
	const id = useLabelableId({ id: idProp });
	const collatorFilter = useCoreFilter({ locale });
	const [queryChangedAfterOpen, setQueryChangedAfterOpen] = import_react.useState(false);
	const [closeQuery, setCloseQuery] = import_react.useState(null);
	const listRef = import_react.useRef([]);
	const labelsRef = import_react.useRef([]);
	const popupRef = import_react.useRef(null);
	const inputRef = import_react.useRef(null);
	const startDismissRef = import_react.useRef(null);
	const endDismissRef = import_react.useRef(null);
	const emptyRef = import_react.useRef(null);
	const keyboardActiveRef = import_react.useRef(true);
	const hadInputClearRef = import_react.useRef(false);
	const chipsContainerRef = import_react.useRef(null);
	const clearRef = import_react.useRef(null);
	const selectionEventRef = import_react.useRef(null);
	const lastHighlightRef = import_react.useRef(INITIAL_LAST_HIGHLIGHT);
	const pendingQueryHighlightRef = import_react.useRef(null);
	/**
	* Contains the currently visible list of item values post-filtering.
	*/
	const valuesRef = import_react.useRef([]);
	/**
	* Contains all item values in a stable, unfiltered order.
	* This is only used when `items` prop is not provided.
	* It accumulates values on first mount and does not remove them on unmount due to
	* filtering, providing a stable index for selected value tracking.
	*/
	const allValuesRef = import_react.useRef([]);
	const disabled = fieldDisabled || disabledProp;
	const name = fieldName ?? nameProp;
	const multiple = selectionMode === "multiple";
	const single = selectionMode === "single";
	const hasInputValue = inputValueProp !== void 0 || defaultInputValueProp !== void 0;
	const hasItems = items !== void 0;
	const hasFilteredItemsProp = filteredItemsProp !== void 0;
	let autoHighlightMode;
	if (autoHighlight === "always") autoHighlightMode = "always";
	else autoHighlightMode = autoHighlight ? "input-change" : false;
	const [selectedValue, setSelectedValueUnwrapped] = useControlled({
		controlled: selectedValueProp,
		default: multiple ? defaultSelectedValue ?? EMPTY_ARRAY : defaultSelectedValue,
		name: "Combobox",
		state: "selectedValue"
	});
	const filter = import_react.useMemo(() => {
		if (filterProp === null) return () => true;
		if (filterProp !== void 0) return filterProp;
		if (single && !queryChangedAfterOpen) return createSingleSelectionCollatorFilter(collatorFilter, itemToStringLabel, selectedValue);
		return createCollatorItemFilter(collatorFilter, itemToStringLabel);
	}, [
		filterProp,
		single,
		selectedValue,
		queryChangedAfterOpen,
		collatorFilter,
		itemToStringLabel
	]);
	const initialDefaultInputValue = useRefWithInit(() => {
		if (hasInputValue) return defaultInputValueProp ?? "";
		if (single) return stringifyAsLabel(selectedValue, itemToStringLabel);
		return "";
	}).current;
	const [inputValue, setInputValueUnwrapped] = useControlled({
		controlled: inputValueProp,
		default: initialDefaultInputValue,
		name: "Combobox",
		state: "inputValue"
	});
	const [open, setOpenUnwrapped] = useControlled({
		controlled: props.open,
		default: props.defaultOpen,
		name: "Combobox",
		state: "open"
	});
	const isGrouped = isGroupedItems(items);
	const query = closeQuery ?? (inputValue === "" ? "" : String(inputValue).trim());
	const selectedLabelString = single ? stringifyAsLabel(selectedValue, itemToStringLabel) : "";
	const shouldBypassFiltering = single && !queryChangedAfterOpen && query !== "" && selectedLabelString !== "" && selectedLabelString.length === query.length && collatorFilter.contains(selectedLabelString, query);
	const filterQuery = shouldBypassFiltering ? "" : query;
	const shouldIgnoreExternalFiltering = hasItems && hasFilteredItemsProp && shouldBypassFiltering;
	const flatItems = import_react.useMemo(() => {
		if (!items) return EMPTY_ARRAY;
		if (isGrouped) return items.flatMap((group) => group.items);
		return items;
	}, [items, isGrouped]);
	const filteredItems = import_react.useMemo(() => {
		if (filteredItemsProp && !shouldIgnoreExternalFiltering) return filteredItemsProp;
		if (!items) return EMPTY_ARRAY;
		if (isGrouped) {
			const groupedItems = items;
			const resultingGroups = [];
			let currentCount = 0;
			for (const group of groupedItems) {
				if (limit > -1 && currentCount >= limit) break;
				const candidateItems = filterQuery === "" ? group.items : group.items.filter((item) => filter(item, filterQuery, itemToStringLabel));
				if (candidateItems.length === 0) continue;
				const remainingLimit = limit > -1 ? limit - currentCount : Infinity;
				const itemsToTake = candidateItems.slice(0, remainingLimit);
				if (itemsToTake.length > 0) {
					const newGroup = {
						...group,
						items: itemsToTake
					};
					resultingGroups.push(newGroup);
					currentCount += itemsToTake.length;
				}
			}
			return resultingGroups;
		}
		if (filterQuery === "") return limit > -1 ? flatItems.slice(0, limit) : flatItems;
		const limitedItems = [];
		for (const item of flatItems) {
			if (limit > -1 && limitedItems.length >= limit) break;
			if (filter(item, filterQuery, itemToStringLabel)) limitedItems.push(item);
		}
		return limitedItems;
	}, [
		filteredItemsProp,
		shouldIgnoreExternalFiltering,
		items,
		isGrouped,
		filterQuery,
		limit,
		filter,
		itemToStringLabel,
		flatItems
	]);
	const flatFilteredItems = import_react.useMemo(() => {
		if (isGrouped) return filteredItems.flatMap((g) => g.items);
		return filteredItems;
	}, [filteredItems, isGrouped]);
	const store = useRefWithInit(() => new Store({
		id,
		labelId: void 0,
		selectedValue,
		open,
		filter,
		query,
		items,
		selectionMode,
		listRef,
		labelsRef,
		popupRef,
		emptyRef,
		inputRef,
		startDismissRef,
		endDismissRef,
		keyboardActiveRef,
		chipsContainerRef,
		clearRef,
		valuesRef,
		allValuesRef,
		selectionEventRef,
		name,
		disabled,
		readOnly,
		required,
		grid,
		isGrouped,
		virtualized,
		openOnInputClick,
		itemToStringLabel,
		isItemEqualToValue,
		modal,
		autoHighlight: autoHighlightMode,
		submitOnItemClick,
		hasInputValue,
		mounted: false,
		forceMounted: false,
		transitionStatus: "idle",
		inline: inlineProp,
		activeIndex: null,
		selectedIndex: null,
		popupProps: {},
		inputProps: {},
		triggerProps: {},
		positionerElement: null,
		listElement: null,
		triggerElement: null,
		inputElement: null,
		inputGroupElement: null,
		popupSide: null,
		openMethod: null,
		inputInsidePopup: true,
		onOpenChangeComplete: onOpenChangeCompleteProp || NOOP,
		setOpen: NOOP,
		setInputValue: NOOP,
		setSelectedValue: NOOP,
		setIndices: NOOP,
		onItemHighlighted: NOOP,
		handleSelection: NOOP,
		getItemProps: () => EMPTY_OBJECT,
		forceMount: NOOP,
		requestSubmit: NOOP
	})).current;
	const fieldRawValue = selectionMode === "none" ? inputValue : selectedValue;
	const fieldStringValue = import_react.useMemo(() => {
		if (selectionMode === "none") return fieldRawValue;
		if (Array.isArray(selectedValue)) return selectedValue.map((value) => stringifyAsValue(value, itemToStringValue));
		return stringifyAsValue(selectedValue, itemToStringValue);
	}, [
		fieldRawValue,
		itemToStringValue,
		selectionMode,
		selectedValue
	]);
	const onItemHighlighted = useStableCallback(onItemHighlightedProp);
	const onOpenChangeComplete = useStableCallback(onOpenChangeCompleteProp);
	const activeIndex = useStore(store, selectors$2.activeIndex);
	const selectedIndex = useStore(store, selectors$2.selectedIndex);
	const positionerElement = useStore(store, selectors$2.positionerElement);
	const listElement = useStore(store, selectors$2.listElement);
	const triggerElement = useStore(store, selectors$2.triggerElement);
	const inputElement = useStore(store, selectors$2.inputElement);
	const inputGroupElement = useStore(store, selectors$2.inputGroupElement);
	const inline = useStore(store, selectors$2.inline);
	const inputInsidePopup = useStore(store, selectors$2.inputInsidePopup);
	const triggerRef = useValueAsRef(triggerElement);
	const { mounted, setMounted, transitionStatus } = useTransitionStatus(open);
	const { openMethod, triggerProps } = useOpenInteractionType(open);
	useField({
		id,
		name,
		commit: validation.commit,
		value: fieldRawValue,
		controlRef: inputInsidePopup ? triggerRef : inputRef,
		getValue: () => fieldStringValue
	});
	const forceMount = useStableCallback(() => {
		if (items) labelsRef.current = flatFilteredItems.map((item) => stringifyAsLabel(item, itemToStringLabel));
		else store.set("forceMounted", true);
	});
	const initialSelectedValueRef = import_react.useRef(selectedValue);
	useIsoLayoutEffect(() => {
		if (selectedValue !== initialSelectedValueRef.current) forceMount();
	}, [forceMount, selectedValue]);
	const setIndices = useStableCallback((options) => {
		store.update(options);
		const type = options.type || "none";
		if (options.activeIndex === void 0) return;
		if (options.activeIndex === null) {
			if (lastHighlightRef.current !== INITIAL_LAST_HIGHLIGHT) {
				lastHighlightRef.current = INITIAL_LAST_HIGHLIGHT;
				onItemHighlighted(void 0, createGenericEventDetails(type, void 0, { index: -1 }));
			}
		} else {
			const activeValue = valuesRef.current[options.activeIndex];
			lastHighlightRef.current = {
				value: activeValue,
				index: options.activeIndex
			};
			onItemHighlighted(activeValue, createGenericEventDetails(type, void 0, { index: options.activeIndex }));
		}
	});
	const setInputValue = useStableCallback((next, eventDetails) => {
		hadInputClearRef.current = eventDetails.reason === inputClear;
		props.onInputValueChange?.(next, eventDetails);
		if (eventDetails.isCanceled) return;
		if (eventDetails.reason === "input-change") {
			const event = eventDetails.event;
			const inputType = event.inputType;
			if (event.type === "compositionend" || inputType != null && inputType !== "" && inputType !== "insertReplacementText") {
				const hasQuery = next.trim() !== "";
				if (hasQuery) setQueryChangedAfterOpen(true);
				pendingQueryHighlightRef.current = { hasQuery };
				if (hasQuery && autoHighlightMode && store.state.activeIndex == null) store.set("activeIndex", 0);
			}
		}
		setInputValueUnwrapped(next);
	});
	const setOpen = useStableCallback((nextOpen, eventDetails) => {
		if (open === nextOpen) return;
		if (eventDetails.reason === "escape-key" && hasItems && flatFilteredItems.length === 0 && !store.state.emptyRef.current) eventDetails.allowPropagation();
		props.onOpenChange?.(nextOpen, eventDetails);
		if (eventDetails.isCanceled) return;
		if (!nextOpen && queryChangedAfterOpen) {
			if (single) {
				if (!inline) setCloseQuery(query);
				if (query === "") setQueryChangedAfterOpen(false);
			} else if (multiple) {
				if (inline || inputInsidePopup) setIndices({ activeIndex: null });
				else setCloseQuery(query);
				setInputValue("", createChangeEventDetails(inputClear, eventDetails.event));
			}
		}
		setOpenUnwrapped(nextOpen);
		if (!nextOpen && inputInsidePopup && (eventDetails.reason === "focus-out" || eventDetails.reason === "outside-press")) {
			setTouched(true);
			setFocused(false);
			if (validationMode === "onBlur") {
				const valueToValidate = selectionMode === "none" ? inputValue : selectedValue;
				validation.commit(valueToValidate);
			}
		}
	});
	const setSelectedValue = useStableCallback((nextValue, eventDetails) => {
		onSelectedValueChange?.(nextValue, eventDetails);
		if (eventDetails.isCanceled) return;
		setSelectedValueUnwrapped(nextValue);
		if (selectionMode === "none" && popupRef.current && fillInputOnItemPress || single && !store.state.inputInsidePopup) setInputValue(stringifyAsLabel(nextValue, itemToStringLabel), createChangeEventDetails(eventDetails.reason, eventDetails.event));
		if (single && nextValue != null && eventDetails.reason !== "input-change" && queryChangedAfterOpen && !inline) setCloseQuery(query);
	});
	const handleSelection = useStableCallback((event, passedValue) => {
		let itemValue = passedValue;
		if (itemValue === void 0) {
			if (activeIndex === null) return;
			itemValue = valuesRef.current[activeIndex];
		}
		const targetEl = getTarget(event);
		const overrideEvent = selectionEventRef.current ?? event;
		selectionEventRef.current = null;
		const eventDetails = createChangeEventDetails(itemPress, overrideEvent);
		const href = targetEl?.closest("a")?.getAttribute("href");
		if (href) {
			if (href.startsWith("#")) setOpen(false, eventDetails);
			return;
		}
		if (multiple) {
			const currentSelectedValue = Array.isArray(selectedValue) ? selectedValue : [];
			setSelectedValue(selectedValueIncludes(currentSelectedValue, itemValue, store.state.isItemEqualToValue) ? removeItem(currentSelectedValue, itemValue, store.state.isItemEqualToValue) : [...currentSelectedValue, itemValue], eventDetails);
			if (!(inputRef.current ? inputRef.current.value.trim() !== "" : false)) return;
			if (store.state.inputInsidePopup) setInputValue("", createChangeEventDetails(inputClear, eventDetails.event));
			else setOpen(false, eventDetails);
		} else {
			setSelectedValue(itemValue, eventDetails);
			setOpen(false, eventDetails);
		}
	});
	const requestSubmit = useStableCallback(() => {
		if (!store.state.submitOnItemClick) return;
		const form = store.state.inputElement?.form;
		if (form && typeof form.requestSubmit === "function") form.requestSubmit();
	});
	const handleUnmount = useStableCallback(() => {
		setMounted(false);
		onOpenChangeComplete?.(false);
		setQueryChangedAfterOpen(false);
		setCloseQuery(null);
		if (selectionMode === "none") setIndices({
			activeIndex: null,
			selectedIndex: null
		});
		else setIndices({ activeIndex: null });
		if (multiple && inputRef.current && inputRef.current.value !== "" && !hadInputClearRef.current) setInputValue("", createChangeEventDetails(inputClear));
		if (single) if (store.state.inputInsidePopup) {
			if (inputRef.current && inputRef.current.value !== "") setInputValue("", createChangeEventDetails(inputClear));
		} else {
			const stringVal = stringifyAsLabel(selectedValue, itemToStringLabel);
			if (inputRef.current && inputRef.current.value !== stringVal) setInputValue(stringVal, createChangeEventDetails(stringVal === "" ? inputClear : none));
		}
	});
	const resolvedPopupRef = import_react.useMemo(() => {
		if (inline && positionerElement) return { current: positionerElement.closest("[role=\"dialog\"]") };
		return popupRef;
	}, [inline, positionerElement]);
	useOpenChangeComplete({
		enabled: !props.actionsRef,
		open,
		ref: resolvedPopupRef,
		onComplete() {
			if (!open) handleUnmount();
		}
	});
	import_react.useImperativeHandle(props.actionsRef, () => ({ unmount: handleUnmount }), [handleUnmount]);
	useIsoLayoutEffect(function syncSelectedIndex() {
		if (open || selectionMode === "none") return;
		const registry = items ? flatItems : allValuesRef.current;
		if (multiple) {
			const currentValue = Array.isArray(selectedValue) ? selectedValue : [];
			const lastValue = currentValue[currentValue.length - 1];
			const lastIndex = findItemIndex(registry, lastValue, isItemEqualToValue);
			setIndices({ selectedIndex: lastIndex === -1 ? null : lastIndex });
		} else {
			const index = findItemIndex(registry, selectedValue, isItemEqualToValue);
			setIndices({ selectedIndex: index === -1 ? null : index });
		}
	}, [
		open,
		selectedValue,
		items,
		selectionMode,
		flatItems,
		multiple,
		isItemEqualToValue,
		setIndices
	]);
	useIsoLayoutEffect(() => {
		if (items) {
			valuesRef.current = flatFilteredItems;
			listRef.current.length = flatFilteredItems.length;
		}
	}, [items, flatFilteredItems]);
	useIsoLayoutEffect(() => {
		const pendingHighlight = pendingQueryHighlightRef.current;
		if (pendingHighlight) {
			if (pendingHighlight.hasQuery) {
				if (autoHighlightMode) store.set("activeIndex", 0);
			} else if (autoHighlightMode === "always") store.set("activeIndex", 0);
			pendingQueryHighlightRef.current = null;
		}
		if (!open && !inline) return;
		const candidateItems = hasItems || hasFilteredItemsProp ? flatFilteredItems : valuesRef.current;
		const storeActiveIndex = store.state.activeIndex;
		if (storeActiveIndex == null) {
			if (autoHighlightMode === "always" && candidateItems.length > 0) {
				store.set("activeIndex", 0);
				return;
			}
			if (lastHighlightRef.current !== INITIAL_LAST_HIGHLIGHT) {
				lastHighlightRef.current = INITIAL_LAST_HIGHLIGHT;
				store.state.onItemHighlighted(void 0, createGenericEventDetails(none, void 0, { index: -1 }));
			}
			return;
		}
		if (storeActiveIndex >= candidateItems.length) {
			if (lastHighlightRef.current !== INITIAL_LAST_HIGHLIGHT) {
				lastHighlightRef.current = INITIAL_LAST_HIGHLIGHT;
				store.state.onItemHighlighted(void 0, createGenericEventDetails(none, void 0, { index: -1 }));
			}
			store.set("activeIndex", null);
			return;
		}
		const itemValue = candidateItems[storeActiveIndex];
		const previouslyHighlightedItemValue = lastHighlightRef.current.value;
		const isSameItem = previouslyHighlightedItemValue !== NO_ACTIVE_VALUE && compareItemEquality(itemValue, previouslyHighlightedItemValue, store.state.isItemEqualToValue);
		if (lastHighlightRef.current.index !== storeActiveIndex || !isSameItem) {
			lastHighlightRef.current = {
				value: itemValue,
				index: storeActiveIndex
			};
			store.state.onItemHighlighted(itemValue, createGenericEventDetails(none, void 0, { index: storeActiveIndex }));
		}
	}, [
		activeIndex,
		autoHighlightMode,
		hasFilteredItemsProp,
		hasItems,
		flatFilteredItems,
		inline,
		open,
		store
	]);
	useIsoLayoutEffect(() => {
		if (selectionMode === "none") {
			setFilled(String(inputValue) !== "");
			return;
		}
		setFilled(multiple ? Array.isArray(selectedValue) && selectedValue.length > 0 : selectedValue != null);
	}, [
		setFilled,
		selectionMode,
		inputValue,
		selectedValue,
		multiple
	]);
	import_react.useEffect(() => {
		if (hasItems && autoHighlightMode && flatFilteredItems.length === 0) setIndices({ activeIndex: null });
	}, [
		hasItems,
		autoHighlightMode,
		flatFilteredItems.length,
		setIndices
	]);
	useValueChanged(query, () => {
		if (!open || query === "" || query === String(initialDefaultInputValue)) return;
		setQueryChangedAfterOpen(true);
	});
	useValueChanged(selectedValue, () => {
		if (selectionMode === "none") return;
		clearErrors(name);
		setDirty(selectedValue !== validityData.initialValue);
		if (shouldValidateOnChange()) validation.commit(selectedValue);
		else validation.commit(selectedValue, true);
		if (single && !hasInputValue && !inputInsidePopup) {
			const nextInputValue = stringifyAsLabel(selectedValue, itemToStringLabel);
			if (inputValue !== nextInputValue) setInputValue(nextInputValue, createChangeEventDetails(none));
		}
	});
	useValueChanged(inputValue, () => {
		if (selectionMode !== "none") return;
		clearErrors(name);
		setDirty(inputValue !== validityData.initialValue);
		if (shouldValidateOnChange()) validation.commit(inputValue);
		else validation.commit(inputValue, true);
	});
	useValueChanged(items, () => {
		if (!single || hasInputValue || inputInsidePopup || queryChangedAfterOpen) return;
		const nextInputValue = stringifyAsLabel(selectedValue, itemToStringLabel);
		if (inputValue !== nextInputValue) setInputValue(nextInputValue, createChangeEventDetails(none));
	});
	const floatingRootContext = useFloatingRootContext({
		open: inline ? true : open,
		onOpenChange: setOpen,
		elements: {
			reference: inputInsidePopup ? triggerElement : inputElement,
			floating: positionerElement
		}
	});
	let ariaHasPopup;
	let ariaExpanded;
	if (!inline) {
		ariaHasPopup = grid ? "grid" : "listbox";
		ariaExpanded = open ? "true" : "false";
	}
	const { getReferenceProps, getFloatingProps, getItemProps } = useInteractions([
		import_react.useMemo(() => {
			const isPlainInput = inputElement?.tagName === "INPUT";
			const shouldTreatAsInput = inputElement == null || isPlainInput;
			const shouldApplyAria = shouldTreatAsInput || open;
			const reference = shouldTreatAsInput ? {
				autoComplete: "off",
				spellCheck: "false",
				autoCorrect: "off",
				autoCapitalize: "none"
			} : {};
			if (shouldApplyAria) {
				reference.role = "combobox";
				reference["aria-expanded"] = ariaExpanded;
				reference["aria-haspopup"] = ariaHasPopup;
				reference["aria-controls"] = open ? listElement?.id : void 0;
				reference["aria-autocomplete"] = autoComplete;
			}
			return {
				reference,
				floating: { role: "presentation" }
			};
		}, [
			inputElement,
			open,
			ariaExpanded,
			ariaHasPopup,
			listElement?.id,
			autoComplete
		]),
		useClick(floatingRootContext, {
			enabled: !readOnly && !disabled && openOnInputClick,
			event: "mousedown-only",
			toggle: false,
			touchOpenDelay: inputInsidePopup ? 0 : 50,
			reason: inputPress
		}),
		useDismiss(floatingRootContext, {
			enabled: !readOnly && !disabled && !inline,
			outsidePressEvent: {
				mouse: "sloppy",
				touch: "intentional"
			},
			bubbles: inline ? true : void 0,
			outsidePress(event) {
				const target = getTarget(event);
				return !contains(triggerElement, target) && !contains(clearRef.current, target) && !contains(chipsContainerRef.current, target) && !contains(inputGroupElement, target);
			}
		}),
		useListNavigation(floatingRootContext, {
			enabled: !readOnly && !disabled,
			id,
			listRef,
			activeIndex,
			selectedIndex,
			virtual: true,
			loopFocus,
			allowEscape: loopFocus && !autoHighlightMode,
			focusItemOnOpen: queryChangedAfterOpen || selectionMode === "none" && !autoHighlightMode ? false : "auto",
			focusItemOnHover: highlightItemOnHover,
			resetOnPointerLeave: !keepHighlight,
			cols: grid ? 2 : 1,
			orientation: grid ? "horizontal" : void 0,
			disabledIndices: EMPTY_ARRAY,
			onNavigate(nextActiveIndex, event) {
				if (!event && !open || transitionStatus === "ending") return;
				if (!event) setIndices({ activeIndex: nextActiveIndex });
				else setIndices({
					activeIndex: nextActiveIndex,
					type: keyboardActiveRef.current ? "keyboard" : "pointer"
				});
			}
		})
	]);
	useOnFirstRender(() => {
		store.update({
			inline: inlineProp,
			popupProps: getFloatingProps(),
			inputProps: getReferenceProps(),
			triggerProps,
			getItemProps,
			setOpen,
			setInputValue,
			setSelectedValue,
			setIndices,
			onItemHighlighted,
			handleSelection,
			forceMount,
			requestSubmit
		});
	});
	useIsoLayoutEffect(() => {
		store.update({
			id,
			selectedValue,
			open,
			mounted,
			transitionStatus,
			items,
			inline: inlineProp,
			popupProps: getFloatingProps(),
			inputProps: getReferenceProps(),
			triggerProps,
			openMethod,
			getItemProps,
			selectionMode,
			name,
			disabled,
			readOnly,
			required,
			grid,
			isGrouped,
			virtualized,
			onOpenChangeComplete,
			openOnInputClick,
			itemToStringLabel,
			modal,
			autoHighlight: autoHighlightMode,
			isItemEqualToValue,
			submitOnItemClick,
			hasInputValue,
			requestSubmit
		});
	}, [
		store,
		id,
		selectedValue,
		open,
		mounted,
		transitionStatus,
		items,
		getFloatingProps,
		getReferenceProps,
		getItemProps,
		openMethod,
		triggerProps,
		selectionMode,
		name,
		disabled,
		readOnly,
		required,
		validation,
		grid,
		isGrouped,
		virtualized,
		onOpenChangeComplete,
		openOnInputClick,
		itemToStringLabel,
		modal,
		isItemEqualToValue,
		submitOnItemClick,
		hasInputValue,
		inlineProp,
		requestSubmit,
		autoHighlightMode
	]);
	const hiddenInputRef = useMergedRefs(inputRefProp, validation.inputRef);
	const itemsContextValue = import_react.useMemo(() => ({
		query,
		hasItems,
		filteredItems,
		flatFilteredItems
	}), [
		query,
		hasItems,
		filteredItems,
		flatFilteredItems
	]);
	const serializedValue = import_react.useMemo(() => {
		if (Array.isArray(fieldRawValue)) return "";
		return stringifyAsValue(fieldRawValue, itemToStringValue);
	}, [fieldRawValue, itemToStringValue]);
	const hasMultipleSelection = multiple && Array.isArray(selectedValue) && selectedValue.length > 0;
	const hiddenInputName = multiple || selectionMode === "none" ? void 0 : name;
	const hiddenInputs = import_react.useMemo(() => {
		if (!multiple || !Array.isArray(selectedValue) || !name) return null;
		return selectedValue.map((value) => {
			const currentSerializedValue = stringifyAsValue(value, itemToStringValue);
			return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", {
				type: "hidden",
				name,
				value: currentSerializedValue
			}, currentSerializedValue);
		});
	}, [
		multiple,
		selectedValue,
		name,
		itemToStringValue
	]);
	const children = /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [
		props.children,
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", {
			...validation.getInputValidationProps({
				onFocus() {
					if (inputInsidePopup) {
						triggerElement?.focus();
						return;
					}
					(inputRef.current || triggerElement)?.focus();
				},
				onChange(event) {
					if (event.nativeEvent.defaultPrevented) return;
					const nextValue = event.target.value;
					const details = createChangeEventDetails(none, event.nativeEvent);
					function handleChange() {
						if (multiple) return;
						if (selectionMode === "none") {
							setDirty(nextValue !== validityData.initialValue);
							setInputValue(nextValue, details);
							if (shouldValidateOnChange()) validation.commit(nextValue);
							return;
						}
						const matchingValue = valuesRef.current.find((v) => {
							if (stringifyAsValue(v, itemToStringValue).toLowerCase() === nextValue.toLowerCase()) return true;
							return false;
						});
						if (matchingValue != null) {
							setDirty(matchingValue !== validityData.initialValue);
							setSelectedValue?.(matchingValue, details);
							if (shouldValidateOnChange()) validation.commit(matchingValue);
						}
					}
					if (items) handleChange();
					else {
						forceMount();
						queueMicrotask(handleChange);
					}
				}
			}),
			id: id && hiddenInputName == null ? `${id}-hidden-input` : void 0,
			name: hiddenInputName,
			autoComplete: formAutoComplete,
			disabled,
			required: required && !hasMultipleSelection,
			readOnly,
			value: serializedValue,
			ref: hiddenInputRef,
			style: hiddenInputName ? visuallyHiddenInput : visuallyHidden,
			tabIndex: -1,
			"aria-hidden": true
		}),
		hiddenInputs
	] });
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ComboboxRootContext.Provider, {
		value: store,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ComboboxFloatingContext.Provider, {
			value: floatingRootContext,
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ComboboxDerivedItemsContext.Provider, {
				value: itemsContextValue,
				children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ComboboxInputValueContext.Provider, {
					value: inputValue,
					children
				})
			})
		})
	});
}
//#endregion
//#region node_modules/@base-ui/react/esm/autocomplete/root/AutocompleteRoot.js
/**
* Groups all parts of the autocomplete.
* Doesn't render its own HTML element.
*
* Documentation: [Base UI Autocomplete](https://base-ui.com/react/components/autocomplete)
*/
function AutocompleteRoot(props) {
	const { openOnInputClick = false, value, defaultValue, onValueChange, mode = "list", itemToStringValue, ...other } = props;
	const enableInline = mode === "inline" || mode === "both";
	const staticItems = mode === "inline" || mode === "none";
	const isControlled = value !== void 0;
	const [internalValue, setInternalValue] = import_react.useState(defaultValue ?? "");
	const [inlineInputValue, setInlineInputValue] = import_react.useState("");
	import_react.useEffect(() => {
		if (isControlled) setInlineInputValue("");
	}, [value, isControlled]);
	let resolvedInputValue;
	if (enableInline && inlineInputValue !== "") resolvedInputValue = inlineInputValue;
	else if (isControlled) resolvedInputValue = value ?? "";
	else resolvedInputValue = internalValue;
	const handleValueChange = useStableCallback((nextValue, eventDetails) => {
		setInlineInputValue("");
		if (!isControlled) setInternalValue(nextValue);
		onValueChange?.(nextValue, eventDetails);
	});
	const collator = useCoreFilter();
	const baseFilter = import_react.useMemo(() => {
		if (other.filter !== void 0) return other.filter;
		return collator.contains;
	}, [other.filter, collator]);
	const resolvedQuery = String(isControlled ? value : internalValue).trim();
	const resolvedFilter = import_react.useMemo(() => {
		if (mode !== "both") return staticItems ? null : baseFilter;
		if (baseFilter === null) return null;
		return (item, _query, toString) => {
			return baseFilter(item, resolvedQuery, toString);
		};
	}, [
		baseFilter,
		mode,
		resolvedQuery,
		staticItems
	]);
	const handleItemHighlighted = useStableCallback((highlightedValue, eventDetails) => {
		props.onItemHighlighted?.(highlightedValue, eventDetails);
		if (eventDetails.reason === "pointer") return;
		if (enableInline) if (highlightedValue == null) setInlineInputValue("");
		else setInlineInputValue(stringifyAsLabel(highlightedValue, itemToStringValue));
		else setInlineInputValue("");
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AriaCombobox, {
		...other,
		itemToStringLabel: itemToStringValue,
		openOnInputClick,
		selectionMode: "none",
		fillInputOnItemPress: true,
		filter: resolvedFilter,
		autoComplete: mode,
		inputValue: resolvedInputValue,
		defaultInputValue: defaultValue,
		onInputValueChange: handleValueChange,
		onItemHighlighted: handleItemHighlighted
	});
}
//#endregion
//#region node_modules/@base-ui/react/esm/autocomplete/value/AutocompleteValue.js
/**
* The current value of the autocomplete.
* Doesn't render its own HTML element.
*
* Documentation: [Base UI Autocomplete](https://base-ui.com/react/components/autocomplete)
*/
function AutocompleteValue(props) {
	const { children } = props;
	const inputValue = useComboboxInputValueContext();
	let returnValue = null;
	if (typeof children === "function") returnValue = children(String(inputValue));
	else if (children != null) returnValue = children;
	else returnValue = inputValue;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_react.Fragment, { children: returnValue });
}
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/utils/stateAttributesMapping.js
var triggerStateAttributesMapping = {
	...pressableTriggerOpenStateMapping,
	...fieldValidityMapping,
	popupSide: (side) => side ? { "data-popup-side": side } : null,
	listEmpty: (empty) => empty ? { "data-list-empty": "" } : null
};
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/trigger/ComboboxTrigger.js
var BOUNDARY_OFFSET$1 = 2;
/**
* A button that opens the popup.
* Renders a `<button>` element.
*/
var ComboboxTrigger = /* @__PURE__ */ import_react.forwardRef(function ComboboxTrigger(componentProps, forwardedRef) {
	const { render, className, nativeButton = true, disabled: disabledProp = false, id: idProp, ...elementProps } = componentProps;
	const { state: fieldState, disabled: fieldDisabled, setTouched, setFocused, validationMode, validation } = useFieldRootContext();
	const { labelId: fieldLabelId } = useLabelableContext();
	const store = useComboboxRootContext();
	const { filteredItems } = useComboboxDerivedItemsContext();
	const selectionMode = useStore(store, selectors$2.selectionMode);
	const comboboxDisabled = useStore(store, selectors$2.disabled);
	const readOnly = useStore(store, selectors$2.readOnly);
	const required = useStore(store, selectors$2.required);
	const mounted = useStore(store, selectors$2.mounted);
	const popupSideValue = useStore(store, selectors$2.popupSide);
	const positionerElement = useStore(store, selectors$2.positionerElement);
	const listElement = useStore(store, selectors$2.listElement);
	const triggerProps = useStore(store, selectors$2.triggerProps);
	const triggerElement = useStore(store, selectors$2.triggerElement);
	const inputInsidePopup = useStore(store, selectors$2.inputInsidePopup);
	const rootId = useStore(store, selectors$2.id);
	const comboboxLabelId = useStore(store, selectors$2.labelId);
	const open = useStore(store, selectors$2.open);
	const selectedValue = useStore(store, selectors$2.selectedValue);
	const activeIndex = useStore(store, selectors$2.activeIndex);
	const selectedIndex = useStore(store, selectors$2.selectedIndex);
	const hasSelectedValue = useStore(store, selectors$2.hasSelectedValue);
	const floatingRootContext = useComboboxFloatingContext();
	const inputValue = useComboboxInputValueContext();
	const focusTimeout = useTimeout();
	const disabled = fieldDisabled || comboboxDisabled || disabledProp;
	const listEmpty = filteredItems.length === 0;
	const popupSide = mounted && positionerElement ? popupSideValue : null;
	useLabelableId({ id: inputInsidePopup ? idProp : void 0 });
	const id = inputInsidePopup ? idProp ?? rootId : idProp;
	const ariaLabelledBy = resolveAriaLabelledBy(fieldLabelId, comboboxLabelId);
	const currentPointerTypeRef = import_react.useRef("");
	function trackPointerType(event) {
		currentPointerTypeRef.current = event.pointerType;
	}
	const domReference = floatingRootContext.useState("domReferenceElement");
	import_react.useEffect(() => {
		if (!inputInsidePopup) return;
		if (triggerElement && triggerElement !== domReference) floatingRootContext.set("domReferenceElement", triggerElement);
	}, [
		triggerElement,
		domReference,
		floatingRootContext,
		inputInsidePopup
	]);
	const { reference: triggerTypeaheadProps } = useTypeahead(floatingRootContext, {
		enabled: !open && !readOnly && !comboboxDisabled && selectionMode === "single",
		listRef: store.state.labelsRef,
		activeIndex,
		selectedIndex,
		onMatch(index) {
			const nextSelectedValue = store.state.valuesRef.current[index];
			if (nextSelectedValue !== void 0) store.state.setSelectedValue(nextSelectedValue, createChangeEventDetails("none"));
		}
	});
	const { reference: triggerClickProps } = useClick(floatingRootContext, {
		enabled: !readOnly && !comboboxDisabled,
		event: "mousedown"
	});
	const { buttonRef, getButtonProps } = useButton({
		native: nativeButton,
		disabled
	});
	const state = {
		...fieldState,
		open,
		disabled,
		popupSide,
		listEmpty,
		placeholder: !hasSelectedValue
	};
	return useRenderElement("button", componentProps, {
		ref: [
			forwardedRef,
			buttonRef,
			useStableCallback((element) => {
				store.set("triggerElement", element);
			})
		],
		state,
		props: [
			triggerProps,
			triggerClickProps,
			triggerTypeaheadProps,
			{
				id,
				tabIndex: inputInsidePopup ? 0 : -1,
				role: inputInsidePopup ? "combobox" : void 0,
				"aria-expanded": open ? "true" : "false",
				"aria-haspopup": inputInsidePopup ? "dialog" : "listbox",
				"aria-controls": open ? listElement?.id : void 0,
				"aria-required": inputInsidePopup ? required || void 0 : void 0,
				"aria-labelledby": ariaLabelledBy,
				onPointerDown: trackPointerType,
				onPointerEnter: trackPointerType,
				onFocus() {
					setFocused(true);
					if (disabled || readOnly) return;
					focusTimeout.start(0, store.state.forceMount);
				},
				onBlur(event) {
					if (contains(positionerElement, event.relatedTarget)) return;
					setTouched(true);
					setFocused(false);
					if (validationMode === "onBlur") {
						const valueToValidate = selectionMode === "none" ? inputValue : selectedValue;
						validation.commit(valueToValidate);
					}
				},
				onMouseDown(event) {
					if (disabled || readOnly) return;
					if (!inputInsidePopup) floatingRootContext.set("domReferenceElement", event.currentTarget);
					store.state.forceMount();
					if (currentPointerTypeRef.current !== "touch") {
						store.state.inputRef.current?.focus();
						if (!inputInsidePopup) event.preventDefault();
					}
					if (open) return;
					const doc = ownerDocument(event.currentTarget);
					function handleMouseUp(mouseEvent) {
						if (!triggerElement) return;
						const mouseUpTarget = getTarget(mouseEvent);
						const positioner = store.state.positionerElement;
						const list = store.state.listElement;
						if (contains(triggerElement, mouseUpTarget) || contains(positioner, mouseUpTarget) || contains(list, mouseUpTarget) || mouseUpTarget === triggerElement) return;
						const bounds = getPseudoElementBounds(triggerElement);
						const withinHorizontal = mouseEvent.clientX >= bounds.left - BOUNDARY_OFFSET$1 && mouseEvent.clientX <= bounds.right + BOUNDARY_OFFSET$1;
						const withinVertical = mouseEvent.clientY >= bounds.top - BOUNDARY_OFFSET$1 && mouseEvent.clientY <= bounds.bottom + BOUNDARY_OFFSET$1;
						if (withinHorizontal && withinVertical) return;
						store.state.setOpen(false, createChangeEventDetails("cancel-open", mouseEvent));
					}
					if (inputInsidePopup) doc.addEventListener("mouseup", handleMouseUp, { once: true });
				},
				onKeyDown(event) {
					if (disabled || readOnly) return;
					if (event.key === "ArrowDown" || event.key === "ArrowUp") {
						stopEvent(event);
						store.state.setOpen(true, createChangeEventDetails(listNavigation, event.nativeEvent));
						store.state.inputRef.current?.focus();
					}
				}
			},
			validation ? validation.getValidationProps(elementProps) : elementProps,
			getButtonProps
		],
		stateAttributesMapping: triggerStateAttributesMapping
	});
});
ComboboxTrigger.displayName = "ComboboxTrigger";
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/chips/ComboboxChipsContext.js
var ComboboxChipsContext = /* @__PURE__ */ import_react.createContext(void 0);
ComboboxChipsContext.displayName = "ComboboxChipsContext";
function useComboboxChipsContext() {
	return import_react.useContext(ComboboxChipsContext);
}
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/positioner/ComboboxPositionerContext.js
var ComboboxPositionerContext = /* @__PURE__ */ import_react.createContext(void 0);
ComboboxPositionerContext.displayName = "ComboboxPositionerContext";
function useComboboxPositionerContext(optional) {
	const context = import_react.useContext(ComboboxPositionerContext);
	if (context === void 0 && !optional) throw new Error("Base UI: <Combobox.Popup> and <Combobox.Arrow> must be used within the <Combobox.Positioner> component");
	return context;
}
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/utils/ComboboxInternalDismissButton.js
/**
* @internal
*/
var ComboboxInternalDismissButton = /* @__PURE__ */ import_react.forwardRef(function ComboboxInternalDismissButton(_, forwardedRef) {
	const store = useComboboxRootContext();
	const { buttonRef, getButtonProps } = useButton({ native: false });
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
		ref: useMergedRefs(forwardedRef, buttonRef),
		...getButtonProps({ onClick: useStableCallback((event) => {
			store.state.setOpen(false, createChangeEventDetails(closePress, event.nativeEvent, event.currentTarget));
		}) }),
		"aria-label": "Dismiss",
		tabIndex: void 0,
		style: visuallyHiddenInput
	});
});
ComboboxInternalDismissButton.displayName = "ComboboxInternalDismissButton";
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/input/ComboboxInput.js
/**
* A text input to search for items in the list.
* Renders an `<input>` element.
*/
var ComboboxInput = /* @__PURE__ */ import_react.forwardRef(function ComboboxInput(componentProps, forwardedRef) {
	const { render, className, disabled: disabledProp = false, id: idProp, ...elementProps } = componentProps;
	const { state: fieldState, disabled: fieldDisabled, setTouched, setFocused, validationMode, validation } = useFieldRootContext();
	const { labelId: fieldLabelId } = useLabelableContext();
	const comboboxChipsContext = useComboboxChipsContext();
	const positioning = useComboboxPositionerContext(true);
	const hasPositionerParent = Boolean(positioning);
	const store = useComboboxRootContext();
	const { filteredItems } = useComboboxDerivedItemsContext();
	const inputValue = useComboboxInputValueContext();
	const direction = useDirection();
	const required = useStore(store, selectors$2.required);
	const comboboxDisabled = useStore(store, selectors$2.disabled);
	const readOnly = useStore(store, selectors$2.readOnly);
	const name = useStore(store, selectors$2.name);
	const selectionMode = useStore(store, selectors$2.selectionMode);
	const autoHighlightMode = useStore(store, selectors$2.autoHighlight);
	const inputProps = useStore(store, selectors$2.inputProps);
	const triggerProps = useStore(store, selectors$2.triggerProps);
	const open = useStore(store, selectors$2.open);
	const mounted = useStore(store, selectors$2.mounted);
	const selectedValue = useStore(store, selectors$2.selectedValue);
	const popupSideValue = useStore(store, selectors$2.popupSide);
	const positionerElement = useStore(store, selectors$2.positionerElement);
	const rootId = useStore(store, selectors$2.id);
	const inline = useStore(store, selectors$2.inline);
	const modal = useStore(store, selectors$2.modal);
	const autoHighlightEnabled = Boolean(autoHighlightMode);
	const popupSide = mounted && positionerElement ? popupSideValue : null;
	const disabled = fieldDisabled || comboboxDisabled || disabledProp;
	const listEmpty = filteredItems.length === 0;
	const isInsidePopup = hasPositionerParent || inline;
	const focusManagerModal = !isInsidePopup || modal;
	const id = useBaseUiId(idProp ?? (!isInsidePopup ? rootId : void 0));
	const ariaLabelledBy = resolveAriaLabelledBy(fieldLabelId, void 0);
	const fieldStateForInput = hasPositionerParent ? DEFAULT_FIELD_STATE_ATTRIBUTES : fieldState;
	const [composingValue, setComposingValue] = import_react.useState(null);
	const isComposingRef = import_react.useRef(false);
	const lastActiveIndexRef = import_react.useRef(null);
	const shouldRestoreActiveIndexRef = import_react.useRef(false);
	const setInputElement = useStableCallback((element) => {
		const nextIsInsidePopup = hasPositionerParent || store.state.inline;
		if (nextIsInsidePopup && !store.state.hasInputValue) store.state.setInputValue("", createChangeEventDetails(none));
		store.update({
			inputElement: element,
			inputInsidePopup: nextIsInsidePopup
		});
	});
	const validationProps = hasPositionerParent || !validation ? elementProps : validation.getValidationProps(elementProps);
	const state = {
		...fieldStateForInput,
		open,
		disabled,
		readOnly,
		popupSide,
		listEmpty
	};
	function handleKeyDown(event) {
		if (!comboboxChipsContext) return;
		let nextIndex;
		const { highlightedChipIndex } = comboboxChipsContext;
		if (highlightedChipIndex !== void 0) {
			if (event.key === "ArrowLeft") {
				event.preventDefault();
				if (highlightedChipIndex > 0) nextIndex = highlightedChipIndex - 1;
				else nextIndex = void 0;
			} else if (event.key === "ArrowRight") {
				event.preventDefault();
				if (highlightedChipIndex < selectedValue.length - 1) nextIndex = highlightedChipIndex + 1;
				else nextIndex = void 0;
			} else if (event.key === "Backspace" || event.key === "Delete") {
				event.preventDefault();
				const computedNextIndex = highlightedChipIndex >= selectedValue.length - 1 ? selectedValue.length - 2 : highlightedChipIndex;
				nextIndex = computedNextIndex >= 0 ? computedNextIndex : void 0;
				store.state.setIndices({
					activeIndex: null,
					selectedIndex: null,
					type: "keyboard"
				});
			}
			return nextIndex;
		}
		if (event.key === "ArrowLeft" && (event.currentTarget.selectionStart ?? 0) === 0 && selectedValue.length > 0) {
			event.preventDefault();
			nextIndex = Math.max(selectedValue.length - 1, 0);
		} else if (event.key === "Backspace" && event.currentTarget.value === "" && selectedValue.length > 0) {
			store.state.setIndices({
				activeIndex: null,
				selectedIndex: null,
				type: "keyboard"
			});
			event.preventDefault();
		}
		return nextIndex;
	}
	const element = useRenderElement("input", componentProps, {
		state,
		ref: [
			forwardedRef,
			store.state.inputRef,
			setInputElement
		],
		props: [
			inputProps,
			triggerProps,
			{
				type: "text",
				value: componentProps.value ?? composingValue ?? inputValue,
				"aria-readonly": readOnly || void 0,
				"aria-required": required || void 0,
				"aria-labelledby": ariaLabelledBy,
				disabled,
				readOnly,
				required: selectionMode === "none" ? required : void 0,
				...selectionMode === "none" && name && { name },
				id,
				onFocus() {
					setFocused(true);
					if (!inline || !shouldRestoreActiveIndexRef.current) return;
					shouldRestoreActiveIndexRef.current = false;
					const nextActiveIndex = lastActiveIndexRef.current;
					if (nextActiveIndex == null || !Object.hasOwn(store.state.valuesRef.current, nextActiveIndex)) return;
					store.state.setIndices({ activeIndex: nextActiveIndex });
				},
				onBlur() {
					setTouched(true);
					setFocused(false);
					const activeIndex = store.state.activeIndex;
					if (inline && activeIndex !== null && autoHighlightMode !== "always") {
						lastActiveIndexRef.current = activeIndex;
						shouldRestoreActiveIndexRef.current = true;
						store.state.setIndices({ activeIndex: null });
					}
					if (validationMode === "onBlur") {
						const valueToValidate = selectionMode === "none" ? inputValue : selectedValue;
						validation.commit(valueToValidate);
					}
				},
				onCompositionStart(event) {
					if (isAndroid) return;
					isComposingRef.current = true;
					setComposingValue(event.currentTarget.value);
				},
				onCompositionEnd(event) {
					isComposingRef.current = false;
					const next = event.currentTarget.value;
					setComposingValue(null);
					store.state.setInputValue(next, createChangeEventDetails(inputChange, event.nativeEvent));
				},
				onChange(event) {
					const inputType = event.nativeEvent.inputType;
					const autofillLikeInput = !inputType || inputType === "insertReplacementText";
					const shouldOpenOnInput = isComposingRef.current || !autofillLikeInput;
					if (isComposingRef.current) {
						const nextVal = event.currentTarget.value;
						setComposingValue(nextVal);
						if (nextVal === "" && !store.state.openOnInputClick && !store.state.inputInsidePopup) store.state.setOpen(false, createChangeEventDetails(inputClear, event.nativeEvent));
						const trimmed = nextVal.trim();
						const shouldMaintainHighlight = autoHighlightEnabled && trimmed !== "";
						if (!readOnly && !disabled && trimmed) {
							if (shouldOpenOnInput) {
								store.state.setOpen(true, createChangeEventDetails(inputChange, event.nativeEvent));
								if (!autoHighlightEnabled) store.state.setIndices({
									activeIndex: null,
									selectedIndex: null,
									type: store.state.keyboardActiveRef.current ? "keyboard" : "pointer"
								});
							}
						}
						if (open && store.state.activeIndex !== null && !shouldMaintainHighlight) store.state.setIndices({
							activeIndex: null,
							selectedIndex: null,
							type: store.state.keyboardActiveRef.current ? "keyboard" : "pointer"
						});
						return;
					}
					store.state.setInputValue(event.currentTarget.value, createChangeEventDetails(inputChange, event.nativeEvent));
					const empty = event.currentTarget.value === "";
					const clearDetails = createChangeEventDetails(inputClear, event.nativeEvent);
					if (empty && !store.state.inputInsidePopup) {
						if (selectionMode === "single") store.state.setSelectedValue(null, clearDetails);
						if (!store.state.openOnInputClick) store.state.setOpen(false, clearDetails);
					}
					const trimmed = event.currentTarget.value.trim();
					if (!readOnly && !disabled && trimmed) {
						if (shouldOpenOnInput) {
							store.state.setOpen(true, createChangeEventDetails(inputChange, event.nativeEvent));
							if (!autoHighlightEnabled) store.state.setIndices({
								activeIndex: null,
								selectedIndex: null,
								type: store.state.keyboardActiveRef.current ? "keyboard" : "pointer"
							});
						}
					}
					if (open && store.state.activeIndex !== null && !autoHighlightEnabled) store.state.setIndices({
						activeIndex: null,
						selectedIndex: null,
						type: store.state.keyboardActiveRef.current ? "keyboard" : "pointer"
					});
				},
				onKeyDown(event) {
					if (disabled || readOnly) return;
					if (event.ctrlKey || event.shiftKey || event.altKey || event.metaKey) return;
					store.state.keyboardActiveRef.current = true;
					const input = event.currentTarget;
					const scrollAmount = input.scrollWidth - input.clientWidth;
					const isRTL = direction === "rtl";
					if (event.key === "Home") {
						stopEvent(event);
						const cursor = isFirefox && isRTL ? input.value.length : 0;
						input.setSelectionRange(cursor, cursor);
						input.scrollLeft = 0;
						return;
					}
					if (event.key === "End") {
						stopEvent(event);
						const cursor = isFirefox && isRTL ? 0 : input.value.length;
						input.setSelectionRange(cursor, cursor);
						input.scrollLeft = isRTL ? -scrollAmount : scrollAmount;
						return;
					}
					if (!mounted && event.key === "Escape") {
						const isClear = selectionMode === "multiple" && Array.isArray(selectedValue) ? selectedValue.length === 0 : selectedValue === null;
						const details = createChangeEventDetails(escapeKey, event.nativeEvent);
						const value = selectionMode === "multiple" ? [] : null;
						store.state.setInputValue("", details);
						store.state.setSelectedValue(value, details);
						if (!isClear && !store.state.inline && !details.isPropagationAllowed) event.stopPropagation();
						return;
					}
					if (comboboxChipsContext && event.key === "Backspace" && input.value === "" && comboboxChipsContext.highlightedChipIndex === void 0 && Array.isArray(selectedValue) && selectedValue.length > 0) {
						const newValue = selectedValue.slice(0, -1);
						store.state.setIndices({
							activeIndex: null,
							selectedIndex: null,
							type: store.state.keyboardActiveRef.current ? "keyboard" : "pointer"
						});
						store.state.setSelectedValue(newValue, createChangeEventDetails(none, event.nativeEvent));
						return;
					}
					const hadHighlightedChip = comboboxChipsContext?.highlightedChipIndex !== void 0;
					const nextIndex = handleKeyDown(event);
					comboboxChipsContext?.setHighlightedChipIndex(nextIndex);
					if (nextIndex !== void 0) comboboxChipsContext?.chipsRef.current[nextIndex]?.focus();
					else if (hadHighlightedChip) store.state.inputRef.current?.focus();
					if (event.which === 229) return;
					if (event.key === "Enter" && open) {
						const activeIndex = store.state.activeIndex;
						const nativeEvent = event.nativeEvent;
						if (activeIndex === null) {
							if (inline) return;
							store.state.setOpen(false, createChangeEventDetails(none, nativeEvent));
							return;
						}
						stopEvent(event);
						const listItem = store.state.listRef.current[activeIndex];
						if (listItem) {
							store.state.selectionEventRef.current = nativeEvent;
							listItem.click();
							store.state.selectionEventRef.current = null;
						}
					}
				},
				onPointerMove() {
					store.state.keyboardActiveRef.current = false;
				},
				onPointerDown() {
					store.state.keyboardActiveRef.current = false;
				}
			},
			validationProps
		],
		stateAttributesMapping: triggerStateAttributesMapping
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [open && focusManagerModal && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ComboboxInternalDismissButton, { ref: store.state.startDismissRef }), element] });
});
ComboboxInput.displayName = "ComboboxInput";
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/input-group/ComboboxInputGroup.js
/**
* A wrapper for the input and its associated controls.
* Renders a `<div>` element.
*/
var ComboboxInputGroup = /* @__PURE__ */ import_react.forwardRef(function ComboboxInputGroup(componentProps, forwardedRef) {
	const { render, className, ...elementProps } = componentProps;
	const { state: fieldState, disabled: fieldDisabled } = useFieldRootContext();
	const store = useComboboxRootContext();
	const { filteredItems } = useComboboxDerivedItemsContext();
	const open = useStore(store, selectors$2.open);
	const mounted = useStore(store, selectors$2.mounted);
	const popupSideValue = useStore(store, selectors$2.popupSide);
	const positionerElement = useStore(store, selectors$2.positionerElement);
	const comboboxDisabled = useStore(store, selectors$2.disabled);
	const readOnly = useStore(store, selectors$2.readOnly);
	const hasSelectedValue = useStore(store, selectors$2.hasSelectedValue);
	const selectionMode = useStore(store, selectors$2.selectionMode);
	const popupSide = mounted && positionerElement ? popupSideValue : null;
	const disabled = fieldDisabled || comboboxDisabled;
	const listEmpty = filteredItems.length === 0;
	const placeholder = selectionMode === "none" ? false : !hasSelectedValue;
	const state = {
		...fieldState,
		open,
		disabled,
		readOnly,
		popupSide,
		listEmpty,
		placeholder
	};
	return useRenderElement("div", componentProps, {
		ref: [forwardedRef, useStableCallback((element) => {
			store.set("inputGroupElement", element);
		})],
		props: [{ role: "group" }, elementProps],
		state,
		stateAttributesMapping: triggerStateAttributesMapping
	});
});
ComboboxInputGroup.displayName = "ComboboxInputGroup";
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/icon/ComboboxIcon.js
/**
* An icon that indicates that the trigger button opens the popup.
* Renders a `<span>` element.
*/
var ComboboxIcon = /* @__PURE__ */ import_react.forwardRef(function ComboboxIcon(componentProps, forwardedRef) {
	const { className, render, ...elementProps } = componentProps;
	return useRenderElement("span", componentProps, {
		ref: forwardedRef,
		props: [{
			"aria-hidden": true,
			children: "▼"
		}, elementProps]
	});
});
ComboboxIcon.displayName = "ComboboxIcon";
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/clear/ComboboxClear.js
var stateAttributesMapping$22 = {
	...transitionStatusMapping,
	...triggerOpenStateMapping$1
};
/**
* Clears the value when clicked.
* Renders a `<button>` element.
*/
var ComboboxClear = /* @__PURE__ */ import_react.forwardRef(function ComboboxClear(componentProps, forwardedRef) {
	const { render, className, disabled: disabledProp = false, nativeButton = true, keepMounted = false, ...elementProps } = componentProps;
	const { disabled: fieldDisabled } = useFieldRootContext();
	const store = useComboboxRootContext();
	const selectionMode = useStore(store, selectors$2.selectionMode);
	const comboboxDisabled = useStore(store, selectors$2.disabled);
	const readOnly = useStore(store, selectors$2.readOnly);
	const open = useStore(store, selectors$2.open);
	const selectedValue = useStore(store, selectors$2.selectedValue);
	const hasSelectionChips = useStore(store, selectors$2.hasSelectionChips);
	const inputValue = useComboboxInputValueContext();
	let visible = false;
	if (selectionMode === "none") visible = inputValue !== "";
	else if (selectionMode === "single") visible = selectedValue != null;
	else visible = hasSelectionChips;
	const disabled = fieldDisabled || comboboxDisabled || disabledProp;
	const { buttonRef, getButtonProps } = useButton({
		native: nativeButton,
		disabled
	});
	const { mounted, transitionStatus, setMounted } = useTransitionStatus(visible);
	const state = {
		disabled,
		open,
		transitionStatus
	};
	useOpenChangeComplete({
		open: visible,
		ref: store.state.clearRef,
		onComplete() {
			if (!visible) setMounted(false);
		}
	});
	const element = useRenderElement("button", componentProps, {
		state,
		ref: [
			forwardedRef,
			buttonRef,
			store.state.clearRef
		],
		props: [
			{
				tabIndex: -1,
				children: "x",
				onMouseDown(event) {
					event.preventDefault();
				},
				onClick(event) {
					if (disabled || readOnly) return;
					const keyboardActiveRef = store.state.keyboardActiveRef;
					store.state.setInputValue("", createChangeEventDetails(clearPress, event.nativeEvent));
					if (selectionMode !== "none") {
						store.state.setSelectedValue(Array.isArray(selectedValue) ? [] : null, createChangeEventDetails(clearPress, event.nativeEvent));
						store.state.setIndices({
							activeIndex: null,
							selectedIndex: null,
							type: keyboardActiveRef.current ? "keyboard" : "pointer"
						});
					} else store.state.setIndices({
						activeIndex: null,
						type: keyboardActiveRef.current ? "keyboard" : "pointer"
					});
					store.state.inputRef.current?.focus();
				}
			},
			elementProps,
			getButtonProps
		],
		stateAttributesMapping: stateAttributesMapping$22
	});
	if (!(keepMounted || mounted)) return null;
	return element;
});
ComboboxClear.displayName = "ComboboxClear";
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/collection/GroupCollectionContext.js
var GroupCollectionContext = /* @__PURE__ */ import_react.createContext(null);
GroupCollectionContext.displayName = "GroupCollectionContext";
function useGroupCollectionContext() {
	return import_react.useContext(GroupCollectionContext);
}
function GroupCollectionProvider(props) {
	const { children, items } = props;
	const contextValue = import_react.useMemo(() => ({ items }), [items]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GroupCollectionContext.Provider, {
		value: contextValue,
		children
	});
}
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/collection/ComboboxCollection.js
/**
* Renders filtered list items.
* Doesn't render its own HTML element.
*
* If rendering a flat list, pass a function child to the `List` component instead, which implicitly wraps it.
*/
function ComboboxCollection(props) {
	const { children } = props;
	const { filteredItems } = useComboboxDerivedItemsContext();
	const groupContext = useGroupCollectionContext();
	const itemsToRender = groupContext ? groupContext.items : filteredItems;
	if (!itemsToRender) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_react.Fragment, { children: itemsToRender.map(children) });
}
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/list/ComboboxList.js
/**
* A list container for the items.
* Renders a `<div>` element.
*/
var ComboboxList = /* @__PURE__ */ import_react.forwardRef(function ComboboxList(componentProps, forwardedRef) {
	var _ComboboxCollection;
	const { render, className, children, ...elementProps } = componentProps;
	const store = useComboboxRootContext();
	const floatingRootContext = useComboboxFloatingContext();
	const hasPositionerContext = Boolean(useComboboxPositionerContext(true));
	const { filteredItems } = useComboboxDerivedItemsContext();
	const items = useStore(store, selectors$2.items);
	const labelsRef = useStore(store, selectors$2.labelsRef);
	const listRef = useStore(store, selectors$2.listRef);
	const selectionMode = useStore(store, selectors$2.selectionMode);
	const grid = useStore(store, selectors$2.grid);
	const popupProps = useStore(store, selectors$2.popupProps);
	const disabled = useStore(store, selectors$2.disabled);
	const readOnly = useStore(store, selectors$2.readOnly);
	const virtualized = useStore(store, selectors$2.virtualized);
	const multiple = selectionMode === "multiple";
	const empty = filteredItems.length === 0;
	const setPositionerElement = useStableCallback((element) => {
		store.set("positionerElement", element);
	});
	const setListElement = useStableCallback((element) => {
		store.set("listElement", element);
	});
	const resolvedChildren = import_react.useMemo(() => {
		if (typeof children === "function") return _ComboboxCollection || (_ComboboxCollection = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ComboboxCollection, { children }));
		return children;
	}, [children]);
	const state = { empty };
	const floatingId = floatingRootContext.useState("floatingId");
	const element = useRenderElement("div", componentProps, {
		state,
		ref: [
			forwardedRef,
			setListElement,
			hasPositionerContext ? null : setPositionerElement
		],
		props: [
			popupProps,
			{
				children: resolvedChildren,
				tabIndex: -1,
				id: floatingId,
				role: grid ? "grid" : "listbox",
				"aria-multiselectable": multiple ? "true" : void 0,
				onKeyDown(event) {
					if (disabled || readOnly) return;
					if (event.key === "Enter") {
						const activeIndex = store.state.activeIndex;
						if (activeIndex == null) return;
						stopEvent(event);
						const nativeEvent = event.nativeEvent;
						const listItem = store.state.listRef.current[activeIndex];
						if (listItem) {
							store.state.selectionEventRef.current = nativeEvent;
							listItem.click();
							store.state.selectionEventRef.current = null;
						}
					}
				},
				onKeyDownCapture() {
					store.state.keyboardActiveRef.current = true;
				},
				onPointerMoveCapture() {
					store.state.keyboardActiveRef.current = false;
				}
			},
			elementProps
		]
	});
	if (virtualized) return element;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CompositeList, {
		elementsRef: listRef,
		labelsRef: items ? void 0 : labelsRef,
		children: element
	});
});
ComboboxList.displayName = "ComboboxList";
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/status/ComboboxStatus.js
/**
* Displays a status message whose content changes are announced politely to screen readers.
* Useful for conveying the status of an asynchronously loaded list.
* Renders a `<div>` element.
*/
var ComboboxStatus = /* @__PURE__ */ import_react.forwardRef(function ComboboxStatus(componentProps, forwardedRef) {
	const { render, className, ...elementProps } = componentProps;
	return useRenderElement("div", componentProps, {
		ref: forwardedRef,
		props: [{
			role: "status",
			"aria-live": "polite",
			"aria-atomic": true
		}, elementProps]
	});
});
ComboboxStatus.displayName = "ComboboxStatus";
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/portal/ComboboxPortalContext.js
var ComboboxPortalContext = /* @__PURE__ */ import_react.createContext(void 0);
ComboboxPortalContext.displayName = "ComboboxPortalContext";
function useComboboxPortalContext() {
	const context = import_react.useContext(ComboboxPortalContext);
	if (context === void 0) throw new Error("Base UI: <Combobox.Portal> is missing.");
	return context;
}
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/portal/ComboboxPortal.js
/**
* A portal element that moves the popup to a different part of the DOM.
* By default, the portal element is appended to `<body>`.
* Renders a `<div>` element.
*/
var ComboboxPortal = /* @__PURE__ */ import_react.forwardRef(function ComboboxPortal(props, forwardedRef) {
	const { keepMounted = false, ...portalProps } = props;
	const store = useComboboxRootContext();
	const mounted = useStore(store, selectors$2.mounted);
	const forceMounted = useStore(store, selectors$2.forceMounted);
	if (!(mounted || keepMounted || forceMounted)) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ComboboxPortalContext.Provider, {
		value: keepMounted,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FloatingPortal, {
			ref: forwardedRef,
			...portalProps
		})
	});
});
ComboboxPortal.displayName = "ComboboxPortal";
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/backdrop/ComboboxBackdrop.js
var stateAttributesMapping$21 = {
	...popupStateMapping,
	...transitionStatusMapping
};
/**
* An overlay displayed beneath the popup.
* Renders a `<div>` element.
*/
var ComboboxBackdrop = /* @__PURE__ */ import_react.forwardRef(function ComboboxBackdrop(componentProps, forwardedRef) {
	const { className, render, ...elementProps } = componentProps;
	const store = useComboboxRootContext();
	const open = useStore(store, selectors$2.open);
	const mounted = useStore(store, selectors$2.mounted);
	return useRenderElement("div", componentProps, {
		state: {
			open,
			transitionStatus: useStore(store, selectors$2.transitionStatus)
		},
		ref: forwardedRef,
		stateAttributesMapping: stateAttributesMapping$21,
		props: [{
			role: "presentation",
			hidden: !mounted,
			style: {
				userSelect: "none",
				WebkitUserSelect: "none"
			}
		}, elementProps]
	});
});
ComboboxBackdrop.displayName = "ComboboxBackdrop";
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/positioner/ComboboxPositioner.js
/**
* Positions the popup against the trigger.
* Renders a `<div>` element.
*/
var ComboboxPositioner = /* @__PURE__ */ import_react.forwardRef(function ComboboxPositioner(componentProps, forwardedRef) {
	const { render, className, anchor, positionMethod = "absolute", side = "bottom", align = "center", sideOffset = 0, alignOffset = 0, collisionBoundary = "clipping-ancestors", collisionPadding = 5, arrowPadding = 5, sticky = false, disableAnchorTracking = false, collisionAvoidance = DROPDOWN_COLLISION_AVOIDANCE, ...elementProps } = componentProps;
	const store = useComboboxRootContext();
	const { filteredItems } = useComboboxDerivedItemsContext();
	const floatingRootContext = useComboboxFloatingContext();
	const keepMounted = useComboboxPortalContext();
	const modal = useStore(store, selectors$2.modal);
	const open = useStore(store, selectors$2.open);
	const mounted = useStore(store, selectors$2.mounted);
	const openMethod = useStore(store, selectors$2.openMethod);
	const triggerElement = useStore(store, selectors$2.triggerElement);
	const inputElement = useStore(store, selectors$2.inputElement);
	const inputGroupElement = useStore(store, selectors$2.inputGroupElement);
	const inputInsidePopup = useStore(store, selectors$2.inputInsidePopup);
	const transitionStatus = useStore(store, selectors$2.transitionStatus);
	const empty = filteredItems.length === 0;
	const positioning = useAnchorPositioning({
		anchor: anchor ?? (inputInsidePopup ? triggerElement : inputGroupElement ?? inputElement),
		floatingRootContext,
		positionMethod,
		mounted,
		side,
		sideOffset,
		align,
		alignOffset,
		arrowPadding,
		collisionBoundary,
		collisionPadding,
		sticky,
		disableAnchorTracking,
		keepMounted,
		collisionAvoidance,
		lazyFlip: true
	});
	useScrollLock(open && modal && openMethod !== "touch", triggerElement);
	const defaultProps = import_react.useMemo(() => {
		const style = { ...positioning.positionerStyles };
		if (!open) style.pointerEvents = "none";
		return {
			role: "presentation",
			hidden: !mounted,
			style
		};
	}, [
		open,
		mounted,
		positioning.positionerStyles
	]);
	const state = {
		open,
		side: positioning.side,
		align: positioning.align,
		anchorHidden: positioning.anchorHidden,
		empty
	};
	useIsoLayoutEffect(() => {
		store.set("popupSide", positioning.side);
	}, [store, positioning.side]);
	const contextValue = import_react.useMemo(() => ({
		side: positioning.side,
		align: positioning.align,
		arrowRef: positioning.arrowRef,
		arrowUncentered: positioning.arrowUncentered,
		arrowStyles: positioning.arrowStyles,
		anchorHidden: positioning.anchorHidden,
		isPositioned: positioning.isPositioned
	}), [
		positioning.side,
		positioning.align,
		positioning.arrowRef,
		positioning.arrowUncentered,
		positioning.arrowStyles,
		positioning.anchorHidden,
		positioning.isPositioned
	]);
	const element = useRenderElement("div", componentProps, {
		state,
		ref: [forwardedRef, useStableCallback((element) => {
			store.set("positionerElement", element);
		})],
		props: [
			defaultProps,
			getDisabledMountTransitionStyles(transitionStatus),
			elementProps
		],
		stateAttributesMapping: popupStateMapping
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(ComboboxPositionerContext.Provider, {
		value: contextValue,
		children: [mounted && modal && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(InternalBackdrop, {
			inert: inertValue(!open),
			cutout: inputGroupElement ?? inputElement ?? triggerElement
		}), element]
	});
});
ComboboxPositioner.displayName = "ComboboxPositioner";
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/popup/ComboboxPopup.js
var stateAttributesMapping$20 = {
	...popupStateMapping,
	...transitionStatusMapping
};
/**
* A container for the list.
* Renders a `<div>` element.
*/
var ComboboxPopup = /* @__PURE__ */ import_react.forwardRef(function ComboboxPopup(componentProps, forwardedRef) {
	const { render, className, initialFocus, finalFocus, ...elementProps } = componentProps;
	const store = useComboboxRootContext();
	const positioning = useComboboxPositionerContext();
	const floatingRootContext = useComboboxFloatingContext();
	const { filteredItems } = useComboboxDerivedItemsContext();
	const mounted = useStore(store, selectors$2.mounted);
	const open = useStore(store, selectors$2.open);
	const openMethod = useStore(store, selectors$2.openMethod);
	const transitionStatus = useStore(store, selectors$2.transitionStatus);
	const inputInsidePopup = useStore(store, selectors$2.inputInsidePopup);
	const inputElement = useStore(store, selectors$2.inputElement);
	const modal = useStore(store, selectors$2.modal);
	const empty = filteredItems.length === 0;
	useOpenChangeComplete({
		open,
		ref: store.state.popupRef,
		onComplete() {
			if (open) store.state.onOpenChangeComplete(true);
		}
	});
	const element = useRenderElement("div", componentProps, {
		state: {
			open,
			side: positioning.side,
			align: positioning.align,
			anchorHidden: positioning.anchorHidden,
			transitionStatus,
			empty
		},
		ref: [forwardedRef, store.state.popupRef],
		props: [
			{
				role: inputInsidePopup ? "dialog" : "presentation",
				tabIndex: -1,
				onFocus(event) {
					const target = getTarget(event.nativeEvent);
					if (openMethod !== "touch" && (contains(store.state.listElement, target) || target === event.currentTarget)) store.state.inputRef.current?.focus();
				}
			},
			getDisabledMountTransitionStyles(transitionStatus),
			elementProps
		],
		stateAttributesMapping: stateAttributesMapping$20
	});
	const resolvedInitialFocus = initialFocus === void 0 ? inputInsidePopup ? (interactionType) => interactionType === "touch" ? store.state.popupRef.current : inputElement : false : initialFocus;
	let resolvedFinalFocus;
	if (finalFocus != null) resolvedFinalFocus = finalFocus;
	else resolvedFinalFocus = inputInsidePopup ? void 0 : false;
	const focusManagerModal = !inputInsidePopup || modal;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FloatingFocusManager, {
		context: floatingRootContext,
		disabled: !mounted,
		modal: focusManagerModal,
		openInteractionType: openMethod,
		initialFocus: resolvedInitialFocus,
		returnFocus: resolvedFinalFocus,
		getInsideElements: () => [store.state.startDismissRef.current, store.state.endDismissRef.current],
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [element, focusManagerModal && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ComboboxInternalDismissButton, { ref: store.state.endDismissRef })] })
	});
});
ComboboxPopup.displayName = "ComboboxPopup";
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/arrow/ComboboxArrow.js
/**
* Displays an element positioned against the anchor.
* Renders a `<div>` element.
*/
var ComboboxArrow = /* @__PURE__ */ import_react.forwardRef(function ComboboxArrow(componentProps, forwardedRef) {
	const { className, render, ...elementProps } = componentProps;
	const store = useComboboxRootContext();
	const { arrowRef, side, align, arrowUncentered, arrowStyles } = useComboboxPositionerContext();
	const state = {
		open: useStore(store, selectors$2.open),
		side,
		align,
		uncentered: arrowUncentered
	};
	return useRenderElement("div", componentProps, {
		ref: [arrowRef, forwardedRef],
		stateAttributesMapping: popupStateMapping,
		state,
		props: {
			style: arrowStyles,
			"aria-hidden": true,
			...elementProps
		}
	});
});
ComboboxArrow.displayName = "ComboboxArrow";
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/group/ComboboxGroupContext.js
var ComboboxGroupContext = /* @__PURE__ */ import_react.createContext(void 0);
ComboboxGroupContext.displayName = "ComboboxGroupContext";
function useComboboxGroupContext() {
	const context = import_react.useContext(ComboboxGroupContext);
	if (context === void 0) throw new Error("Base UI: ComboboxGroupContext is missing. ComboboxGroup parts must be placed within <Combobox.Group>.");
	return context;
}
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/group/ComboboxGroup.js
/**
* Groups related items with the corresponding label.
* Renders a `<div>` element.
*/
var ComboboxGroup = /* @__PURE__ */ import_react.forwardRef(function ComboboxGroup(componentProps, forwardedRef) {
	const { render, className, items, ...elementProps } = componentProps;
	const [labelId, setLabelId] = import_react.useState();
	const contextValue = import_react.useMemo(() => ({
		labelId,
		setLabelId,
		items
	}), [
		labelId,
		setLabelId,
		items
	]);
	const element = useRenderElement("div", componentProps, {
		ref: forwardedRef,
		props: [{
			role: "group",
			"aria-labelledby": labelId
		}, elementProps]
	});
	const wrappedElement = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ComboboxGroupContext.Provider, {
		value: contextValue,
		children: element
	});
	if (items) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(GroupCollectionProvider, {
		items,
		children: wrappedElement
	});
	return wrappedElement;
});
ComboboxGroup.displayName = "ComboboxGroup";
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/group-label/ComboboxGroupLabel.js
/**
* An accessible label that is automatically associated with its parent group.
* Renders a `<div>` element.
*/
var ComboboxGroupLabel = /* @__PURE__ */ import_react.forwardRef(function ComboboxGroupLabel(componentProps, forwardedRef) {
	const { render, className, id: idProp, ...elementProps } = componentProps;
	const { setLabelId } = useComboboxGroupContext();
	const id = useBaseUiId(idProp);
	useIsoLayoutEffect(() => {
		setLabelId(id);
		return () => {
			setLabelId(void 0);
		};
	}, [id, setLabelId]);
	return useRenderElement("div", componentProps, {
		ref: forwardedRef,
		props: [{ id }, elementProps]
	});
});
ComboboxGroupLabel.displayName = "ComboboxGroupLabel";
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/item/ComboboxItemContext.js
var ComboboxItemContext = /* @__PURE__ */ import_react.createContext(void 0);
ComboboxItemContext.displayName = "ComboboxItemContext";
function useComboboxItemContext() {
	const context = import_react.useContext(ComboboxItemContext);
	if (!context) throw new Error("Base UI: ComboboxItemContext is missing. ComboboxItem parts must be placed within <Combobox.Item>.");
	return context;
}
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/row/ComboboxRowContext.js
var ComboboxRowContext = /* @__PURE__ */ import_react.createContext(false);
ComboboxRowContext.displayName = "ComboboxRowContext";
function useComboboxRowContext() {
	return import_react.useContext(ComboboxRowContext);
}
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/item/ComboboxItem.js
/**
* An individual item in the list.
* Renders a `<div>` element.
*/
var import_react_dom = /* @__PURE__ */ __toESM(require_react_dom(), 1);
var ComboboxItem = /* @__PURE__ */ import_react.memo(/* @__PURE__ */ import_react.forwardRef(function ComboboxItem(componentProps, forwardedRef) {
	const { render, className, value: itemValue = null, index: indexProp, disabled = false, nativeButton = false, ...elementProps } = componentProps;
	const didPointerDownRef = import_react.useRef(false);
	const textRef = import_react.useRef(null);
	const listItem = useCompositeListItem({
		index: indexProp,
		textRef,
		indexGuessBehavior: IndexGuessBehavior.GuessFromOrder
	});
	const store = useComboboxRootContext();
	const isRow = useComboboxRowContext();
	const { flatFilteredItems, hasItems } = useComboboxDerivedItemsContext();
	const open = useStore(store, selectors$2.open);
	const selectionMode = useStore(store, selectors$2.selectionMode);
	const readOnly = useStore(store, selectors$2.readOnly);
	const virtualized = useStore(store, selectors$2.virtualized);
	const isItemEqualToValue = useStore(store, selectors$2.isItemEqualToValue);
	const selectable = selectionMode !== "none";
	const index = indexProp ?? (virtualized ? findItemIndex(flatFilteredItems, itemValue, isItemEqualToValue) : listItem.index);
	const hasRegistered = listItem.index !== -1;
	const rootId = useStore(store, selectors$2.id);
	const highlighted = useStore(store, selectors$2.isActive, index);
	const matchesSelectedValue = useStore(store, selectors$2.isSelected, itemValue);
	const getItemProps = useStore(store, selectors$2.getItemProps);
	const itemRef = import_react.useRef(null);
	const id = rootId != null && hasRegistered ? `${rootId}-${index}` : void 0;
	const selected = matchesSelectedValue && selectable;
	useIsoLayoutEffect(() => {
		if (!(hasRegistered && (virtualized || indexProp != null))) return;
		const list = store.state.listRef.current;
		list[index] = itemRef.current;
		return () => {
			delete list[index];
		};
	}, [
		hasRegistered,
		virtualized,
		index,
		indexProp,
		store
	]);
	useIsoLayoutEffect(() => {
		if (!hasRegistered || hasItems) return;
		const visibleMap = store.state.valuesRef.current;
		visibleMap[index] = itemValue;
		if (selectionMode !== "none") store.state.allValuesRef.current.push(itemValue);
		return () => {
			delete visibleMap[index];
		};
	}, [
		hasRegistered,
		hasItems,
		index,
		itemValue,
		store,
		selectionMode
	]);
	useIsoLayoutEffect(() => {
		if (!open) {
			didPointerDownRef.current = false;
			return;
		}
		if (!hasRegistered || hasItems) return;
		const selectedValue = store.state.selectedValue;
		if (compareItemEquality(itemValue, Array.isArray(selectedValue) ? selectedValue[selectedValue.length - 1] : selectedValue, isItemEqualToValue)) store.set("selectedIndex", index);
	}, [
		hasRegistered,
		hasItems,
		open,
		store,
		index,
		itemValue,
		isItemEqualToValue
	]);
	const state = {
		disabled,
		selected,
		highlighted
	};
	const rootProps = getItemProps({
		active: highlighted,
		selected
	});
	rootProps.id = void 0;
	rootProps.onFocus = void 0;
	const { getButtonProps, buttonRef } = useButton({
		disabled,
		focusableWhenDisabled: true,
		native: nativeButton,
		composite: true
	});
	function commitSelection(nativeEvent) {
		function selectItem() {
			store.state.handleSelection(nativeEvent, itemValue);
		}
		if (store.state.submitOnItemClick) {
			import_react_dom.flushSync(selectItem);
			store.state.requestSubmit();
		} else selectItem();
	}
	const defaultProps = {
		id,
		role: isRow ? "gridcell" : "option",
		"aria-selected": selectable ? selected : void 0,
		tabIndex: void 0,
		onPointerDownCapture(event) {
			didPointerDownRef.current = true;
			event.preventDefault();
		},
		onClick(event) {
			if (disabled || readOnly) return;
			commitSelection(event.nativeEvent);
		},
		onMouseUp(event) {
			const pointerStartedOnItem = didPointerDownRef.current;
			didPointerDownRef.current = false;
			if (disabled || readOnly || event.button !== 0 || pointerStartedOnItem || !highlighted) return;
			commitSelection(event.nativeEvent);
		}
	};
	const element = useRenderElement("div", componentProps, {
		ref: [
			buttonRef,
			forwardedRef,
			listItem.ref,
			itemRef
		],
		state,
		props: [
			rootProps,
			defaultProps,
			elementProps,
			getButtonProps
		]
	});
	const contextValue = import_react.useMemo(() => ({
		selected,
		textRef
	}), [selected, textRef]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ComboboxItemContext.Provider, {
		value: contextValue,
		children: element
	});
}));
ComboboxItem.displayName = "ComboboxItem";
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/row/ComboboxRow.js
/**
* Displays a single row of items in a grid list.
* Enable `grid` on the root component to turn the listbox into a grid.
* Renders a `<div>` element.
*/
var ComboboxRow = /* @__PURE__ */ import_react.forwardRef(function ComboboxRow(componentProps, forwardedRef) {
	const { render, className, ...elementProps } = componentProps;
	const element = useRenderElement("div", componentProps, {
		ref: forwardedRef,
		props: [{ role: "row" }, elementProps]
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ComboboxRowContext.Provider, {
		value: true,
		children: element
	});
});
ComboboxRow.displayName = "ComboboxRow";
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/empty/ComboboxEmpty.js
/**
* Renders its children only when the list is empty.
* Requires the `items` prop on the root component.
* Announces changes politely to screen readers.
* Renders a `<div>` element.
*/
var ComboboxEmpty = /* @__PURE__ */ import_react.forwardRef(function ComboboxEmpty(componentProps, forwardedRef) {
	const { render, className, children: childrenProp, ...elementProps } = componentProps;
	const { filteredItems } = useComboboxDerivedItemsContext();
	const store = useComboboxRootContext();
	const children = filteredItems.length === 0 ? childrenProp : null;
	return useRenderElement("div", componentProps, {
		ref: [forwardedRef, store.state.emptyRef],
		props: [{
			children,
			role: "status",
			"aria-live": "polite",
			"aria-atomic": true
		}, elementProps]
	});
});
ComboboxEmpty.displayName = "ComboboxEmpty";
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/root/utils/useFilteredItems.js
/**
* Returns the internally filtered items.
*/
function useFilteredItems() {
	return useComboboxDerivedItemsContext().filteredItems;
}
//#endregion
//#region node_modules/@base-ui/react/esm/autocomplete/index.parts.js
var index_parts_exports$2 = /* @__PURE__ */ __exportAll({
	Arrow: () => ComboboxArrow,
	Backdrop: () => ComboboxBackdrop,
	Clear: () => ComboboxClear,
	Collection: () => ComboboxCollection,
	Empty: () => ComboboxEmpty,
	Group: () => ComboboxGroup,
	GroupLabel: () => ComboboxGroupLabel,
	Icon: () => ComboboxIcon,
	Input: () => ComboboxInput,
	InputGroup: () => ComboboxInputGroup,
	Item: () => ComboboxItem,
	List: () => ComboboxList,
	Popup: () => ComboboxPopup,
	Portal: () => ComboboxPortal,
	Positioner: () => ComboboxPositioner,
	Root: () => AutocompleteRoot,
	Row: () => ComboboxRow,
	Separator: () => Separator,
	Status: () => ComboboxStatus,
	Trigger: () => ComboboxTrigger,
	Value: () => AutocompleteValue,
	useFilter: () => useCoreFilter,
	useFilteredItems: () => useFilteredItems
});
//#endregion
//#region node_modules/@base-ui/react/esm/checkbox/root/CheckboxRootDataAttributes.js
var CheckboxRootDataAttributes = /* @__PURE__ */ function(CheckboxRootDataAttributes) {
	/**
	* Present when the checkbox is checked.
	*/
	CheckboxRootDataAttributes["checked"] = "data-checked";
	/**
	* Present when the checkbox is not checked.
	*/
	CheckboxRootDataAttributes["unchecked"] = "data-unchecked";
	/**
	* Present when the checkbox is in an indeterminate state.
	*/
	CheckboxRootDataAttributes["indeterminate"] = "data-indeterminate";
	/**
	* Present when the checkbox is disabled.
	*/
	CheckboxRootDataAttributes["disabled"] = "data-disabled";
	/**
	* Present when the checkbox is readonly.
	*/
	CheckboxRootDataAttributes["readonly"] = "data-readonly";
	/**
	* Present when the checkbox is required.
	*/
	CheckboxRootDataAttributes["required"] = "data-required";
	/**
	* Present when the checkbox is in valid state (when wrapped in Field.Root).
	*/
	CheckboxRootDataAttributes["valid"] = "data-valid";
	/**
	* Present when the checkbox is in invalid state (when wrapped in Field.Root).
	*/
	CheckboxRootDataAttributes["invalid"] = "data-invalid";
	/**
	* Present when the checkbox has been touched (when wrapped in Field.Root).
	*/
	CheckboxRootDataAttributes["touched"] = "data-touched";
	/**
	* Present when the checkbox's value has changed (when wrapped in Field.Root).
	*/
	CheckboxRootDataAttributes["dirty"] = "data-dirty";
	/**
	* Present when the checkbox is checked (when wrapped in Field.Root).
	*/
	CheckboxRootDataAttributes["filled"] = "data-filled";
	/**
	* Present when the checkbox is focused (when wrapped in Field.Root).
	*/
	CheckboxRootDataAttributes["focused"] = "data-focused";
	return CheckboxRootDataAttributes;
}({});
//#endregion
//#region node_modules/@base-ui/react/esm/checkbox/utils/useStateAttributesMapping.js
function useStateAttributesMapping(state) {
	return import_react.useMemo(() => ({
		checked(value) {
			if (state.indeterminate) return {};
			if (value) return { [CheckboxRootDataAttributes.checked]: "" };
			return { [CheckboxRootDataAttributes.unchecked]: "" };
		},
		...fieldValidityMapping
	}), [state.indeterminate]);
}
//#endregion
//#region node_modules/@base-ui/react/esm/field/item/FieldItemContext.js
var FieldItemContext = /* @__PURE__ */ import_react.createContext({ disabled: false });
FieldItemContext.displayName = "FieldItemContext";
function useFieldItemContext() {
	return import_react.useContext(FieldItemContext);
}
//#endregion
//#region node_modules/@base-ui/react/esm/checkbox-group/CheckboxGroupContext.js
var CheckboxGroupContext = /* @__PURE__ */ import_react.createContext(void 0);
CheckboxGroupContext.displayName = "CheckboxGroupContext";
function useCheckboxGroupContext(optional = true) {
	const context = import_react.useContext(CheckboxGroupContext);
	if (context === void 0 && !optional) throw new Error("Base UI: CheckboxGroupContext is missing. CheckboxGroup parts must be placed within <CheckboxGroup>.");
	return context;
}
//#endregion
//#region node_modules/@base-ui/react/esm/checkbox/root/CheckboxRootContext.js
var CheckboxRootContext = /* @__PURE__ */ import_react.createContext(void 0);
CheckboxRootContext.displayName = "CheckboxRootContext";
function useCheckboxRootContext() {
	const context = import_react.useContext(CheckboxRootContext);
	if (context === void 0) throw new Error("Base UI: CheckboxRootContext is missing. Checkbox parts must be placed within <Checkbox.Root>.");
	return context;
}
//#endregion
//#region node_modules/@base-ui/react/esm/checkbox/root/CheckboxRoot.js
var PARENT_CHECKBOX = "data-parent";
/**
* Represents the checkbox itself.
* Renders a `<span>` element and a hidden `<input>` beside.
*
* Documentation: [Base UI Checkbox](https://base-ui.com/react/components/checkbox)
*/
var CheckboxRoot = /* @__PURE__ */ import_react.forwardRef(function CheckboxRoot(componentProps, forwardedRef) {
	const { checked: checkedProp, className, defaultChecked = false, "aria-labelledby": ariaLabelledByProp, disabled: disabledProp = false, id: idProp, indeterminate = false, inputRef: inputRefProp, name: nameProp, onCheckedChange: onCheckedChangeProp, parent = false, readOnly = false, render, required = false, uncheckedValue, value: valueProp, nativeButton = false, ...elementProps } = componentProps;
	const { clearErrors } = useFormContext();
	const { disabled: rootDisabled, name: fieldName, setDirty, setFilled, setFocused, setTouched, state: fieldState, validationMode, validityData, shouldValidateOnChange, validation: localValidation } = useFieldRootContext();
	const fieldItemContext = useFieldItemContext();
	const { labelId, controlId, registerControlId, getDescriptionProps } = useLabelableContext();
	const groupContext = useCheckboxGroupContext();
	const parentContext = groupContext?.parent;
	const isGroupedWithParent = parentContext && groupContext.allValues;
	const disabled = rootDisabled || fieldItemContext.disabled || groupContext?.disabled || disabledProp;
	const name = fieldName ?? nameProp;
	const value = valueProp ?? name;
	const id = useBaseUiId();
	const parentId = useBaseUiId();
	let inputId = controlId;
	if (isGroupedWithParent) inputId = parent ? parentId : `${parentContext.id}-${value}`;
	else if (idProp) inputId = idProp;
	let groupProps = {};
	if (isGroupedWithParent) {
		if (parent) groupProps = groupContext.parent.getParentProps();
		else if (value) groupProps = groupContext.parent.getChildProps(value);
	}
	const onCheckedChange = useStableCallback(onCheckedChangeProp);
	const { checked: groupChecked = checkedProp, indeterminate: groupIndeterminate = indeterminate, onCheckedChange: groupOnChange, ...otherGroupProps } = groupProps;
	const groupValue = groupContext?.value;
	const setGroupValue = groupContext?.setValue;
	const defaultGroupValue = groupContext?.defaultValue;
	const controlRef = import_react.useRef(null);
	const controlSourceRef = useRefWithInit(() => Symbol("checkbox-control"));
	const hasRegisteredRef = import_react.useRef(false);
	const { getButtonProps, buttonRef } = useButton({
		disabled,
		native: nativeButton
	});
	const validation = groupContext?.validation ?? localValidation;
	const [checked, setCheckedState] = useControlled({
		controlled: value && groupValue && !parent ? groupValue.includes(value) : groupChecked,
		default: value && defaultGroupValue && !parent ? defaultGroupValue.includes(value) : defaultChecked,
		name: "Checkbox",
		state: "checked"
	});
	useIsoLayoutEffect(() => {
		if (registerControlId === NOOP) return;
		hasRegisteredRef.current = true;
		registerControlId(controlSourceRef.current, inputId);
	}, [
		inputId,
		groupContext,
		registerControlId,
		parent,
		controlSourceRef
	]);
	import_react.useEffect(() => {
		const controlSource = controlSourceRef.current;
		return () => {
			if (!hasRegisteredRef.current || registerControlId === NOOP) return;
			hasRegisteredRef.current = false;
			registerControlId(controlSource, void 0);
		};
	}, [registerControlId, controlSourceRef]);
	useField({
		enabled: !groupContext,
		id,
		commit: validation.commit,
		value: checked,
		controlRef,
		name,
		getValue: () => checked
	});
	const inputRef = import_react.useRef(null);
	const mergedInputRef = useMergedRefs(inputRefProp, inputRef, validation.inputRef);
	const ariaLabelledBy = useAriaLabelledBy(ariaLabelledByProp, labelId, inputRef, !nativeButton, inputId ?? void 0);
	useIsoLayoutEffect(() => {
		if (inputRef.current) {
			inputRef.current.indeterminate = groupIndeterminate;
			if (checked) setFilled(true);
		}
	}, [
		checked,
		groupIndeterminate,
		setFilled
	]);
	useValueChanged(checked, () => {
		if (groupContext && !parent) return;
		clearErrors(name);
		setFilled(checked);
		setDirty(checked !== validityData.initialValue);
		if (shouldValidateOnChange()) validation.commit(checked);
		else validation.commit(checked, true);
	});
	const inputProps = mergeProps({
		checked,
		disabled,
		name: parent ? void 0 : name,
		id: nativeButton ? void 0 : inputId ?? void 0,
		required,
		ref: mergedInputRef,
		style: name ? visuallyHiddenInput : visuallyHidden,
		tabIndex: -1,
		type: "checkbox",
		"aria-hidden": true,
		onChange(event) {
			if (event.nativeEvent.defaultPrevented) return;
			const nextChecked = event.target.checked;
			const details = createChangeEventDetails(none, event.nativeEvent);
			groupOnChange?.(nextChecked, details);
			onCheckedChange(nextChecked, details);
			if (details.isCanceled) return;
			setCheckedState(nextChecked);
			if (value && groupValue && setGroupValue && !parent) setGroupValue(nextChecked ? [...groupValue, value] : groupValue.filter((item) => item !== value), details);
		},
		onFocus() {
			controlRef.current?.focus();
		}
	}, valueProp !== void 0 ? { value: (groupContext ? checked && valueProp : valueProp) || "" } : EMPTY_OBJECT, getDescriptionProps, groupContext ? validation.getValidationProps : validation.getInputValidationProps);
	const computedChecked = isGroupedWithParent ? Boolean(groupChecked) : checked;
	const computedIndeterminate = isGroupedWithParent ? groupIndeterminate || indeterminate : indeterminate;
	import_react.useEffect(() => {
		if (!parentContext || !value) return;
		const disabledStates = parentContext.disabledStatesRef.current;
		disabledStates.set(value, disabled);
		return () => {
			disabledStates.delete(value);
		};
	}, [
		parentContext,
		disabled,
		value
	]);
	const state = import_react.useMemo(() => ({
		...fieldState,
		checked: computedChecked,
		disabled,
		readOnly,
		required,
		indeterminate: computedIndeterminate
	}), [
		fieldState,
		computedChecked,
		disabled,
		readOnly,
		required,
		computedIndeterminate
	]);
	const stateAttributesMapping = useStateAttributesMapping(state);
	const element = useRenderElement("span", componentProps, {
		state,
		ref: [
			buttonRef,
			controlRef,
			forwardedRef,
			groupContext?.registerControlRef
		],
		props: [
			{
				id: nativeButton ? inputId ?? void 0 : id,
				role: "checkbox",
				"aria-checked": groupIndeterminate ? "mixed" : checked,
				"aria-readonly": readOnly || void 0,
				"aria-required": required || void 0,
				"aria-labelledby": ariaLabelledBy,
				[PARENT_CHECKBOX]: parent ? "" : void 0,
				onFocus() {
					setFocused(true);
				},
				onBlur() {
					const inputEl = inputRef.current;
					if (!inputEl) return;
					setTouched(true);
					setFocused(false);
					if (validationMode === "onBlur") validation.commit(groupContext ? groupValue : inputEl.checked);
				},
				onClick(event) {
					if (readOnly || disabled) return;
					event.preventDefault();
					inputRef.current?.dispatchEvent(new PointerEvent("click", {
						bubbles: true,
						shiftKey: event.shiftKey,
						ctrlKey: event.ctrlKey,
						altKey: event.altKey,
						metaKey: event.metaKey
					}));
				}
			},
			getDescriptionProps,
			validation.getValidationProps,
			elementProps,
			otherGroupProps,
			getButtonProps
		],
		stateAttributesMapping
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(CheckboxRootContext.Provider, {
		value: state,
		children: [
			element,
			!checked && !groupContext && name && !parent && uncheckedValue !== void 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", {
				type: "hidden",
				name,
				value: uncheckedValue
			}),
			/* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", { ...inputProps })
		]
	});
});
CheckboxRoot.displayName = "CheckboxRoot";
//#endregion
//#region node_modules/@base-ui/react/esm/checkbox/indicator/CheckboxIndicator.js
/**
* Indicates whether the checkbox is ticked.
* Renders a `<span>` element.
*
* Documentation: [Base UI Checkbox](https://base-ui.com/react/components/checkbox)
*/
var CheckboxIndicator = /* @__PURE__ */ import_react.forwardRef(function CheckboxIndicator(componentProps, forwardedRef) {
	const { render, className, keepMounted = false, ...elementProps } = componentProps;
	const rootState = useCheckboxRootContext();
	const rendered = rootState.checked || rootState.indeterminate;
	const { mounted, transitionStatus, setMounted } = useTransitionStatus(rendered);
	const indicatorRef = import_react.useRef(null);
	const state = {
		...rootState,
		transitionStatus
	};
	useOpenChangeComplete({
		open: rendered,
		ref: indicatorRef,
		onComplete() {
			if (!rendered) setMounted(false);
		}
	});
	const baseStateAttributesMapping = useStateAttributesMapping(rootState);
	const stateAttributesMapping = import_react.useMemo(() => ({
		...baseStateAttributesMapping,
		...transitionStatusMapping,
		...fieldValidityMapping
	}), [baseStateAttributesMapping]);
	const shouldRender = keepMounted || mounted;
	const element = useRenderElement("span", componentProps, {
		ref: [forwardedRef, indicatorRef],
		state,
		stateAttributesMapping,
		props: elementProps
	});
	if (!shouldRender) return null;
	return element;
});
CheckboxIndicator.displayName = "CheckboxIndicator";
//#endregion
//#region node_modules/@base-ui/react/esm/checkbox/index.parts.js
var index_parts_exports$4 = /* @__PURE__ */ __exportAll({
	Indicator: () => CheckboxIndicator,
	Root: () => CheckboxRoot
});
//#endregion
//#region node_modules/@base-ui/react/esm/checkbox-group/useCheckboxGroupParent.js
var EMPTY$1 = [];
function useCheckboxGroupParent(params) {
	const { allValues = EMPTY$1, value = EMPTY$1, onValueChange: onValueChangeProp } = params;
	const uncontrolledStateRef = import_react.useRef(value);
	const disabledStatesRef = import_react.useRef(/* @__PURE__ */ new Map());
	const [status, setStatus] = import_react.useState("mixed");
	const id = useBaseUiId();
	const checked = value.length === allValues.length;
	const indeterminate = value.length !== allValues.length && value.length > 0;
	const onValueChange = useStableCallback(onValueChangeProp);
	const getParentProps = import_react.useCallback(() => ({
		id,
		indeterminate,
		checked,
		"aria-controls": allValues.map((v) => `${id}-${v}`).join(" "),
		onCheckedChange(_, eventDetails) {
			const uncontrolledState = uncontrolledStateRef.current;
			const none = allValues.filter((v) => disabledStatesRef.current.get(v) && uncontrolledState.includes(v));
			const all = allValues.filter((v) => !disabledStatesRef.current.get(v) || disabledStatesRef.current.get(v) && uncontrolledState.includes(v));
			if (uncontrolledState.length === all.length || uncontrolledState.length === 0) {
				if (value.length === all.length) onValueChange(none, eventDetails);
				else onValueChange(all, eventDetails);
				return;
			}
			if (status === "mixed") {
				onValueChange(all, eventDetails);
				setStatus("on");
			} else if (status === "on") {
				onValueChange(none, eventDetails);
				setStatus("off");
			} else if (status === "off") {
				onValueChange(uncontrolledState, eventDetails);
				setStatus("mixed");
			}
		}
	}), [
		allValues,
		checked,
		id,
		indeterminate,
		onValueChange,
		status,
		value.length
	]);
	const getChildProps = import_react.useCallback((childValue) => ({
		checked: value.includes(childValue),
		onCheckedChange(nextChecked, eventDetails) {
			const newValue = value.slice();
			if (nextChecked) newValue.push(childValue);
			else newValue.splice(newValue.indexOf(childValue), 1);
			uncontrolledStateRef.current = newValue;
			onValueChange(newValue, eventDetails);
			setStatus("mixed");
		}
	}), [onValueChange, value]);
	return import_react.useMemo(() => ({
		id,
		indeterminate,
		getParentProps,
		getChildProps,
		disabledStatesRef
	}), [
		id,
		indeterminate,
		getParentProps,
		getChildProps
	]);
}
//#endregion
//#region node_modules/@base-ui/react/esm/checkbox-group/CheckboxGroup.js
/**
* Provides a shared state to a series of checkboxes.
*
* Documentation: [Base UI Checkbox Group](https://base-ui.com/react/components/checkbox-group)
*/
var CheckboxGroup = /* @__PURE__ */ import_react.forwardRef(function CheckboxGroup(componentProps, forwardedRef) {
	const { allValues, className, defaultValue, disabled: disabledProp = false, id: idProp, onValueChange, render, value: externalValue, ...elementProps } = componentProps;
	const { disabled: fieldDisabled, name: fieldName, state: fieldState, validation, setFilled, setDirty, shouldValidateOnChange, validityData } = useFieldRootContext();
	const { labelId, getDescriptionProps } = useLabelableContext();
	const { clearErrors } = useFormContext();
	const disabled = fieldDisabled || disabledProp;
	const [value, setValueUnwrapped] = useControlled({
		controlled: externalValue,
		default: defaultValue,
		name: "CheckboxGroup",
		state: "value"
	});
	const setValue = useStableCallback((v, eventDetails) => {
		onValueChange?.(v, eventDetails);
		if (eventDetails.isCanceled) return;
		setValueUnwrapped(v);
	});
	const parent = useCheckboxGroupParent({
		allValues,
		value: externalValue,
		onValueChange
	});
	const id = useBaseUiId(idProp);
	const controlRef = import_react.useRef(null);
	const registerControlRef = import_react.useCallback((element) => {
		if (controlRef.current == null && element != null && !element.hasAttribute("data-parent")) controlRef.current = element;
	}, []);
	useField({
		enabled: !!fieldName,
		id,
		commit: validation.commit,
		value,
		controlRef,
		name: fieldName,
		getValue: () => value
	});
	const resolvedValue = value ?? EMPTY_ARRAY;
	useValueChanged(resolvedValue, () => {
		if (fieldName) clearErrors(fieldName);
		const initialValue = Array.isArray(validityData.initialValue) ? validityData.initialValue : EMPTY_ARRAY;
		setFilled(resolvedValue.length > 0);
		setDirty(!areArraysEqual(resolvedValue, initialValue));
		if (shouldValidateOnChange()) validation.commit(resolvedValue);
		else validation.commit(resolvedValue, true);
	});
	const state = {
		...fieldState,
		disabled
	};
	const contextValue = import_react.useMemo(() => ({
		allValues,
		value,
		defaultValue,
		setValue,
		parent,
		disabled,
		validation,
		registerControlRef
	}), [
		allValues,
		value,
		defaultValue,
		setValue,
		parent,
		disabled,
		validation,
		registerControlRef
	]);
	const element = useRenderElement("div", componentProps, {
		state,
		ref: forwardedRef,
		props: [
			{
				role: "group",
				"aria-labelledby": labelId
			},
			getDescriptionProps,
			elementProps
		],
		stateAttributesMapping: fieldValidityMapping
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CheckboxGroupContext.Provider, {
		value: contextValue,
		children: element
	});
});
CheckboxGroup.displayName = "CheckboxGroup";
//#endregion
//#region node_modules/@base-ui/react/esm/collapsible/root/stateAttributesMapping.js
var collapsibleStateAttributesMapping = {
	...collapsibleOpenStateMapping,
	...transitionStatusMapping
};
//#endregion
//#region node_modules/@base-ui/react/esm/collapsible/root/CollapsibleRoot.js
/**
* Groups all parts of the collapsible.
* Renders a `<div>` element.
*
* Documentation: [Base UI Collapsible](https://base-ui.com/react/components/collapsible)
*/
var CollapsibleRoot = /* @__PURE__ */ import_react.forwardRef(function CollapsibleRoot(componentProps, forwardedRef) {
	const { render, className, defaultOpen = false, disabled = false, onOpenChange: onOpenChangeProp, open, ...elementProps } = componentProps;
	const onOpenChange = useStableCallback(onOpenChangeProp);
	const collapsible = useCollapsibleRoot({
		open,
		defaultOpen,
		onOpenChange,
		disabled
	});
	const state = import_react.useMemo(() => ({
		open: collapsible.open,
		disabled: collapsible.disabled,
		transitionStatus: collapsible.transitionStatus
	}), [
		collapsible.open,
		collapsible.disabled,
		collapsible.transitionStatus
	]);
	const contextValue = import_react.useMemo(() => ({
		...collapsible,
		onOpenChange,
		state
	}), [
		collapsible,
		onOpenChange,
		state
	]);
	const element = useRenderElement("div", componentProps, {
		state,
		ref: forwardedRef,
		props: elementProps,
		stateAttributesMapping: collapsibleStateAttributesMapping
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CollapsibleRootContext.Provider, {
		value: contextValue,
		children: element
	});
});
CollapsibleRoot.displayName = "CollapsibleRoot";
//#endregion
//#region node_modules/@base-ui/react/esm/collapsible/trigger/CollapsibleTrigger.js
var stateAttributesMapping$19 = {
	...triggerOpenStateMapping,
	...transitionStatusMapping
};
/**
* A button that opens and closes the collapsible panel.
* Renders a `<button>` element.
*
* Documentation: [Base UI Collapsible](https://base-ui.com/react/components/collapsible)
*/
var CollapsibleTrigger = /* @__PURE__ */ import_react.forwardRef(function CollapsibleTrigger(componentProps, forwardedRef) {
	const { panelId, open, handleTrigger, state, disabled: contextDisabled } = useCollapsibleRootContext();
	const { className, disabled = contextDisabled, id, render, nativeButton = true, ...elementProps } = componentProps;
	const { getButtonProps, buttonRef } = useButton({
		disabled,
		focusableWhenDisabled: true,
		native: nativeButton
	});
	const props = import_react.useMemo(() => ({
		"aria-controls": open ? panelId : void 0,
		"aria-expanded": open,
		onClick: handleTrigger
	}), [
		panelId,
		open,
		handleTrigger
	]);
	return useRenderElement("button", componentProps, {
		state,
		ref: [forwardedRef, buttonRef],
		props: [
			props,
			elementProps,
			getButtonProps
		],
		stateAttributesMapping: stateAttributesMapping$19
	});
});
CollapsibleTrigger.displayName = "CollapsibleTrigger";
//#endregion
//#region node_modules/@base-ui/react/esm/collapsible/panel/CollapsiblePanelCssVars.js
var CollapsiblePanelCssVars = /* @__PURE__ */ function(CollapsiblePanelCssVars) {
	/**
	* The collapsible panel's height.
	* @type {number}
	*/
	CollapsiblePanelCssVars["collapsiblePanelHeight"] = "--collapsible-panel-height";
	/**
	* The collapsible panel's width.
	* @type {number}
	*/
	CollapsiblePanelCssVars["collapsiblePanelWidth"] = "--collapsible-panel-width";
	return CollapsiblePanelCssVars;
}({});
//#endregion
//#region node_modules/@base-ui/react/esm/collapsible/panel/CollapsiblePanel.js
/**
* A panel with the collapsible contents.
* Renders a `<div>` element.
*
* Documentation: [Base UI Collapsible](https://base-ui.com/react/components/collapsible)
*/
var CollapsiblePanel = /* @__PURE__ */ import_react.forwardRef(function CollapsiblePanel(componentProps, forwardedRef) {
	const { className, hiddenUntilFound: hiddenUntilFoundProp, keepMounted: keepMountedProp, render, id: idProp, ...elementProps } = componentProps;
	useIsoLayoutEffect(() => {
		if (hiddenUntilFoundProp && keepMountedProp === false) warn("The `keepMounted={false}` prop on a Collapsible will be ignored when using `hiddenUntilFound` since it requires the Panel to remain mounted even when closed.");
	}, [hiddenUntilFoundProp, keepMountedProp]);
	const { abortControllerRef, animationTypeRef, height, mounted, onOpenChange, open, panelId, panelRef, runOnceAnimationsFinish, setDimensions, setHiddenUntilFound, setKeepMounted, setMounted, setPanelIdState, setOpen, setVisible, state, transitionDimensionRef, visible, width, transitionStatus } = useCollapsibleRootContext();
	const hiddenUntilFound = hiddenUntilFoundProp ?? false;
	const keepMounted = keepMountedProp ?? false;
	useIsoLayoutEffect(() => {
		if (idProp) {
			setPanelIdState(idProp);
			return () => {
				setPanelIdState(void 0);
			};
		}
	}, [idProp, setPanelIdState]);
	useIsoLayoutEffect(() => {
		setHiddenUntilFound(hiddenUntilFound);
	}, [setHiddenUntilFound, hiddenUntilFound]);
	useIsoLayoutEffect(() => {
		setKeepMounted(keepMounted);
	}, [setKeepMounted, keepMounted]);
	const { props } = useCollapsiblePanel({
		abortControllerRef,
		animationTypeRef,
		externalRef: forwardedRef,
		height,
		hiddenUntilFound,
		id: panelId,
		keepMounted,
		mounted,
		onOpenChange,
		open,
		panelRef,
		runOnceAnimationsFinish,
		setDimensions,
		setMounted,
		setOpen,
		setVisible,
		transitionDimensionRef,
		visible,
		width
	});
	useOpenChangeComplete({
		open: open && transitionStatus === "idle",
		ref: panelRef,
		onComplete() {
			if (!open) return;
			setDimensions({
				height: void 0,
				width: void 0
			});
		}
	});
	const element = useRenderElement("div", componentProps, {
		state: import_react.useMemo(() => ({
			...state,
			transitionStatus
		}), [state, transitionStatus]),
		ref: [forwardedRef, panelRef],
		props: [
			props,
			{ style: {
				[CollapsiblePanelCssVars.collapsiblePanelHeight]: height === void 0 ? "auto" : `${height}px`,
				[CollapsiblePanelCssVars.collapsiblePanelWidth]: width === void 0 ? "auto" : `${width}px`
			} },
			elementProps
		],
		stateAttributesMapping: collapsibleStateAttributesMapping
	});
	if (!(keepMounted || hiddenUntilFound || !keepMounted && mounted)) return null;
	return element;
});
CollapsiblePanel.displayName = "CollapsiblePanel";
//#endregion
//#region node_modules/@base-ui/react/esm/collapsible/index.parts.js
var index_parts_exports$5 = /* @__PURE__ */ __exportAll({
	Panel: () => CollapsiblePanel,
	Root: () => CollapsibleRoot,
	Trigger: () => CollapsibleTrigger
});
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/root/ComboboxRoot.js
/**
* Groups all parts of the combobox.
* Doesn't render its own HTML element.
*
* Documentation: [Base UI Combobox](https://base-ui.com/react/components/combobox)
*/
function ComboboxRoot(props) {
	const { multiple = false, defaultValue, value, onValueChange, autoComplete, ...other } = props;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(AriaCombobox, {
		...other,
		selectionMode: multiple ? "multiple" : "single",
		selectedValue: value,
		defaultSelectedValue: defaultValue,
		onSelectedValueChange: onValueChange,
		formAutoComplete: autoComplete
	});
}
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/label/ComboboxLabel.js
/**
* An accessible label that is automatically associated with the combobox trigger.
* Renders a `<div>` element.
*
* Documentation: [Base UI Combobox](https://base-ui.com/react/components/combobox)
*/
var ComboboxLabel = /* @__PURE__ */ import_react.forwardRef(function ComboboxLabel(componentProps, forwardedRef) {
	const { render, className, ...elementProps } = componentProps;
	const elementPropsWithoutId = elementProps;
	delete elementPropsWithoutId.id;
	const fieldRootContext = useFieldRootContext();
	const store = useComboboxRootContext();
	const inputInsidePopup = useStore(store, selectors$2.inputInsidePopup);
	const triggerElement = useStore(store, selectors$2.triggerElement);
	const inputElement = useStore(store, selectors$2.inputElement);
	const rootId = useStore(store, selectors$2.id);
	const defaultLabelId = getDefaultLabelId(rootId);
	const localControlId = triggerElement?.id ?? (inputInsidePopup ? rootId : void 0);
	import_react.useEffect(() => {
		if (!inputElement || inputInsidePopup) return;
		error(`<Combobox.Label> labels <Combobox.Trigger> only. When <Combobox.Input> is the form control, use a native <label> or <Field.Label> instead.${SafeReact.captureOwnerStack?.() || ""}`);
	}, [inputElement, inputInsidePopup]);
	const labelProps = useLabel({
		id: defaultLabelId,
		fallbackControlId: localControlId,
		setLabelId(nextLabelId) {
			store.set("labelId", nextLabelId);
		}
	});
	return useRenderElement("div", componentProps, {
		ref: forwardedRef,
		state: fieldRootContext.state,
		props: [labelProps, elementProps],
		stateAttributesMapping: fieldValidityMapping
	});
});
ComboboxLabel.displayName = "ComboboxLabel";
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/value/ComboboxValue.js
/**
* The current value of the combobox.
* Doesn't render its own HTML element.
*
* Documentation: [Base UI Combobox](https://base-ui.com/react/components/combobox)
*/
function ComboboxValue(props) {
	const { children: childrenProp, placeholder } = props;
	const store = useComboboxRootContext();
	const itemToStringLabel = useStore(store, selectors$2.itemToStringLabel);
	const selectedValue = useStore(store, selectors$2.selectedValue);
	const items = useStore(store, selectors$2.items);
	const multiple = useStore(store, selectors$2.selectionMode) === "multiple";
	const hasSelectedValue = useStore(store, selectors$2.hasSelectedValue);
	const shouldCheckNullItemLabel = !hasSelectedValue && placeholder != null && childrenProp == null;
	const hasNullLabel = useStore(store, selectors$2.hasNullItemLabel, shouldCheckNullItemLabel);
	let children = null;
	if (typeof childrenProp === "function") children = childrenProp(selectedValue);
	else if (childrenProp != null) children = childrenProp;
	else if (!hasSelectedValue && placeholder != null && !hasNullLabel) children = placeholder;
	else if (multiple && Array.isArray(selectedValue)) children = resolveMultipleLabels(selectedValue, items, itemToStringLabel);
	else children = resolveSelectedLabel(selectedValue, items, itemToStringLabel);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_react.Fragment, { children });
}
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/item-indicator/ComboboxItemIndicator.js
/**
* Indicates whether the item is selected.
* Renders a `<span>` element.
*/
var ComboboxItemIndicator = /* @__PURE__ */ import_react.forwardRef(function ComboboxItemIndicator(componentProps, forwardedRef) {
	const keepMounted = componentProps.keepMounted ?? false;
	const { selected } = useComboboxItemContext();
	if (!(keepMounted || selected)) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Inner$1, {
		...componentProps,
		ref: forwardedRef
	});
});
ComboboxItemIndicator.displayName = "ComboboxItemIndicator";
var Inner$1 = /* @__PURE__ */ import_react.memo(/* @__PURE__ */ import_react.forwardRef((componentProps, forwardedRef) => {
	const { render, className, keepMounted, ...elementProps } = componentProps;
	const { selected } = useComboboxItemContext();
	const indicatorRef = import_react.useRef(null);
	const { transitionStatus, setMounted } = useTransitionStatus(selected);
	const element = useRenderElement("span", componentProps, {
		ref: [forwardedRef, indicatorRef],
		state: {
			selected,
			transitionStatus
		},
		props: [{
			"aria-hidden": true,
			children: "✔️"
		}, elementProps],
		stateAttributesMapping: transitionStatusMapping
	});
	useOpenChangeComplete({
		open: selected,
		ref: indicatorRef,
		onComplete() {
			if (!selected) setMounted(false);
		}
	});
	return element;
}));
Inner$1.displayName = "Inner";
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/chips/ComboboxChips.js
/**
* A container for the chips in a multiselectable input.
* Renders a `<div>` element.
*/
var ComboboxChips = /* @__PURE__ */ import_react.forwardRef(function ComboboxChips(componentProps, forwardedRef) {
	const { render, className, ...elementProps } = componentProps;
	const store = useComboboxRootContext();
	const open = useStore(store, selectors$2.open);
	const hasSelectionChips = useStore(store, selectors$2.hasSelectionChips);
	const [highlightedChipIndex, setHighlightedChipIndex] = import_react.useState(void 0);
	if (open && highlightedChipIndex !== void 0) setHighlightedChipIndex(void 0);
	const chipsRef = import_react.useRef([]);
	const element = useRenderElement("div", componentProps, {
		ref: [forwardedRef, store.state.chipsContainerRef],
		props: [hasSelectionChips ? { role: "toolbar" } : EMPTY_OBJECT, elementProps]
	});
	const contextValue = import_react.useMemo(() => ({
		highlightedChipIndex,
		setHighlightedChipIndex,
		chipsRef
	}), [
		highlightedChipIndex,
		setHighlightedChipIndex,
		chipsRef
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ComboboxChipsContext.Provider, {
		value: contextValue,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CompositeList, {
			elementsRef: chipsRef,
			children: element
		})
	});
});
ComboboxChips.displayName = "ComboboxChips";
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/chip/ComboboxChipContext.js
var ComboboxChipContext = /* @__PURE__ */ import_react.createContext(void 0);
ComboboxChipContext.displayName = "ComboboxChipContext";
function useComboboxChipContext() {
	const context = import_react.useContext(ComboboxChipContext);
	if (!context) throw new Error("useComboboxChipContext must be used within a ComboboxChip");
	return context;
}
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/chip/ComboboxChip.js
/**
* An individual chip that represents a value in a multiselectable input.
* Renders a `<div>` element.
*/
var ComboboxChip = /* @__PURE__ */ import_react.forwardRef(function ComboboxChip(componentProps, forwardedRef) {
	const { render, className, ...elementProps } = componentProps;
	const store = useComboboxRootContext();
	const { setHighlightedChipIndex, chipsRef } = useComboboxChipsContext();
	const disabled = useStore(store, selectors$2.disabled);
	const readOnly = useStore(store, selectors$2.readOnly);
	const selectedValue = useStore(store, selectors$2.selectedValue);
	const { ref, index } = useCompositeListItem();
	function handleKeyDown(event) {
		let nextIndex = index;
		if (event.key === "ArrowLeft") {
			event.preventDefault();
			if (index > 0) nextIndex = index - 1;
			else nextIndex = void 0;
		} else if (event.key === "ArrowRight") {
			event.preventDefault();
			if (index < selectedValue.length - 1) nextIndex = index + 1;
			else nextIndex = void 0;
		} else if (event.key === "Backspace" || event.key === "Delete") {
			const computedNextIndex = index >= selectedValue.length - 1 ? selectedValue.length - 2 : index;
			nextIndex = computedNextIndex >= 0 ? computedNextIndex : void 0;
			stopEvent(event);
			store.state.setIndices({
				activeIndex: null,
				selectedIndex: null,
				type: "keyboard"
			});
			store.state.setSelectedValue(selectedValue.filter((_, i) => i !== index), createChangeEventDetails(none, event.nativeEvent));
		} else if (event.key === "Enter" || event.key === " ") {
			stopEvent(event);
			nextIndex = void 0;
		} else if (event.key === "ArrowDown" || event.key === "ArrowUp") {
			stopEvent(event);
			store.state.setOpen(true, createChangeEventDetails(listNavigation, event.nativeEvent));
			nextIndex = void 0;
		} else if (event.key.length === 1 && !event.ctrlKey && !event.metaKey && !event.altKey) nextIndex = void 0;
		return nextIndex;
	}
	const element = useRenderElement("div", componentProps, {
		ref: [forwardedRef, ref],
		state: { disabled },
		props: [{
			tabIndex: -1,
			"aria-disabled": disabled || void 0,
			"aria-readonly": readOnly || void 0,
			onKeyDown(event) {
				if (disabled || readOnly) return;
				const nextIndex = handleKeyDown(event);
				import_react_dom.flushSync(() => {
					setHighlightedChipIndex(nextIndex);
				});
				if (nextIndex === void 0) store.state.inputRef.current?.focus();
				else chipsRef.current[nextIndex]?.focus();
			},
			onMouseDown(event) {
				if (readOnly) return;
				event.preventDefault();
				if (disabled) return;
				store.state.inputRef.current?.focus();
			}
		}, elementProps]
	});
	const contextValue = import_react.useMemo(() => ({ index }), [index]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ComboboxChipContext.Provider, {
		value: contextValue,
		children: element
	});
});
ComboboxChip.displayName = "ComboboxChip";
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/chip-remove/ComboboxChipRemove.js
/**
* A button to remove a chip.
* Renders a `<button>` element.
*/
var ComboboxChipRemove = /* @__PURE__ */ import_react.forwardRef(function ComboboxChipRemove(componentProps, forwardedRef) {
	const { render, className, disabled: disabledProp = false, nativeButton = true, ...elementProps } = componentProps;
	const store = useComboboxRootContext();
	const { index } = useComboboxChipContext();
	const comboboxDisabled = useStore(store, selectors$2.disabled);
	const readOnly = useStore(store, selectors$2.readOnly);
	const selectedValue = useStore(store, selectors$2.selectedValue);
	const isItemEqualToValue = useStore(store, selectors$2.isItemEqualToValue);
	const disabled = comboboxDisabled || disabledProp;
	const { buttonRef, getButtonProps } = useButton({
		native: nativeButton,
		disabled: disabled || readOnly,
		focusableWhenDisabled: true
	});
	const state = { disabled };
	function clearActiveIndexForRemovedItem(removedItem) {
		const activeIndex = store.state.activeIndex;
		if (activeIndex == null) return;
		const removedIndex = findItemIndex(store.state.valuesRef.current, removedItem, isItemEqualToValue);
		if (removedIndex !== -1 && activeIndex === removedIndex) store.state.setIndices({
			activeIndex: null,
			type: store.state.keyboardActiveRef.current ? "keyboard" : "pointer"
		});
	}
	function removeChip(event) {
		const eventDetails = createChangeEventDetails(chipRemovePress, event.nativeEvent);
		const removedItem = selectedValue[index];
		clearActiveIndexForRemovedItem(removedItem);
		store.state.setSelectedValue(selectedValue.filter((_, i) => i !== index), eventDetails);
		store.state.inputRef.current?.focus();
		return eventDetails;
	}
	return useRenderElement("button", componentProps, {
		ref: [forwardedRef, buttonRef],
		state,
		props: [
			{
				tabIndex: -1,
				onClick(event) {
					if (disabled || readOnly) return;
					if (!removeChip(event).isPropagationAllowed) event.stopPropagation();
				},
				onKeyDown(event) {
					if (disabled || readOnly) return;
					if (event.key === "Enter" || event.key === " ") {
						if (!removeChip(event).isPropagationAllowed) stopEvent(event);
					}
				}
			},
			elementProps,
			getButtonProps
		]
	});
});
ComboboxChipRemove.displayName = "ComboboxChipRemove";
//#endregion
//#region node_modules/@base-ui/react/esm/combobox/index.parts.js
var index_parts_exports$6 = /* @__PURE__ */ __exportAll({
	Arrow: () => ComboboxArrow,
	Backdrop: () => ComboboxBackdrop,
	Chip: () => ComboboxChip,
	ChipRemove: () => ComboboxChipRemove,
	Chips: () => ComboboxChips,
	Clear: () => ComboboxClear,
	Collection: () => ComboboxCollection,
	Empty: () => ComboboxEmpty,
	Group: () => ComboboxGroup,
	GroupLabel: () => ComboboxGroupLabel,
	Icon: () => ComboboxIcon,
	Input: () => ComboboxInput,
	InputGroup: () => ComboboxInputGroup,
	Item: () => ComboboxItem,
	ItemIndicator: () => ComboboxItemIndicator,
	Label: () => ComboboxLabel,
	List: () => ComboboxList,
	Popup: () => ComboboxPopup,
	Portal: () => ComboboxPortal,
	Positioner: () => ComboboxPositioner,
	Root: () => ComboboxRoot,
	Row: () => ComboboxRow,
	Separator: () => Separator,
	Status: () => ComboboxStatus,
	Trigger: () => ComboboxTrigger,
	Value: () => ComboboxValue,
	useFilter: () => useComboboxFilter,
	useFilteredItems: () => useFilteredItems
});
//#endregion
//#region node_modules/@base-ui/react/esm/direction-provider/DirectionProvider.js
/**
* Enables RTL behavior for Base UI components.
*
* Documentation: [Base UI Direction Provider](https://base-ui.com/react/utils/direction-provider)
*/
var DirectionProvider = function DirectionProvider(props) {
	const { direction = "ltr" } = props;
	const contextValue = import_react.useMemo(() => ({ direction }), [direction]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DirectionContext.Provider, {
		value: contextValue,
		children: props.children
	});
};
DirectionProvider.displayName = "DirectionProvider";
//#endregion
//#region node_modules/@base-ui/react/esm/csp-provider/CSPProvider.js
/**
* Provides a default Content Security Policy (CSP) configuration for Base UI components that
* require inline `<style>` or `<script>` tags.
*
* Documentation: [Base UI CSP Provider](https://base-ui.com/react/utils/csp-provider)
*/
function CSPProvider(props) {
	const { children, nonce, disableStyleElements } = props;
	const contextValue = import_react.useMemo(() => ({
		nonce,
		disableStyleElements
	}), [nonce, disableStyleElements]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CSPContext.Provider, {
		value: contextValue,
		children
	});
}
//#endregion
//#region node_modules/@base-ui/react/esm/drawer/popup/DrawerPopupCssVars.js
var DrawerPopupCssVars = /* @__PURE__ */ function(DrawerPopupCssVars) {
	/**
	* The number of nested drawers that are currently open.
	* @type {number}
	*/
	DrawerPopupCssVars["nestedDrawers"] = "--nested-drawers";
	/**
	* The height of the drawer popup.
	* @type {CSS length}
	*/
	DrawerPopupCssVars["height"] = "--drawer-height";
	/**
	* The height of the frontmost open drawer in the current nested drawer stack.
	* @type {CSS length}
	*/
	DrawerPopupCssVars["frontmostHeight"] = "--drawer-frontmost-height";
	/**
	* The swipe movement on the X axis.
	* @type {CSS length}
	*/
	DrawerPopupCssVars["swipeMovementX"] = "--drawer-swipe-movement-x";
	/**
	* The swipe movement on the Y axis.
	* @type {CSS length}
	*/
	DrawerPopupCssVars["swipeMovementY"] = "--drawer-swipe-movement-y";
	/**
	* The snap point offset used for translating the drawer.
	* @type {CSS length}
	*/
	DrawerPopupCssVars["snapPointOffset"] = "--drawer-snap-point-offset";
	/**
	* A scalar (0.1-1) used to scale the swipe release transition duration in CSS.
	* @type {number}
	*/
	DrawerPopupCssVars["swipeStrength"] = "--drawer-swipe-strength";
	return DrawerPopupCssVars;
}({});
//#endregion
//#region node_modules/@base-ui/react/esm/drawer/backdrop/DrawerBackdropCssVars.js
var DrawerBackdropCssVars = /* @__PURE__ */ function(DrawerBackdropCssVars) {
	/**
	* The swipe progress of the drawer gesture.
	* @type {number}
	*/
	DrawerBackdropCssVars["swipeProgress"] = "--drawer-swipe-progress";
	return DrawerBackdropCssVars;
}({});
//#endregion
//#region node_modules/@base-ui/react/esm/drawer/backdrop/DrawerBackdrop.js
var stateAttributesMapping$18 = {
	...popupStateMapping,
	...transitionStatusMapping
};
/**
* An overlay displayed beneath the popup.
* Renders a `<div>` element.
*
* Documentation: [Base UI Drawer](https://base-ui.com/react/components/drawer)
*/
var DrawerBackdrop = /* @__PURE__ */ import_react.forwardRef(function DrawerBackdrop(componentProps, forwardedRef) {
	const { render, className, forceRender = false, ...elementProps } = componentProps;
	const { store } = useDialogRootContext();
	const open = store.useState("open");
	const nested = store.useState("nested");
	const mounted = store.useState("mounted");
	return useRenderElement("div", componentProps, {
		state: {
			open,
			transitionStatus: store.useState("transitionStatus")
		},
		ref: [store.context.backdropRef, forwardedRef],
		stateAttributesMapping: stateAttributesMapping$18,
		props: [{
			role: "presentation",
			hidden: !mounted,
			style: {
				pointerEvents: !open ? "none" : void 0,
				userSelect: "none",
				WebkitUserSelect: "none",
				[DrawerBackdropCssVars.swipeProgress]: "0",
				[DrawerPopupCssVars.swipeStrength]: "1"
			}
		}, elementProps],
		enabled: forceRender || !nested
	});
});
DrawerBackdrop.displayName = "DrawerBackdrop";
//#endregion
//#region node_modules/@base-ui/react/esm/drawer/close/DrawerClose.js
/**
* A button that closes the drawer.
* Renders a `<button>` element.
*
* Documentation: [Base UI Drawer](https://base-ui.com/react/components/drawer)
*/
var DrawerClose = DialogClose;
//#endregion
//#region node_modules/@base-ui/react/esm/drawer/content/DrawerContentDataAttributes.js
var DRAWER_CONTENT_ATTRIBUTE = "data-drawer-content";
//#endregion
//#region node_modules/@base-ui/react/esm/drawer/content/DrawerContent.js
/**
* A container for the drawer contents.
* Renders a `<div>` element.
*
* Documentation: [Base UI Drawer](https://base-ui.com/react/components/drawer)
*/
var DrawerContent = /* @__PURE__ */ import_react.forwardRef(function DrawerContent(componentProps, forwardedRef) {
	const { render, className, ...elementProps } = componentProps;
	useDialogRootContext();
	return useRenderElement("div", componentProps, {
		ref: forwardedRef,
		props: [{ [DRAWER_CONTENT_ATTRIBUTE]: "" }, elementProps]
	});
});
DrawerContent.displayName = "DrawerContent";
//#endregion
//#region node_modules/@base-ui/react/esm/drawer/description/DrawerDescription.js
/**
* A paragraph with additional information about the drawer.
* Renders a `<p>` element.
*
* Documentation: [Base UI Drawer](https://base-ui.com/react/components/drawer)
*/
var DrawerDescription = DialogDescription;
//#endregion
//#region node_modules/@base-ui/react/esm/drawer/provider/DrawerProviderContext.js
var DrawerProviderContext = /* @__PURE__ */ import_react.createContext(void 0);
DrawerProviderContext.displayName = "DrawerProviderContext";
function useDrawerProviderContext(optional) {
	const context = import_react.useContext(DrawerProviderContext);
	if (optional === false && context === void 0) throw new Error("Base UI: DrawerProviderContext is missing. Use <Drawer.Provider>.");
	return context;
}
//#endregion
//#region node_modules/@base-ui/react/esm/drawer/indent/DrawerIndent.js
var stateAttributesMapping$17 = { active(value) {
	if (value) return { "data-active": "" };
	return { "data-inactive": "" };
} };
/**
* A wrapper element intended to contain your app's main UI.
* Applies `data-active` when any drawer within the nearest `<Drawer.Provider>` is open.
* Renders a `<div>` element.
*
* Documentation: [Base UI Drawer](https://base-ui.com/react/components/drawer)
*/
var DrawerIndent = /* @__PURE__ */ import_react.forwardRef(function DrawerIndent(componentProps, forwardedRef) {
	const { render, className, ...elementProps } = componentProps;
	const providerContext = useDrawerProviderContext(true);
	const active = providerContext?.active ?? false;
	const visualStateStore = providerContext?.visualStateStore;
	const indentRef = import_react.useRef(null);
	useIsoLayoutEffect(() => {
		const element = indentRef.current;
		if (!element || !visualStateStore) return;
		const syncVisualState = () => {
			const { swipeProgress, frontmostHeight } = visualStateStore.getSnapshot();
			if (swipeProgress <= 0) element.style.setProperty(DrawerBackdropCssVars.swipeProgress, "0");
			else element.style.setProperty(DrawerBackdropCssVars.swipeProgress, `${swipeProgress}`);
			if (frontmostHeight <= 0) element.style.removeProperty(DrawerPopupCssVars.height);
			else element.style.setProperty(DrawerPopupCssVars.height, `${frontmostHeight}px`);
		};
		syncVisualState();
		const unsubscribe = visualStateStore.subscribe(syncVisualState);
		return () => {
			unsubscribe();
			element.style.setProperty(DrawerBackdropCssVars.swipeProgress, "0");
			element.style.removeProperty(DrawerPopupCssVars.height);
		};
	}, [visualStateStore]);
	return useRenderElement("div", componentProps, {
		ref: [forwardedRef, indentRef],
		state: { active },
		props: [{ style: { [DrawerBackdropCssVars.swipeProgress]: "0" } }, elementProps],
		stateAttributesMapping: stateAttributesMapping$17
	});
});
DrawerIndent.displayName = "DrawerIndent";
//#endregion
//#region node_modules/@base-ui/react/esm/drawer/indent-background/DrawerIndentBackground.js
var stateAttributesMapping$16 = { active(value) {
	if (value) return { "data-active": "" };
	return { "data-inactive": "" };
} };
/**
* An element placed before `<Drawer.Indent>` to render a background layer that can be styled based on whether any drawer is open.
* Renders a `<div>` element.
*
* Documentation: [Base UI Drawer](https://base-ui.com/react/components/drawer)
*/
var DrawerIndentBackground = /* @__PURE__ */ import_react.forwardRef(function DrawerIndentBackground(componentProps, forwardedRef) {
	const { render, className, ...elementProps } = componentProps;
	return useRenderElement("div", componentProps, {
		ref: forwardedRef,
		state: { active: useDrawerProviderContext(true)?.active ?? false },
		props: elementProps,
		stateAttributesMapping: stateAttributesMapping$16
	});
});
DrawerIndentBackground.displayName = "DrawerIndentBackground";
//#endregion
//#region node_modules/@base-ui/react/esm/drawer/popup/DrawerPopupDataAttributes.js
var DrawerPopupDataAttributes = function(DrawerPopupDataAttributes) {
	/**
	* Present when the drawer is open.
	*/
	DrawerPopupDataAttributes[DrawerPopupDataAttributes["open"] = CommonPopupDataAttributes.open] = "open";
	/**
	* Present when the drawer is closed.
	*/
	DrawerPopupDataAttributes[DrawerPopupDataAttributes["closed"] = CommonPopupDataAttributes.closed] = "closed";
	/**
	* Present when the drawer is animating in.
	*/
	DrawerPopupDataAttributes[DrawerPopupDataAttributes["startingStyle"] = CommonPopupDataAttributes.startingStyle] = "startingStyle";
	/**
	* Present when the drawer is animating out.
	*/
	DrawerPopupDataAttributes[DrawerPopupDataAttributes["endingStyle"] = CommonPopupDataAttributes.endingStyle] = "endingStyle";
	/**
	* Present when the drawer is at the expanded (full-height) snap point.
	*/
	DrawerPopupDataAttributes["expanded"] = "data-expanded";
	/**
	* Present when a nested drawer is open.
	*/
	DrawerPopupDataAttributes["nestedDrawerOpen"] = "data-nested-drawer-open";
	/**
	* Present when a nested drawer is being swiped.
	*/
	DrawerPopupDataAttributes["nestedDrawerSwiping"] = "data-nested-drawer-swiping";
	/**
	* Present when the drawer is dismissed by swiping.
	*/
	DrawerPopupDataAttributes["swipeDismiss"] = "data-swipe-dismiss";
	/**
	* Indicates the swipe direction.
	* @type {'up' | 'down' | 'left' | 'right'}
	*/
	DrawerPopupDataAttributes["swipeDirection"] = "data-swipe-direction";
	/**
	* Present when the drawer is being swiped.
	*/
	DrawerPopupDataAttributes["swiping"] = "data-swiping";
	return DrawerPopupDataAttributes;
}({});
//#endregion
//#region node_modules/@base-ui/react/esm/drawer/root/DrawerRootContext.js
var DrawerRootContext = /* @__PURE__ */ import_react.createContext(void 0);
DrawerRootContext.displayName = "DrawerRootContext";
function useDrawerRootContext(optional) {
	const drawerRootContext = import_react.useContext(DrawerRootContext);
	if (optional === false && drawerRootContext === void 0) throw new Error("Base UI: DrawerRootContext is missing. Drawer parts must be placed within <Drawer.Root>.");
	return drawerRootContext;
}
//#endregion
//#region node_modules/@base-ui/react/esm/drawer/root/useDrawerSnapPoints.js
function resolveSnapPointValue(snapPoint, viewportHeight, rootFontSize) {
	if (!Number.isFinite(viewportHeight) || viewportHeight <= 0) return null;
	if (typeof snapPoint === "number") {
		if (!Number.isFinite(snapPoint)) return null;
		if (snapPoint <= 1) return clamp(snapPoint, 0, 1) * viewportHeight;
		return snapPoint;
	}
	const trimmed = snapPoint.trim();
	if (trimmed.endsWith("px")) {
		const value = Number.parseFloat(trimmed);
		return Number.isFinite(value) ? value : null;
	}
	if (trimmed.endsWith("rem")) {
		const value = Number.parseFloat(trimmed);
		return Number.isFinite(value) ? value * rootFontSize : null;
	}
	return null;
}
function findClosestSnapPoint(height, points) {
	let closest = null;
	let closestDistance = Infinity;
	for (const point of points) {
		const distance = Math.abs(point.height - height);
		if (distance < closestDistance) {
			closestDistance = distance;
			closest = point;
		}
	}
	return closest;
}
function useDrawerSnapPoints() {
	const { store } = useDialogRootContext();
	const { snapPoints, activeSnapPoint, setActiveSnapPoint, popupHeight } = useDrawerRootContext();
	const viewportElement = store.useState("viewportElement");
	const [viewportHeight, setViewportHeight] = import_react.useState(0);
	const [rootFontSize, setRootFontSize] = import_react.useState(16);
	const measureViewportHeight = useStableCallback(() => {
		const html = ownerDocument(viewportElement).documentElement;
		if (viewportElement) setViewportHeight(viewportElement.offsetHeight);
		if (!viewportElement) setViewportHeight(html.clientHeight);
		const fontSize = parseFloat(getComputedStyle(html).fontSize);
		if (Number.isFinite(fontSize)) setRootFontSize(fontSize);
	});
	useIsoLayoutEffect(() => {
		measureViewportHeight();
		if (!viewportElement || typeof ResizeObserver !== "function") return;
		const resizeObserver = new ResizeObserver(measureViewportHeight);
		resizeObserver.observe(viewportElement);
		return () => {
			resizeObserver.disconnect();
		};
	}, [measureViewportHeight, viewportElement]);
	const resolvedSnapPoints = import_react.useMemo(() => {
		if (!snapPoints || snapPoints.length === 0 || viewportHeight <= 0 || popupHeight <= 0) return [];
		const maxHeight = Math.min(popupHeight, viewportHeight);
		if (!Number.isFinite(maxHeight) || maxHeight <= 0) return [];
		const resolved = snapPoints.map((value) => {
			const resolvedHeight = resolveSnapPointValue(value, viewportHeight, rootFontSize);
			if (resolvedHeight === null || !Number.isFinite(resolvedHeight)) return null;
			const clampedHeight = clamp(resolvedHeight, 0, maxHeight);
			return {
				value,
				height: clampedHeight,
				offset: Math.max(0, popupHeight - clampedHeight)
			};
		}).filter((point) => Boolean(point));
		if (resolved.length <= 1) return resolved;
		const deduped = [];
		const seenHeights = [];
		for (let index = resolved.length - 1; index >= 0; index -= 1) {
			const point = resolved[index];
			if (seenHeights.some((height) => Math.abs(height - point.height) <= 1)) continue;
			seenHeights.push(point.height);
			deduped.push(point);
		}
		deduped.reverse();
		return deduped;
	}, [
		popupHeight,
		rootFontSize,
		snapPoints,
		viewportHeight
	]);
	return {
		snapPoints,
		activeSnapPoint,
		setActiveSnapPoint,
		popupHeight,
		viewportHeight,
		resolvedSnapPoints,
		activeSnapPointOffset: import_react.useMemo(() => {
			if (activeSnapPoint === void 0) return resolvedSnapPoints[0];
			if (activeSnapPoint === null) return;
			const exactMatch = resolvedSnapPoints.find((point) => Object.is(point.value, activeSnapPoint));
			if (exactMatch) return exactMatch;
			const maxHeight = Math.min(popupHeight, viewportHeight);
			const resolvedHeight = resolveSnapPointValue(activeSnapPoint, viewportHeight, rootFontSize);
			if (resolvedHeight === null || !Number.isFinite(resolvedHeight)) return;
			return findClosestSnapPoint(clamp(resolvedHeight, 0, maxHeight), resolvedSnapPoints) ?? void 0;
		}, [
			activeSnapPoint,
			popupHeight,
			resolvedSnapPoints,
			rootFontSize,
			viewportHeight
		])?.offset ?? null
	};
}
//#endregion
//#region node_modules/@base-ui/react/esm/drawer/viewport/DrawerViewportContext.js
var DrawerViewportContext = /* @__PURE__ */ import_react.createContext(null);
DrawerViewportContext.displayName = "DrawerViewportContext";
function useDrawerViewportContext(optional) {
	const context = import_react.useContext(DrawerViewportContext);
	if (optional === false && context === null) throw new Error("Base UI: DrawerViewportContext is missing. Drawer parts must be placed within <Drawer.Viewport>.");
	return context;
}
//#endregion
//#region node_modules/@base-ui/react/esm/drawer/popup/DrawerPopup.js
var drawerSwipeVarsRegistered = false;
/**
* Removes inheritance of high-frequency drawer swipe CSS variables, which
* reduces style recalculation cost in complex drawers with deep subtrees.
* Child elements that need these values can still opt-in by using `inherit`.
* See https://motion.dev/blog/web-animation-performance-tier-list
* under the "Improving CSS variable performance" section.
*/
function removeCSSVariableInheritance() {
	if (drawerSwipeVarsRegistered) return;
	if (typeof CSS !== "undefined" && "registerProperty" in CSS) {
		[
			DrawerPopupCssVars.swipeMovementX,
			DrawerPopupCssVars.swipeMovementY,
			DrawerPopupCssVars.snapPointOffset
		].forEach((name) => {
			try {
				CSS.registerProperty({
					name,
					syntax: "<length>",
					inherits: false,
					initialValue: "0px"
				});
			} catch {}
		});
		[{
			name: DrawerBackdropCssVars.swipeProgress,
			initialValue: "0"
		}, {
			name: DrawerPopupCssVars.swipeStrength,
			initialValue: "1"
		}].forEach(({ name, initialValue }) => {
			try {
				CSS.registerProperty({
					name,
					syntax: "<number>",
					inherits: false,
					initialValue
				});
			} catch {}
		});
	}
	drawerSwipeVarsRegistered = true;
}
var stateAttributesMapping$15 = {
	...popupStateMapping,
	...transitionStatusMapping,
	expanded(value) {
		return value ? { [DrawerPopupDataAttributes.expanded]: "" } : null;
	},
	nestedDrawerOpen(value) {
		return value ? { [DrawerPopupDataAttributes.nestedDrawerOpen]: "" } : null;
	},
	nestedDrawerSwiping(value) {
		return value ? { [DrawerPopupDataAttributes.nestedDrawerSwiping]: "" } : null;
	},
	swipeDirection(value) {
		return value ? { [DrawerPopupDataAttributes.swipeDirection]: value } : null;
	},
	swiping(value) {
		return value ? { [DrawerPopupDataAttributes.swiping]: "" } : null;
	}
};
/**
* A container for the drawer contents.
* Renders a `<div>` element.
*
* Documentation: [Base UI Drawer](https://base-ui.com/react/components/drawer)
*/
var DrawerPopup = /* @__PURE__ */ import_react.forwardRef(function DrawerPopup(componentProps, forwardedRef) {
	const { className, finalFocus, initialFocus, render, ...elementProps } = componentProps;
	const { store } = useDialogRootContext();
	const { swipeDirection, frontmostHeight, hasNestedDrawer, nestedSwiping, nestedSwipeProgressStore, onPopupHeightChange, notifyParentFrontmostHeight, notifyParentHasNestedDrawer } = useDrawerRootContext();
	const descriptionElementId = store.useState("descriptionElementId");
	const disablePointerDismissal = store.useState("disablePointerDismissal");
	const floatingRootContext = store.useState("floatingRootContext");
	const rootPopupProps = store.useState("popupProps");
	const modal = store.useState("modal");
	const mounted = store.useState("mounted");
	const nested = store.useState("nested");
	const nestedOpenDialogCount = store.useState("nestedOpenDialogCount");
	const transitionStatus = store.useState("transitionStatus");
	const open = store.useState("open");
	const openMethod = store.useState("openMethod");
	const titleElementId = store.useState("titleElementId");
	const role = store.useState("role");
	const nestedDrawerOpen = nestedOpenDialogCount > 0;
	const swipe = useDrawerViewportContext(true);
	const swiping = swipe?.swiping ?? false;
	const swipeStrength = swipe?.swipeStrength ?? null;
	const { snapPoints, activeSnapPoint, activeSnapPointOffset } = useDrawerSnapPoints();
	useDialogPortalContext();
	const [popupHeight, setPopupHeight] = import_react.useState(0);
	const popupHeightRef = import_react.useRef(0);
	const measureHeight = useStableCallback(() => {
		const popupElement = store.context.popupRef.current;
		if (!popupElement) return;
		const offsetHeight = popupElement.offsetHeight;
		if (popupHeightRef.current > 0 && frontmostHeight > popupHeightRef.current && offsetHeight > popupHeightRef.current) return;
		if (popupHeightRef.current > 0 && hasNestedDrawer) {
			const oldHeight = popupHeightRef.current;
			setPopupHeight(oldHeight);
			onPopupHeightChange(oldHeight);
			return;
		}
		const nextHeight = offsetHeight;
		if (nextHeight === popupHeightRef.current) return;
		popupHeightRef.current = nextHeight;
		setPopupHeight(nextHeight);
		onPopupHeightChange(nextHeight);
	});
	useIsoLayoutEffect(() => {
		if (!mounted) {
			popupHeightRef.current = 0;
			setPopupHeight(0);
			onPopupHeightChange(0);
			return;
		}
		const popupElement = store.context.popupRef.current;
		if (!popupElement) return;
		removeCSSVariableInheritance();
		measureHeight();
		if (typeof ResizeObserver !== "function") return;
		const resizeObserver = new ResizeObserver(measureHeight);
		resizeObserver.observe(popupElement);
		return () => {
			resizeObserver.disconnect();
		};
	}, [
		measureHeight,
		mounted,
		nestedDrawerOpen,
		onPopupHeightChange,
		store.context.popupRef
	]);
	useIsoLayoutEffect(() => {
		const popupRef = store.context.popupRef;
		const syncNestedSwipeProgress = () => {
			const popupElement = popupRef.current;
			if (!popupElement) return;
			const progress = nestedSwipeProgressStore.getSnapshot();
			if (progress > 0) popupElement.style.setProperty(DrawerBackdropCssVars.swipeProgress, `${progress}`);
			else popupElement.style.setProperty(DrawerBackdropCssVars.swipeProgress, "0");
		};
		syncNestedSwipeProgress();
		const unsubscribe = nestedSwipeProgressStore.subscribe(syncNestedSwipeProgress);
		return () => {
			unsubscribe();
			const popupElement = popupRef.current;
			if (popupElement) popupElement.style.setProperty(DrawerBackdropCssVars.swipeProgress, "0");
		};
	}, [nestedSwipeProgressStore, store.context.popupRef]);
	import_react.useEffect(() => {
		if (!open) return;
		notifyParentFrontmostHeight?.(frontmostHeight);
		return () => {
			notifyParentFrontmostHeight?.(0);
		};
	}, [
		frontmostHeight,
		open,
		notifyParentFrontmostHeight
	]);
	import_react.useEffect(() => {
		if (!notifyParentHasNestedDrawer) return;
		notifyParentHasNestedDrawer(open || transitionStatus === "ending");
		return () => {
			notifyParentHasNestedDrawer(false);
		};
	}, [
		notifyParentHasNestedDrawer,
		open,
		transitionStatus
	]);
	useOpenChangeComplete({
		open,
		ref: store.context.popupRef,
		onComplete() {
			if (open) store.context.onOpenChangeComplete?.(true);
		}
	});
	const resolvedInitialFocus = initialFocus === void 0 ? store.context.popupRef : initialFocus;
	const state = {
		open,
		nested,
		transitionStatus,
		expanded: activeSnapPoint === 1,
		nestedDrawerOpen,
		nestedDrawerSwiping: nestedSwiping,
		swipeDirection,
		swiping
	};
	let popupHeightCssVarValue;
	if (popupHeight && !(!hasNestedDrawer && transitionStatus !== "ending")) popupHeightCssVarValue = `${popupHeight}px`;
	const shouldApplySnapPoints = snapPoints && snapPoints.length > 0 && (swipeDirection === "down" || swipeDirection === "up");
	let snapPointOffsetValue = null;
	if (shouldApplySnapPoints && activeSnapPointOffset !== null) snapPointOffsetValue = swipeDirection === "up" ? -activeSnapPointOffset : activeSnapPointOffset;
	let dragStyles = swipe ? swipe.getDragStyles() : EMPTY_OBJECT;
	if (shouldApplySnapPoints && swipeDirection === "down") {
		const baseOffset = activeSnapPointOffset ?? 0;
		const movementValue = Number.parseFloat(String(dragStyles[DrawerPopupCssVars.swipeMovementY] ?? 0));
		const nextOffset = Number.isFinite(movementValue) ? baseOffset + movementValue : baseOffset;
		if (swiping && nextOffset < 0 && Number.isFinite(movementValue)) {
			const overshoot = Math.abs(nextOffset);
			const dampedMovement = -Math.sqrt(overshoot) - baseOffset;
			dragStyles = {
				...dragStyles,
				transform: void 0,
				[DrawerPopupCssVars.swipeMovementY]: `${dampedMovement}px`
			};
		} else dragStyles = {
			...dragStyles,
			transform: void 0
		};
	}
	const element = useRenderElement("div", componentProps, {
		state,
		props: [
			rootPopupProps,
			{
				"aria-labelledby": titleElementId,
				"aria-describedby": descriptionElementId,
				role,
				tabIndex: -1,
				hidden: !mounted,
				onKeyDown(event) {
					if (COMPOSITE_KEYS.has(event.key)) event.stopPropagation();
				},
				style: {
					...dragStyles,
					[DrawerBackdropCssVars.swipeProgress]: "0",
					[DrawerPopupCssVars.nestedDrawers]: nestedOpenDialogCount,
					[DrawerPopupCssVars.height]: popupHeightCssVarValue,
					[DrawerPopupCssVars.snapPointOffset]: typeof snapPointOffsetValue === "number" ? `${snapPointOffsetValue}px` : "0px",
					[DrawerPopupCssVars.frontmostHeight]: frontmostHeight ? `${frontmostHeight}px` : void 0,
					[DrawerPopupCssVars.swipeStrength]: typeof swipeStrength === "number" && Number.isFinite(swipeStrength) && swipeStrength > 0 ? `${swipeStrength}` : "1"
				}
			},
			elementProps
		],
		ref: [
			forwardedRef,
			store.context.popupRef,
			store.useStateSetter("popupElement")
		],
		stateAttributesMapping: stateAttributesMapping$15
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FloatingFocusManager, {
		context: floatingRootContext,
		openInteractionType: openMethod,
		disabled: !mounted,
		closeOnFocusOut: !disablePointerDismissal,
		initialFocus: resolvedInitialFocus,
		returnFocus: finalFocus,
		modal: modal !== false,
		restoreFocus: "popup",
		children: element
	});
});
DrawerPopup.displayName = "DrawerPopup";
//#endregion
//#region node_modules/@base-ui/react/esm/drawer/portal/DrawerPortal.js
/**
* A portal element that moves the popup to a different part of the DOM.
* By default, the portal element is appended to `<body>`.
* Renders a `<div>` element.
*
* Documentation: [Base UI Drawer](https://base-ui.com/react/components/drawer)
*/
var DrawerPortal = DialogPortal;
//#endregion
//#region node_modules/@base-ui/react/esm/drawer/provider/DrawerProvider.js
/**
* Provides a shared context for coordinating global Drawer UI, such as indent/background effects based on whether any Drawer is open.
* Doesn't render its own HTML element.
*
* Documentation: [Base UI Drawer](https://base-ui.com/react/components/drawer)
*/
function DrawerProvider(props) {
	const { children } = props;
	const [openById, setOpenById] = import_react.useState(() => /* @__PURE__ */ new Map());
	const [visualStateStore] = import_react.useState(createVisualStateStore);
	const setDrawerOpen = useStableCallback((drawerId, open) => {
		setOpenById((prev) => {
			if (prev.get(drawerId) === open) return prev;
			const next = new Map(prev);
			next.set(drawerId, open);
			return next;
		});
	});
	const removeDrawer = useStableCallback((drawerId) => {
		setOpenById((prev) => {
			if (!prev.has(drawerId)) return prev;
			const next = new Map(prev);
			next.delete(drawerId);
			return next;
		});
	});
	const active = import_react.useMemo(() => {
		for (const open of openById.values()) if (open) return true;
		return false;
	}, [openById]);
	const contextValue = import_react.useMemo(() => ({
		setDrawerOpen,
		removeDrawer,
		active,
		visualStateStore
	}), [
		active,
		removeDrawer,
		setDrawerOpen,
		visualStateStore
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DrawerProviderContext.Provider, {
		value: contextValue,
		children
	});
}
function createVisualStateStore() {
	let state = {
		swipeProgress: 0,
		frontmostHeight: 0
	};
	const listeners = /* @__PURE__ */ new Set();
	return {
		getSnapshot: () => state,
		set(nextState) {
			let nextSwipeProgress = state.swipeProgress;
			if (nextState.swipeProgress !== void 0) nextSwipeProgress = Number.isFinite(nextState.swipeProgress) ? nextState.swipeProgress : 0;
			let nextFrontmostHeight = state.frontmostHeight;
			if (nextState.frontmostHeight !== void 0) nextFrontmostHeight = Number.isFinite(nextState.frontmostHeight) ? nextState.frontmostHeight : 0;
			if (nextSwipeProgress === state.swipeProgress && nextFrontmostHeight === state.frontmostHeight) return;
			state = {
				swipeProgress: nextSwipeProgress,
				frontmostHeight: nextFrontmostHeight
			};
			listeners.forEach((listener) => {
				listener();
			});
		},
		subscribe(listener) {
			listeners.add(listener);
			return () => {
				listeners.delete(listener);
			};
		}
	};
}
//#endregion
//#region node_modules/@base-ui/react/esm/drawer/root/DrawerRoot.js
var _DrawerProviderReport, _DrawerProviderReport2;
/**
* Groups all parts of the drawer.
* Doesn't render its own HTML element.
*
* Documentation: [Base UI Drawer](https://base-ui.com/react/components/drawer)
*/
function DrawerRoot(props) {
	const { children, open: openProp, defaultOpen = false, onOpenChange, onOpenChangeComplete, disablePointerDismissal = false, modal = true, actionsRef, handle, triggerId: triggerIdProp, defaultTriggerId: defaultTriggerIdProp = null, swipeDirection = "down", snapToSequentialPoints = false, snapPoints, snapPoint: snapPointProp, defaultSnapPoint, onSnapPointChange: onSnapPointChangeProp } = props;
	const onSnapPointChange = useStableCallback(onSnapPointChangeProp);
	const parentDrawerRootContext = useDrawerRootContext(true);
	const notifyParentSwipeProgressChange = parentDrawerRootContext?.onNestedSwipeProgressChange;
	const notifyParentFrontmostHeight = parentDrawerRootContext?.onNestedFrontmostHeightChange;
	const notifyParentSwipingChange = parentDrawerRootContext?.onNestedSwipingChange;
	const notifyParentHasNestedDrawer = parentDrawerRootContext?.onNestedDrawerPresenceChange;
	const [popupHeight, setPopupHeight] = import_react.useState(0);
	const [frontmostHeight, setFrontmostHeight] = import_react.useState(0);
	const [hasNestedDrawer, setHasNestedDrawer] = import_react.useState(false);
	const [nestedSwiping, setNestedSwiping] = import_react.useState(false);
	const [nestedSwipeProgressStore] = import_react.useState(createNestedSwipeProgressStore);
	const resolvedDefaultSnapPoint = defaultSnapPoint !== void 0 ? defaultSnapPoint : snapPoints?.[0] ?? null;
	const isSnapPointControlled = snapPointProp !== void 0;
	const [activeSnapPoint, setActiveSnapPointUnwrapped] = useControlled({
		controlled: snapPointProp,
		default: resolvedDefaultSnapPoint,
		name: "Drawer",
		state: "snapPoint"
	});
	const isNestedDrawerOpenRef = import_react.useRef(false);
	const setActiveSnapPoint = useStableCallback((nextSnapPoint, eventDetails) => {
		const resolvedEventDetails = eventDetails ?? createChangeEventDetails("none");
		onSnapPointChange?.(nextSnapPoint, resolvedEventDetails);
		if (resolvedEventDetails.isCanceled) return;
		setActiveSnapPointUnwrapped(nextSnapPoint);
	});
	const resolvedActiveSnapPoint = import_react.useMemo(() => {
		if (isSnapPointControlled) return activeSnapPoint;
		if (!snapPoints || snapPoints.length === 0) return activeSnapPoint;
		if (activeSnapPoint === null || !snapPoints.some((snapPoint) => Object.is(snapPoint, activeSnapPoint))) return resolvedDefaultSnapPoint;
		return activeSnapPoint;
	}, [
		activeSnapPoint,
		isSnapPointControlled,
		resolvedDefaultSnapPoint,
		snapPoints
	]);
	const onPopupHeightChange = useStableCallback((height) => {
		setPopupHeight(height);
		if (!isNestedDrawerOpenRef.current && height > 0) setFrontmostHeight(height);
	});
	const onNestedFrontmostHeightChange = useStableCallback((height) => {
		if (height > 0) {
			isNestedDrawerOpenRef.current = true;
			setFrontmostHeight(height);
			return;
		}
		isNestedDrawerOpenRef.current = false;
		if (popupHeight > 0) setFrontmostHeight(popupHeight);
	});
	const onNestedDrawerPresenceChange = useStableCallback((present) => {
		setHasNestedDrawer(present);
	});
	const onNestedSwipeProgressChange = useStableCallback((progress) => {
		nestedSwipeProgressStore.set(progress);
		notifyParentSwipeProgressChange?.(progress);
	});
	const onNestedSwipingChange = useStableCallback((swiping) => {
		setNestedSwiping(swiping);
		notifyParentSwipingChange?.(swiping);
	});
	const handleOpenChange = useStableCallback((nextOpen, eventDetails) => {
		onOpenChange?.(nextOpen, eventDetails);
		if (eventDetails.isCanceled) return;
		if (!nextOpen && snapPoints && snapPoints.length > 0) setActiveSnapPoint(resolvedDefaultSnapPoint, createChangeEventDetails(eventDetails.reason, eventDetails.event, eventDetails.trigger));
	});
	const contextValue = import_react.useMemo(() => ({
		swipeDirection,
		snapToSequentialPoints,
		snapPoints,
		activeSnapPoint: resolvedActiveSnapPoint,
		setActiveSnapPoint,
		frontmostHeight,
		popupHeight,
		hasNestedDrawer,
		nestedSwiping,
		nestedSwipeProgressStore,
		onNestedDrawerPresenceChange,
		onPopupHeightChange,
		onNestedFrontmostHeightChange,
		onNestedSwipingChange,
		onNestedSwipeProgressChange,
		notifyParentFrontmostHeight,
		notifyParentSwipingChange,
		notifyParentSwipeProgressChange,
		notifyParentHasNestedDrawer
	}), [
		resolvedActiveSnapPoint,
		frontmostHeight,
		hasNestedDrawer,
		nestedSwiping,
		nestedSwipeProgressStore,
		notifyParentHasNestedDrawer,
		notifyParentSwipeProgressChange,
		notifyParentSwipingChange,
		notifyParentFrontmostHeight,
		onNestedDrawerPresenceChange,
		onNestedFrontmostHeightChange,
		onNestedSwipeProgressChange,
		onNestedSwipingChange,
		onPopupHeightChange,
		popupHeight,
		setActiveSnapPoint,
		snapPoints,
		snapToSequentialPoints,
		swipeDirection
	]);
	const resolvedChildren = typeof children === "function" ? (payload) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [_DrawerProviderReport || (_DrawerProviderReport = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DrawerProviderReporter, {})), children(payload)] }) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [_DrawerProviderReport2 || (_DrawerProviderReport2 = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DrawerProviderReporter, {})), children] });
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DrawerRootContext.Provider, {
		value: contextValue,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DialogRoot, {
			open: openProp,
			defaultOpen,
			onOpenChange: handleOpenChange,
			onOpenChangeComplete,
			disablePointerDismissal,
			modal,
			actionsRef,
			handle,
			triggerId: triggerIdProp,
			defaultTriggerId: defaultTriggerIdProp,
			children: resolvedChildren
		})
	});
}
function createNestedSwipeProgressStore() {
	let progress = 0;
	const listeners = /* @__PURE__ */ new Set();
	return {
		getSnapshot: () => progress,
		set(nextProgress) {
			const resolved = Number.isFinite(nextProgress) ? nextProgress : 0;
			if (resolved === progress) return;
			progress = resolved;
			listeners.forEach((listener) => {
				listener();
			});
		},
		subscribe(listener) {
			listeners.add(listener);
			return () => {
				listeners.delete(listener);
			};
		}
	};
}
function DrawerProviderReporter() {
	const drawerId = useId();
	const providerContext = useDrawerProviderContext(true);
	const dialogRootContext = useDialogRootContext(false);
	const open = dialogRootContext.store.useState("open");
	const nestedOpenDialogCount = dialogRootContext.store.useState("nestedOpenDialogCount");
	const popupElement = dialogRootContext.store.useState("popupElement");
	const isTopmost = nestedOpenDialogCount === 0;
	import_react.useEffect(() => {
		if (!providerContext || drawerId == null) return;
		return () => {
			providerContext.removeDrawer(drawerId);
		};
	}, [drawerId, providerContext]);
	import_react.useEffect(() => {
		if (drawerId == null) return;
		providerContext?.setDrawerOpen(drawerId, open);
	}, [
		drawerId,
		open,
		providerContext
	]);
	import_react.useEffect(() => {
		if (!open || !isTopmost || !isAndroid) return;
		const CloseWatcherCtor = getWindow(popupElement).CloseWatcher;
		if (!CloseWatcherCtor) return;
		function handleCloseWatcher(event) {
			if (!dialogRootContext.store.select("open")) return;
			dialogRootContext.store.setOpen(false, createChangeEventDetails(closeWatcher, event));
		}
		const closeWatcher$1 = new CloseWatcherCtor();
		closeWatcher$1.addEventListener("close", handleCloseWatcher);
		return () => {
			closeWatcher$1.removeEventListener("close", handleCloseWatcher);
			closeWatcher$1.destroy();
		};
	}, [
		dialogRootContext.store,
		isTopmost,
		open,
		popupElement
	]);
	return null;
}
//#endregion
//#region node_modules/@base-ui/react/esm/utils/scrollable.js
function isScrollable(element, axis) {
	const style = getComputedStyle$1(element);
	if (axis === "vertical") {
		const overflowY = style.overflowY;
		return (overflowY === "auto" || overflowY === "scroll") && element.scrollHeight > element.clientHeight;
	}
	const overflowX = style.overflowX;
	return (overflowX === "auto" || overflowX === "scroll") && element.scrollWidth > element.clientWidth;
}
function hasScrollableAncestor(target, root, axes) {
	let node = target;
	while (node && node !== root) {
		for (const axis of axes) if (isScrollable(node, axis)) return true;
		node = node.parentElement;
	}
	return false;
}
function findScrollableTouchTarget(target, root, axis = "vertical") {
	let node = isHTMLElement(target) ? target : null;
	while (node && node !== root) {
		if (isScrollable(node, axis)) return node;
		node = node.parentElement;
	}
	return isScrollable(root, axis) ? root : null;
}
//#endregion
//#region node_modules/@base-ui/react/esm/utils/getElementAtPoint.js
function getElementAtPoint(doc, x, y) {
	return typeof doc?.elementFromPoint === "function" ? doc.elementFromPoint(x, y) : null;
}
//#endregion
//#region node_modules/@base-ui/react/esm/utils/useSwipeDismiss.js
var DEFAULT_SWIPE_THRESHOLD = 40;
var REVERSE_CANCEL_THRESHOLD$1 = 10;
var MIN_DRAG_THRESHOLD$1 = 1;
var MIN_VELOCITY_DURATION_MS = 50;
var MIN_RELEASE_VELOCITY_DURATION_MS = 16;
var MAX_RELEASE_VELOCITY_AGE_MS = 80;
var DEFAULT_IGNORE_SELECTOR = "button,a,input,select,textarea,label,[role=\"button\"]";
function getDisplacement$1(direction, deltaX, deltaY) {
	switch (direction) {
		case "up": return -deltaY;
		case "down": return deltaY;
		case "left": return -deltaX;
		case "right": return deltaX;
		default: return 0;
	}
}
function getElementTransform$1(element) {
	const transform = window.getComputedStyle(element).transform;
	let translateX = 0;
	let translateY = 0;
	let scale = 1;
	if (transform && transform !== "none") {
		const matrix = transform.match(/matrix(?:3d)?\(([^)]+)\)/);
		if (matrix) {
			const values = matrix[1].split(", ").map(parseFloat);
			if (values.length === 6) {
				translateX = values[4];
				translateY = values[5];
				scale = Math.sqrt(values[0] * values[0] + values[1] * values[1]);
			} else if (values.length === 16) {
				translateX = values[12];
				translateY = values[13];
				scale = values[0];
			}
		}
	}
	return {
		x: translateX,
		y: translateY,
		scale
	};
}
function getValidTimeStamp(timeStamp) {
	return Number.isFinite(timeStamp) && timeStamp > 0 ? timeStamp : null;
}
function hasPrimaryMouseButton(buttons) {
	return buttons % 2 === 1;
}
function safelyChangePointerCapture(element, pointerId, method) {
	const pointerCaptureMethod = element[method];
	if (typeof pointerCaptureMethod !== "function") return;
	try {
		pointerCaptureMethod.call(element, pointerId);
	} catch (error) {
		if (error && typeof error === "object" && "name" in error && error.name === "NotFoundError") return;
		throw error;
	}
}
function useSwipeDismiss(options) {
	const { enabled, directions, elementRef, movementCssVars, canStart, ignoreSelectorWhenTouch = true, ignoreScrollableAncestors = false, swipeThreshold: swipeThresholdProp, onDismiss, onProgress, onSwipeStart, onRelease, onSwipingChange, trackDrag = true } = options;
	const ignoreSelector = DEFAULT_IGNORE_SELECTOR;
	const primaryDirection = directions.length === 1 ? directions[0] : void 0;
	const swipeThresholdDefault = Math.max(0, typeof swipeThresholdProp === "number" ? swipeThresholdProp : DEFAULT_SWIPE_THRESHOLD);
	const allowLeft = directions.includes("left");
	const allowRight = directions.includes("right");
	const allowUp = directions.includes("up");
	const allowDown = directions.includes("down");
	const hasHorizontal = allowLeft || allowRight;
	const hasVertical = allowUp || allowDown;
	const scrollAxes = import_react.useMemo(() => {
		const axes = [];
		if (hasVertical) axes.push("vertical");
		if (hasHorizontal) axes.push("horizontal");
		return axes;
	}, [hasHorizontal, hasVertical]);
	const [currentSwipeDirection, setCurrentSwipeDirection] = import_react.useState(void 0);
	const [isSwiping, setIsSwiping] = import_react.useState(false);
	const [isRealSwipe, setIsRealSwipe] = import_react.useState(false);
	const [dragDismissed, setDragDismissed] = import_react.useState(false);
	const [dragOffset, setDragOffset] = import_react.useState({
		x: 0,
		y: 0
	});
	const [initialTransform, setInitialTransform] = import_react.useState({
		x: 0,
		y: 0,
		scale: 1
	});
	const [lockedDirection, setLockedDirection] = import_react.useState(null);
	const dragStartPosRef = import_react.useRef({
		x: 0,
		y: 0
	});
	const dragOffsetRef = import_react.useRef({
		x: 0,
		y: 0
	});
	const lastMovePosRef = import_react.useRef(null);
	const initialTransformRef = import_react.useRef({
		x: 0,
		y: 0,
		scale: 1
	});
	const intendedSwipeDirectionRef = import_react.useRef(void 0);
	const maxSwipeDisplacementRef = import_react.useRef(0);
	const cancelledSwipeRef = import_react.useRef(false);
	const swipeCancelBaselineRef = import_react.useRef({
		x: 0,
		y: 0
	});
	const isFirstPointerMoveRef = import_react.useRef(false);
	const pendingSwipeRef = import_react.useRef(false);
	const pendingSwipeStartPosRef = import_react.useRef(null);
	const swipeFromScrollableRef = import_react.useRef(false);
	const sawPrimaryButtonsOnMoveRef = import_react.useRef(false);
	const elementSizeRef = import_react.useRef({
		width: 0,
		height: 0
	});
	const swipeProgressRef = import_react.useRef(0);
	const swipeThresholdRef = import_react.useRef(swipeThresholdDefault);
	const swipeStartTimeRef = import_react.useRef(null);
	const lastDragSampleRef = import_react.useRef(null);
	const lastDragVelocityRef = import_react.useRef({
		x: 0,
		y: 0
	});
	const lastProgressDetailsRef = import_react.useRef(null);
	const isSwipingRef = import_react.useRef(false);
	const setSwiping = useStableCallback((nextSwiping) => {
		if (isSwipingRef.current === nextSwiping) return;
		isSwipingRef.current = nextSwiping;
		setIsSwiping(nextSwiping);
		onSwipingChange?.(nextSwiping);
	});
	function resolveSwipeThreshold(direction) {
		if (!direction) return;
		if (typeof swipeThresholdProp !== "function") {
			swipeThresholdRef.current = swipeThresholdDefault;
			return;
		}
		const element = elementRef.current;
		if (!element) return;
		const value = swipeThresholdProp({
			element,
			direction
		});
		swipeThresholdRef.current = Math.max(0, value);
	}
	const updateSwipeProgress = useStableCallback((progress, details) => {
		const nextProgress = Number.isFinite(progress) ? clamp(progress, 0, 1) : 0;
		const progressChanged = nextProgress !== swipeProgressRef.current;
		let detailsChanged = false;
		if (details) {
			const lastDetails = lastProgressDetailsRef.current;
			detailsChanged = !lastDetails || lastDetails.deltaX !== details.deltaX || lastDetails.deltaY !== details.deltaY || lastDetails.direction !== details.direction;
		}
		if (!progressChanged && !detailsChanged) return;
		swipeProgressRef.current = nextProgress;
		if (details) lastProgressDetailsRef.current = details;
		else if (progressChanged) lastProgressDetailsRef.current = null;
		onProgress?.(nextProgress, details);
	});
	function recordDragSample(offset, timeStamp) {
		if (timeStamp === null) return;
		const lastSample = lastDragSampleRef.current;
		if (lastSample && timeStamp > lastSample.time) {
			const durationMs = Math.max(timeStamp - lastSample.time, MIN_RELEASE_VELOCITY_DURATION_MS);
			lastDragVelocityRef.current = {
				x: (offset.x - lastSample.x) / durationMs,
				y: (offset.y - lastSample.y) / durationMs
			};
		}
		lastDragSampleRef.current = {
			x: offset.x,
			y: offset.y,
			time: timeStamp
		};
	}
	const reset = import_react.useCallback(() => {
		setCurrentSwipeDirection(void 0);
		setSwiping(false);
		setIsRealSwipe(false);
		setDragDismissed(false);
		setDragOffset({
			x: 0,
			y: 0
		});
		setInitialTransform({
			x: 0,
			y: 0,
			scale: 1
		});
		setLockedDirection(null);
		updateSwipeProgress(0);
		swipeThresholdRef.current = swipeThresholdDefault;
		dragStartPosRef.current = {
			x: 0,
			y: 0
		};
		dragOffsetRef.current = {
			x: 0,
			y: 0
		};
		initialTransformRef.current = {
			x: 0,
			y: 0,
			scale: 1
		};
		intendedSwipeDirectionRef.current = void 0;
		maxSwipeDisplacementRef.current = 0;
		cancelledSwipeRef.current = false;
		swipeCancelBaselineRef.current = {
			x: 0,
			y: 0
		};
		isFirstPointerMoveRef.current = false;
		lastMovePosRef.current = null;
		pendingSwipeRef.current = false;
		pendingSwipeStartPosRef.current = null;
		swipeFromScrollableRef.current = false;
		sawPrimaryButtonsOnMoveRef.current = false;
		elementSizeRef.current = {
			width: 0,
			height: 0
		};
		swipeStartTimeRef.current = null;
		lastDragSampleRef.current = null;
		lastDragVelocityRef.current = {
			x: 0,
			y: 0
		};
		lastProgressDetailsRef.current = null;
	}, [
		setSwiping,
		swipeThresholdDefault,
		updateSwipeProgress
	]);
	import_react.useEffect(() => {
		if (typeof swipeThresholdProp !== "function") swipeThresholdRef.current = swipeThresholdDefault;
	}, [swipeThresholdDefault, swipeThresholdProp]);
	function getPrimaryPointerPosition(event) {
		if ("touches" in event) {
			const touch = event.touches[0];
			return touch ? {
				x: touch.clientX,
				y: touch.clientY
			} : null;
		}
		return {
			x: event.clientX,
			y: event.clientY
		};
	}
	function isTouchLikeEvent(event) {
		if ("touches" in event) return true;
		return event.pointerType === "touch";
	}
	function getTargetAtPoint(position, nativeEvent) {
		return getElementAtPoint(ownerDocument(elementRef.current), position.x, position.y) ?? getTarget(nativeEvent);
	}
	function findGestureScrollableTouchTarget(target, root) {
		if (hasHorizontal && !hasVertical) return findScrollableTouchTarget(target, root, "horizontal");
		if (hasVertical && !hasHorizontal) return findScrollableTouchTarget(target, root, "vertical");
		return findScrollableTouchTarget(target, root, "vertical") ?? findScrollableTouchTarget(target, root, "horizontal");
	}
	function startSwipeAtPosition(event, position, startOptions) {
		swipeFromScrollableRef.current = false;
		const touchLike = isTouchLikeEvent(event);
		const target = getTargetAtPoint(position, event.nativeEvent);
		const body = ownerDocument(elementRef.current).body;
		const scrollableTarget = touchLike && body ? findGestureScrollableTouchTarget(target, body) : null;
		const ignoreScrollableTarget = startOptions?.ignoreScrollableTarget ?? false;
		if (scrollableTarget && !ignoreScrollableTarget) return false;
		swipeFromScrollableRef.current = Boolean(scrollableTarget && ignoreScrollableTarget);
		if ((target ? target.closest(ignoreSelector) : false) && (!touchLike || ignoreSelectorWhenTouch)) return false;
		const element = elementRef.current;
		if (ignoreScrollableAncestors && element && target && scrollAxes.length > 0) {
			if (!(startOptions?.ignoreScrollableAncestors ?? false) && hasScrollableAncestor(target, element, scrollAxes)) return false;
		}
		cancelledSwipeRef.current = false;
		intendedSwipeDirectionRef.current = void 0;
		maxSwipeDisplacementRef.current = 0;
		dragStartPosRef.current = position;
		swipeStartTimeRef.current = getValidTimeStamp(event.timeStamp);
		swipeCancelBaselineRef.current = position;
		lastMovePosRef.current = position;
		if (element) {
			elementSizeRef.current = {
				width: element.offsetWidth,
				height: element.offsetHeight
			};
			resolveSwipeThreshold(primaryDirection);
			const transform = getElementTransform$1(element);
			initialTransformRef.current = transform;
			dragOffsetRef.current = {
				x: transform.x,
				y: transform.y
			};
			setInitialTransform(transform);
			setDragOffset({
				x: transform.x,
				y: transform.y
			});
			recordDragSample({
				x: transform.x,
				y: transform.y
			}, swipeStartTimeRef.current);
			if (!("touches" in event)) safelyChangePointerCapture(element, event.pointerId, "setPointerCapture");
		}
		onSwipeStart?.(event.nativeEvent);
		setSwiping(true);
		setIsRealSwipe(false);
		setLockedDirection(null);
		isFirstPointerMoveRef.current = true;
		updateSwipeProgress(0);
		return true;
	}
	function resetPendingSwipeState() {
		clearPendingSwipeStartState();
		swipeFromScrollableRef.current = false;
		lastMovePosRef.current = null;
	}
	function clearPendingSwipeStartState() {
		pendingSwipeRef.current = false;
		pendingSwipeStartPosRef.current = null;
	}
	function cancelSwipeInteraction(event) {
		resetPendingSwipeState();
		if (!isSwipingRef.current) return;
		setSwiping(false);
		setIsRealSwipe(false);
		setLockedDirection(null);
		const resolvedInitialTransform = trackDrag ? initialTransform : initialTransformRef.current;
		dragOffsetRef.current = {
			x: resolvedInitialTransform.x,
			y: resolvedInitialTransform.y
		};
		setDragOffset({
			x: resolvedInitialTransform.x,
			y: resolvedInitialTransform.y
		});
		setCurrentSwipeDirection(void 0);
		sawPrimaryButtonsOnMoveRef.current = false;
		const element = elementRef.current;
		if (element) safelyChangePointerCapture(element, event.pointerId, "releasePointerCapture");
		updateSwipeProgress(0, {
			deltaX: 0,
			deltaY: 0,
			direction: void 0
		});
	}
	function applyDirectionalDamping(deltaX, deltaY) {
		const exponent = (value) => value >= 0 ? value ** .5 : -(Math.abs(value) ** .5);
		const dampAxis = (delta, allowNegative, allowPositive) => {
			if (!allowNegative && delta < 0) return exponent(delta);
			if (!allowPositive && delta > 0) return exponent(delta);
			return delta;
		};
		return {
			x: hasHorizontal ? dampAxis(deltaX, allowLeft, allowRight) : exponent(deltaX),
			y: hasVertical ? dampAxis(deltaY, allowUp, allowDown) : exponent(deltaY)
		};
	}
	function canSwipeFromScrollEdgeOnPendingMove(scrollTarget, deltaX, deltaY) {
		const absDeltaX = Math.abs(deltaX);
		const absDeltaY = Math.abs(deltaY);
		if (hasVertical && deltaY !== 0 && (!hasHorizontal || absDeltaY >= absDeltaX)) {
			const maxScrollTop = Math.max(0, scrollTarget.scrollHeight - scrollTarget.clientHeight);
			const atTop = scrollTarget.scrollTop <= 0;
			const atBottom = scrollTarget.scrollTop >= maxScrollTop;
			return deltaY > 0 && atTop && allowDown || deltaY < 0 && atBottom && allowUp;
		}
		if (hasHorizontal && deltaX !== 0 && (!hasVertical || absDeltaX > absDeltaY)) {
			const maxScrollLeft = Math.max(0, scrollTarget.scrollWidth - scrollTarget.clientWidth);
			const atLeft = scrollTarget.scrollLeft <= 0;
			const atRight = scrollTarget.scrollLeft >= maxScrollLeft;
			return deltaX > 0 && atLeft && allowRight || deltaX < 0 && atRight && allowLeft;
		}
		return null;
	}
	const handleStart = useStableCallback((event) => {
		if (!enabled) return;
		if (event.defaultPrevented || event.nativeEvent.defaultPrevented) return;
		if (!("touches" in event) && event.button !== 0) return;
		const startPos = getPrimaryPointerPosition(event);
		if (!startPos) return;
		pendingSwipeRef.current = true;
		pendingSwipeStartPosRef.current = startPos;
		swipeFromScrollableRef.current = false;
		sawPrimaryButtonsOnMoveRef.current = false;
		if (!(canStart ? canStart(startPos, {
			nativeEvent: event.nativeEvent,
			direction: primaryDirection
		}) : true)) return;
		if (startSwipeAtPosition(event, startPos)) clearPendingSwipeStartState();
	});
	function handleMoveCore(event, position, movement) {
		if (!enabled || !isSwipingRef.current) return;
		const target = getTarget(event.nativeEvent);
		if (isTouchLikeEvent(event) && !swipeFromScrollableRef.current) {
			const boundaryElement = event.currentTarget;
			if (findGestureScrollableTouchTarget(target, boundaryElement)) return;
		}
		if (!("touches" in event)) event.preventDefault();
		if (isFirstPointerMoveRef.current) {
			dragStartPosRef.current = position;
			const moveTime = getValidTimeStamp(event.timeStamp);
			if (moveTime !== null) swipeStartTimeRef.current = moveTime;
			isFirstPointerMoveRef.current = false;
		}
		const clientX = position.x;
		const clientY = position.y;
		const movementX = movement.x;
		const movementY = movement.y;
		if (movementY < 0 && clientY > swipeCancelBaselineRef.current.y || movementY > 0 && clientY < swipeCancelBaselineRef.current.y) swipeCancelBaselineRef.current = {
			x: swipeCancelBaselineRef.current.x,
			y: clientY
		};
		if (movementX < 0 && clientX > swipeCancelBaselineRef.current.x || movementX > 0 && clientX < swipeCancelBaselineRef.current.x) swipeCancelBaselineRef.current = {
			x: clientX,
			y: swipeCancelBaselineRef.current.y
		};
		const deltaX = clientX - dragStartPosRef.current.x;
		const deltaY = clientY - dragStartPosRef.current.y;
		const cancelDeltaY = clientY - swipeCancelBaselineRef.current.y;
		const cancelDeltaX = clientX - swipeCancelBaselineRef.current.x;
		if (!isRealSwipe) {
			if (Math.sqrt(deltaX * deltaX + deltaY * deltaY) >= MIN_DRAG_THRESHOLD$1) {
				setIsRealSwipe(true);
				if (lockedDirection === null) {
					if (hasHorizontal && hasVertical) setLockedDirection(Math.abs(deltaX) > Math.abs(deltaY) ? "horizontal" : "vertical");
				}
			}
		}
		let candidate;
		if (!intendedSwipeDirectionRef.current) {
			if (lockedDirection === "vertical") {
				if (deltaY > 0) candidate = "down";
				else if (deltaY < 0) candidate = "up";
			} else if (lockedDirection === "horizontal") {
				if (deltaX > 0) candidate = "right";
				else if (deltaX < 0) candidate = "left";
			} else if (Math.abs(deltaX) >= Math.abs(deltaY)) candidate = deltaX > 0 ? "right" : "left";
			else candidate = deltaY > 0 ? "down" : "up";
			if (candidate) {
				if (candidate === "left" && allowLeft || candidate === "right" && allowRight || candidate === "up" && allowUp || candidate === "down" && allowDown) {
					intendedSwipeDirectionRef.current = candidate;
					maxSwipeDisplacementRef.current = getDisplacement$1(candidate, deltaX, deltaY);
					setCurrentSwipeDirection(candidate);
					resolveSwipeThreshold(candidate);
				}
			}
		} else {
			const direction = intendedSwipeDirectionRef.current;
			const currentDisplacement = getDisplacement$1(direction, cancelDeltaX, cancelDeltaY);
			if (currentDisplacement > swipeThresholdRef.current) {
				cancelledSwipeRef.current = false;
				setCurrentSwipeDirection(direction);
			} else if (!(allowLeft && allowRight) && !(allowUp && allowDown) && maxSwipeDisplacementRef.current - currentDisplacement >= REVERSE_CANCEL_THRESHOLD$1) cancelledSwipeRef.current = true;
		}
		const dampedDelta = applyDirectionalDamping(deltaX, deltaY);
		let newOffsetX = initialTransformRef.current.x;
		let newOffsetY = initialTransformRef.current.y;
		if (lockedDirection === "horizontal") {
			if (hasHorizontal) newOffsetX += dampedDelta.x;
		} else if (lockedDirection === "vertical") {
			if (hasVertical) newOffsetY += dampedDelta.y;
		} else {
			if (hasHorizontal) newOffsetX += dampedDelta.x;
			if (hasVertical) newOffsetY += dampedDelta.y;
		}
		dragOffsetRef.current = {
			x: newOffsetX,
			y: newOffsetY
		};
		if (trackDrag) setDragOffset({
			x: newOffsetX,
			y: newOffsetY
		});
		recordDragSample({
			x: newOffsetX,
			y: newOffsetY
		}, getValidTimeStamp(event.timeStamp));
		const dragDeltaX = newOffsetX - initialTransformRef.current.x;
		const dragDeltaY = newOffsetY - initialTransformRef.current.y;
		const swipeDirectionDetails = intendedSwipeDirectionRef.current;
		const progressDirection = primaryDirection ?? intendedSwipeDirectionRef.current;
		if (!progressDirection) {
			updateSwipeProgress(0, {
				deltaX: dragDeltaX,
				deltaY: dragDeltaY,
				direction: swipeDirectionDetails
			});
			return;
		}
		const size = progressDirection === "left" || progressDirection === "right" ? elementSizeRef.current.width : elementSizeRef.current.height;
		const scale = initialTransformRef.current.scale || 1;
		if (size <= 0 || scale <= 0) {
			updateSwipeProgress(0, {
				deltaX: dragDeltaX,
				deltaY: dragDeltaY,
				direction: swipeDirectionDetails
			});
			return;
		}
		const progressDisplacement = getDisplacement$1(progressDirection, newOffsetX - initialTransformRef.current.x, newOffsetY - initialTransformRef.current.y);
		if (progressDisplacement <= 0) {
			updateSwipeProgress(0, {
				deltaX: dragDeltaX,
				deltaY: dragDeltaY,
				direction: swipeDirectionDetails
			});
			return;
		}
		updateSwipeProgress(progressDisplacement / (size * scale), {
			deltaX: dragDeltaX,
			deltaY: dragDeltaY,
			direction: swipeDirectionDetails
		});
	}
	const handleMove = useStableCallback((event) => {
		const currentPos = getPrimaryPointerPosition(event);
		if (!currentPos) return;
		if (!("touches" in event)) {
			const hasPrimaryButton = hasPrimaryMouseButton(event.buttons);
			if (hasPrimaryButton) sawPrimaryButtonsOnMoveRef.current = true;
			const lostPrimaryButtonDuringSwipe = event.buttons === 0 && sawPrimaryButtonsOnMoveRef.current;
			if (event.buttons !== 0 && !hasPrimaryButton || lostPrimaryButtonDuringSwipe) {
				cancelSwipeInteraction(event);
				return;
			}
		}
		if (!isSwiping && pendingSwipeRef.current) {
			if (!isTouchLikeEvent(event) && (event.defaultPrevented || event.nativeEvent.defaultPrevented)) {
				resetPendingSwipeState();
				return;
			}
			if (canStart ? canStart(currentPos, {
				nativeEvent: event.nativeEvent,
				direction: primaryDirection
			}) : true) {
				const pendingStartPos = pendingSwipeStartPosRef.current;
				let ignoreScrollableOnStart = false;
				if (isTouchLikeEvent(event)) {
					const element = elementRef.current;
					if (pendingStartPos && element) {
						const target = getTargetAtPoint(currentPos, event.nativeEvent);
						const body = ownerDocument(element).body;
						const scrollTarget = body ? findGestureScrollableTouchTarget(target, body) : null;
						if (scrollTarget && (contains(element, scrollTarget) || contains(scrollTarget, element))) {
							const canSwipeFromEdge = canSwipeFromScrollEdgeOnPendingMove(scrollTarget, currentPos.x - pendingStartPos.x, currentPos.y - pendingStartPos.y);
							if (canSwipeFromEdge === false) return;
							if (canSwipeFromEdge === true) ignoreScrollableOnStart = true;
						}
					}
				}
				if (startSwipeAtPosition(event, currentPos, {
					ignoreScrollableTarget: ignoreScrollableOnStart,
					ignoreScrollableAncestors: ignoreScrollableOnStart
				})) if (pendingStartPos && ignoreScrollableOnStart) {
					clearPendingSwipeStartState();
					dragStartPosRef.current = pendingStartPos;
					swipeCancelBaselineRef.current = pendingStartPos;
					lastMovePosRef.current = pendingStartPos;
					isFirstPointerMoveRef.current = false;
				} else {
					clearPendingSwipeStartState();
					swipeFromScrollableRef.current = false;
				}
			}
		}
		const previousPos = lastMovePosRef.current;
		const movement = previousPos === null ? {
			x: 0,
			y: 0
		} : {
			x: currentPos.x - previousPos.x,
			y: currentPos.y - previousPos.y
		};
		lastMovePosRef.current = currentPos;
		handleMoveCore(event, currentPos, movement);
	});
	const handleEnd = useStableCallback((event) => {
		if (!enabled) return;
		const resolvedDragOffset = dragOffsetRef.current;
		const resolvedInitialTransform = initialTransformRef.current;
		const releaseDeltaX = resolvedDragOffset.x - resolvedInitialTransform.x;
		const releaseDeltaY = resolvedDragOffset.y - resolvedInitialTransform.y;
		const progressDetails = {
			deltaX: releaseDeltaX,
			deltaY: releaseDeltaY,
			direction: currentSwipeDirection ?? intendedSwipeDirectionRef.current
		};
		if (!isSwipingRef.current) {
			resetPendingSwipeState();
			updateSwipeProgress(0, progressDetails);
			return;
		}
		setSwiping(false);
		setIsRealSwipe(false);
		setLockedDirection(null);
		resetPendingSwipeState();
		sawPrimaryButtonsOnMoveRef.current = false;
		const element = elementRef.current;
		if (element) {
			if (!("touches" in event)) safelyChangePointerCapture(element, event.pointerId, "releasePointerCapture");
		}
		const deltaX = releaseDeltaX;
		const deltaY = releaseDeltaY;
		const startTime = swipeStartTimeRef.current;
		const endTime = getValidTimeStamp(event.timeStamp);
		const durationMs = startTime !== null && endTime !== null && endTime > startTime ? endTime - startTime : 0;
		const velocityDurationMs = durationMs > 0 ? Math.max(durationMs, MIN_VELOCITY_DURATION_MS) : 0;
		const velocityX = velocityDurationMs > 0 ? deltaX / velocityDurationMs : 0;
		const velocityY = velocityDurationMs > 0 ? deltaY / velocityDurationMs : 0;
		let releaseVelocityX = lastDragVelocityRef.current.x;
		let releaseVelocityY = lastDragVelocityRef.current.y;
		const lastSample = lastDragSampleRef.current;
		if (lastSample && endTime !== null && endTime >= lastSample.time) {
			const ageMs = endTime - lastSample.time;
			if (ageMs <= MAX_RELEASE_VELOCITY_AGE_MS) {
				const sampleDurationMs = Math.max(ageMs, MIN_RELEASE_VELOCITY_DURATION_MS);
				const deltaFromLastSampleX = resolvedDragOffset.x - lastSample.x;
				const deltaFromLastSampleY = resolvedDragOffset.y - lastSample.y;
				const sampleVelocityX = deltaFromLastSampleX / sampleDurationMs;
				const sampleVelocityY = deltaFromLastSampleY / sampleDurationMs;
				if (sampleVelocityX !== 0) releaseVelocityX = sampleVelocityX;
				if (sampleVelocityY !== 0) releaseVelocityY = sampleVelocityY;
			} else {
				releaseVelocityX = 0;
				releaseVelocityY = 0;
			}
		}
		const releaseDecision = onRelease?.({
			event: event.nativeEvent,
			direction: currentSwipeDirection ?? intendedSwipeDirectionRef.current,
			deltaX,
			deltaY,
			velocityX,
			velocityY,
			releaseVelocityX,
			releaseVelocityY
		});
		const hasReleaseDecision = typeof releaseDecision === "boolean";
		if (cancelledSwipeRef.current && !hasReleaseDecision) {
			dragOffsetRef.current = {
				x: resolvedInitialTransform.x,
				y: resolvedInitialTransform.y
			};
			setDragOffset({
				x: resolvedInitialTransform.x,
				y: resolvedInitialTransform.y
			});
			setCurrentSwipeDirection(void 0);
			updateSwipeProgress(0, progressDetails);
			return;
		}
		let shouldClose = false;
		let dismissDirection;
		if (hasReleaseDecision) {
			shouldClose = releaseDecision;
			dismissDirection = currentSwipeDirection ?? intendedSwipeDirectionRef.current ?? primaryDirection;
		} else for (const direction of directions) {
			switch (direction) {
				case "right":
					if (deltaX > swipeThresholdRef.current) {
						shouldClose = true;
						dismissDirection = "right";
					}
					break;
				case "left":
					if (deltaX < -swipeThresholdRef.current) {
						shouldClose = true;
						dismissDirection = "left";
					}
					break;
				case "down":
					if (deltaY > swipeThresholdRef.current) {
						shouldClose = true;
						dismissDirection = "down";
					}
					break;
				case "up":
					if (deltaY < -swipeThresholdRef.current) {
						shouldClose = true;
						dismissDirection = "up";
					}
					break;
				default: break;
			}
			if (shouldClose) break;
		}
		if (shouldClose && dismissDirection) {
			setCurrentSwipeDirection(dismissDirection);
			setDragDismissed(true);
			onDismiss?.(event.nativeEvent, { direction: dismissDirection });
		} else {
			dragOffsetRef.current = {
				x: resolvedInitialTransform.x,
				y: resolvedInitialTransform.y
			};
			setDragOffset({
				x: resolvedInitialTransform.x,
				y: resolvedInitialTransform.y
			});
			setCurrentSwipeDirection(void 0);
			updateSwipeProgress(0, progressDetails);
		}
	});
	const getDragStyles = import_react.useCallback(() => {
		const resolvedDragOffset = trackDrag ? dragOffset : dragOffsetRef.current;
		const resolvedInitialTransform = trackDrag ? initialTransform : initialTransformRef.current;
		if (!isSwiping && resolvedDragOffset.x === resolvedInitialTransform.x && resolvedDragOffset.y === resolvedInitialTransform.y && !dragDismissed) return {
			[movementCssVars.x]: "0px",
			[movementCssVars.y]: "0px"
		};
		const deltaX = resolvedDragOffset.x - resolvedInitialTransform.x;
		const deltaY = resolvedDragOffset.y - resolvedInitialTransform.y;
		return {
			transition: isSwiping ? "none" : void 0,
			transform: isSwiping ? `translateX(${resolvedDragOffset.x}px) translateY(${resolvedDragOffset.y}px) scale(${resolvedInitialTransform.scale})` : void 0,
			[movementCssVars.x]: `${deltaX}px`,
			[movementCssVars.y]: `${deltaY}px`
		};
	}, [
		dragDismissed,
		dragOffset,
		initialTransform,
		isSwiping,
		movementCssVars,
		trackDrag
	]);
	return {
		swiping: isSwiping,
		swipeDirection: currentSwipeDirection,
		dragDismissed,
		getPointerProps: import_react.useCallback(() => {
			if (!enabled) return {};
			return {
				onPointerDown: handleStart,
				onPointerMove: handleMove,
				onPointerUp: handleEnd,
				onPointerCancel: handleEnd
			};
		}, [
			enabled,
			handleEnd,
			handleMove,
			handleStart
		]),
		getTouchProps: import_react.useCallback(() => {
			if (!enabled) return {};
			return {
				onTouchStart: handleStart,
				onTouchMove: handleMove,
				onTouchEnd: handleEnd,
				onTouchCancel: handleEnd
			};
		}, [
			enabled,
			handleEnd,
			handleMove,
			handleStart
		]),
		getDragStyles,
		reset
	};
}
//#endregion
//#region node_modules/@base-ui/react/esm/drawer/swipe-area/DrawerSwipeAreaDataAttributes.js
var DrawerSwipeAreaDataAttributes = function(DrawerSwipeAreaDataAttributes) {
	/**
	* Present when the drawer is open.
	*/
	DrawerSwipeAreaDataAttributes[DrawerSwipeAreaDataAttributes["open"] = CommonPopupDataAttributes.open] = "open";
	/**
	* Present when the drawer is closed.
	*/
	DrawerSwipeAreaDataAttributes[DrawerSwipeAreaDataAttributes["closed"] = CommonPopupDataAttributes.closed] = "closed";
	/**
	* Present when the swipe area is disabled.
	*/
	DrawerSwipeAreaDataAttributes["disabled"] = "data-disabled";
	/**
	* Indicates the swipe direction.
	* @type {'up' | 'down' | 'left' | 'right'}
	*/
	DrawerSwipeAreaDataAttributes["swipeDirection"] = "data-swipe-direction";
	/**
	* Present when the drawer is being swiped.
	*/
	DrawerSwipeAreaDataAttributes["swiping"] = "data-swiping";
	return DrawerSwipeAreaDataAttributes;
}({});
//#endregion
//#region node_modules/@base-ui/react/esm/drawer/swipe-area/DrawerSwipeArea.js
var DEFAULT_SWIPE_OPEN_RATIO = .5;
var MIN_SWIPE_START_DISTANCE = 1;
var VELOCITY_THRESHOLD = .1;
var FALLBACK_SWIPE_OPEN_THRESHOLD = 40;
var SWIPE_AREA_OPEN_HOOK = { [DrawerSwipeAreaDataAttributes.open]: "" };
var SWIPE_AREA_CLOSED_HOOK = { [DrawerSwipeAreaDataAttributes.closed]: "" };
var SWIPE_AREA_SWIPING_HOOK = { [DrawerSwipeAreaDataAttributes.swiping]: "" };
var SWIPE_AREA_DISABLED_HOOK = { [DrawerSwipeAreaDataAttributes.disabled]: "" };
var stateAttributesMapping$14 = {
	open(value) {
		return value ? SWIPE_AREA_OPEN_HOOK : SWIPE_AREA_CLOSED_HOOK;
	},
	swiping(value) {
		return value ? SWIPE_AREA_SWIPING_HOOK : null;
	},
	swipeDirection(value) {
		return value ? { [DrawerSwipeAreaDataAttributes.swipeDirection]: value } : null;
	},
	disabled(value) {
		return value ? SWIPE_AREA_DISABLED_HOOK : null;
	}
};
var oppositeSwipeDirection = {
	up: "down",
	down: "up",
	left: "right",
	right: "left"
};
function resolveTouchAction(direction) {
	return direction === "left" || direction === "right" ? "pan-y" : "pan-x";
}
/**
* An invisible area that listens for swipe gestures to open the drawer.
* Renders a `<div>` element.
*
* Documentation: [Base UI Drawer](https://base-ui.com/react/components/drawer)
*/
var DrawerSwipeArea = /* @__PURE__ */ import_react.forwardRef(function DrawerSwipeArea(componentProps, forwardedRef) {
	const { className, render, disabled = false, swipeDirection: swipeDirectionProp, ...elementProps } = componentProps;
	const { store } = useDialogRootContext();
	const { swipeDirection, frontmostHeight } = useDrawerRootContext();
	const providerContext = useDrawerProviderContext(true);
	const [swipeActive, setSwipeActive] = import_react.useState(false);
	const releaseDismissTimeout = useTimeout();
	const swipeAreaRef = import_react.useRef(null);
	const swipeStartEventRef = import_react.useRef(null);
	const openedBySwipeRef = import_react.useRef(false);
	const dragDeltaRef = import_react.useRef({
		x: 0,
		y: 0
	});
	const closedOffsetRef = import_react.useRef(null);
	const appliedSwipeStylesRef = import_react.useRef(false);
	const popupTransitionRef = import_react.useRef(null);
	const swipeAreaId = useBaseUiId(componentProps.id);
	const registerTrigger = useTriggerRegistration(swipeAreaId, store);
	const open = store.useState("open");
	const resolvedSwipeDirection = swipeDirectionProp ?? oppositeSwipeDirection[swipeDirection];
	const dismissDirection = oppositeSwipeDirection[resolvedSwipeDirection];
	const enabled = !disabled && (!open || swipeActive);
	const resetDragDelta = useStableCallback(() => {
		dragDeltaRef.current.x = 0;
		dragDeltaRef.current.y = 0;
	});
	function disableDismissForSwipe() {
		releaseDismissTimeout.clear();
		store.context.outsidePressEnabledRef.current = false;
	}
	function enableDismissAfterRelease() {
		releaseDismissTimeout.start(0, () => {
			store.context.outsidePressEnabledRef.current = true;
		});
	}
	function resolvePopupSize() {
		const popupElement = store.context.popupRef.current;
		if (!popupElement) return null;
		const size = dismissDirection === "left" || dismissDirection === "right" ? popupElement.offsetWidth : popupElement.offsetHeight;
		if (size <= 0) return null;
		return size;
	}
	function resolveClosedOffset() {
		const offset = resolvePopupSize();
		if (offset == null) return null;
		const popupElement = store.context.popupRef.current;
		if (!popupElement) return offset;
		const isHorizontal = dismissDirection === "left" || dismissDirection === "right";
		const transform = getElementTransform$1(popupElement);
		const transformOffset = isHorizontal ? transform.x : transform.y;
		if (Number.isFinite(transformOffset) && Math.abs(transformOffset) > .5) return Math.min(offset, Math.abs(transformOffset));
		return offset;
	}
	function resolveSwipeOpenThreshold() {
		const popupSize = resolvePopupSize();
		if (popupSize == null) return FALLBACK_SWIPE_OPEN_THRESHOLD;
		return popupSize * DEFAULT_SWIPE_OPEN_RATIO;
	}
	function applySwipeMovement() {
		if (!swipeActive) return;
		const popupElement = store.context.popupRef.current;
		if (!popupElement) return;
		if (!store.select("open") || !store.select("mounted")) return;
		if (closedOffsetRef.current == null) closedOffsetRef.current = resolveClosedOffset();
		const closedOffset = closedOffsetRef.current;
		if (!closedOffset || !Number.isFinite(closedOffset) || closedOffset <= 0) return;
		const { x, y } = dragDeltaRef.current;
		const displacement = getDisplacement$1(resolvedSwipeDirection, x, y);
		const clampedDisplacement = Math.max(0, displacement);
		const movement = (closedOffset - (clampedDisplacement > closedOffset ? closedOffset + Math.sqrt(clampedDisplacement - closedOffset) : clampedDisplacement)) * (dismissDirection === "left" || dismissDirection === "up" ? -1 : 1);
		const isHorizontal = dismissDirection === "left" || dismissDirection === "right";
		const movementX = isHorizontal ? movement : 0;
		const movementY = isHorizontal ? 0 : movement;
		const openProgress = Math.max(0, Math.min(1, clampedDisplacement / closedOffset));
		const backdropProgress = Math.max(0, Math.min(1, 1 - openProgress));
		popupElement.style.setProperty(DrawerPopupCssVars.swipeMovementX, `${movementX}px`);
		popupElement.style.setProperty(DrawerPopupCssVars.swipeMovementY, `${movementY}px`);
		popupElement.setAttribute(DrawerPopupDataAttributes.swiping, "");
		if (popupTransitionRef.current === null) popupTransitionRef.current = popupElement.style.transition;
		popupElement.style.transition = "none";
		const backdropElement = store.context.backdropRef.current;
		if (backdropElement) {
			backdropElement.setAttribute(DrawerPopupDataAttributes.swiping, "");
			backdropElement.style.setProperty(DrawerBackdropCssVars.swipeProgress, `${backdropProgress}`);
			if (openProgress > 0 && frontmostHeight > 0) backdropElement.style.setProperty(DrawerPopupCssVars.height, `${frontmostHeight}px`);
			else backdropElement.style.removeProperty(DrawerPopupCssVars.height);
		}
		providerContext?.visualStateStore.set({
			swipeProgress: openProgress,
			frontmostHeight: openProgress > 0 ? frontmostHeight : 0
		});
		appliedSwipeStylesRef.current = true;
	}
	const clearSwipeStyles = useStableCallback(() => {
		const popupElement = store.context.popupRef.current;
		if (popupElement && appliedSwipeStylesRef.current) {
			popupElement.style.removeProperty(DrawerPopupCssVars.swipeMovementX);
			popupElement.style.removeProperty(DrawerPopupCssVars.swipeMovementY);
			popupElement.removeAttribute(DrawerPopupDataAttributes.swiping);
		}
		if (popupElement && popupTransitionRef.current !== null) {
			popupElement.style.transition = popupTransitionRef.current;
			popupTransitionRef.current = null;
		}
		const backdropElement = store.context.backdropRef.current;
		if (backdropElement) {
			backdropElement.removeAttribute(DrawerPopupDataAttributes.swiping);
			backdropElement.style.setProperty(DrawerBackdropCssVars.swipeProgress, "0");
			backdropElement.style.removeProperty(DrawerPopupCssVars.height);
		}
		providerContext?.visualStateStore.set({
			swipeProgress: 0,
			frontmostHeight: 0
		});
		appliedSwipeStylesRef.current = false;
	});
	function openDrawer(event) {
		if (store.select("open")) return;
		openedBySwipeRef.current = true;
		store.setOpen(true, createChangeEventDetails(swipe, event, swipeAreaRef.current ?? void 0));
	}
	function closeDrawer(event) {
		if (!store.select("open")) return;
		store.setOpen(false, createChangeEventDetails(swipe, event, swipeAreaRef.current ?? void 0));
	}
	const swipe$2 = useSwipeDismiss({
		enabled,
		directions: [resolvedSwipeDirection],
		elementRef: swipeAreaRef,
		trackDrag: false,
		movementCssVars: {
			x: DrawerPopupCssVars.swipeMovementX,
			y: DrawerPopupCssVars.swipeMovementY
		},
		onSwipeStart(event) {
			disableDismissForSwipe();
			swipeStartEventRef.current = event;
			openedBySwipeRef.current = false;
			setSwipeActive(true);
			resetDragDelta();
		},
		onProgress(_progress, details) {
			if (!details) return;
			if (!swipeStartEventRef.current) return;
			dragDeltaRef.current.x = details.deltaX;
			dragDeltaRef.current.y = details.deltaY;
			if (details.direction !== resolvedSwipeDirection) return;
			if (getDisplacement$1(resolvedSwipeDirection, details.deltaX, details.deltaY) < MIN_SWIPE_START_DISTANCE && !openedBySwipeRef.current) return;
			if (!openedBySwipeRef.current) openDrawer(swipeStartEventRef.current);
			applySwipeMovement();
		},
		onRelease({ event, direction, deltaX, deltaY, releaseVelocityX, releaseVelocityY }) {
			const displacement = getDisplacement$1(resolvedSwipeDirection, deltaX, deltaY);
			const releaseVelocity = getDisplacement$1(resolvedSwipeDirection, releaseVelocityX, releaseVelocityY);
			const threshold = resolveSwipeOpenThreshold();
			if (threshold != null && direction === resolvedSwipeDirection && (threshold != null && displacement >= threshold || releaseVelocity >= VELOCITY_THRESHOLD) && !disabled) {
				if (!store.select("open")) openDrawer(event);
			} else if (openedBySwipeRef.current) closeDrawer(event);
			swipeStartEventRef.current = null;
			openedBySwipeRef.current = false;
			setSwipeActive(false);
			closedOffsetRef.current = null;
			enableDismissAfterRelease();
			resetDragDelta();
			clearSwipeStyles();
			return false;
		}
	});
	const swipePointerProps = swipe$2.getPointerProps();
	const swipeTouchProps = swipe$2.getTouchProps();
	const resetSwipe = swipe$2.reset;
	import_react.useEffect(() => {
		if (!enabled) {
			resetSwipe();
			resetDragDelta();
			clearSwipeStyles();
			setSwipeActive(false);
			openedBySwipeRef.current = false;
			swipeStartEventRef.current = null;
			closedOffsetRef.current = null;
		}
	}, [
		clearSwipeStyles,
		enabled,
		resetDragDelta,
		resetSwipe
	]);
	import_react.useEffect(() => {
		return () => {
			store.context.outsidePressEnabledRef.current = true;
		};
	}, [store]);
	return useRenderElement("div", componentProps, {
		state: {
			open,
			swiping: swipe$2.swiping,
			swipeDirection: resolvedSwipeDirection,
			disabled
		},
		ref: [
			forwardedRef,
			swipeAreaRef,
			registerTrigger
		],
		stateAttributesMapping: stateAttributesMapping$14,
		props: [
			{
				role: "presentation",
				"aria-hidden": true,
				style: {
					pointerEvents: !enabled ? "none" : void 0,
					touchAction: resolveTouchAction(resolvedSwipeDirection)
				},
				onPointerDown(event) {
					if (event.pointerType === "touch") return;
					swipePointerProps.onPointerDown?.(event);
					if (event.cancelable) event.preventDefault();
				},
				onPointerMove(event) {
					if (event.pointerType === "touch") return;
					swipePointerProps.onPointerMove?.(event);
				},
				onPointerUp(event) {
					if (event.pointerType === "touch") return;
					swipePointerProps.onPointerUp?.(event);
				},
				onPointerCancel(event) {
					if (event.pointerType === "touch") return;
					swipePointerProps.onPointerCancel?.(event);
				}
			},
			swipeTouchProps,
			swipeAreaId ? { id: swipeAreaId } : void 0,
			elementProps
		]
	});
});
DrawerSwipeArea.displayName = "DrawerSwipeArea";
//#endregion
//#region node_modules/@base-ui/react/esm/drawer/title/DrawerTitle.js
/**
* A heading that labels the drawer.
* Renders an `<h2>` element.
*
* Documentation: [Base UI Drawer](https://base-ui.com/react/components/drawer)
*/
var DrawerTitle = DialogTitle;
//#endregion
//#region node_modules/@base-ui/react/esm/drawer/trigger/DrawerTrigger.js
/**
* A button that opens the drawer.
* Renders a `<button>` element.
*
* Documentation: [Base UI Drawer](https://base-ui.com/react/components/drawer)
*/
var DrawerTrigger = DialogTrigger;
//#endregion
//#region node_modules/@base-ui/react/esm/drawer/viewport/DrawerViewport.js
var MIN_SWIPE_THRESHOLD = 10;
var FAST_SWIPE_VELOCITY = .5;
var SNAP_VELOCITY_THRESHOLD = .5;
var SNAP_VELOCITY_MULTIPLIER = 300;
var MAX_SNAP_VELOCITY = 4;
var MIN_SWIPE_RELEASE_VELOCITY = .2;
var MAX_SWIPE_RELEASE_VELOCITY = 4;
var MIN_SWIPE_RELEASE_DURATION_MS = 80;
var MAX_SWIPE_RELEASE_DURATION_MS = 360;
var MIN_SWIPE_RELEASE_SCALAR = .1;
var MAX_SWIPE_RELEASE_SCALAR = 1;
var DRAWER_CONTENT_SELECTOR = `[${DRAWER_CONTENT_ATTRIBUTE}]`;
/**
* A positioning container for the drawer popup that can be made scrollable.
* Renders a `<div>` element.
*
* Documentation: [Base UI Drawer](https://base-ui.com/react/components/drawer)
*/
var DrawerViewport = /* @__PURE__ */ import_react.forwardRef(function DrawerViewport(props, forwardedRef) {
	const { className, render, children, ...elementProps } = props;
	const { store } = useDialogRootContext();
	const { swipeDirection, notifyParentSwipingChange, notifyParentSwipeProgressChange, frontmostHeight, snapToSequentialPoints } = useDrawerRootContext();
	const visualStateStore = useDrawerProviderContext(true)?.visualStateStore;
	const open = store.useState("open");
	const mounted = store.useState("mounted");
	const nested = store.useState("nested");
	const nestedOpenDialogCount = store.useState("nestedOpenDialogCount");
	const viewportElement = store.useState("viewportElement");
	const popupElementState = store.useState("popupElement");
	const nestedDrawerOpen = nestedOpenDialogCount > 0;
	const scrollAxis = swipeDirection === "left" || swipeDirection === "right" ? "horizontal" : "vertical";
	const isVerticalScrollAxis = scrollAxis === "vertical";
	const crossScrollAxis = isVerticalScrollAxis ? "horizontal" : "vertical";
	const { snapPoints, resolvedSnapPoints, activeSnapPoint, activeSnapPointOffset, setActiveSnapPoint, popupHeight } = useDrawerSnapPoints();
	const [swipeRelease, setSwipeRelease] = import_react.useState(null);
	const pendingSwipeCloseSnapPointRef = import_react.useRef(void 0);
	const resetSwipeRef = import_react.useRef(null);
	const controlledDismissFrame = useAnimationFrame();
	const nestedSwipeActiveRef = import_react.useRef(false);
	const lastPointerTypeRef = import_react.useRef("");
	const ignoreNextTouchStartFromPenRef = import_react.useRef(false);
	const ignoreTouchSwipeRef = import_react.useRef(false);
	const touchScrollStateRef = import_react.useRef(null);
	const snapPointRange = import_react.useMemo(() => {
		if (!snapPoints || snapPoints.length < 2) return null;
		if (swipeDirection !== "down" && swipeDirection !== "up") return null;
		if (resolvedSnapPoints.length < 2) return null;
		const offsets = resolvedSnapPoints.map((point) => point.offset).filter((offset) => Number.isFinite(offset)).sort((a, b) => a - b);
		if (offsets.length < 2) return null;
		const minOffset = offsets[0];
		const nextOffset = offsets[1];
		const maxOffset = offsets[offsets.length - 1];
		let range = nextOffset - minOffset;
		if (!Number.isFinite(range) || range <= 0) {
			const fallbackRange = maxOffset - minOffset;
			if (!Number.isFinite(fallbackRange) || fallbackRange <= 0) return null;
			range = fallbackRange;
		}
		return {
			minOffset,
			range
		};
	}, [
		resolvedSnapPoints,
		snapPoints,
		swipeDirection
	]);
	const snapPointProgress = import_react.useMemo(() => {
		if (!snapPointRange || activeSnapPointOffset === null) return null;
		return clamp((activeSnapPointOffset - snapPointRange.minOffset) / snapPointRange.range, 0, 1);
	}, [activeSnapPointOffset, snapPointRange]);
	const swipeDirections = import_react.useMemo(() => {
		if (snapPoints && snapPoints.length > 0 && (swipeDirection === "down" || swipeDirection === "up")) return swipeDirection === "down" ? ["down", "up"] : ["up", "down"];
		return [swipeDirection];
	}, [snapPoints, swipeDirection]);
	const setSwipeDismissed = useStableCallback((dismissed) => {
		setSwipeDismissedElements(store.context.popupRef.current, store.context.backdropRef.current, dismissed);
	});
	const clearSwipeRelease = useStableCallback(() => {
		setSwipeDismissed(false);
		store.context.popupRef.current?.removeAttribute(TransitionStatusDataAttributes.endingStyle);
		setSwipeRelease(null);
	});
	const applySwipeProgress = useStableCallback(({ resolvedProgress, shouldTrackProgress, notifyParent }) => {
		const isActive = open && !nested && shouldTrackProgress;
		const swipeProgress = isActive ? resolvedProgress : 0;
		if (notifyParent && notifyParentSwipeProgressChange) notifyParentSwipeProgressChange(open && shouldTrackProgress ? resolvedProgress : 0);
		visualStateStore?.set({
			swipeProgress,
			frontmostHeight: swipeProgress > 0 ? frontmostHeight : 0
		});
		const backdropElement = store.context.backdropRef.current;
		if (!backdropElement) return;
		if (!isActive || swipeProgress <= 0) {
			backdropElement.style.setProperty(DrawerBackdropCssVars.swipeProgress, "0");
			backdropElement.style.removeProperty(DrawerPopupCssVars.height);
			return;
		}
		backdropElement.style.setProperty(DrawerBackdropCssVars.swipeProgress, `${swipeProgress}`);
		if (frontmostHeight > 0) backdropElement.style.setProperty(DrawerPopupCssVars.height, `${frontmostHeight}px`);
		else backdropElement.style.removeProperty(DrawerPopupCssVars.height);
	});
	function resolveSwipeRelease({ direction, deltaX, deltaY, velocityX, velocityY, releaseVelocityX, releaseVelocityY }) {
		if (!direction) return null;
		const popupElement = store.context.popupRef.current;
		if (!popupElement) return null;
		const size = direction === "left" || direction === "right" ? popupElement.offsetWidth : popupElement.offsetHeight;
		if (!Number.isFinite(size) || size <= 0) return null;
		const axisDelta = direction === "left" || direction === "right" ? deltaX : deltaY;
		const snapPointBaseOffset = snapPoints && snapPoints.length > 0 ? activeSnapPointOffset ?? 0 : 0;
		let baseOffset = 0;
		if (direction === "down") baseOffset = snapPointBaseOffset;
		else if (direction === "up") baseOffset = -snapPointBaseOffset;
		const translation = baseOffset + axisDelta;
		const translationAlongDirection = direction === "left" || direction === "up" ? -translation : translation;
		const remainingDistance = Math.max(0, size - translationAlongDirection);
		if (!Number.isFinite(remainingDistance) || remainingDistance <= 0) return null;
		const axisVelocity = direction === "left" || direction === "right" ? releaseVelocityX : releaseVelocityY;
		const fallbackVelocity = direction === "left" || direction === "right" ? velocityX : velocityY;
		const resolvedVelocity = Math.abs(axisVelocity) > 0 && Number.isFinite(axisVelocity) ? axisVelocity : fallbackVelocity;
		const directionalVelocity = direction === "left" || direction === "up" ? -resolvedVelocity : resolvedVelocity;
		if (!Number.isFinite(directionalVelocity) || directionalVelocity <= MIN_SWIPE_RELEASE_VELOCITY) return null;
		const durationMs = clamp(remainingDistance / clamp(directionalVelocity, MIN_SWIPE_RELEASE_VELOCITY, MAX_SWIPE_RELEASE_VELOCITY), MIN_SWIPE_RELEASE_DURATION_MS, MAX_SWIPE_RELEASE_DURATION_MS);
		if (!Number.isFinite(durationMs)) return null;
		const durationScalar = clamp(MIN_SWIPE_RELEASE_SCALAR + (durationMs - MIN_SWIPE_RELEASE_DURATION_MS) / (MAX_SWIPE_RELEASE_DURATION_MS - MIN_SWIPE_RELEASE_DURATION_MS) * (MAX_SWIPE_RELEASE_SCALAR - MIN_SWIPE_RELEASE_SCALAR), MIN_SWIPE_RELEASE_SCALAR, MAX_SWIPE_RELEASE_SCALAR);
		if (!Number.isFinite(durationScalar) || durationScalar <= 0) return null;
		return durationScalar;
	}
	function updateNestedSwipeActive(details) {
		if (nestedSwipeActiveRef.current || !details) return;
		const direction = details.direction ?? swipeDirection;
		const delta = direction === "left" || direction === "right" ? details.deltaX : details.deltaY;
		if (!Number.isFinite(delta) || Math.abs(delta) < MIN_SWIPE_THRESHOLD) return;
		nestedSwipeActiveRef.current = true;
		notifyParentSwipingChange?.(true);
	}
	const swipe$1 = useSwipeDismiss({
		enabled: mounted && !nestedDrawerOpen,
		directions: swipeDirections,
		elementRef: store.context.popupRef,
		ignoreSelectorWhenTouch: false,
		ignoreScrollableAncestors: true,
		movementCssVars: {
			x: DrawerPopupCssVars.swipeMovementX,
			y: DrawerPopupCssVars.swipeMovementY
		},
		onSwipeStart(event) {
			if ("touches" in event || "pointerType" in event && event.pointerType === "touch") return;
			const popupElement = store.context.popupRef.current;
			if (!popupElement) return;
			const selection = ownerDocument(popupElement).getSelection?.();
			if (!selection || selection.isCollapsed) return;
			const anchorElement = isElement(selection.anchorNode) ? selection.anchorNode : selection.anchorNode?.parentElement;
			const focusElement = isElement(selection.focusNode) ? selection.focusNode : selection.focusNode?.parentElement;
			if (!contains(popupElement, anchorElement) && !contains(popupElement, focusElement)) return;
			selection.removeAllRanges();
		},
		onSwipingChange(swiping) {
			setBackdropSwipingAttribute(store.context.backdropRef.current, swiping);
			if (!swiping) {
				nestedSwipeActiveRef.current = false;
				notifyParentSwipingChange?.(false);
			}
		},
		swipeThreshold({ element, direction }) {
			return getBaseSwipeThreshold(element, direction);
		},
		canStart(position, details) {
			const popupElement = store.context.popupRef.current;
			if (!popupElement) return false;
			const doc = popupElement.ownerDocument;
			const elementAtPoint = getElementAtPoint(doc, position.x, position.y);
			if (!elementAtPoint || !contains(popupElement, elementAtPoint)) return false;
			const nativeEvent = details.nativeEvent;
			if (("touches" in nativeEvent || "pointerType" in nativeEvent && nativeEvent.pointerType === "touch") && shouldIgnoreSwipeForTextSelection(doc, popupElement)) return false;
			if (nativeEvent.type === "touchstart" && isSwipeIgnoredTarget(elementAtPoint)) return false;
			return true;
		},
		onProgress(progress, details) {
			updateNestedSwipeActive(details);
			const currentDirection = details?.direction ?? swipe$1.swipeDirection;
			const isDismissSwipe = currentDirection === void 0 || currentDirection === swipeDirection;
			const hasSnapPoints = Boolean(snapPoints && snapPoints.length > 0);
			const shouldTrackProgress = hasSnapPoints && (swipeDirection === "down" || swipeDirection === "up") || !hasSnapPoints || swipeDirection === "left" || swipeDirection === "right" || isDismissSwipe;
			let resolvedProgress = progress;
			if (snapPointRange && popupHeight > 0) {
				if (details && Number.isFinite(details.deltaY)) resolvedProgress = clamp((clamp((activeSnapPointOffset ?? snapPointRange.minOffset) + details.deltaY, 0, popupHeight) - snapPointRange.minOffset) / snapPointRange.range, 0, 1);
				else if (snapPointProgress !== null) resolvedProgress = snapPointProgress;
				else if (currentDirection === "down" || currentDirection === "up") {
					const displacement = progress * popupHeight;
					const baseOffset = activeSnapPointOffset ?? snapPointRange.minOffset;
					resolvedProgress = clamp(((currentDirection === "down" ? baseOffset + displacement : baseOffset - displacement) - snapPointRange.minOffset) / snapPointRange.range, 0, 1);
				}
			}
			applySwipeProgress({
				resolvedProgress,
				shouldTrackProgress,
				notifyParent: true
			});
		},
		onRelease({ event, deltaX, deltaY, direction, velocityX, velocityY, releaseVelocityX, releaseVelocityY }) {
			const swipeReleasePayload = {
				deltaX,
				deltaY,
				velocityX,
				velocityY,
				releaseVelocityX,
				releaseVelocityY
			};
			function startSwipeRelease(resolvedDirection) {
				const popupElement = store.context.popupRef.current;
				if (!popupElement) return;
				notifyParentSwipingChange?.(false);
				setSwipeDismissed(true);
				popupElement.style.removeProperty("transition");
				popupElement.setAttribute(TransitionStatusDataAttributes.endingStyle, "");
				import_react_dom.flushSync(() => {
					setSwipeRelease(resolveSwipeRelease({
						direction: resolvedDirection,
						...swipeReleasePayload
					}));
				});
			}
			if (!snapPoints || snapPoints.length === 0) {
				if (!direction) {
					clearSwipeRelease();
					return;
				}
				const element = store.context.popupRef.current;
				if (!element) {
					clearSwipeRelease();
					return;
				}
				const baseThreshold = getBaseSwipeThreshold(element, direction);
				const delta = direction === "left" || direction === "right" ? deltaX : deltaY;
				if (!Number.isFinite(delta)) {
					clearSwipeRelease();
					return;
				}
				const directionalDelta = direction === "left" || direction === "up" ? -delta : delta;
				if (directionalDelta <= 0) {
					clearSwipeRelease();
					return false;
				}
				const velocity = direction === "left" || direction === "right" ? velocityX : velocityY;
				if ((direction === "left" || direction === "up" ? -velocity : velocity) >= FAST_SWIPE_VELOCITY && directionalDelta > 0) {
					startSwipeRelease(direction);
					return true;
				}
				const shouldClose = directionalDelta > baseThreshold;
				if (shouldClose) startSwipeRelease(direction);
				else clearSwipeRelease();
				return shouldClose;
			}
			if (swipeDirection !== "down" && swipeDirection !== "up") {
				clearSwipeRelease();
				return;
			}
			if (!popupHeight || resolvedSnapPoints.length === 0) {
				clearSwipeRelease();
				return;
			}
			const dragDelta = swipeDirection === "down" ? deltaY : -deltaY;
			if (!Number.isFinite(dragDelta)) {
				clearSwipeRelease();
				return;
			}
			const dragDirection = Math.sign(dragDelta);
			const releaseDirectionalVelocity = swipeDirection === "down" ? releaseVelocityY : -releaseVelocityY;
			const fallbackDirectionalVelocity = swipeDirection === "down" ? velocityY : -velocityY;
			let resolvedDirectionalVelocity = Number.isFinite(releaseDirectionalVelocity) ? releaseDirectionalVelocity : fallbackDirectionalVelocity;
			if (dragDirection !== 0 && Math.abs(dragDelta) >= MIN_SWIPE_THRESHOLD && Number.isFinite(resolvedDirectionalVelocity)) {
				const velocityDirection = Math.sign(resolvedDirectionalVelocity);
				if (velocityDirection !== 0 && velocityDirection !== dragDirection) resolvedDirectionalVelocity = fallbackDirectionalVelocity;
			}
			const currentOffset = activeSnapPointOffset ?? 0;
			const dragTargetOffset = clamp(currentOffset + dragDelta, 0, popupHeight);
			const velocityOffset = Number.isFinite(resolvedDirectionalVelocity) && Math.abs(resolvedDirectionalVelocity) >= SNAP_VELOCITY_THRESHOLD ? clamp(resolvedDirectionalVelocity, -MAX_SNAP_VELOCITY, MAX_SNAP_VELOCITY) * SNAP_VELOCITY_MULTIPLIER : 0;
			const targetOffset = snapToSequentialPoints ? dragTargetOffset : clamp(dragTargetOffset + velocityOffset, 0, popupHeight);
			const snapPointEventDetails = createChangeEventDetails(swipe, event);
			const closeFromSnapPoints = () => {
				pendingSwipeCloseSnapPointRef.current = activeSnapPoint;
				setActiveSnapPoint?.(null, snapPointEventDetails);
				startSwipeRelease(swipeDirection);
				return true;
			};
			if (snapToSequentialPoints) {
				const orderedSnapPoints = [...resolvedSnapPoints].sort((first, second) => first.offset - second.offset);
				if (orderedSnapPoints.length === 0) {
					clearSwipeRelease();
					return false;
				}
				let currentIndex = 0;
				let closestDistance = Math.abs(currentOffset - orderedSnapPoints[0].offset);
				for (let index = 1; index < orderedSnapPoints.length; index += 1) {
					const distance = Math.abs(currentOffset - orderedSnapPoints[index].offset);
					if (distance < closestDistance) {
						closestDistance = distance;
						currentIndex = index;
					}
				}
				let targetSnapPoint = orderedSnapPoints[0];
				closestDistance = Math.abs(targetOffset - targetSnapPoint.offset);
				for (const snapPoint of orderedSnapPoints) {
					const distance = Math.abs(targetOffset - snapPoint.offset);
					if (distance < closestDistance) {
						closestDistance = distance;
						targetSnapPoint = snapPoint;
					}
				}
				const velocityDirection = Math.sign(resolvedDirectionalVelocity);
				const shouldAdvance = dragDirection !== 0 && velocityDirection !== 0 && velocityDirection === dragDirection && Math.abs(resolvedDirectionalVelocity) >= SNAP_VELOCITY_THRESHOLD;
				let effectiveTargetOffset = targetOffset;
				if (shouldAdvance) {
					const adjacentIndex = clamp(currentIndex + dragDirection, 0, orderedSnapPoints.length - 1);
					if (adjacentIndex !== currentIndex) {
						const adjacentPoint = orderedSnapPoints[adjacentIndex];
						if (dragDirection > 0 ? targetOffset < adjacentPoint.offset : targetOffset > adjacentPoint.offset) {
							targetSnapPoint = adjacentPoint;
							effectiveTargetOffset = adjacentPoint.offset;
						}
					} else if (dragDirection > 0) return closeFromSnapPoints();
				}
				const closeOffset = popupHeight;
				if (Math.abs(effectiveTargetOffset - closeOffset) < Math.abs(effectiveTargetOffset - targetSnapPoint.offset)) return closeFromSnapPoints();
				setActiveSnapPoint?.(targetSnapPoint.value, snapPointEventDetails);
				clearSwipeRelease();
				return false;
			}
			if (resolvedDirectionalVelocity >= FAST_SWIPE_VELOCITY && dragDelta > 0) return closeFromSnapPoints();
			let closestSnapPoint = resolvedSnapPoints[0];
			let closestDistance = Math.abs(targetOffset - closestSnapPoint.offset);
			for (const snapPoint of resolvedSnapPoints) {
				const distance = Math.abs(targetOffset - snapPoint.offset);
				if (distance < closestDistance) {
					closestDistance = distance;
					closestSnapPoint = snapPoint;
				}
			}
			const closeOffset = popupHeight;
			if (Math.abs(targetOffset - closeOffset) < closestDistance) return closeFromSnapPoints();
			setActiveSnapPoint?.(closestSnapPoint.value, snapPointEventDetails);
			clearSwipeRelease();
			return false;
		},
		onDismiss(event) {
			visualStateStore?.set({
				swipeProgress: 0,
				frontmostHeight: 0
			});
			const backdropElement = store.context.backdropRef.current;
			if (backdropElement) {
				backdropElement.style.setProperty(DrawerBackdropCssVars.swipeProgress, "0");
				backdropElement.style.removeProperty(DrawerPopupCssVars.height);
			}
			const dismissEventDetails = createChangeEventDetails(swipe, event);
			store.setOpen(false, dismissEventDetails);
			if (dismissEventDetails.isCanceled) {
				const pendingSnapPoint = pendingSwipeCloseSnapPointRef.current;
				if (pendingSnapPoint !== void 0) setActiveSnapPoint?.(pendingSnapPoint, createChangeEventDetails(swipe, event));
				pendingSwipeCloseSnapPointRef.current = void 0;
				resetSwipeRef.current?.();
				clearSwipeRelease();
				return;
			}
			if (store.select("open")) {
				const savedEvent = event;
				controlledDismissFrame.request(() => {
					if (store.select("open")) {
						const pendingSnapPoint = pendingSwipeCloseSnapPointRef.current;
						if (pendingSnapPoint !== void 0) setActiveSnapPoint?.(pendingSnapPoint, createChangeEventDetails(swipe, savedEvent));
						pendingSwipeCloseSnapPointRef.current = void 0;
						clearSwipeRelease();
						resetSwipeRef.current?.();
					} else pendingSwipeCloseSnapPointRef.current = void 0;
				});
				return;
			}
			pendingSwipeCloseSnapPointRef.current = void 0;
			setSwipeDismissed(true);
		}
	});
	const swipePointerProps = swipe$1.getPointerProps();
	const swipeTouchProps = swipe$1.getTouchProps();
	const resetSwipe = swipe$1.reset;
	resetSwipeRef.current = resetSwipe;
	import_react.useEffect(() => {
		const rootElement = viewportElement ?? popupElementState;
		if (!rootElement) return;
		const resolvedRootElement = rootElement;
		const doc = ownerDocument(resolvedRootElement);
		const win = getWindow(doc);
		function handleNativeTouchMove(event) {
			if (ignoreTouchSwipeRef.current) return;
			const touchState = touchScrollStateRef.current;
			const touch = event.touches[0];
			if (!touch || !touchState) return;
			const drawerAxisDelta = isVerticalScrollAxis ? touch.clientY - touchState.lastY : touch.clientX - touchState.lastX;
			if (isEventOnRangeInput(event, win)) {
				touchState.allowSwipe = false;
				updateTouchScrollPosition(touchState, touch);
				return;
			}
			if (event.touches.length === 2) {
				updateTouchScrollPosition(touchState, touch);
				return;
			}
			if (shouldIgnoreSwipeForTextSelection(doc, resolvedRootElement) || !open || !mounted || nestedDrawerOpen) {
				updateTouchScrollPosition(touchState, touch);
				return;
			}
			if (preserveNativeCrossAxisScrollOnMove(touchState, touch, isVerticalScrollAxis)) {
				updateTouchScrollPosition(touchState, touch);
				return;
			}
			const scrollTarget = touchState.scrollTarget;
			if (!scrollTarget || scrollTarget === doc.documentElement || scrollTarget === doc.body) {
				if (event.cancelable) event.preventDefault();
				updateTouchScrollPosition(touchState, touch);
				return;
			}
			if (!hasScrollableContentOnAxis(scrollTarget, scrollAxis)) {
				if (event.cancelable) event.preventDefault();
				updateTouchScrollPosition(touchState, touch);
				return;
			}
			const delta = drawerAxisDelta;
			if (delta !== 0) {
				const canSwipeFromScrollEdge = canSwipeFromScrollEdgeOnMove(scrollTarget, scrollAxis, swipeDirection, delta);
				if (!touchState.allowSwipe) if (!event.cancelable) touchState.allowSwipe = false;
				else if (canSwipeFromScrollEdge) {
					touchState.allowSwipe = true;
					event.preventDefault();
				} else touchState.allowSwipe = false;
				else if (event.cancelable) event.preventDefault();
			}
			updateTouchScrollPosition(touchState, touch);
		}
		doc.addEventListener("touchmove", handleNativeTouchMove, {
			passive: false,
			capture: true
		});
		return () => {
			doc.removeEventListener("touchmove", handleNativeTouchMove, { capture: true });
		};
	}, [
		mounted,
		nestedDrawerOpen,
		open,
		popupElementState,
		isVerticalScrollAxis,
		scrollAxis,
		swipeDirection,
		viewportElement
	]);
	import_react.useEffect(() => {
		if (!snapPointRange || swipe$1.swiping) return;
		applySwipeProgress({
			resolvedProgress: !open || nested ? 0 : snapPointProgress ?? 0,
			shouldTrackProgress: true,
			notifyParent: false
		});
	}, [
		applySwipeProgress,
		frontmostHeight,
		nested,
		notifyParentSwipeProgressChange,
		open,
		snapPointProgress,
		snapPointRange,
		swipe$1.swiping,
		store,
		visualStateStore
	]);
	import_react.useEffect(() => {
		if (!notifyParentSwipeProgressChange) return;
		if (!open) notifyParentSwipeProgressChange(0);
		return () => {
			notifyParentSwipeProgressChange(0);
		};
	}, [notifyParentSwipeProgressChange, open]);
	import_react.useEffect(() => {
		if (open) {
			resetSwipe();
			clearSwipeRelease();
		}
	}, [
		clearSwipeRelease,
		open,
		resetSwipe
	]);
	import_react.useEffect(() => {
		return () => {
			visualStateStore?.set({
				swipeProgress: 0,
				frontmostHeight: 0
			});
			setBackdropSwipingAttribute(store.context.backdropRef.current, false);
			notifyParentSwipingChange?.(false);
		};
	}, [
		notifyParentSwipingChange,
		store,
		visualStateStore
	]);
	const swipeProviderValue = import_react.useMemo(() => ({
		swiping: swipe$1.swiping,
		getDragStyles: swipe$1.getDragStyles,
		swipeStrength: swipeRelease ?? null,
		setSwipeDismissed(dismissed) {
			setSwipeDismissedElements(store.context.popupRef.current, store.context.backdropRef.current, dismissed);
		}
	}), [
		store,
		swipe$1.getDragStyles,
		swipe$1.swiping,
		swipeRelease
	]);
	function resetTouchTrackingState() {
		ignoreTouchSwipeRef.current = false;
		touchScrollStateRef.current = null;
		lastPointerTypeRef.current = "";
		ignoreNextTouchStartFromPenRef.current = false;
	}
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DialogViewport, {
		ref: forwardedRef,
		className,
		render,
		...mergeProps(elementProps, {
			onPointerDown(event) {
				lastPointerTypeRef.current = event.pointerType;
				ignoreNextTouchStartFromPenRef.current = event.pointerType === "pen";
				if (!open || !mounted || nestedDrawerOpen) return;
				const elementAtPoint = getElementAtPoint(ownerDocument(event.currentTarget), event.clientX, event.clientY);
				if (isSwipeIgnoredTarget(elementAtPoint) || isDrawerContentTarget(elementAtPoint)) return;
				if (event.pointerType === "touch") return;
				swipePointerProps.onPointerDown?.(event);
			},
			onPointerMove(event) {
				if (event.pointerType === "touch") return;
				swipePointerProps.onPointerMove?.(event);
			},
			onPointerUp(event) {
				if (lastPointerTypeRef.current === event.pointerType) lastPointerTypeRef.current = "";
				if (event.pointerType === "touch") return;
				swipePointerProps.onPointerUp?.(event);
			},
			onPointerCancel(event) {
				if (lastPointerTypeRef.current === event.pointerType) lastPointerTypeRef.current = "";
				if (event.pointerType === "touch") return;
				swipePointerProps.onPointerCancel?.(event);
			},
			onTouchStart(event) {
				if (lastPointerTypeRef.current === "pen" && ignoreNextTouchStartFromPenRef.current) {
					ignoreNextTouchStartFromPenRef.current = false;
					ignoreTouchSwipeRef.current = false;
					touchScrollStateRef.current = null;
					return;
				}
				if (!open || !mounted || nestedDrawerOpen) {
					ignoreTouchSwipeRef.current = false;
					touchScrollStateRef.current = null;
					return;
				}
				const touch = event.touches[0];
				if (!touch) return;
				if (isReactTouchEventOnRangeInput(event)) {
					ignoreTouchSwipeRef.current = false;
					touchScrollStateRef.current = null;
					return;
				}
				ignoreTouchSwipeRef.current = isSwipeIgnoredTarget(getElementAtPoint(ownerDocument(event.currentTarget), touch.clientX, touch.clientY));
				if (ignoreTouchSwipeRef.current) {
					touchScrollStateRef.current = null;
					return;
				}
				const rootElement = viewportElement ?? popupElementState;
				const target = isElement(event.target) ? event.target : null;
				let scrollTarget = null;
				let hasCrossAxisScrollableContent = false;
				if (rootElement && target && contains(rootElement, target)) {
					scrollTarget = findScrollableTouchTarget(target, rootElement, scrollAxis);
					hasCrossAxisScrollableContent = findScrollableTouchTarget(target, rootElement, crossScrollAxis) != null;
				}
				let allowSwipe = null;
				if (scrollTarget) allowSwipe = isAtSwipeStartEdge(scrollTarget, scrollAxis, swipeDirection) ? null : false;
				touchScrollStateRef.current = {
					startX: touch.clientX,
					startY: touch.clientY,
					lastX: touch.clientX,
					lastY: touch.clientY,
					scrollTarget,
					hasCrossAxisScrollableContent,
					allowSwipe,
					preserveNativeCrossAxisScroll: false
				};
				swipeTouchProps.onTouchStart?.(event);
			},
			onTouchMove(event) {
				if (ignoreTouchSwipeRef.current) return;
				if (isReactTouchEventOnRangeInput(event)) return;
				const touchState = touchScrollStateRef.current;
				if (touchState?.preserveNativeCrossAxisScroll) return;
				if (touchState?.allowSwipe === false || touchState?.scrollTarget != null && !touchState.allowSwipe) return;
				swipeTouchProps.onTouchMove?.(event);
			},
			onTouchEnd(event) {
				resetTouchTrackingState();
				swipeTouchProps.onTouchEnd?.(event);
			},
			onTouchCancel(event) {
				resetTouchTrackingState();
				swipeTouchProps.onTouchCancel?.(event);
			},
			["data-nested-dialog-open"]: void 0
		}),
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(DrawerViewportContext.Provider, {
			value: swipeProviderValue,
			children
		})
	});
});
DrawerViewport.displayName = "DrawerViewport";
function setSwipeDismissedElements(popupElement, backdropElement, dismissed) {
	if (dismissed) {
		popupElement?.setAttribute(DrawerPopupDataAttributes.swipeDismiss, "");
		backdropElement?.setAttribute(DrawerPopupDataAttributes.swipeDismiss, "");
		return;
	}
	popupElement?.removeAttribute(DrawerPopupDataAttributes.swipeDismiss);
	backdropElement?.removeAttribute(DrawerPopupDataAttributes.swipeDismiss);
}
function setBackdropSwipingAttribute(backdropElement, swiping) {
	if (!backdropElement) return;
	if (swiping) {
		backdropElement.setAttribute(DrawerPopupDataAttributes.swiping, "");
		return;
	}
	backdropElement.removeAttribute(DrawerPopupDataAttributes.swiping);
}
function isSwipeIgnoredTarget(target) {
	return Boolean(target?.closest(BASE_UI_SWIPE_IGNORE_SELECTOR));
}
function isDrawerContentTarget(target) {
	return Boolean(target?.closest(DRAWER_CONTENT_SELECTOR));
}
function getBaseSwipeThreshold(element, direction) {
	const size = direction === "left" || direction === "right" ? element.offsetWidth : element.offsetHeight;
	return Math.max(size * .5, MIN_SWIPE_THRESHOLD);
}
function isRangeInput(target, win) {
	return target instanceof win.HTMLInputElement && target.type === "range";
}
function isTextSelectionControl(target) {
	if (!isElement(target)) return false;
	return target.tagName === "INPUT" || target.tagName === "TEXTAREA";
}
function hasExpandedSelectionWithinTarget(selection, target) {
	const anchorElement = isElement(selection.anchorNode) ? selection.anchorNode : selection.anchorNode?.parentElement;
	const focusElement = isElement(selection.focusNode) ? selection.focusNode : selection.focusNode?.parentElement;
	return selection.containsNode(target, true) || contains(target, anchorElement) || contains(target, focusElement);
}
function shouldIgnoreSwipeForTextSelection(doc, rootElement) {
	const activeElement = doc.activeElement;
	if (Boolean(activeElement && contains(rootElement, activeElement)) && isTextSelectionControl(activeElement)) {
		const { selectionStart, selectionEnd } = activeElement;
		if (selectionStart != null && selectionEnd != null && selectionStart < selectionEnd) return true;
	}
	const selection = doc.getSelection?.();
	if (!selection || selection.isCollapsed) return false;
	return hasExpandedSelectionWithinTarget(selection, rootElement);
}
function isEventOnRangeInput(event, win) {
	const composedPath = event.composedPath();
	if (composedPath) return composedPath.some((pathTarget) => isRangeInput(pathTarget, win));
	return isRangeInput(event.target, win);
}
function isReactTouchEventOnRangeInput(event) {
	return isEventOnRangeInput(event.nativeEvent, getWindow(event.currentTarget));
}
function updateTouchScrollPosition(touchState, touch) {
	touchState.lastX = touch.clientX;
	touchState.lastY = touch.clientY;
}
function preserveNativeCrossAxisScrollOnMove(touchState, touch, isVerticalScrollAxis) {
	if (touchState.preserveNativeCrossAxisScroll) return true;
	if (touchState.allowSwipe === true || !touchState.hasCrossAxisScrollableContent) return false;
	const drawerAxisGestureDelta = isVerticalScrollAxis ? touch.clientY - touchState.startY : touch.clientX - touchState.startX;
	const crossAxisGestureDelta = isVerticalScrollAxis ? touch.clientX - touchState.startX : touch.clientY - touchState.startY;
	const absDrawerAxisGestureDelta = Math.abs(drawerAxisGestureDelta);
	const absCrossAxisGestureDelta = Math.abs(crossAxisGestureDelta);
	if (absCrossAxisGestureDelta < 6 || absCrossAxisGestureDelta <= absDrawerAxisGestureDelta + 2) return false;
	touchState.preserveNativeCrossAxisScroll = true;
	return true;
}
function hasScrollableContentOnAxis(scrollTarget, axis) {
	return axis === "vertical" ? scrollTarget.scrollHeight > scrollTarget.clientHeight : scrollTarget.scrollWidth > scrollTarget.clientWidth;
}
function getScrollMetrics(scrollTarget, axis) {
	if (axis === "vertical") {
		const max = Math.max(0, scrollTarget.scrollHeight - scrollTarget.clientHeight);
		return {
			offset: scrollTarget.scrollTop,
			max
		};
	}
	const max = Math.max(0, scrollTarget.scrollWidth - scrollTarget.clientWidth);
	return {
		offset: scrollTarget.scrollLeft,
		max
	};
}
function isAtSwipeStartEdge(scrollTarget, axis, direction) {
	const { offset, max } = getScrollMetrics(scrollTarget, axis);
	const dismissFromStartEdge = shouldDismissFromStartEdge(direction, axis);
	if (dismissFromStartEdge === null) return false;
	return dismissFromStartEdge ? offset <= 0 : offset >= max;
}
function canSwipeFromScrollEdgeOnMove(scrollTarget, axis, direction, delta) {
	const { offset, max } = getScrollMetrics(scrollTarget, axis);
	const dismissFromStartEdge = shouldDismissFromStartEdge(direction, axis);
	if (dismissFromStartEdge === null) return false;
	if (!(dismissFromStartEdge ? delta > 0 : delta < 0)) return false;
	return dismissFromStartEdge ? offset <= 0 : offset >= max;
}
function shouldDismissFromStartEdge(direction, axis) {
	if (axis === "vertical") {
		if (direction === "down") return true;
		if (direction === "up") return false;
		return null;
	}
	if (direction === "right") return true;
	if (direction === "left") return false;
	return null;
}
//#endregion
//#region node_modules/@base-ui/react/esm/drawer/index.parts.js
var index_parts_exports$9 = /* @__PURE__ */ __exportAll({
	Backdrop: () => DrawerBackdrop,
	Close: () => DrawerClose,
	Content: () => DrawerContent,
	Description: () => DrawerDescription,
	Handle: () => DialogHandle,
	Indent: () => DrawerIndent,
	IndentBackground: () => DrawerIndentBackground,
	Popup: () => DrawerPopup,
	Portal: () => DrawerPortal,
	Provider: () => DrawerProvider,
	Root: () => DrawerRoot,
	SwipeArea: () => DrawerSwipeArea,
	Title: () => DrawerTitle,
	Trigger: () => DrawerTrigger,
	Viewport: () => DrawerViewport,
	createHandle: () => createDialogHandle
});
//#endregion
//#region node_modules/@base-ui/react/esm/fieldset/root/FieldsetRootContext.js
var FieldsetRootContext = /* @__PURE__ */ import_react.createContext({
	legendId: void 0,
	setLegendId: () => {},
	disabled: void 0
});
FieldsetRootContext.displayName = "FieldsetRootContext";
function useFieldsetRootContext(optional = false) {
	const context = import_react.useContext(FieldsetRootContext);
	if (!context && !optional) throw new Error("Base UI: FieldsetRootContext is missing. Fieldset parts must be placed within <Fieldset.Root>.");
	return context;
}
//#endregion
//#region node_modules/@base-ui/react/esm/labelable-provider/LabelableProvider.js
/**
* @internal
*/
var LabelableProvider = function LabelableProvider(props) {
	const defaultId = useBaseUiId();
	const initialControlId = props.controlId === void 0 ? defaultId : props.controlId;
	const [controlId, setControlIdState] = import_react.useState(initialControlId);
	const [labelId, setLabelId] = import_react.useState(props.labelId);
	const [messageIds, setMessageIds] = import_react.useState([]);
	const registrationsRef = useRefWithInit(() => /* @__PURE__ */ new Map());
	const { messageIds: parentMessageIds } = useLabelableContext();
	const registerControlId = useStableCallback((source, nextId) => {
		const registrations = registrationsRef.current;
		if (nextId === void 0) {
			registrations.delete(source);
			return;
		}
		registrations.set(source, nextId);
		setControlIdState((prev) => {
			if (registrations.size === 0) return;
			let nextControlId;
			for (const id of registrations.values()) {
				if (prev !== void 0 && id === prev) return prev;
				if (nextControlId === void 0) nextControlId = id;
			}
			return nextControlId;
		});
	});
	const getDescriptionProps = import_react.useCallback((externalProps) => {
		return mergeProps({ "aria-describedby": parentMessageIds.concat(messageIds).join(" ") || void 0 }, externalProps);
	}, [parentMessageIds, messageIds]);
	const contextValue = import_react.useMemo(() => ({
		controlId,
		registerControlId,
		labelId,
		setLabelId,
		messageIds,
		setMessageIds,
		getDescriptionProps
	}), [
		controlId,
		registerControlId,
		labelId,
		setLabelId,
		messageIds,
		setMessageIds,
		getDescriptionProps
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LabelableContext.Provider, {
		value: contextValue,
		children: props.children
	});
};
LabelableProvider.displayName = "LabelableProvider";
//#endregion
//#region node_modules/@base-ui/react/esm/field/root/useFieldValidation.js
var validityKeys = Object.keys(DEFAULT_VALIDITY_STATE);
function isOnlyValueMissing(state) {
	if (!state || state.valid || !state.valueMissing) return false;
	let onlyValueMissing = false;
	for (const key of validityKeys) {
		if (key === "valid") continue;
		if (key === "valueMissing") onlyValueMissing = state[key];
		if (state[key]) onlyValueMissing = false;
	}
	return onlyValueMissing;
}
function useFieldValidation(params) {
	const { formRef, clearErrors } = useFormContext();
	const { setValidityData, validate, validityData, validationDebounceTime, invalid, markedDirtyRef, state, name, shouldValidateOnChange } = params;
	const { controlId, getDescriptionProps } = useLabelableContext();
	const timeout = useTimeout();
	const inputRef = import_react.useRef(null);
	const commit = useStableCallback(async (value, revalidate = false) => {
		const element = inputRef.current;
		if (!element) return;
		if (revalidate) {
			if (state.valid !== false) return;
			const currentNativeValidity = element.validity;
			if (!currentNativeValidity.valueMissing) {
				const nextValidityData = {
					value,
					state: {
						...DEFAULT_VALIDITY_STATE,
						valid: true
					},
					error: "",
					errors: [],
					initialValue: validityData.initialValue
				};
				element.setCustomValidity("");
				if (controlId) {
					const currentFieldData = formRef.current.fields.get(controlId);
					if (currentFieldData) formRef.current.fields.set(controlId, {
						...currentFieldData,
						...getCombinedFieldValidityData(nextValidityData, false)
					});
				}
				setValidityData(nextValidityData);
				return;
			}
			const currentNativeValidityObject = validityKeys.reduce((acc, key) => {
				acc[key] = currentNativeValidity[key];
				return acc;
			}, {});
			if (!currentNativeValidityObject.valid && !isOnlyValueMissing(currentNativeValidityObject)) return;
		}
		function getState(el) {
			const computedState = validityKeys.reduce((acc, key) => {
				acc[key] = el.validity[key];
				return acc;
			}, {});
			let hasOnlyValueMissingError = false;
			for (const key of validityKeys) {
				if (key === "valid") continue;
				if (key === "valueMissing" && computedState[key]) hasOnlyValueMissingError = true;
				else if (computedState[key]) return computedState;
			}
			if (hasOnlyValueMissingError && !markedDirtyRef.current) {
				computedState.valid = true;
				computedState.valueMissing = false;
			}
			return computedState;
		}
		timeout.clear();
		let result = null;
		let validationErrors = [];
		const nextState = getState(element);
		let defaultValidationMessage;
		const validateOnChange = shouldValidateOnChange();
		if (element.validationMessage && !validateOnChange) {
			defaultValidationMessage = element.validationMessage;
			validationErrors = [element.validationMessage];
		} else {
			const resultOrPromise = validate(value, Array.from(formRef.current.fields.values()).reduce((acc, field) => {
				if (field.name) acc[field.name] = field.getValue();
				return acc;
			}, {}));
			if (typeof resultOrPromise === "object" && resultOrPromise !== null && "then" in resultOrPromise) result = await resultOrPromise;
			else result = resultOrPromise;
			if (result !== null) {
				nextState.valid = false;
				nextState.customError = true;
				if (Array.isArray(result)) {
					validationErrors = result;
					element.setCustomValidity(result.join("\n"));
				} else if (result) {
					validationErrors = [result];
					element.setCustomValidity(result);
				}
			} else if (validateOnChange) {
				element.setCustomValidity("");
				nextState.customError = false;
				if (element.validationMessage) {
					defaultValidationMessage = element.validationMessage;
					validationErrors = [element.validationMessage];
				} else if (element.validity.valid && !nextState.valid) nextState.valid = true;
			}
		}
		const nextValidityData = {
			value,
			state: nextState,
			error: defaultValidationMessage ?? (Array.isArray(result) ? result[0] : result ?? ""),
			errors: validationErrors,
			initialValue: validityData.initialValue
		};
		if (controlId) {
			const currentFieldData = formRef.current.fields.get(controlId);
			if (currentFieldData) formRef.current.fields.set(controlId, {
				...currentFieldData,
				...getCombinedFieldValidityData(nextValidityData, invalid)
			});
		}
		setValidityData(nextValidityData);
	});
	const getValidationProps = import_react.useCallback((externalProps = {}) => mergeProps(getDescriptionProps, state.valid === false ? { "aria-invalid": true } : EMPTY_OBJECT, externalProps), [getDescriptionProps, state.valid]);
	const getInputValidationProps = import_react.useCallback((externalProps = {}) => mergeProps({ onChange(event) {
		if (event.nativeEvent.defaultPrevented) return;
		clearErrors(name);
		if (!shouldValidateOnChange()) {
			commit(event.currentTarget.value, true);
			return;
		}
		const element = event.currentTarget;
		if (element.value === "") {
			commit(element.value);
			return;
		}
		timeout.clear();
		if (validationDebounceTime) timeout.start(validationDebounceTime, () => {
			commit(element.value);
		});
		else commit(element.value);
	} }, getValidationProps(externalProps)), [
		getValidationProps,
		clearErrors,
		name,
		timeout,
		commit,
		validationDebounceTime,
		shouldValidateOnChange
	]);
	return import_react.useMemo(() => ({
		getValidationProps,
		getInputValidationProps,
		inputRef,
		commit
	}), [
		getValidationProps,
		getInputValidationProps,
		commit
	]);
}
//#endregion
//#region node_modules/@base-ui/react/esm/field/root/FieldRoot.js
/**
* @internal
*/
var FieldRootInner = /* @__PURE__ */ import_react.forwardRef(function FieldRootInner(componentProps, forwardedRef) {
	const { errors, validationMode: formValidationMode, submitAttemptedRef } = useFormContext();
	const { render, className, validate: validateProp, validationDebounceTime = 0, validationMode = formValidationMode, name, disabled: disabledProp = false, invalid: invalidProp, dirty: dirtyProp, touched: touchedProp, actionsRef, ...elementProps } = componentProps;
	const { disabled: disabledFieldset } = useFieldsetRootContext();
	const validate = useStableCallback(validateProp || (() => null));
	const disabled = disabledFieldset || disabledProp;
	const [touchedState, setTouchedUnwrapped] = import_react.useState(false);
	const [dirtyState, setDirtyUnwrapped] = import_react.useState(false);
	const [filled, setFilled] = import_react.useState(false);
	const [focused, setFocused] = import_react.useState(false);
	const dirty = dirtyProp ?? dirtyState;
	const touched = touchedProp ?? touchedState;
	const markedDirtyRef = import_react.useRef(false);
	const setDirty = useStableCallback((value) => {
		if (dirtyProp !== void 0) return;
		if (value) markedDirtyRef.current = true;
		setDirtyUnwrapped(value);
	});
	const setTouched = useStableCallback((value) => {
		if (touchedProp !== void 0) return;
		setTouchedUnwrapped(value);
	});
	const shouldValidateOnChange = useStableCallback(() => validationMode === "onChange" || validationMode === "onSubmit" && submitAttemptedRef.current);
	const hasFormError = !!name && Object.hasOwn(errors, name) && errors[name] !== void 0;
	const invalid = invalidProp === true || hasFormError;
	const [validityData, setValidityData] = import_react.useState({
		state: DEFAULT_VALIDITY_STATE,
		error: "",
		errors: [],
		value: null,
		initialValue: null
	});
	const valid = !invalid && validityData.state.valid;
	const state = import_react.useMemo(() => ({
		disabled,
		touched,
		dirty,
		valid,
		filled,
		focused
	}), [
		disabled,
		touched,
		dirty,
		valid,
		filled,
		focused
	]);
	const validation = useFieldValidation({
		setValidityData,
		validate,
		validityData,
		validationDebounceTime,
		invalid,
		markedDirtyRef,
		state,
		name,
		shouldValidateOnChange
	});
	const handleImperativeValidate = import_react.useCallback(() => {
		markedDirtyRef.current = true;
		validation.commit(validityData.value);
	}, [validation, validityData]);
	import_react.useImperativeHandle(actionsRef, () => ({ validate: handleImperativeValidate }), [handleImperativeValidate]);
	const contextValue = import_react.useMemo(() => ({
		invalid,
		name,
		validityData,
		setValidityData,
		disabled,
		touched,
		setTouched,
		dirty,
		setDirty,
		filled,
		setFilled,
		focused,
		setFocused,
		validate,
		validationMode,
		validationDebounceTime,
		shouldValidateOnChange,
		state,
		markedDirtyRef,
		validation
	}), [
		invalid,
		name,
		validityData,
		disabled,
		touched,
		setTouched,
		dirty,
		setDirty,
		filled,
		setFilled,
		focused,
		setFocused,
		validate,
		validationMode,
		validationDebounceTime,
		shouldValidateOnChange,
		state,
		validation
	]);
	const element = useRenderElement("div", componentProps, {
		ref: forwardedRef,
		state,
		props: elementProps,
		stateAttributesMapping: fieldValidityMapping
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FieldRootContext.Provider, {
		value: contextValue,
		children: element
	});
});
FieldRootInner.displayName = "FieldRootInner";
var FieldRoot = /* @__PURE__ */ import_react.forwardRef(function FieldRoot(componentProps, forwardedRef) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LabelableProvider, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FieldRootInner, {
		...componentProps,
		ref: forwardedRef
	}) });
});
FieldRoot.displayName = "FieldRoot";
//#endregion
//#region node_modules/@base-ui/react/esm/field/label/FieldLabel.js
/**
* An accessible label that is automatically associated with the field control.
* Renders a `<label>` element.
*
* Documentation: [Base UI Field](https://base-ui.com/react/components/field)
*/
var FieldLabel = /* @__PURE__ */ import_react.forwardRef(function FieldLabel(componentProps, forwardedRef) {
	const { render, className, id: idProp, nativeLabel = true, ...elementProps } = componentProps;
	const fieldRootContext = useFieldRootContext(false);
	const { labelId } = useLabelableContext();
	const labelRef = import_react.useRef(null);
	const labelProps = useLabel({
		id: labelId ?? idProp,
		native: nativeLabel
	});
	import_react.useEffect(() => {
		if (!labelRef.current) return;
		const isLabelTag = labelRef.current.tagName === "LABEL";
		if (nativeLabel) {
			if (!isLabelTag) error(`<Field.Label> expected a <label> element because the \`nativeLabel\` prop is true. Rendering a non-<label> disables native label association, so \`htmlFor\` will not work. Use a real <label> in the \`render\` prop, or set \`nativeLabel\` to \`false\`.${SafeReact.captureOwnerStack?.() || ""}`);
		} else if (isLabelTag) error(`<Field.Label> expected a non-<label> element because the \`nativeLabel\` prop is false. Rendering a <label> assumes native label behavior while Base UI treats it as non-native, which can cause unexpected pointer behavior. Use a non-<label> in the \`render\` prop, or set \`nativeLabel\` to \`true\`.${SafeReact.captureOwnerStack?.() || ""}`);
	}, [nativeLabel]);
	return useRenderElement("label", componentProps, {
		ref: [forwardedRef, labelRef],
		state: fieldRootContext.state,
		props: [labelProps, elementProps],
		stateAttributesMapping: fieldValidityMapping
	});
});
FieldLabel.displayName = "FieldLabel";
//#endregion
//#region node_modules/@base-ui/react/esm/field/error/FieldError.js
var stateAttributesMapping$13 = {
	...fieldValidityMapping,
	...transitionStatusMapping
};
/**
* An error message displayed if the field control fails validation.
* Renders a `<div>` element.
*
* Documentation: [Base UI Field](https://base-ui.com/react/components/field)
*/
var FieldError = /* @__PURE__ */ import_react.forwardRef(function FieldError(componentProps, forwardedRef) {
	const { render, id: idProp, className, match, ...elementProps } = componentProps;
	const id = useBaseUiId(idProp);
	const { validityData, state: fieldState, name } = useFieldRootContext(false);
	const { setMessageIds } = useLabelableContext();
	const { errors } = useFormContext();
	const formError = name ? errors[name] : null;
	let rendered = false;
	if (formError || match === true) rendered = true;
	else if (match) rendered = Boolean(validityData.state[match]);
	else rendered = validityData.state.valid === false;
	const { mounted, transitionStatus, setMounted } = useTransitionStatus(rendered);
	useIsoLayoutEffect(() => {
		if (!rendered || !id) return;
		setMessageIds((v) => v.concat(id));
		return () => {
			setMessageIds((v) => v.filter((item) => item !== id));
		};
	}, [
		rendered,
		id,
		setMessageIds
	]);
	const errorRef = import_react.useRef(null);
	const [lastRenderedMessage, setLastRenderedMessage] = import_react.useState(null);
	const [lastRenderedMessageKey, setLastRenderedMessageKey] = import_react.useState(null);
	const errorMessage = formError || (validityData.errors.length > 1 ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)("ul", { children: validityData.errors.map((message) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("li", { children: message }, message)) }) : validityData.error);
	let errorKey = validityData.error;
	if (formError != null) errorKey = Array.isArray(formError) ? JSON.stringify(formError) : formError;
	else if (validityData.errors.length > 1) errorKey = JSON.stringify(validityData.errors);
	if (rendered && errorKey !== lastRenderedMessageKey) {
		setLastRenderedMessageKey(errorKey);
		setLastRenderedMessage(errorMessage);
	}
	useOpenChangeComplete({
		open: rendered,
		ref: errorRef,
		onComplete() {
			if (!rendered) setMounted(false);
		}
	});
	const state = {
		...fieldState,
		transitionStatus
	};
	const element = useRenderElement("div", componentProps, {
		ref: [forwardedRef, errorRef],
		state,
		props: [{
			id,
			children: rendered ? errorMessage : lastRenderedMessage
		}, elementProps],
		stateAttributesMapping: stateAttributesMapping$13,
		enabled: mounted
	});
	if (!mounted) return null;
	return element;
});
FieldError.displayName = "FieldError";
//#endregion
//#region node_modules/@base-ui/react/esm/field/description/FieldDescription.js
/**
* A paragraph with additional information about the field.
* Renders a `<p>` element.
*
* Documentation: [Base UI Field](https://base-ui.com/react/components/field)
*/
var FieldDescription = /* @__PURE__ */ import_react.forwardRef(function FieldDescription(componentProps, forwardedRef) {
	const { render, id: idProp, className, ...elementProps } = componentProps;
	const id = useBaseUiId(idProp);
	const fieldRootContext = useFieldRootContext(false);
	const { setMessageIds } = useLabelableContext();
	useIsoLayoutEffect(() => {
		if (!id) return;
		setMessageIds((v) => v.concat(id));
		return () => {
			setMessageIds((v) => v.filter((item) => item !== id));
		};
	}, [id, setMessageIds]);
	return useRenderElement("p", componentProps, {
		ref: forwardedRef,
		state: fieldRootContext.state,
		props: [{ id }, elementProps],
		stateAttributesMapping: fieldValidityMapping
	});
});
FieldDescription.displayName = "FieldDescription";
//#endregion
//#region node_modules/@base-ui/react/esm/field/validity/FieldValidity.js
/**
* Used to display a custom message based on the field’s validity.
* Requires `children` to be a function that accepts field validity state as an argument.
*
* Documentation: [Base UI Field](https://base-ui.com/react/components/field)
*/
var FieldValidity = function FieldValidity(props) {
	const { children } = props;
	const { validityData, invalid } = useFieldRootContext(false);
	const combinedFieldValidityData = import_react.useMemo(() => getCombinedFieldValidityData(validityData, invalid), [validityData, invalid]);
	const { transitionStatus } = useTransitionStatus(combinedFieldValidityData.state.valid === false);
	const fieldValidityState = import_react.useMemo(() => {
		return {
			...combinedFieldValidityData,
			validity: combinedFieldValidityData.state,
			transitionStatus
		};
	}, [combinedFieldValidityData, transitionStatus]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_react.Fragment, { children: children(fieldValidityState) });
};
FieldValidity.displayName = "FieldValidity";
//#endregion
//#region node_modules/@base-ui/react/esm/field/item/FieldItem.js
/**
* Groups individual items in a checkbox group or radio group with a label and description.
* Renders a `<div>` element.
*
* Documentation: [Base UI Field](https://base-ui.com/react/components/field)
*/
var FieldItem = /* @__PURE__ */ import_react.forwardRef(function FieldItem(componentProps, forwardedRef) {
	const { render, className, disabled: disabledProp = false, ...elementProps } = componentProps;
	const { state, disabled: rootDisabled } = useFieldRootContext(false);
	const disabled = rootDisabled || disabledProp;
	const checkboxGroupContext = useCheckboxGroupContext();
	const parentId = checkboxGroupContext?.parent.id;
	const controlId = checkboxGroupContext?.allValues !== void 0 ? parentId : void 0;
	const fieldItemContext = import_react.useMemo(() => ({ disabled }), [disabled]);
	const element = useRenderElement("div", componentProps, {
		ref: forwardedRef,
		state,
		props: elementProps,
		stateAttributesMapping: fieldValidityMapping
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(LabelableProvider, {
		controlId,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FieldItemContext.Provider, {
			value: fieldItemContext,
			children: element
		})
	});
});
FieldItem.displayName = "FieldItem";
//#endregion
//#region node_modules/@base-ui/react/esm/field/index.parts.js
var index_parts_exports$10 = /* @__PURE__ */ __exportAll({
	Control: () => FieldControl,
	Description: () => FieldDescription,
	Error: () => FieldError,
	Item: () => FieldItem,
	Label: () => FieldLabel,
	Root: () => FieldRoot,
	Validity: () => FieldValidity
});
//#endregion
//#region node_modules/@base-ui/react/esm/fieldset/root/FieldsetRoot.js
/**
* Groups a shared legend with related controls.
* Renders a `<fieldset>` element.
*
* Documentation: [Base UI Fieldset](https://base-ui.com/react/components/fieldset)
*/
var FieldsetRoot = /* @__PURE__ */ import_react.forwardRef(function FieldsetRoot(componentProps, forwardedRef) {
	const { render, className, disabled = false, ...elementProps } = componentProps;
	const [legendId, setLegendId] = import_react.useState(void 0);
	const element = useRenderElement("fieldset", componentProps, {
		ref: forwardedRef,
		state: { disabled },
		props: [{ "aria-labelledby": legendId }, elementProps]
	});
	const contextValue = import_react.useMemo(() => ({
		legendId,
		setLegendId,
		disabled
	}), [
		legendId,
		setLegendId,
		disabled
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FieldsetRootContext.Provider, {
		value: contextValue,
		children: element
	});
});
FieldsetRoot.displayName = "FieldsetRoot";
//#endregion
//#region node_modules/@base-ui/react/esm/fieldset/legend/FieldsetLegend.js
/**
* An accessible label that is automatically associated with the fieldset.
* Renders a `<div>` element.
*
* Documentation: [Base UI Fieldset](https://base-ui.com/react/components/fieldset)
*/
var FieldsetLegend = /* @__PURE__ */ import_react.forwardRef(function FieldsetLegend(componentProps, forwardedRef) {
	const { render, className, id: idProp, ...elementProps } = componentProps;
	const { disabled, setLegendId } = useFieldsetRootContext();
	const id = useBaseUiId(idProp);
	useIsoLayoutEffect(() => {
		setLegendId(id);
		return () => {
			setLegendId(void 0);
		};
	}, [setLegendId, id]);
	return useRenderElement("div", componentProps, {
		state: { disabled: disabled ?? false },
		ref: forwardedRef,
		props: [{ id }, elementProps]
	});
});
FieldsetLegend.displayName = "FieldsetLegend";
//#endregion
//#region node_modules/@base-ui/react/esm/fieldset/index.parts.js
var index_parts_exports$11 = /* @__PURE__ */ __exportAll({
	Legend: () => FieldsetLegend,
	Root: () => FieldsetRoot
});
//#endregion
//#region node_modules/@base-ui/react/esm/form/Form.js
/**
* A native form element with consolidated error handling.
* Renders a `<form>` element.
*
* Documentation: [Base UI Form](https://base-ui.com/react/components/form)
*/
var Form = /* @__PURE__ */ import_react.forwardRef(function Form(componentProps, forwardedRef) {
	const { render, className, validationMode = "onSubmit", errors: externalErrors, onSubmit, onFormSubmit, actionsRef, ...elementProps } = componentProps;
	const formRef = import_react.useRef({ fields: /* @__PURE__ */ new Map() });
	const submittedRef = import_react.useRef(false);
	const submitAttemptedRef = import_react.useRef(false);
	const focusControl = useStableCallback((control) => {
		if (!control) return;
		control.focus();
		if (control.tagName === "INPUT") control.select();
	});
	const [errors, setErrors] = import_react.useState(externalErrors);
	useValueChanged(externalErrors, () => {
		setErrors(externalErrors);
	});
	import_react.useEffect(() => {
		if (!submittedRef.current) return;
		submittedRef.current = false;
		const invalidFields = Array.from(formRef.current.fields.values()).filter((field) => field.validityData.state.valid === false);
		if (invalidFields.length) focusControl(invalidFields[0].controlRef.current);
	}, [errors, focusControl]);
	const handleImperativeValidate = import_react.useCallback((fieldName) => {
		const values = Array.from(formRef.current.fields.values());
		if (fieldName) {
			const namedField = values.find((field) => field.name === fieldName);
			if (namedField) namedField.validate(false);
		} else values.forEach((field) => {
			field.validate(false);
		});
	}, []);
	import_react.useImperativeHandle(actionsRef, () => ({ validate: handleImperativeValidate }), [handleImperativeValidate]);
	const element = useRenderElement("form", componentProps, {
		ref: forwardedRef,
		props: [{
			noValidate: true,
			onSubmit(event) {
				submitAttemptedRef.current = true;
				let values = Array.from(formRef.current.fields.values());
				values.forEach((field) => {
					field.validate();
				});
				values = Array.from(formRef.current.fields.values());
				const invalidFields = values.filter((field) => !field.validityData.state.valid);
				if (invalidFields.length) {
					event.preventDefault();
					focusControl(invalidFields[0].controlRef.current);
				} else {
					submittedRef.current = true;
					onSubmit?.(event);
					if (onFormSubmit) {
						event.preventDefault();
						onFormSubmit(values.reduce((acc, field) => {
							if (field.name) acc[field.name] = field.getValue();
							return acc;
						}, {}), createGenericEventDetails(none, event.nativeEvent));
					}
				}
			}
		}, elementProps]
	});
	const clearErrors = useStableCallback((name) => {
		if (name && errors && EMPTY_OBJECT.hasOwnProperty.call(errors, name)) {
			const nextErrors = { ...errors };
			delete nextErrors[name];
			setErrors(nextErrors);
		}
	});
	const contextValue = import_react.useMemo(() => ({
		formRef,
		validationMode,
		errors: errors ?? EMPTY_OBJECT,
		clearErrors,
		submitAttemptedRef
	}), [
		formRef,
		validationMode,
		errors,
		clearErrors
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FormContext.Provider, {
		value: contextValue,
		children: element
	});
});
Form.displayName = "Form";
//#endregion
//#region node_modules/@base-ui/react/esm/menubar/Menubar.js
var menubarStateAttributesMapping = { hasSubmenuOpen(value) {
	return { "data-has-submenu-open": value ? "true" : "false" };
} };
/**
* The container for menus.
*
* Documentation: [Base UI Menubar](https://base-ui.com/react/components/menubar)
*/
var Menubar = /* @__PURE__ */ import_react.forwardRef(function Menubar(props, forwardedRef) {
	const { orientation = "horizontal", loopFocus = true, render, className, modal = true, disabled = false, id: idProp, ...elementProps } = props;
	const [contentElement, setContentElement] = import_react.useState(null);
	const [hasSubmenuOpen, setHasSubmenuOpen] = import_react.useState(false);
	const { openMethod, triggerProps: interactionTypeProps } = useOpenInteractionType(hasSubmenuOpen);
	useScrollLock(modal && hasSubmenuOpen && openMethod !== "touch", contentElement);
	const id = useBaseUiId(idProp);
	const state = {
		orientation,
		modal,
		hasSubmenuOpen
	};
	const contentRef = import_react.useRef(null);
	const allowMouseUpTriggerRef = import_react.useRef(false);
	const context = import_react.useMemo(() => ({
		contentElement,
		setContentElement,
		setHasSubmenuOpen,
		hasSubmenuOpen,
		modal,
		disabled,
		orientation,
		allowMouseUpTriggerRef,
		rootId: id
	}), [
		contentElement,
		hasSubmenuOpen,
		modal,
		disabled,
		orientation,
		id
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenubarContext.Provider, {
		value: context,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FloatingTree, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MenubarContent, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CompositeRoot, {
			render,
			className,
			state,
			stateAttributesMapping: menubarStateAttributesMapping,
			refs: [
				forwardedRef,
				setContentElement,
				contentRef
			],
			props: [
				{
					role: "menubar",
					id
				},
				interactionTypeProps,
				elementProps
			],
			orientation,
			loopFocus,
			highlightItemOnHover: hasSubmenuOpen
		}) }) })
	});
});
Menubar.displayName = "Menubar";
function MenubarContent(props) {
	const nodeId = useFloatingNodeId();
	const { events: menuEvents } = useFloatingTree();
	const rootContext = useMenubarContext();
	import_react.useEffect(() => {
		function onSubmenuOpenChange(details) {
			if (!details.nodeId || details.parentNodeId !== nodeId) return;
			if (details.open) {
				if (!rootContext.hasSubmenuOpen) rootContext.setHasSubmenuOpen(true);
			} else if (details.reason !== "sibling-open" && details.reason !== "list-navigation") rootContext.setHasSubmenuOpen(false);
		}
		menuEvents.on("menuopenchange", onSubmenuOpenChange);
		return () => {
			menuEvents.off("menuopenchange", onSubmenuOpenChange);
		};
	}, [
		menuEvents,
		nodeId,
		rootContext
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FloatingNode, {
		id: nodeId,
		children: props.children
	});
}
//#endregion
//#region node_modules/@base-ui/react/esm/meter/root/MeterRootContext.js
var MeterRootContext = /* @__PURE__ */ import_react.createContext(void 0);
MeterRootContext.displayName = "MeterRootContext";
function useMeterRootContext() {
	const context = import_react.useContext(MeterRootContext);
	if (context === void 0) throw new Error("Base UI: MeterRootContext is missing. Meter parts must be placed within <Meter.Root>.");
	return context;
}
//#endregion
//#region node_modules/@base-ui/react/esm/meter/root/MeterRoot.js
/**
* Groups all parts of the meter and provides the value for screen readers.
* Renders a `<div>` element.
*
* Documentation: [Base UI Meter](https://base-ui.com/react/components/meter)
*/
var MeterRoot = /* @__PURE__ */ import_react.forwardRef(function MeterRoot(componentProps, forwardedRef) {
	const { format, getAriaValueText, locale, max = 100, min = 0, value: valueProp, render, className, children, ...elementProps } = componentProps;
	const [labelId, setLabelId] = import_react.useState();
	const formattedValue = formatNumberValue(valueProp, locale, format);
	let ariaValuetext = `${valueProp}%`;
	if (getAriaValueText) ariaValuetext = getAriaValueText(formattedValue, valueProp);
	else if (format) ariaValuetext = formattedValue;
	const defaultProps = {
		"aria-labelledby": labelId,
		"aria-valuemax": max,
		"aria-valuemin": min,
		"aria-valuenow": valueProp,
		"aria-valuetext": ariaValuetext,
		role: "meter",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [children, /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
			role: "presentation",
			style: visuallyHidden,
			children: "x"
		})] })
	};
	const contextValue = import_react.useMemo(() => ({
		formattedValue,
		max,
		min,
		setLabelId,
		value: valueProp
	}), [
		formattedValue,
		max,
		min,
		setLabelId,
		valueProp
	]);
	const element = useRenderElement("div", componentProps, {
		ref: forwardedRef,
		props: [defaultProps, elementProps]
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(MeterRootContext.Provider, {
		value: contextValue,
		children: element
	});
});
MeterRoot.displayName = "MeterRoot";
//#endregion
//#region node_modules/@base-ui/react/esm/meter/track/MeterTrack.js
/**
* Contains the meter indicator and represents the entire range of the meter.
* Renders a `<div>` element.
*
* Documentation: [Base UI Meter](https://base-ui.com/react/components/meter)
*/
var MeterTrack = /* @__PURE__ */ import_react.forwardRef(function MeterTrack(componentProps, forwardedRef) {
	const { render, className, ...elementProps } = componentProps;
	return useRenderElement("div", componentProps, {
		ref: forwardedRef,
		props: elementProps
	});
});
MeterTrack.displayName = "MeterTrack";
//#endregion
//#region node_modules/@base-ui/react/esm/meter/indicator/MeterIndicator.js
/**
* Visualizes the position of the value along the range.
* Renders a `<div>` element.
*
* Documentation: [Base UI Meter](https://base-ui.com/react/components/meter)
*/
var MeterIndicator = /* @__PURE__ */ import_react.forwardRef(function MeterIndicator(componentProps, forwardedRef) {
	const { render, className, ...elementProps } = componentProps;
	const context = useMeterRootContext();
	return useRenderElement("div", componentProps, {
		ref: forwardedRef,
		props: [{ style: {
			insetInlineStart: 0,
			height: "inherit",
			width: `${valueToPercent(context.value, context.min, context.max)}%`
		} }, elementProps]
	});
});
MeterIndicator.displayName = "MeterIndicator";
//#endregion
//#region node_modules/@base-ui/react/esm/meter/value/MeterValue.js
/**
* A text element displaying the current value.
* Renders a `<span>` element.
*
* Documentation: [Base UI Meter](https://base-ui.com/react/components/meter)
*/
var MeterValue = /* @__PURE__ */ import_react.forwardRef(function MeterValue(componentProps, forwardedRef) {
	const { className, render, children, ...elementProps } = componentProps;
	const { value, formattedValue } = useMeterRootContext();
	return useRenderElement("span", componentProps, {
		ref: forwardedRef,
		props: [{
			"aria-hidden": true,
			children: typeof children === "function" ? children(formattedValue, value) : (formattedValue || value) ?? ""
		}, elementProps]
	});
});
MeterValue.displayName = "MeterValue";
//#endregion
//#region node_modules/@base-ui/react/esm/meter/label/MeterLabel.js
/**
* An accessible label for the meter.
* Renders a `<span>` element.
*
* Documentation: [Base UI Meter](https://base-ui.com/react/components/meter)
*/
var MeterLabel = /* @__PURE__ */ import_react.forwardRef(function MeterLabel(componentProps, forwardedRef) {
	const { render, className, id: idProp, ...elementProps } = componentProps;
	const { setLabelId } = useMeterRootContext();
	return useRenderElement("span", componentProps, {
		ref: forwardedRef,
		props: [{
			id: useRegisteredLabelId(idProp, setLabelId),
			role: "presentation"
		}, elementProps]
	});
});
MeterLabel.displayName = "MeterLabel";
//#endregion
//#region node_modules/@base-ui/react/esm/meter/index.parts.js
var index_parts_exports$13 = /* @__PURE__ */ __exportAll({
	Indicator: () => MeterIndicator,
	Label: () => MeterLabel,
	Root: () => MeterRoot,
	Track: () => MeterTrack,
	Value: () => MeterValue
});
//#endregion
//#region node_modules/@base-ui/react/esm/navigation-menu/root/NavigationMenuRootContext.js
var NavigationMenuRootContext = /* @__PURE__ */ import_react.createContext(void 0);
NavigationMenuRootContext.displayName = "NavigationMenuRootContext";
NavigationMenuRootContext.displayName = "NavigationMenuRootContext";
function useNavigationMenuRootContext(optional) {
	const context = import_react.useContext(NavigationMenuRootContext);
	if (context === void 0 && !optional) throw new Error("Base UI: NavigationMenuRootContext is missing. Navigation Menu parts must be placed within <NavigationMenu.Root>.");
	return context;
}
var NavigationMenuTreeContext = /* @__PURE__ */ import_react.createContext(void 0);
NavigationMenuTreeContext.displayName = "NavigationMenuTreeContext";
function useNavigationMenuTreeContext() {
	return import_react.useContext(NavigationMenuTreeContext);
}
//#endregion
//#region node_modules/@base-ui/react/esm/navigation-menu/popup/NavigationMenuPopupCssVars.js
var NavigationMenuPopupCssVars = /* @__PURE__ */ function(NavigationMenuPopupCssVars) {
	/**
	* The fixed width of the popup element.
	* @type {number}
	*/
	NavigationMenuPopupCssVars["popupWidth"] = "--popup-width";
	/**
	* The fixed height of the popup element.
	* @type {number}
	*/
	NavigationMenuPopupCssVars["popupHeight"] = "--popup-height";
	return NavigationMenuPopupCssVars;
}({});
//#endregion
//#region node_modules/@base-ui/react/esm/navigation-menu/positioner/NavigationMenuPositionerCssVars.js
var NavigationMenuPositionerCssVars = /* @__PURE__ */ function(NavigationMenuPositionerCssVars) {
	/**
	* The available width between the trigger and the edge of the viewport.
	* @type {number}
	*/
	NavigationMenuPositionerCssVars["availableWidth"] = "--available-width";
	/**
	* The available height between the trigger and the edge of the viewport.
	* @type {number}
	*/
	NavigationMenuPositionerCssVars["availableHeight"] = "--available-height";
	/**
	* The anchor's width.
	* @type {number}
	*/
	NavigationMenuPositionerCssVars["anchorWidth"] = "--anchor-width";
	/**
	* The anchor's height.
	* @type {number}
	*/
	NavigationMenuPositionerCssVars["anchorHeight"] = "--anchor-height";
	/**
	* The coordinates that this element is anchored to. Used for animations and transitions.
	* @type {string}
	*/
	NavigationMenuPositionerCssVars["transformOrigin"] = "--transform-origin";
	/**
	* The fixed width of the positioner element.
	* @type {number}
	*/
	NavigationMenuPositionerCssVars["positionerWidth"] = "--positioner-width";
	/**
	* The fixed height of the positioner element.
	* @type {number}
	*/
	NavigationMenuPositionerCssVars["positionerHeight"] = "--positioner-height";
	return NavigationMenuPositionerCssVars;
}({});
//#endregion
//#region node_modules/@base-ui/react/esm/navigation-menu/root/NavigationMenuRoot.js
var blockedReturnFocusReasons = new Set([
	triggerHover,
	outsidePress,
	focusOut
]);
function setSharedFixedSize(popupElement, positionerElement) {
	const { width, height } = getCssDimensions(popupElement);
	if (width === 0 || height === 0) return;
	popupElement.style.setProperty(NavigationMenuPopupCssVars.popupWidth, `${width}px`);
	popupElement.style.setProperty(NavigationMenuPopupCssVars.popupHeight, `${height}px`);
	positionerElement.style.setProperty(NavigationMenuPositionerCssVars.positionerWidth, `${width}px`);
	positionerElement.style.setProperty(NavigationMenuPositionerCssVars.positionerHeight, `${height}px`);
}
/**
* Groups all parts of the navigation menu.
* Renders a `<nav>` element at the root, or `<div>` element when nested.
*
* Documentation: [Base UI Navigation Menu](https://base-ui.com/react/components/navigation-menu)
*/
var NavigationMenuRoot = /* @__PURE__ */ import_react.forwardRef(function NavigationMenuRoot(componentProps, forwardedRef) {
	const { defaultValue = null, value: valueParam, onValueChange, actionsRef, delay = 50, closeDelay = 50, orientation = "horizontal", onOpenChangeComplete } = componentProps;
	const nested = useFloatingParentNodeId() != null;
	const parentRootContext = useNavigationMenuRootContext(true);
	const [value, setValueUnwrapped] = useControlled({
		controlled: valueParam,
		default: defaultValue,
		name: "NavigationMenu",
		state: "value"
	});
	const open = value != null;
	const closeReasonRef = import_react.useRef(void 0);
	const rootRef = import_react.useRef(null);
	const [positionerElement, setPositionerElement] = import_react.useState(null);
	const [popupElement, setPopupElement] = import_react.useState(null);
	const [viewportElement, setViewportElement] = import_react.useState(null);
	const [viewportTargetElement, setViewportTargetElement] = import_react.useState(null);
	const [activationDirection, setActivationDirection] = import_react.useState(null);
	const [floatingRootContext, setFloatingRootContext] = import_react.useState(void 0);
	const [viewportInert, setViewportInert] = import_react.useState(false);
	const prevTriggerElementRef = import_react.useRef(null);
	const currentContentRef = import_react.useRef(null);
	const beforeInsideRef = import_react.useRef(null);
	const afterInsideRef = import_react.useRef(null);
	const beforeOutsideRef = import_react.useRef(null);
	const afterOutsideRef = import_react.useRef(null);
	const { mounted, setMounted, transitionStatus } = useTransitionStatus(open);
	import_react.useEffect(() => {
		setViewportInert(false);
	}, [value]);
	const setValue = useStableCallback((nextValue, eventDetails) => {
		if (!nextValue) {
			closeReasonRef.current = eventDetails.reason;
			setActivationDirection(null);
			setFloatingRootContext(void 0);
			if (positionerElement && popupElement) setSharedFixedSize(popupElement, positionerElement);
		}
		if (nextValue !== value) onValueChange?.(nextValue, eventDetails);
		if (eventDetails.isCanceled) return;
		setValueUnwrapped(nextValue);
		if (nested && !nextValue && eventDetails.reason === "link-press" && parentRootContext) parentRootContext.setValue(null, eventDetails);
	});
	const handleUnmount = useStableCallback(() => {
		const activeEl = activeElement(ownerDocument(rootRef.current));
		if (!(closeReasonRef.current ? blockedReturnFocusReasons.has(closeReasonRef.current) : false) && isHTMLElement(prevTriggerElementRef.current) && (activeEl === ownerDocument(popupElement).body || contains(popupElement, activeEl)) && popupElement) {
			prevTriggerElementRef.current.focus({ preventScroll: true });
			prevTriggerElementRef.current = void 0;
		}
		setMounted(false);
		onOpenChangeComplete?.(false);
		setActivationDirection(null);
		setFloatingRootContext(void 0);
		currentContentRef.current = null;
		closeReasonRef.current = void 0;
	});
	useOpenChangeComplete({
		enabled: !actionsRef,
		open,
		ref: { current: popupElement },
		onComplete() {
			if (!open) handleUnmount();
		}
	});
	useOpenChangeComplete({
		enabled: !actionsRef,
		open,
		ref: { current: viewportTargetElement },
		onComplete() {
			if (!open) handleUnmount();
		}
	});
	const contextValue = import_react.useMemo(() => ({
		open,
		value,
		setValue,
		mounted,
		transitionStatus,
		positionerElement,
		setPositionerElement,
		popupElement,
		setPopupElement,
		viewportElement,
		setViewportElement,
		viewportTargetElement,
		setViewportTargetElement,
		activationDirection,
		setActivationDirection,
		floatingRootContext,
		setFloatingRootContext,
		currentContentRef,
		nested,
		rootRef,
		beforeInsideRef,
		afterInsideRef,
		beforeOutsideRef,
		afterOutsideRef,
		prevTriggerElementRef,
		delay,
		closeDelay,
		orientation,
		viewportInert,
		setViewportInert
	}), [
		open,
		value,
		setValue,
		mounted,
		transitionStatus,
		positionerElement,
		popupElement,
		viewportElement,
		viewportTargetElement,
		activationDirection,
		floatingRootContext,
		nested,
		delay,
		closeDelay,
		orientation,
		viewportInert
	]);
	const jsx = /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NavigationMenuRootContext.Provider, {
		value: contextValue,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(TreeContext, {
			componentProps,
			forwardedRef,
			children: componentProps.children
		})
	});
	if (!nested) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FloatingTree, { children: jsx });
	return jsx;
});
NavigationMenuRoot.displayName = "NavigationMenuRoot";
function TreeContext(props) {
	const { className, render, defaultValue, value: valueParam, onValueChange, actionsRef, delay, closeDelay, orientation, onOpenChangeComplete, ...elementProps } = props.componentProps;
	const nodeId = useFloatingNodeId();
	const { rootRef, nested } = useNavigationMenuRootContext();
	const { open } = useNavigationMenuRootContext();
	const state = {
		open,
		nested
	};
	const element = useRenderElement(nested ? "div" : "nav", props.componentProps, {
		state,
		ref: [props.forwardedRef, rootRef],
		props: elementProps
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NavigationMenuTreeContext.Provider, {
		value: nodeId,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FloatingNode, {
			id: nodeId,
			children: element
		})
	});
}
//#endregion
//#region node_modules/@base-ui/react/esm/navigation-menu/utils/constants.js
var NAVIGATION_MENU_TRIGGER_IDENTIFIER = "data-base-ui-navigation-menu-trigger";
//#endregion
//#region node_modules/@base-ui/react/esm/navigation-menu/list/NavigationMenuDismissContext.js
var NavigationMenuDismissContext = /* @__PURE__ */ import_react.createContext(void 0);
NavigationMenuDismissContext.displayName = "NavigationMenuDismissContext";
function useNavigationMenuDismissContext() {
	return import_react.useContext(NavigationMenuDismissContext);
}
//#endregion
//#region node_modules/@base-ui/react/esm/navigation-menu/list/NavigationMenuList.js
/**
* Contains a list of navigation menu items.
* Renders a `<ul>` element.
*
* Documentation: [Base UI Navigation Menu](https://base-ui.com/react/components/navigation-menu)
*/
var NavigationMenuList = /* @__PURE__ */ import_react.forwardRef(function NavigationMenuList(componentProps, forwardedRef) {
	const { className, render, ...elementProps } = componentProps;
	const { orientation, open, floatingRootContext, positionerElement, value, closeDelay, viewportElement, nested } = useNavigationMenuRootContext();
	const fallbackContext = import_react.useMemo(() => getEmptyRootContext(), []);
	const context = floatingRootContext || fallbackContext;
	const interactionsEnabled = positionerElement ? true : !value;
	const hoverInteractionsEnabled = positionerElement || viewportElement ? true : !value;
	useHoverFloatingInteraction(context, {
		enabled: Boolean(floatingRootContext) && hoverInteractionsEnabled,
		closeDelay
	});
	const dismiss = useDismiss(context, {
		enabled: interactionsEnabled,
		outsidePressEvent: "intentional",
		outsidePress(event) {
			return getTarget(event)?.closest(`[${NAVIGATION_MENU_TRIGGER_IDENTIFIER}]`) === null;
		}
	});
	const dismissProps = floatingRootContext ? dismiss : void 0;
	const state = { open };
	const defaultProps = nested ? { "aria-orientation": orientation } : { onKeyDown(event) {
		if (orientation === "horizontal" && (event.key === "ArrowLeft" || event.key === "ArrowRight") || orientation === "vertical" && (event.key === "ArrowUp" || event.key === "ArrowDown")) event.stopPropagation();
	} };
	const props = [
		dismissProps?.floating || EMPTY_OBJECT,
		defaultProps,
		elementProps
	];
	const element = useRenderElement("ul", componentProps, {
		state,
		ref: forwardedRef,
		props,
		enabled: nested
	});
	if (nested) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NavigationMenuDismissContext.Provider, {
		value: dismissProps,
		children: element
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NavigationMenuDismissContext.Provider, {
		value: dismissProps,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CompositeRoot, {
			render,
			className,
			state,
			refs: [forwardedRef],
			props,
			loopFocus: false,
			orientation,
			tag: "ul"
		})
	});
});
NavigationMenuList.displayName = "NavigationMenuList";
//#endregion
//#region node_modules/@base-ui/react/esm/navigation-menu/item/NavigationMenuItemContext.js
var NavigationMenuItemContext = /* @__PURE__ */ import_react.createContext(void 0);
NavigationMenuItemContext.displayName = "NavigationMenuItemContext";
function useNavigationMenuItemContext() {
	const value = import_react.useContext(NavigationMenuItemContext);
	if (!value) throw new Error("Base UI: NavigationMenuItem parts must be used within a <NavigationMenu.Item>.");
	return value;
}
//#endregion
//#region node_modules/@base-ui/react/esm/navigation-menu/item/NavigationMenuItem.js
/**
* An individual navigation menu item.
* Renders a `<li>` element.
*
* Documentation: [Base UI Navigation Menu](https://base-ui.com/react/components/navigation-menu)
*/
var NavigationMenuItem = /* @__PURE__ */ import_react.forwardRef(function NavigationMenuItem(componentProps, forwardedRef) {
	const { className, render, value: valueProp, ...elementProps } = componentProps;
	const fallbackValue = useBaseUiId();
	const value = valueProp ?? fallbackValue;
	const element = useRenderElement("li", componentProps, {
		ref: forwardedRef,
		props: elementProps
	});
	const contextValue = import_react.useMemo(() => ({ value }), [value]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NavigationMenuItemContext.Provider, {
		value: contextValue,
		children: element
	});
});
NavigationMenuItem.displayName = "NavigationMenuItem";
//#endregion
//#region node_modules/@base-ui/react/esm/navigation-menu/content/NavigationMenuContent.js
var stateAttributesMapping$12 = {
	...popupStateMapping,
	...transitionStatusMapping,
	activationDirection(value) {
		if (!value) return null;
		return { "data-activation-direction": value };
	}
};
/**
* A container for the content of the navigation menu item that is moved into the popup
* when the item is active.
* Renders a `<div>` element.
*
* Documentation: [Base UI Navigation Menu](https://base-ui.com/react/components/navigation-menu)
*/
var NavigationMenuContent = /* @__PURE__ */ import_react.forwardRef(function NavigationMenuContent(componentProps, forwardedRef) {
	const { className, render, keepMounted = false, ...elementProps } = componentProps;
	const { mounted: popupMounted, viewportElement, value, activationDirection, currentContentRef, viewportTargetElement } = useNavigationMenuRootContext();
	const { value: itemValue } = useNavigationMenuItemContext();
	const nodeId = useNavigationMenuTreeContext();
	const open = popupMounted && value === itemValue;
	const ref = import_react.useRef(null);
	const [hasMountedInPortal, setHasMountedInPortal] = import_react.useState(false);
	const [focusInside, setFocusInside] = import_react.useState(false);
	const { mounted, setMounted, transitionStatus } = useTransitionStatus(open);
	if (mounted && !popupMounted) setMounted(false);
	useOpenChangeComplete({
		ref,
		open,
		onComplete() {
			if (!open) setMounted(false);
		}
	});
	useIsoLayoutEffect(() => {
		if (open && ref.current) currentContentRef.current = ref.current;
	}, [open, currentContentRef]);
	const state = {
		open,
		transitionStatus,
		activationDirection
	};
	const handleCurrentContentRef = import_react.useCallback((node) => {
		if (node) currentContentRef.current = node;
	}, [currentContentRef]);
	const commonProps = {
		onFocus(event) {
			if (getTarget(event.nativeEvent)?.hasAttribute("data-base-ui-focus-guard")) return;
			setFocusInside(true);
		},
		onBlur(event) {
			if (!contains(event.currentTarget, event.relatedTarget)) setFocusInside(false);
		}
	};
	const defaultProps = !open && mounted ? {
		style: {
			position: "absolute",
			top: 0,
			left: 0
		},
		inert: inertValue(!focusInside),
		...commonProps
	} : commonProps;
	const portalContainer = viewportTargetElement || viewportElement;
	const hidden = keepMounted && !mounted;
	const shouldRenderInline = keepMounted && !portalContainer && !hasMountedInPortal;
	if (keepMounted && portalContainer && !hasMountedInPortal) setHasMountedInPortal(true);
	if (shouldRenderInline) return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CompositeRoot, {
		render,
		className,
		state,
		refs: [forwardedRef],
		props: [
			defaultProps,
			{ hidden: true },
			elementProps
		],
		stateAttributesMapping: stateAttributesMapping$12
	});
	if (!portalContainer || !mounted && !keepMounted) return null;
	return /* @__PURE__ */ import_react_dom.createPortal(/* @__PURE__ */ (0, import_jsx_runtime.jsx)(FloatingNode, {
		id: nodeId,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CompositeRoot, {
			render,
			className,
			state,
			refs: [
				forwardedRef,
				ref,
				handleCurrentContentRef
			],
			props: [
				defaultProps,
				hidden ? { hidden: true } : EMPTY_OBJECT,
				elementProps
			],
			stateAttributesMapping: stateAttributesMapping$12
		})
	}), portalContainer);
});
NavigationMenuContent.displayName = "NavigationMenuContent";
//#endregion
//#region node_modules/@base-ui/react/esm/navigation-menu/utils/isOutsideMenuEvent.js
function isOutsideMenuEvent({ currentTarget, relatedTarget }, params) {
	const { popupElement, viewportElement, rootRef, tree, nodeId } = params;
	const nodeChildrenContains = tree ? getNodeChildren(tree.nodesRef.current, nodeId).some((node) => contains(node.context?.elements.floating, relatedTarget)) : [];
	if (!popupElement) return !contains(rootRef.current, relatedTarget) && !nodeChildrenContains;
	const floatingElement = popupElement || viewportElement;
	return !contains(floatingElement, currentTarget) && !contains(floatingElement, relatedTarget) && !contains(rootRef.current, relatedTarget) && !nodeChildrenContains && !(contains(floatingElement, relatedTarget) && relatedTarget?.hasAttribute("data-base-ui-focus-guard"));
}
//#endregion
//#region node_modules/@base-ui/react/esm/navigation-menu/trigger/NavigationMenuTrigger.js
var DEFAULT_SIZE = {
	width: 0,
	height: 0
};
/**
* Opens the navigation menu popup when hovered or clicked, revealing the
* associated content.
* Renders a `<button>` element.
*
* Documentation: [Base UI Navigation Menu](https://base-ui.com/react/components/navigation-menu)
*/
var NavigationMenuTrigger = /* @__PURE__ */ import_react.forwardRef(function NavigationMenuTrigger(componentProps, forwardedRef) {
	const { className, render, nativeButton = true, disabled, ...elementProps } = componentProps;
	const { value, setValue, mounted, open, positionerElement, setActivationDirection, setFloatingRootContext, popupElement, viewportElement, transitionStatus, rootRef, beforeOutsideRef, afterOutsideRef, afterInsideRef, beforeInsideRef, prevTriggerElementRef, currentContentRef, delay, closeDelay, orientation, setViewportInert, nested } = useNavigationMenuRootContext();
	const { value: itemValue } = useNavigationMenuItemContext();
	const nodeId = useNavigationMenuTreeContext();
	const tree = useFloatingTree();
	const dismissProps = useNavigationMenuDismissContext();
	const stickIfOpenTimeout = useTimeout();
	const focusFrame = useAnimationFrame();
	const mutationFrame = useAnimationFrame();
	const resizeFrame = useAnimationFrame();
	const sizeFrame = useAnimationFrame();
	const [triggerElement, setTriggerElement] = import_react.useState(null);
	const [stickIfOpen, setStickIfOpen] = import_react.useState(true);
	const [pointerType, setPointerType] = import_react.useState("");
	const triggerElementRef = import_react.useRef(null);
	const allowFocusRef = import_react.useRef(false);
	const prevSizeRef = import_react.useRef(DEFAULT_SIZE);
	const animationAbortControllerRef = import_react.useRef(null);
	const skipAutoSizeSyncRef = import_react.useRef(false);
	const isActiveItem = open && value === itemValue;
	const isActiveItemRef = useValueAsRef(isActiveItem);
	const interactionsEnabled = positionerElement ? true : !value;
	const hoverFloatingElement = positionerElement || viewportElement;
	const hoverInteractionsEnabled = hoverFloatingElement ? true : !value;
	const runOnceAnimationsFinish = useAnimationsFinished(popupElement, false, false);
	const handleTriggerElement = import_react.useCallback((element) => {
		triggerElementRef.current = element;
		setTriggerElement(element);
	}, []);
	import_react.useEffect(() => {
		animationAbortControllerRef.current?.abort();
	}, [isActiveItem]);
	function setAutoSizes() {
		if (!popupElement) return;
		popupElement.style.setProperty(NavigationMenuPopupCssVars.popupWidth, "auto");
		popupElement.style.setProperty(NavigationMenuPopupCssVars.popupHeight, "auto");
	}
	function clearFixedSizes() {
		if (!popupElement || !positionerElement) return;
		popupElement.style.removeProperty(NavigationMenuPopupCssVars.popupWidth);
		popupElement.style.removeProperty(NavigationMenuPopupCssVars.popupHeight);
		positionerElement.style.removeProperty(NavigationMenuPositionerCssVars.positionerWidth);
		positionerElement.style.removeProperty(NavigationMenuPositionerCssVars.positionerHeight);
	}
	function setSharedFixedSizes(width, height) {
		if (!popupElement || !positionerElement) return;
		popupElement.style.setProperty(NavigationMenuPopupCssVars.popupWidth, `${width}px`);
		popupElement.style.setProperty(NavigationMenuPopupCssVars.popupHeight, `${height}px`);
		positionerElement.style.setProperty(NavigationMenuPositionerCssVars.positionerWidth, `${width}px`);
		positionerElement.style.setProperty(NavigationMenuPositionerCssVars.positionerHeight, `${height}px`);
	}
	function scheduleAutoSizeReset() {
		animationAbortControllerRef.current = new AbortController();
		runOnceAnimationsFinish(setAutoSizes, animationAbortControllerRef.current.signal);
	}
	const handleValueChange = useStableCallback((currentWidth, currentHeight, options = {}) => {
		if (!popupElement || !positionerElement) return;
		const { syncPositioner = false } = options;
		clearFixedSizes();
		const { width, height } = getCssDimensions(popupElement);
		const measuredWidth = width || prevSizeRef.current.width;
		const measuredHeight = height || prevSizeRef.current.height;
		if (currentHeight === 0 || currentWidth === 0) {
			currentWidth = measuredWidth;
			currentHeight = measuredHeight;
		}
		popupElement.style.setProperty(NavigationMenuPopupCssVars.popupWidth, `${currentWidth}px`);
		popupElement.style.setProperty(NavigationMenuPopupCssVars.popupHeight, `${currentHeight}px`);
		positionerElement.style.setProperty(NavigationMenuPositionerCssVars.positionerWidth, `${syncPositioner ? currentWidth : measuredWidth}px`);
		positionerElement.style.setProperty(NavigationMenuPositionerCssVars.positionerHeight, `${syncPositioner ? currentHeight : measuredHeight}px`);
		sizeFrame.request(() => {
			popupElement.style.setProperty(NavigationMenuPopupCssVars.popupWidth, `${measuredWidth}px`);
			popupElement.style.setProperty(NavigationMenuPopupCssVars.popupHeight, `${measuredHeight}px`);
			if (syncPositioner) {
				positionerElement.style.setProperty(NavigationMenuPositionerCssVars.positionerWidth, `${measuredWidth}px`);
				positionerElement.style.setProperty(NavigationMenuPositionerCssVars.positionerHeight, `${measuredHeight}px`);
			}
			scheduleAutoSizeReset();
		});
	});
	const handleInterruptedMutationResize = useStableCallback((currentWidth, currentHeight) => {
		if (!popupElement || !positionerElement) return;
		sizeFrame.cancel();
		mutationFrame.cancel();
		animationAbortControllerRef.current?.abort();
		animationAbortControllerRef.current = null;
		if (currentWidth === 0 || currentHeight === 0) return;
		setSharedFixedSizes(currentWidth, currentHeight);
		mutationFrame.request(() => {
			mutationFrame.request(() => {
				clearFixedSizes();
				const { width, height } = getCssDimensions(popupElement);
				const measuredWidth = width || currentWidth || prevSizeRef.current.width;
				const measuredHeight = height || currentHeight || prevSizeRef.current.height;
				setSharedFixedSizes(currentWidth, currentHeight);
				sizeFrame.request(() => {
					setSharedFixedSizes(measuredWidth, measuredHeight);
					scheduleAutoSizeReset();
				});
			});
		});
	});
	const syncCurrentSize = useStableCallback(() => {
		if (!popupElement || !positionerElement) return;
		sizeFrame.cancel();
		animationAbortControllerRef.current?.abort();
		animationAbortControllerRef.current = null;
		clearFixedSizes();
		const { width, height } = getCssDimensions(popupElement);
		if (width === 0 || height === 0) return;
		prevSizeRef.current = {
			width,
			height
		};
		setAutoSizes();
		positionerElement.style.setProperty(NavigationMenuPositionerCssVars.positionerWidth, `${width}px`);
		positionerElement.style.setProperty(NavigationMenuPositionerCssVars.positionerHeight, `${height}px`);
	});
	const getMutationBaseline = useStableCallback(() => {
		if (!popupElement) return {
			size: prevSizeRef.current,
			syncPositioner: false
		};
		const popupWidth = popupElement.style.getPropertyValue(NavigationMenuPopupCssVars.popupWidth);
		const popupHeight = popupElement.style.getPropertyValue(NavigationMenuPopupCssVars.popupHeight);
		if (!(popupWidth !== "" && popupWidth !== "auto" && popupHeight !== "" && popupHeight !== "auto")) return {
			size: prevSizeRef.current,
			syncPositioner: false
		};
		return {
			size: {
				width: popupElement.offsetWidth || prevSizeRef.current.width,
				height: popupElement.offsetHeight || prevSizeRef.current.height
			},
			syncPositioner: true
		};
	});
	import_react.useEffect(() => {
		if (!open) {
			stickIfOpenTimeout.clear();
			mutationFrame.cancel();
			resizeFrame.cancel();
			sizeFrame.cancel();
			animationAbortControllerRef.current?.abort();
			animationAbortControllerRef.current = null;
			skipAutoSizeSyncRef.current = false;
			setPointerType("");
		}
	}, [
		stickIfOpenTimeout,
		open,
		mutationFrame,
		resizeFrame,
		sizeFrame
	]);
	import_react.useEffect(() => {
		if (!mounted) prevSizeRef.current = DEFAULT_SIZE;
	}, [mounted]);
	import_react.useEffect(() => {
		if (!popupElement || typeof ResizeObserver !== "function") return;
		const resizeObserver = new ResizeObserver(() => {
			prevSizeRef.current = {
				width: popupElement.offsetWidth,
				height: popupElement.offsetHeight
			};
		});
		resizeObserver.observe(popupElement);
		return () => {
			resizeObserver.disconnect();
		};
	}, [popupElement]);
	import_react.useEffect(() => {
		if (!open || !isActiveItem || !popupElement || !positionerElement) return;
		const win = getWindow(positionerElement);
		function handleResize() {
			resizeFrame.cancel();
			resizeFrame.request(syncCurrentSize);
		}
		win.addEventListener("resize", handleResize);
		return () => {
			resizeFrame.cancel();
			win.removeEventListener("resize", handleResize);
		};
	}, [
		open,
		isActiveItem,
		popupElement,
		positionerElement,
		resizeFrame,
		syncCurrentSize
	]);
	import_react.useEffect(() => {
		const observedElement = currentContentRef.current;
		if (!observedElement || !popupElement || !isActiveItem || typeof MutationObserver !== "function") return;
		const mutationObserver = new MutationObserver(() => {
			if (transitionStatus === "starting" || popupElement.hasAttribute(TransitionStatusDataAttributes.startingStyle)) {
				syncCurrentSize();
				return;
			}
			const { size, syncPositioner } = getMutationBaseline();
			if (syncPositioner) {
				handleInterruptedMutationResize(size.width, size.height);
				return;
			}
			handleValueChange(size.width, size.height);
		});
		mutationObserver.observe(observedElement, {
			childList: true,
			subtree: true,
			characterData: true
		});
		return () => {
			mutationObserver.disconnect();
		};
	}, [
		currentContentRef,
		popupElement,
		isActiveItem,
		transitionStatus,
		getMutationBaseline,
		handleInterruptedMutationResize,
		handleValueChange,
		syncCurrentSize
	]);
	import_react.useEffect(() => {
		if (isActiveItem && open && popupElement && allowFocusRef.current) {
			allowFocusRef.current = false;
			focusFrame.request(() => {
				beforeOutsideRef.current?.focus();
			});
		}
		return () => {
			focusFrame.cancel();
		};
	}, [
		beforeOutsideRef,
		focusFrame,
		isActiveItem,
		open,
		popupElement
	]);
	useIsoLayoutEffect(() => {
		if (isActiveItemRef.current && open && popupElement) {
			if (transitionStatus === "starting") {
				if (currentContentRef.current?.querySelector("[data-nested]") != null) {
					sizeFrame.request(syncCurrentSize);
					return () => {
						sizeFrame.cancel();
					};
				}
			}
			if (skipAutoSizeSyncRef.current) {
				skipAutoSizeSyncRef.current = false;
				return;
			}
			handleValueChange(0, 0);
		}
	}, [
		currentContentRef,
		handleValueChange,
		isActiveItemRef,
		open,
		popupElement,
		sizeFrame,
		syncCurrentSize,
		transitionStatus
	]);
	function handleOpenChange(nextOpen, eventDetails) {
		const isHover = eventDetails.reason === triggerHover;
		if (!interactionsEnabled) return;
		if (pointerType === "touch" && isHover) return;
		if (!nextOpen && value !== itemValue) return;
		function changeState() {
			if (isHover) {
				setStickIfOpen(true);
				stickIfOpenTimeout.clear();
				stickIfOpenTimeout.start(500, () => {
					setStickIfOpen(false);
				});
			}
			if (nextOpen) setValue(itemValue, eventDetails);
			else {
				setValue(null, eventDetails);
				setPointerType("");
			}
		}
		if (isHover) import_react_dom.flushSync(changeState);
		else changeState();
	}
	const context = useFloatingRootContext({
		open,
		onOpenChange: handleOpenChange,
		elements: {
			reference: triggerElement,
			floating: hoverFloatingElement
		}
	});
	const hoverInteractionState = useHoverInteractionSharedState(context);
	import_react.useEffect(() => {
		if (!open) {
			context.context.dataRef.current.openEvent = void 0;
			hoverInteractionState.pointerType = void 0;
			hoverInteractionState.interactedInside = false;
			hoverInteractionState.restTimeoutPending = false;
			hoverInteractionState.openChangeTimeout.clear();
			hoverInteractionState.restTimeout.clear();
			clearSafePolygonPointerEventsMutation(hoverInteractionState);
		}
	}, [
		context,
		hoverInteractionState,
		open
	]);
	const getInlineHandleCloseContext = useStableCallback(() => {
		if (!nested || positionerElement || !triggerElementRef.current || !hoverFloatingElement) return null;
		return getHandleCloseContext(triggerElementRef.current, hoverFloatingElement, nodeId);
	});
	function getScope() {
		return triggerElementRef.current?.closest("ul") ?? null;
	}
	const hoverProps = useHoverReferenceInteraction(context, {
		enabled: hoverInteractionsEnabled,
		move: false,
		handleClose: safePolygon({
			blockPointerEvents: pointerType !== "touch" && (!isWebKit || nested),
			getScope
		}),
		restMs: mounted && positionerElement ? 0 : delay,
		delay: { close: closeDelay },
		triggerElementRef,
		getHandleCloseContext: getInlineHandleCloseContext
	});
	const { getReferenceProps } = useInteractions([import_react.useMemo(() => hoverProps ? { reference: hoverProps } : void 0, [hoverProps]), useClick(context, {
		enabled: interactionsEnabled,
		stickIfOpen,
		toggle: isActiveItem
	})]);
	useIsoLayoutEffect(() => {
		if (isActiveItem) {
			setFloatingRootContext(context);
			prevTriggerElementRef.current = triggerElement;
		}
	}, [
		isActiveItem,
		context,
		setFloatingRootContext,
		prevTriggerElementRef,
		triggerElement
	]);
	function handleActivation(event) {
		import_react_dom.flushSync(() => {
			const prevTriggerRect = prevTriggerElementRef.current?.getBoundingClientRect();
			if (mounted && prevTriggerRect && triggerElement) {
				const nextTriggerRect = triggerElement.getBoundingClientRect();
				const isMovingRight = nextTriggerRect.left > prevTriggerRect.left;
				const isMovingDown = nextTriggerRect.top > prevTriggerRect.top;
				if (orientation === "horizontal" && nextTriggerRect.left !== prevTriggerRect.left) setActivationDirection(isMovingRight ? "right" : "left");
				else if (orientation === "vertical" && nextTriggerRect.top !== prevTriggerRect.top) setActivationDirection(isMovingDown ? "down" : "up");
			}
			if (event.type !== "click" && value != null) context.context.dataRef.current.openEvent = void 0;
			if (pointerType === "touch" && event.type !== "click") return;
			if (value != null) setValue(itemValue, createChangeEventDetails(event.type === "mouseenter" ? triggerHover : triggerPress, event.nativeEvent));
			if (event.type === "mouseenter" && nested && !positionerElement && pointerType !== "touch" && hoverFloatingElement && isHTMLElement(event.currentTarget)) {
				const scopeElement = getScope();
				if (!scopeElement) return;
				applySafePolygonPointerEventsMutation(hoverInteractionState, {
					scopeElement,
					referenceElement: event.currentTarget,
					floatingElement: hoverFloatingElement
				});
			}
		});
	}
	const handleOpenEvent = useStableCallback((event) => {
		if (!popupElement || !positionerElement) {
			handleActivation(event);
			return;
		}
		const { width, height } = getCssDimensions(popupElement);
		const shouldSkipAutoSizeSync = value != null && value !== itemValue && (event.type === "click" || pointerType !== "touch");
		handleActivation(event);
		if (shouldSkipAutoSizeSync) skipAutoSizeSyncRef.current = true;
		handleValueChange(width, height);
	});
	const state = { open: isActiveItem };
	function handleSetPointerType(event) {
		setPointerType(event.pointerType);
	}
	const defaultProps = {
		tabIndex: 0,
		onMouseEnter: handleOpenEvent,
		onClick: handleOpenEvent,
		onPointerEnter: handleSetPointerType,
		onPointerDown: handleSetPointerType,
		"aria-expanded": isActiveItem,
		"aria-controls": isActiveItem ? popupElement?.id : void 0,
		[NAVIGATION_MENU_TRIGGER_IDENTIFIER]: "",
		onFocus() {
			if (!isActiveItem) return;
			setViewportInert(false);
		},
		onMouseMove() {
			allowFocusRef.current = false;
		},
		onKeyDown(event) {
			allowFocusRef.current = true;
			if (nested) return;
			const openHorizontal = orientation === "horizontal" && event.key === "ArrowDown";
			const openVertical = orientation === "vertical" && event.key === "ArrowRight";
			if (openHorizontal || openVertical) {
				setValue(itemValue, createChangeEventDetails(listNavigation, event.nativeEvent));
				handleOpenEvent(event);
				stopEvent(event);
			}
		},
		onBlur(event) {
			if (positionerElement && popupElement && isOutsideMenuEvent({
				currentTarget: event.currentTarget,
				relatedTarget: event.relatedTarget
			}, {
				popupElement,
				rootRef,
				tree,
				nodeId
			})) setValue(null, createChangeEventDetails(focusOut, event.nativeEvent));
		}
	};
	const { getButtonProps, buttonRef } = useButton({
		disabled,
		focusableWhenDisabled: true,
		native: nativeButton
	});
	const referenceElement = hoverFloatingElement;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)(CompositeItem, {
		tag: "button",
		render,
		className,
		state,
		stateAttributesMapping: pressableTriggerOpenStateMapping,
		refs: [
			forwardedRef,
			handleTriggerElement,
			buttonRef
		],
		props: [
			getReferenceProps,
			dismissProps?.reference || EMPTY_ARRAY,
			defaultProps,
			elementProps,
			getButtonProps
		]
	}), isActiveItem && /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(FocusGuard, {
			ref: beforeOutsideRef,
			onFocus: (event) => {
				if (referenceElement && isOutsideEvent(event, referenceElement)) beforeInsideRef.current?.focus();
				else getPreviousTabbable(triggerElement)?.focus();
			}
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
			"aria-owns": viewportElement?.id,
			style: ownerVisuallyHidden
		}),
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(FocusGuard, {
			ref: afterOutsideRef,
			onFocus: (event) => {
				if (referenceElement && isOutsideEvent(event, referenceElement)) (afterInsideRef.current && isTabbable(afterInsideRef.current) ? afterInsideRef.current : triggerElement)?.focus();
				else {
					let nextTabbable = getNextTabbable(triggerElement);
					if (nested && !positionerElement && referenceElement && nextTabbable && contains(referenceElement, nextTabbable)) nextTabbable = getTabbableAfterElement(afterInsideRef.current);
					nextTabbable?.focus();
					if ((!nested || positionerElement) && !contains(rootRef.current, nextTabbable)) setValue(null, createChangeEventDetails("focus-out", event.nativeEvent));
				}
			}
		})
	] })] });
});
NavigationMenuTrigger.displayName = "NavigationMenuTrigger";
function getPlacementFromElements(domReferenceElement, floatingElement) {
	const referenceRect = domReferenceElement.getBoundingClientRect();
	const floatingRect = floatingElement.getBoundingClientRect();
	const referenceCenterX = referenceRect.left + referenceRect.width / 2;
	const referenceCenterY = referenceRect.top + referenceRect.height / 2;
	const floatingCenterX = floatingRect.left + floatingRect.width / 2;
	const floatingCenterY = floatingRect.top + floatingRect.height / 2;
	const deltaX = floatingCenterX - referenceCenterX;
	const deltaY = floatingCenterY - referenceCenterY;
	if (Math.abs(deltaX) >= Math.abs(deltaY)) return deltaX >= 0 ? "right" : "left";
	return deltaY >= 0 ? "bottom" : "top";
}
function getHandleCloseContext(domReferenceElement, floatingElement, nodeId) {
	return {
		placement: getPlacementFromElements(domReferenceElement, floatingElement),
		elements: {
			domReference: domReferenceElement,
			floating: floatingElement
		},
		nodeId
	};
}
//#endregion
//#region node_modules/@base-ui/react/esm/navigation-menu/portal/NavigationMenuPortalContext.js
var NavigationMenuPortalContext = /* @__PURE__ */ import_react.createContext(void 0);
NavigationMenuPortalContext.displayName = "NavigationMenuPortalContext";
function useNavigationMenuPortalContext() {
	const value = import_react.useContext(NavigationMenuPortalContext);
	if (value === void 0) throw new Error("Base UI: <NavigationMenu.Portal> is missing.");
	return value;
}
//#endregion
//#region node_modules/@base-ui/react/esm/navigation-menu/portal/NavigationMenuPortal.js
/**
* A portal element that moves the popup to a different part of the DOM.
* By default, the portal element is appended to `<body>`.
* Renders a `<div>` element.
*
* Documentation: [Base UI Navigation Menu](https://base-ui.com/react/components/navigation-menu)
*/
var NavigationMenuPortal = /* @__PURE__ */ import_react.forwardRef(function NavigationMenuPortal(props, forwardedRef) {
	const { keepMounted = false, ...portalProps } = props;
	const { mounted } = useNavigationMenuRootContext();
	if (!(mounted || keepMounted)) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NavigationMenuPortalContext.Provider, {
		value: keepMounted,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FloatingPortal, {
			ref: forwardedRef,
			...portalProps
		})
	});
});
NavigationMenuPortal.displayName = "NavigationMenuPortal";
//#endregion
//#region node_modules/@base-ui/react/esm/navigation-menu/positioner/NavigationMenuPositionerContext.js
var NavigationMenuPositionerContext = /* @__PURE__ */ import_react.createContext(void 0);
NavigationMenuPositionerContext.displayName = "NavigationMenuPositionerContext";
function useNavigationMenuPositionerContext(optional = false) {
	const context = import_react.useContext(NavigationMenuPositionerContext);
	if (!context && !optional) throw new Error("Base UI: NavigationMenuPositionerContext is missing. NavigationMenuPositioner parts must be placed within <NavigationMenu.Positioner>.");
	return context;
}
//#endregion
//#region node_modules/@base-ui/react/esm/navigation-menu/positioner/NavigationMenuPositioner.js
var EMPTY_ROOT_CONTEXT$1 = getEmptyRootContext();
/**
* Positions the navigation menu against the currently active trigger.
* Renders a `<div>` element.
*
* Documentation: [Base UI Navigation Menu](https://base-ui.com/react/components/navigation-menu)
*/
var NavigationMenuPositioner = /* @__PURE__ */ import_react.forwardRef(function NavigationMenuPositioner(componentProps, forwardedRef) {
	const { open, mounted, positionerElement, setPositionerElement, floatingRootContext, nested, transitionStatus } = useNavigationMenuRootContext();
	const { className, render, anchor, positionMethod = "absolute", side = "bottom", align = "center", sideOffset = 0, alignOffset = 0, collisionBoundary = "clipping-ancestors", collisionPadding = 5, collisionAvoidance = nested ? POPUP_COLLISION_AVOIDANCE : DROPDOWN_COLLISION_AVOIDANCE, arrowPadding = 5, sticky = false, disableAnchorTracking = false, ...elementProps } = componentProps;
	const keepMounted = useNavigationMenuPortalContext();
	const nodeId = useNavigationMenuTreeContext();
	const resizeTimeout = useTimeout();
	const [instant, setInstant] = import_react.useState(false);
	const positionerRef = import_react.useRef(null);
	const prevTriggerElementRef = import_react.useRef(null);
	import_react.useEffect(() => {
		if (!positionerElement) return;
		function onFocus(event) {
			if (positionerElement && isOutsideEvent(event)) (event.type === "focusin" ? enableFocusInside : disableFocusInside)(positionerElement);
		}
		positionerElement.addEventListener("focusin", onFocus, true);
		positionerElement.addEventListener("focusout", onFocus, true);
		return () => {
			positionerElement.removeEventListener("focusin", onFocus, true);
			positionerElement.removeEventListener("focusout", onFocus, true);
		};
	}, [positionerElement]);
	const domReference = (floatingRootContext || EMPTY_ROOT_CONTEXT$1).useState("domReferenceElement");
	const positioning = useAnchorPositioning({
		anchor: anchor ?? domReference ?? prevTriggerElementRef,
		positionMethod,
		mounted,
		side,
		sideOffset,
		align,
		alignOffset,
		arrowPadding,
		collisionBoundary,
		collisionPadding,
		sticky,
		disableAnchorTracking,
		keepMounted,
		floatingRootContext,
		collisionAvoidance,
		nodeId,
		adaptiveOrigin
	});
	const defaultProps = import_react.useMemo(() => {
		const hiddenStyles = {};
		if (!open) hiddenStyles.pointerEvents = "none";
		return {
			role: "presentation",
			hidden: !mounted,
			style: {
				...positioning.positionerStyles,
				...hiddenStyles
			}
		};
	}, [
		open,
		mounted,
		positioning.positionerStyles
	]);
	const state = {
		open,
		side: positioning.side,
		align: positioning.align,
		anchorHidden: positioning.anchorHidden,
		instant
	};
	import_react.useEffect(() => {
		if (!open) return;
		function handleResize() {
			import_react_dom.flushSync(() => {
				setInstant(true);
			});
			resizeTimeout.start(100, () => {
				setInstant(false);
			});
		}
		const win = getWindow(positionerElement);
		win.addEventListener("resize", handleResize);
		return () => {
			win.removeEventListener("resize", handleResize);
		};
	}, [
		open,
		resizeTimeout,
		positionerElement
	]);
	const element = useRenderElement("div", componentProps, {
		state,
		ref: [
			forwardedRef,
			setPositionerElement,
			positionerRef
		],
		props: [
			defaultProps,
			getDisabledMountTransitionStyles(transitionStatus),
			elementProps
		],
		stateAttributesMapping: popupStateMapping
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NavigationMenuPositionerContext.Provider, {
		value: positioning,
		children: element
	});
});
NavigationMenuPositioner.displayName = "NavigationMenuPositioner";
//#endregion
//#region node_modules/@base-ui/react/esm/navigation-menu/viewport/NavigationMenuViewport.js
var EMPTY_ROOT_CONTEXT = getEmptyRootContext();
function Guards({ children }) {
	const { beforeInsideRef, beforeOutsideRef, afterInsideRef, afterOutsideRef, positionerElement, viewportElement, floatingRootContext } = useNavigationMenuRootContext();
	const hasPositioner = Boolean(useNavigationMenuPositionerContext(true));
	const referenceElement = positionerElement || viewportElement;
	if (!floatingRootContext && !hasPositioner) return children;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(FocusGuard, {
			ref: beforeInsideRef,
			onFocus: (event) => {
				if (referenceElement && isOutsideEvent(event, referenceElement)) getNextTabbable(referenceElement)?.focus();
				else beforeOutsideRef.current?.focus();
			}
		}),
		children,
		/* @__PURE__ */ (0, import_jsx_runtime.jsx)(FocusGuard, {
			ref: afterInsideRef,
			onFocus: (event) => {
				if (referenceElement && isOutsideEvent(event, referenceElement)) getPreviousTabbable(referenceElement)?.focus();
				else afterOutsideRef.current?.focus();
			}
		})
	] });
}
/**
* The clipping viewport of the navigation menu's current content.
* Renders a `<div>` element.
*
* Documentation: [Base UI Navigation Menu](https://base-ui.com/react/components/navigation-menu)
*/
var NavigationMenuViewport = /* @__PURE__ */ import_react.forwardRef(function NavigationMenuViewport(componentProps, forwardedRef) {
	const { className, render, children, id: idProp, ...elementProps } = componentProps;
	const id = useId(idProp);
	const { setViewportElement, setViewportTargetElement, floatingRootContext, prevTriggerElementRef, viewportInert, setViewportInert } = useNavigationMenuRootContext();
	const positioning = useNavigationMenuPositionerContext(true);
	const hasPositioner = Boolean(positioning);
	const domReference = (floatingRootContext || EMPTY_ROOT_CONTEXT).useState("domReferenceElement");
	useIsoLayoutEffect(() => {
		if (domReference) prevTriggerElementRef.current = domReference;
	}, [domReference, prevTriggerElementRef]);
	const element = useRenderElement("div", componentProps, {
		ref: [forwardedRef, setViewportElement],
		props: [{
			id,
			onBlur(event) {
				const relatedTarget = event.relatedTarget;
				const currentTarget = event.currentTarget;
				if (relatedTarget && !contains(currentTarget, relatedTarget) && relatedTarget !== domReference) setViewportInert(true);
			},
			...!hasPositioner && viewportInert && { inert: inertValue(true) },
			children: hasPositioner ? children : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Guards, { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
				ref: setViewportTargetElement,
				children
			}) })
		}, elementProps]
	});
	return hasPositioner ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Guards, { children: element }) : element;
});
NavigationMenuViewport.displayName = "NavigationMenuViewport";
//#endregion
//#region node_modules/@base-ui/react/esm/navigation-menu/backdrop/NavigationMenuBackdrop.js
var stateAttributesMapping$11 = {
	...popupStateMapping,
	...transitionStatusMapping
};
/**
* A backdrop for the navigation menu popup.
* Renders a `<div>` element.
*
* Documentation: [Base UI Navigation Menu](https://base-ui.com/react/components/navigation-menu)
*/
var NavigationMenuBackdrop = /* @__PURE__ */ import_react.forwardRef(function NavigationMenuBackdrop(componentProps, forwardedRef) {
	const { className, render, ...elementProps } = componentProps;
	const { open, mounted, transitionStatus } = useNavigationMenuRootContext();
	return useRenderElement("div", componentProps, {
		state: {
			open,
			transitionStatus
		},
		ref: forwardedRef,
		props: [{
			role: "presentation",
			hidden: !mounted,
			style: {
				userSelect: "none",
				WebkitUserSelect: "none"
			}
		}, elementProps],
		stateAttributesMapping: stateAttributesMapping$11
	});
});
NavigationMenuBackdrop.displayName = "NavigationMenuBackdrop";
//#endregion
//#region node_modules/@base-ui/react/esm/navigation-menu/popup/NavigationMenuPopup.js
var stateAttributesMapping$10 = {
	...popupStateMapping,
	...transitionStatusMapping
};
/**
* A container for the navigation menu contents.
* Renders a `<nav>` element.
*
* Documentation: [Base UI Navigation Menu](https://base-ui.com/react/components/navigation-menu)
*/
var NavigationMenuPopup = /* @__PURE__ */ import_react.forwardRef(function NavigationMenuPopup(componentProps, forwardedRef) {
	const { className, render, id: idProp, ...elementProps } = componentProps;
	const { open, transitionStatus, setPopupElement } = useNavigationMenuRootContext();
	const positioning = useNavigationMenuPositionerContext();
	const direction = useDirection();
	const id = useBaseUiId(idProp);
	const state = {
		open,
		transitionStatus,
		side: positioning.side,
		align: positioning.align,
		anchorHidden: positioning.anchorHidden
	};
	let isOriginSide = positioning.side === "top";
	let isPhysicalLeft = positioning.side === "left";
	if (direction === "rtl") {
		isOriginSide = isOriginSide || positioning.side === "inline-end";
		isPhysicalLeft = isPhysicalLeft || positioning.side === "inline-end";
	} else {
		isOriginSide = isOriginSide || positioning.side === "inline-start";
		isPhysicalLeft = isPhysicalLeft || positioning.side === "inline-start";
	}
	return useRenderElement("nav", componentProps, {
		state,
		ref: [forwardedRef, setPopupElement],
		props: [{
			id,
			tabIndex: -1,
			style: isOriginSide ? {
				position: "absolute",
				[positioning.side === "top" ? "bottom" : "top"]: "0",
				[isPhysicalLeft ? "right" : "left"]: "0"
			} : {}
		}, elementProps],
		stateAttributesMapping: stateAttributesMapping$10
	});
});
NavigationMenuPopup.displayName = "NavigationMenuPopup";
//#endregion
//#region node_modules/@base-ui/react/esm/navigation-menu/arrow/NavigationMenuArrow.js
/**
* Displays an element pointing toward the navigation menu's current anchor.
* Renders a `<div>` element.
*
* Documentation: [Base UI Navigation Menu](https://base-ui.com/react/components/navigation-menu)
*/
var NavigationMenuArrow = /* @__PURE__ */ import_react.forwardRef(function NavigationMenuArrow(componentProps, forwardedRef) {
	const { className, render, ...elementProps } = componentProps;
	const { open } = useNavigationMenuRootContext();
	const { arrowRef, side, align, arrowUncentered, arrowStyles } = useNavigationMenuPositionerContext();
	return useRenderElement("div", componentProps, {
		state: {
			open,
			side,
			align,
			uncentered: arrowUncentered
		},
		ref: [forwardedRef, arrowRef],
		props: [{
			style: arrowStyles,
			"aria-hidden": true
		}, elementProps],
		stateAttributesMapping: popupStateMapping
	});
});
NavigationMenuArrow.displayName = "NavigationMenuArrow";
//#endregion
//#region node_modules/@base-ui/react/esm/navigation-menu/link/NavigationMenuLink.js
/**
* A link in the navigation menu that can be used to navigate to a different page or section.
* Renders an `<a>` element.
*
* Documentation: [Base UI Navigation Menu](https://base-ui.com/react/components/navigation-menu)
*/
var NavigationMenuLink = /* @__PURE__ */ import_react.forwardRef(function NavigationMenuLink(componentProps, forwardedRef) {
	const { className, render, active = false, closeOnClick = false, ...elementProps } = componentProps;
	const { setValue, popupElement, positionerElement, rootRef } = useNavigationMenuRootContext();
	const nodeId = useNavigationMenuTreeContext();
	const tree = useFloatingTree();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CompositeItem, {
		tag: "a",
		render,
		className,
		state: { active },
		refs: [forwardedRef],
		props: [{
			"aria-current": active ? "page" : void 0,
			tabIndex: void 0,
			onClick(event) {
				if (closeOnClick) setValue(null, createChangeEventDetails(linkPress, event.nativeEvent));
			},
			onBlur(event) {
				if (positionerElement && popupElement && isOutsideMenuEvent({
					currentTarget: event.currentTarget,
					relatedTarget: event.relatedTarget
				}, {
					popupElement,
					rootRef,
					tree,
					nodeId
				})) setValue(null, createChangeEventDetails(focusOut, event.nativeEvent));
			}
		}, elementProps]
	});
});
NavigationMenuLink.displayName = "NavigationMenuLink";
//#endregion
//#region node_modules/@base-ui/react/esm/navigation-menu/icon/NavigationMenuIcon.js
/**
* An icon that indicates that the trigger button opens a menu.
*
* Documentation: [Base UI Navigation Menu](https://base-ui.com/react/components/navigation-menu)
*/
var NavigationMenuIcon = /* @__PURE__ */ import_react.forwardRef(function NavigationMenuIcon(componentProps, forwardedRef) {
	const { className, render, ...elementProps } = componentProps;
	const { value: itemValue } = useNavigationMenuItemContext();
	const { open, value } = useNavigationMenuRootContext();
	return useRenderElement("span", componentProps, {
		state: { open: open && value === itemValue },
		ref: forwardedRef,
		props: [{
			"aria-hidden": true,
			children: "▼"
		}, elementProps],
		stateAttributesMapping: triggerOpenStateMapping$1
	});
});
NavigationMenuIcon.displayName = "NavigationMenuIcon";
//#endregion
//#region node_modules/@base-ui/react/esm/navigation-menu/index.parts.js
var index_parts_exports$14 = /* @__PURE__ */ __exportAll({
	Arrow: () => NavigationMenuArrow,
	Backdrop: () => NavigationMenuBackdrop,
	Content: () => NavigationMenuContent,
	Icon: () => NavigationMenuIcon,
	Item: () => NavigationMenuItem,
	Link: () => NavigationMenuLink,
	List: () => NavigationMenuList,
	Popup: () => NavigationMenuPopup,
	Portal: () => NavigationMenuPortal,
	Positioner: () => NavigationMenuPositioner,
	Root: () => NavigationMenuRoot,
	Trigger: () => NavigationMenuTrigger,
	Viewport: () => NavigationMenuViewport
});
//#endregion
//#region node_modules/@base-ui/utils/esm/useInterval.js
var EMPTY = 0;
var Interval = class Interval extends Timeout {
	static create() {
		return new Interval();
	}
	/**
	* Executes `fn` at `delay` interval, clearing any previously scheduled call.
	*/
	start(delay, fn) {
		this.clear();
		this.currentId = setInterval(() => {
			fn();
		}, delay);
	}
	clear = () => {
		if (this.currentId !== EMPTY) {
			clearInterval(this.currentId);
			this.currentId = EMPTY;
		}
	};
};
/**
* A `setInterval` with automatic cleanup and guard.
*/
function useInterval() {
	const timeout = useRefWithInit(Interval.create).current;
	useOnMount(timeout.disposeEffect);
	return timeout;
}
//#endregion
//#region node_modules/@base-ui/react/esm/number-field/root/NumberFieldRootContext.js
var NumberFieldRootContext = /* @__PURE__ */ import_react.createContext(void 0);
NumberFieldRootContext.displayName = "NumberFieldRootContext";
function useNumberFieldRootContext() {
	const context = import_react.useContext(NumberFieldRootContext);
	if (context === void 0) throw new Error("Base UI: NumberFieldRootContext is missing. NumberField parts must be placed within <NumberField.Root>.");
	return context;
}
//#endregion
//#region node_modules/@base-ui/react/esm/number-field/utils/stateAttributesMapping.js
var stateAttributesMapping$9 = {
	inputValue: () => null,
	value: () => null,
	...fieldValidityMapping
};
//#endregion
//#region node_modules/@base-ui/react/esm/number-field/utils/parse.js
var HAN_NUMERALS = [
	"零",
	"〇",
	"一",
	"二",
	"三",
	"四",
	"五",
	"六",
	"七",
	"八",
	"九"
];
var HAN_NUMERAL_TO_DIGIT = {
	零: "0",
	〇: "0",
	一: "1",
	二: "2",
	三: "3",
	四: "4",
	五: "5",
	六: "6",
	七: "7",
	八: "8",
	九: "9"
};
var ARABIC_NUMERALS = [
	"٠",
	"١",
	"٢",
	"٣",
	"٤",
	"٥",
	"٦",
	"٧",
	"٨",
	"٩"
];
var PERSIAN_NUMERALS = [
	"۰",
	"۱",
	"۲",
	"۳",
	"۴",
	"۵",
	"۶",
	"۷",
	"۸",
	"۹"
];
var FULLWIDTH_NUMERALS = [
	"０",
	"１",
	"２",
	"３",
	"４",
	"５",
	"６",
	"７",
	"８",
	"９"
];
var PERCENTAGES = [
	"%",
	"٪",
	"％",
	"﹪"
];
var PERMILLE = ["‰", "؉"];
var UNICODE_MINUS_SIGNS = [
	"−",
	"－",
	"‒",
	"–",
	"—",
	"﹣"
];
var UNICODE_PLUS_SIGNS = ["＋", "﹢"];
var ARABIC_RE = new RegExp(`[${ARABIC_NUMERALS.join("")}]`, "g");
var PERSIAN_RE = new RegExp(`[${PERSIAN_NUMERALS.join("")}]`, "g");
var FULLWIDTH_RE = new RegExp(`[${FULLWIDTH_NUMERALS.join("")}]`, "g");
var HAN_RE = new RegExp(`[${HAN_NUMERALS.join("")}]`, "g");
var PERCENT_RE = new RegExp(`[${PERCENTAGES.join("")}]`);
var PERMILLE_RE = new RegExp(`[${PERMILLE.join("")}]`);
var ARABIC_DETECT_RE = /[٠١٢٣٤٥٦٧٨٩]/;
var PERSIAN_DETECT_RE = /[۰۱۲۳۴۵۶۷۸۹]/;
var HAN_DETECT_RE = /[零〇一二三四五六七八九]/;
var FULLWIDTH_DETECT_RE = new RegExp(`[${FULLWIDTH_NUMERALS.join("")}]`);
var BASE_NON_NUMERIC_SYMBOLS = [
	".",
	",",
	"．",
	"，",
	"٫",
	"٬"
];
var SPACE_SEPARATOR_RE = /\p{Zs}/u;
var PLUS_SIGNS_WITH_ASCII = ["+", ...UNICODE_PLUS_SIGNS];
var MINUS_SIGNS_WITH_ASCII = ["-", ...UNICODE_MINUS_SIGNS];
var escapeRegExp = (s) => s.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
var escapeClassChar = (s) => s.replace(/[-\\\]^]/g, (m) => `\\${m}`);
var charClassFrom = (chars) => `[${chars.map(escapeClassChar).join("")}]`;
var ANY_MINUS_CLASS = charClassFrom(["-"].concat(UNICODE_MINUS_SIGNS));
var ANY_PLUS_CLASS = charClassFrom(["+"].concat(UNICODE_PLUS_SIGNS));
var ANY_MINUS_RE = new RegExp(ANY_MINUS_CLASS, "gu");
var ANY_PLUS_RE = new RegExp(ANY_PLUS_CLASS, "gu");
var ANY_MINUS_DETECT_RE = new RegExp(ANY_MINUS_CLASS);
var ANY_PLUS_DETECT_RE = new RegExp(ANY_PLUS_CLASS);
function getNumberLocaleDetails(locale, options) {
	const parts = getFormatter(locale, options).formatToParts(11111.1);
	const result = {};
	parts.forEach((part) => {
		result[part.type] = part.value;
	});
	getFormatter(locale).formatToParts(.1).forEach((part) => {
		if (part.type === "decimal") result[part.type] = part.value;
	});
	return result;
}
function parseNumber(formattedNumber, locale, options) {
	if (formattedNumber == null) return null;
	let input = String(formattedNumber).replace(/\p{Cf}/gu, "").trim();
	input = input.replace(ANY_MINUS_RE, "-").replace(ANY_PLUS_RE, "+");
	let isNegative = false;
	const trailing = input.match(/([+-])\s*$/);
	if (trailing) {
		if (trailing[1] === "-") isNegative = true;
		input = input.replace(/([+-])\s*$/, "");
	}
	const leading = input.match(/^\s*([+-])/);
	if (leading) {
		if (leading[1] === "-") isNegative = true;
		input = input.replace(/^\s*[+-]/, "");
	}
	let computedLocale = locale;
	if (computedLocale === void 0) {
		if (ARABIC_DETECT_RE.test(input) || PERSIAN_DETECT_RE.test(input)) computedLocale = "ar";
		else if (HAN_DETECT_RE.test(input)) computedLocale = "zh";
	}
	const { group, decimal, currency } = getNumberLocaleDetails(computedLocale, options);
	const unitParts = getFormatter(computedLocale, options).formatToParts(1).filter((p) => p.type === "unit").map((p) => escapeRegExp(p.value));
	const unitRegex = unitParts.length ? new RegExp(unitParts.join("|"), "g") : null;
	let groupRegex = null;
	if (group) {
		const isSpaceGroup = /\p{Zs}/u.test(group);
		const isApostropheGroup = group === "'" || group === "’";
		if (isSpaceGroup) groupRegex = /\p{Zs}/gu;
		else if (isApostropheGroup) groupRegex = /['’]/g;
		else groupRegex = new RegExp(escapeRegExp(group), "g");
	}
	let unformatted = [
		{
			regex: group ? groupRegex : null,
			replacement: ""
		},
		{
			regex: decimal ? new RegExp(escapeRegExp(decimal), "g") : null,
			replacement: "."
		},
		{
			regex: /．/g,
			replacement: "."
		},
		{
			regex: /，/g,
			replacement: ""
		},
		{
			regex: /٫/g,
			replacement: "."
		},
		{
			regex: /٬/g,
			replacement: ""
		},
		{
			regex: currency ? new RegExp(escapeRegExp(currency), "g") : null,
			replacement: ""
		},
		{
			regex: unitRegex,
			replacement: ""
		},
		{
			regex: ARABIC_RE,
			replacement: (ch) => String(ARABIC_NUMERALS.indexOf(ch))
		},
		{
			regex: PERSIAN_RE,
			replacement: (ch) => String(PERSIAN_NUMERALS.indexOf(ch))
		},
		{
			regex: FULLWIDTH_RE,
			replacement: (ch) => String(FULLWIDTH_NUMERALS.indexOf(ch))
		},
		{
			regex: HAN_RE,
			replacement: (ch) => HAN_NUMERAL_TO_DIGIT[ch]
		}
	].reduce((acc, { regex, replacement }) => {
		return regex ? acc.replace(regex, replacement) : acc;
	}, input);
	const lastDot = unformatted.lastIndexOf(".");
	if (lastDot !== -1) unformatted = `${unformatted.slice(0, lastDot).replace(/\./g, "")}.${unformatted.slice(lastDot + 1).replace(/\./g, "")}`;
	if (/^[-+]?Infinity$/i.test(input) || /[∞]/.test(input)) return null;
	const parseTarget = (isNegative ? "-" : "") + unformatted;
	let num = parseFloat(parseTarget);
	const style = options?.style;
	const isUnitPercent = style === "unit" && options?.unit === "percent";
	const hasPercentSymbol = PERCENT_RE.test(formattedNumber) || style === "percent";
	if (PERMILLE_RE.test(formattedNumber)) num /= 1e3;
	else if (!isUnitPercent && hasPercentSymbol) num /= 100;
	if (Number.isNaN(num)) return null;
	return num;
}
//#endregion
//#region node_modules/@base-ui/react/esm/number-field/utils/validate.js
var STEP_EPSILON_FACTOR = 1e-10;
function getFractionDigits(format) {
	const defaultOptions = getFormatter("en-US").resolvedOptions();
	const minimumFractionDigits = format?.minimumFractionDigits ?? defaultOptions.minimumFractionDigits ?? 0;
	return {
		maximumFractionDigits: Math.max(format?.maximumFractionDigits ?? defaultOptions.maximumFractionDigits ?? 20, minimumFractionDigits),
		minimumFractionDigits
	};
}
function roundToFractionDigits(value, maximumFractionDigits) {
	if (!Number.isFinite(value)) return value;
	const digits = Math.min(Math.max(maximumFractionDigits, 0), 20);
	return Number(value.toFixed(digits));
}
function removeFloatingPointErrors(value, format) {
	const { maximumFractionDigits } = getFractionDigits(format);
	return roundToFractionDigits(value, maximumFractionDigits);
}
function snapToStep(clampedValue, base, step, mode = "directional") {
	if (step === 0) return clampedValue;
	const stepSize = Math.abs(step);
	const direction = Math.sign(step);
	const tolerance = stepSize * STEP_EPSILON_FACTOR * direction;
	const divisor = mode === "nearest" ? step : stepSize;
	const rawSteps = (clampedValue - base + tolerance) / divisor;
	let snappedSteps;
	if (mode === "nearest") snappedSteps = Math.round(rawSteps);
	else if (direction > 0) snappedSteps = Math.floor(rawSteps);
	else snappedSteps = Math.ceil(rawSteps);
	return base + snappedSteps * (mode === "nearest" ? step : stepSize);
}
function toValidatedNumber(value, { step, minWithDefault, maxWithDefault, minWithZeroDefault, format, snapOnStep, small, clamp: shouldClamp }) {
	if (value === null) return value;
	const clampedValue = shouldClamp ? clamp(value, minWithDefault, maxWithDefault) : value;
	if (step != null && snapOnStep) {
		if (step === 0) return removeFloatingPointErrors(clampedValue, format);
		let base = minWithZeroDefault;
		if (!small && minWithDefault !== Number.MIN_SAFE_INTEGER) base = minWithDefault;
		return removeFloatingPointErrors(snapToStep(clampedValue, base, step, small ? "nearest" : "directional"), format);
	}
	return removeFloatingPointErrors(clampedValue, format);
}
//#endregion
//#region node_modules/@base-ui/react/esm/number-field/root/NumberFieldRoot.js
/**
* Groups all parts of the number field and manages its state.
* Renders a `<div>` element.
*
* Documentation: [Base UI Number Field](https://base-ui.com/react/components/number-field)
*/
var NumberFieldRoot = /* @__PURE__ */ import_react.forwardRef(function NumberFieldRoot(componentProps, forwardedRef) {
	const { id: idProp, min, max, smallStep = .1, step: stepProp = 1, largeStep = 10, required = false, disabled: disabledProp = false, readOnly = false, name: nameProp, defaultValue, value: valueProp, onValueChange: onValueChangeProp, onValueCommitted: onValueCommittedProp, allowWheelScrub = false, snapOnStep = false, allowOutOfRange = false, format, locale, render, className, inputRef: inputRefProp, ...elementProps } = componentProps;
	const { setDirty, validityData, disabled: fieldDisabled, setFilled, invalid, name: fieldName, state: fieldState, validation, shouldValidateOnChange } = useFieldRootContext();
	const disabled = fieldDisabled || disabledProp;
	const name = fieldName ?? nameProp;
	const step = stepProp === "any" ? 1 : stepProp;
	const [isScrubbing, setIsScrubbing] = import_react.useState(false);
	const minWithDefault = min ?? Number.MIN_SAFE_INTEGER;
	const maxWithDefault = max ?? Number.MAX_SAFE_INTEGER;
	const minWithZeroDefault = min ?? 0;
	const formatStyle = format?.style;
	const inputRef = import_react.useRef(null);
	const hiddenInputRef = useMergedRefs(inputRefProp, validation.inputRef);
	const id = useLabelableId({ id: idProp });
	const [valueUnwrapped, setValueUnwrapped] = useControlled({
		controlled: valueProp,
		default: defaultValue,
		name: "NumberField",
		state: "value"
	});
	const value = valueUnwrapped ?? null;
	const valueRef = useValueAsRef(value);
	useIsoLayoutEffect(() => {
		setFilled(value !== null);
	}, [setFilled, value]);
	const forceRender = useForcedRerendering();
	const formatOptionsRef = useValueAsRef(format);
	const hasPendingCommitRef = import_react.useRef(false);
	const onValueCommitted = useStableCallback((nextValue, eventDetails) => {
		hasPendingCommitRef.current = false;
		onValueCommittedProp?.(nextValue, eventDetails);
	});
	const startTickTimeout = useTimeout();
	const tickInterval = useInterval();
	const intentionalTouchCheckTimeout = useTimeout();
	const isPressedRef = import_react.useRef(false);
	const movesAfterTouchRef = import_react.useRef(0);
	const allowInputSyncRef = import_react.useRef(true);
	const lastChangedValueRef = import_react.useRef(null);
	const unsubscribeFromGlobalContextMenuRef = import_react.useRef(() => {});
	const [inputValue, setInputValue] = import_react.useState(() => {
		if (valueProp !== void 0) return getControlledInputValue(value, locale, format);
		return formatNumber(value, locale, format);
	});
	const [inputMode, setInputMode] = import_react.useState("numeric");
	const getAllowedNonNumericKeys = useStableCallback(() => {
		const { decimal, group, currency, literal } = getNumberLocaleDetails(locale, format);
		const keys = /* @__PURE__ */ new Set();
		BASE_NON_NUMERIC_SYMBOLS.forEach((symbol) => keys.add(symbol));
		if (decimal) keys.add(decimal);
		if (group) {
			keys.add(group);
			if (SPACE_SEPARATOR_RE.test(group)) keys.add(" ");
		}
		const allowPercentSymbols = formatStyle === "percent" || formatStyle === "unit" && format?.unit === "percent";
		const allowPermilleSymbols = formatStyle === "percent" || formatStyle === "unit" && format?.unit === "permille";
		if (allowPercentSymbols) PERCENTAGES.forEach((key) => keys.add(key));
		if (allowPermilleSymbols) PERMILLE.forEach((key) => keys.add(key));
		if (formatStyle === "currency" && currency) keys.add(currency);
		if (literal) {
			Array.from(literal).forEach((char) => keys.add(char));
			if (SPACE_SEPARATOR_RE.test(literal)) keys.add(" ");
		}
		PLUS_SIGNS_WITH_ASCII.forEach((key) => keys.add(key));
		if (minWithDefault < 0) MINUS_SIGNS_WITH_ASCII.forEach((key) => keys.add(key));
		return keys;
	});
	const getStepAmount = useStableCallback((event) => {
		if (event?.altKey) return smallStep;
		if (event?.shiftKey) return largeStep;
		return step;
	});
	const setValue = useStableCallback((unvalidatedValue, details) => {
		const eventWithOptionalKeyState = details.event;
		const dir = details.direction;
		const reason = details.reason;
		const shouldClampValue = !allowOutOfRange || !(reason === "input-change" || reason === "input-blur" || reason === "input-paste" || reason === "input-clear" || reason === "none");
		const validatedValue = toValidatedNumber(unvalidatedValue, {
			step: dir ? getStepAmount(eventWithOptionalKeyState) * dir : void 0,
			format: formatOptionsRef.current,
			minWithDefault,
			maxWithDefault,
			minWithZeroDefault,
			snapOnStep,
			small: eventWithOptionalKeyState?.altKey ?? false,
			clamp: shouldClampValue
		});
		const isInputReason = details.reason === "input-change" || details.reason === "input-clear" || details.reason === "input-blur" || details.reason === "input-paste" || details.reason === "none";
		const shouldFireChange = validatedValue !== value || isInputReason && (unvalidatedValue !== value || allowInputSyncRef.current === false);
		if (shouldFireChange) {
			lastChangedValueRef.current = validatedValue;
			onValueChangeProp?.(validatedValue, details);
			if (details.isCanceled) return shouldFireChange;
			setValueUnwrapped(validatedValue);
			setDirty(validatedValue !== validityData.initialValue);
			hasPendingCommitRef.current = true;
		}
		if (allowInputSyncRef.current) setInputValue(formatNumber(validatedValue, locale, format));
		forceRender();
		return shouldFireChange;
	});
	const incrementValue = useStableCallback((amount, { direction, currentValue, event, reason }) => {
		const prevValue = currentValue == null ? valueRef.current : currentValue;
		return setValue(typeof prevValue === "number" ? prevValue + amount * direction : Math.max(0, min ?? 0), createChangeEventDetails(reason, event, void 0, { direction }));
	});
	const stopAutoChange = useStableCallback(() => {
		intentionalTouchCheckTimeout.clear();
		startTickTimeout.clear();
		tickInterval.clear();
		unsubscribeFromGlobalContextMenuRef.current();
		movesAfterTouchRef.current = 0;
	});
	const startAutoChange = useStableCallback((isIncrement, triggerEvent) => {
		stopAutoChange();
		if (!inputRef.current) return;
		const win = getWindow(inputRef.current);
		function handleContextMenu(event) {
			event.preventDefault();
		}
		win.addEventListener("contextmenu", handleContextMenu);
		unsubscribeFromGlobalContextMenuRef.current = () => {
			win.removeEventListener("contextmenu", handleContextMenu);
		};
		win.addEventListener("pointerup", (event) => {
			isPressedRef.current = false;
			stopAutoChange();
			onValueCommitted(lastChangedValueRef.current ?? valueRef.current, createGenericEventDetails(isIncrement ? incrementPress : decrementPress, event));
		}, { once: true });
		function tick() {
			return incrementValue(getStepAmount(triggerEvent) ?? 1, {
				direction: isIncrement ? 1 : -1,
				event: triggerEvent,
				reason: isIncrement ? "increment-press" : "decrement-press"
			});
		}
		if (!tick()) {
			stopAutoChange();
			return;
		}
		startTickTimeout.start(400, () => {
			tickInterval.start(60, () => {
				if (!tick()) stopAutoChange();
			});
		});
	});
	useIsoLayoutEffect(function syncFormattedInputValueOnValueChange() {
		if (!allowInputSyncRef.current) return;
		const nextInputValue = valueProp !== void 0 ? getControlledInputValue(value, locale, format) : formatNumber(value, locale, format);
		if (nextInputValue !== inputValue) setInputValue(nextInputValue);
	});
	useIsoLayoutEffect(function setDynamicInputModeForIOS() {
		if (!isIOS) return;
		let computedInputMode = "text";
		if (minWithDefault >= 0) computedInputMode = "decimal";
		setInputMode(computedInputMode);
	}, [minWithDefault, formatStyle]);
	import_react.useEffect(() => {
		return () => stopAutoChange();
	}, [stopAutoChange]);
	import_react.useEffect(function registerElementWheelListener() {
		const element = inputRef.current;
		if (disabled || readOnly || !allowWheelScrub || !element) return;
		function handleWheel(event) {
			if (event.ctrlKey || ownerDocument(inputRef.current).activeElement !== inputRef.current) return;
			event.preventDefault();
			incrementValue(getStepAmount(event) ?? 1, {
				direction: event.deltaY > 0 ? -1 : 1,
				event,
				reason: "wheel"
			});
		}
		element.addEventListener("wheel", handleWheel);
		return () => {
			element.removeEventListener("wheel", handleWheel);
		};
	}, [
		allowWheelScrub,
		incrementValue,
		disabled,
		readOnly,
		largeStep,
		step,
		getStepAmount
	]);
	const state = import_react.useMemo(() => ({
		...fieldState,
		disabled,
		readOnly,
		required,
		value,
		inputValue,
		scrubbing: isScrubbing
	}), [
		fieldState,
		disabled,
		readOnly,
		required,
		value,
		inputValue,
		isScrubbing
	]);
	const contextValue = import_react.useMemo(() => ({
		inputRef,
		inputValue,
		value,
		startAutoChange,
		stopAutoChange,
		minWithDefault,
		maxWithDefault,
		disabled,
		readOnly,
		id,
		setValue,
		incrementValue,
		getStepAmount,
		allowInputSyncRef,
		formatOptionsRef,
		valueRef,
		lastChangedValueRef,
		hasPendingCommitRef,
		isPressedRef,
		intentionalTouchCheckTimeout,
		movesAfterTouchRef,
		name,
		required,
		invalid,
		inputMode,
		getAllowedNonNumericKeys,
		min,
		max,
		setInputValue,
		locale,
		isScrubbing,
		setIsScrubbing,
		state,
		onValueCommitted
	}), [
		inputRef,
		inputValue,
		value,
		startAutoChange,
		stopAutoChange,
		minWithDefault,
		maxWithDefault,
		disabled,
		readOnly,
		id,
		setValue,
		incrementValue,
		getStepAmount,
		formatOptionsRef,
		valueRef,
		intentionalTouchCheckTimeout,
		name,
		required,
		invalid,
		inputMode,
		getAllowedNonNumericKeys,
		min,
		max,
		setInputValue,
		locale,
		isScrubbing,
		state,
		onValueCommitted
	]);
	const element = useRenderElement("div", componentProps, {
		ref: forwardedRef,
		state,
		props: elementProps,
		stateAttributesMapping: stateAttributesMapping$9
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(NumberFieldRootContext.Provider, {
		value: contextValue,
		children: [element, /* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", {
			...validation.getInputValidationProps({
				onFocus() {
					inputRef.current?.focus();
				},
				onChange(event) {
					if (event.nativeEvent.defaultPrevented) return;
					const nextValue = event.currentTarget.valueAsNumber;
					const parsedValue = Number.isNaN(nextValue) ? null : nextValue;
					const details = createChangeEventDetails(none, event.nativeEvent);
					setDirty(parsedValue !== validityData.initialValue);
					setValue(parsedValue, details);
					if (shouldValidateOnChange()) validation.commit(parsedValue);
				}
			}),
			ref: hiddenInputRef,
			type: "number",
			name,
			value: value ?? "",
			min,
			max,
			step: stepProp,
			disabled,
			required,
			"aria-hidden": true,
			tabIndex: -1,
			style: name ? visuallyHiddenInput : visuallyHidden
		})]
	});
});
NumberFieldRoot.displayName = "NumberFieldRoot";
function getControlledInputValue(value, locale, format) {
	return format?.maximumFractionDigits != null || format?.minimumFractionDigits != null ? formatNumber(value, locale, format) : formatNumberMaxPrecision(value, locale, format);
}
//#endregion
//#region node_modules/@base-ui/react/esm/number-field/group/NumberFieldGroup.js
/**
* Groups the input with the increment and decrement buttons.
* Renders a `<div>` element.
*
* Documentation: [Base UI Number Field](https://base-ui.com/react/components/number-field)
*/
var NumberFieldGroup = /* @__PURE__ */ import_react.forwardRef(function NumberFieldGroup(componentProps, forwardedRef) {
	const { render, className, ...elementProps } = componentProps;
	const { state } = useNumberFieldRootContext();
	return useRenderElement("div", componentProps, {
		ref: forwardedRef,
		state,
		props: [{ role: "group" }, elementProps],
		stateAttributesMapping: stateAttributesMapping$9
	});
});
NumberFieldGroup.displayName = "NumberFieldGroup";
//#endregion
//#region node_modules/@base-ui/react/esm/number-field/root/useNumberFieldButton.js
function isTouchLikePointerType(pointerType) {
	return pointerType === "touch" || pointerType === "pen";
}
function useNumberFieldButton(params) {
	const { allowInputSyncRef, disabled, formatOptionsRef, getStepAmount, id, incrementValue, inputRef, inputValue, intentionalTouchCheckTimeout, isIncrement, isPressedRef, locale, movesAfterTouchRef, readOnly, setValue, startAutoChange, stopAutoChange, valueRef, lastChangedValueRef, onValueCommitted } = params;
	const incrementDownCoordsRef = import_react.useRef({
		x: 0,
		y: 0
	});
	const isTouchingButtonRef = import_react.useRef(false);
	const ignoreClickRef = import_react.useRef(false);
	const pointerTypeRef = import_react.useRef("");
	const pressReason = isIncrement ? "increment-press" : "decrement-press";
	function commitValue(nativeEvent) {
		allowInputSyncRef.current = true;
		const parsedValue = parseNumber(inputValue, locale, formatOptionsRef.current);
		if (parsedValue !== null) {
			valueRef.current = parsedValue;
			setValue(parsedValue, createChangeEventDetails(pressReason, nativeEvent, void 0, { direction: isIncrement ? 1 : -1 }));
		}
	}
	return {
		disabled,
		"aria-readonly": readOnly || void 0,
		"aria-label": isIncrement ? "Increase" : "Decrease",
		"aria-controls": id,
		tabIndex: -1,
		style: {
			WebkitUserSelect: "none",
			userSelect: "none"
		},
		onTouchStart() {
			isTouchingButtonRef.current = true;
		},
		onTouchEnd() {
			isTouchingButtonRef.current = false;
		},
		onClick(event) {
			const isDisabled = disabled || readOnly;
			if (event.defaultPrevented || isDisabled || (isTouchLikePointerType(pointerTypeRef.current) ? ignoreClickRef.current : event.detail !== 0)) return;
			commitValue(event.nativeEvent);
			const amount = getStepAmount(event) ?? 1;
			const prev = valueRef.current;
			incrementValue(amount, {
				direction: isIncrement ? 1 : -1,
				event: event.nativeEvent,
				reason: pressReason
			});
			const committed = lastChangedValueRef.current ?? valueRef.current;
			if (committed !== prev) onValueCommitted(committed, createGenericEventDetails(pressReason, event.nativeEvent));
		},
		onPointerDown(event) {
			const isMainButton = !event.button || event.button === 0;
			if (event.defaultPrevented || readOnly || !isMainButton || disabled) return;
			pointerTypeRef.current = event.pointerType;
			ignoreClickRef.current = false;
			isPressedRef.current = true;
			incrementDownCoordsRef.current = {
				x: event.clientX,
				y: event.clientY
			};
			commitValue(event.nativeEvent);
			if (!isTouchLikePointerType(event.pointerType)) {
				event.preventDefault();
				inputRef.current?.focus();
				startAutoChange(isIncrement, event);
			} else intentionalTouchCheckTimeout.start(50, () => {
				const moves = movesAfterTouchRef.current;
				movesAfterTouchRef.current = 0;
				if (isPressedRef.current && moves != null && moves < 3) {
					startAutoChange(isIncrement, event);
					ignoreClickRef.current = true;
				} else {
					ignoreClickRef.current = false;
					stopAutoChange();
				}
			});
		},
		onPointerUp(event) {
			if (isTouchLikePointerType(event.pointerType)) isPressedRef.current = false;
		},
		onPointerMove(event) {
			if (disabled || readOnly || !isTouchLikePointerType(event.pointerType) || !isPressedRef.current) return;
			if (movesAfterTouchRef.current != null) movesAfterTouchRef.current += 1;
			const { x, y } = incrementDownCoordsRef.current;
			const dx = x - event.clientX;
			const dy = y - event.clientY;
			if (dx ** 2 + dy ** 2 > 8 ** 2) stopAutoChange();
		},
		onMouseEnter(event) {
			const isDisabled = disabled || readOnly;
			if (event.defaultPrevented || isDisabled || !isPressedRef.current || isTouchingButtonRef.current || isTouchLikePointerType(pointerTypeRef.current)) return;
			startAutoChange(isIncrement, event);
		},
		onMouseLeave() {
			if (isTouchingButtonRef.current) return;
			stopAutoChange();
		},
		onMouseUp() {
			if (isTouchingButtonRef.current) return;
			stopAutoChange();
		}
	};
}
//#endregion
//#region node_modules/@base-ui/react/esm/number-field/increment/NumberFieldIncrement.js
/**
* A stepper button that increases the field value when clicked.
* Renders an `<button>` element.
*
* Documentation: [Base UI Number Field](https://base-ui.com/react/components/number-field)
*/
var NumberFieldIncrement = /* @__PURE__ */ import_react.forwardRef(function NumberFieldIncrement(componentProps, forwardedRef) {
	const { render, className, disabled: disabledProp = false, nativeButton = true, ...elementProps } = componentProps;
	const { allowInputSyncRef, disabled: contextDisabled, formatOptionsRef, getStepAmount, id, incrementValue, inputRef, inputValue, intentionalTouchCheckTimeout, isPressedRef, locale, maxWithDefault, movesAfterTouchRef, readOnly, setValue, startAutoChange, state, stopAutoChange, value, valueRef, lastChangedValueRef, onValueCommitted } = useNumberFieldRootContext();
	const disabled = disabledProp || contextDisabled || value != null && value >= maxWithDefault;
	const props = useNumberFieldButton({
		isIncrement: true,
		inputRef,
		startAutoChange,
		stopAutoChange,
		inputValue,
		disabled,
		readOnly,
		id,
		setValue,
		getStepAmount,
		incrementValue,
		allowInputSyncRef,
		formatOptionsRef,
		valueRef,
		isPressedRef,
		intentionalTouchCheckTimeout,
		movesAfterTouchRef,
		locale,
		lastChangedValueRef,
		onValueCommitted
	});
	const { getButtonProps, buttonRef } = useButton({
		disabled,
		native: nativeButton,
		focusableWhenDisabled: true
	});
	const buttonState = import_react.useMemo(() => ({
		...state,
		disabled
	}), [state, disabled]);
	return useRenderElement("button", componentProps, {
		ref: [forwardedRef, buttonRef],
		state: buttonState,
		props: [
			props,
			elementProps,
			getButtonProps
		],
		stateAttributesMapping: stateAttributesMapping$9
	});
});
NumberFieldIncrement.displayName = "NumberFieldIncrement";
//#endregion
//#region node_modules/@base-ui/react/esm/number-field/decrement/NumberFieldDecrement.js
/**
* A stepper button that decreases the field value when clicked.
* Renders an `<button>` element.
*
* Documentation: [Base UI Number Field](https://base-ui.com/react/components/number-field)
*/
var NumberFieldDecrement = /* @__PURE__ */ import_react.forwardRef(function NumberFieldDecrement(componentProps, forwardedRef) {
	const { render, className, disabled: disabledProp = false, nativeButton = true, ...elementProps } = componentProps;
	const { allowInputSyncRef, disabled: contextDisabled, formatOptionsRef, getStepAmount, id, incrementValue, inputRef, inputValue, intentionalTouchCheckTimeout, isPressedRef, minWithDefault, movesAfterTouchRef, readOnly, setValue, startAutoChange, state, stopAutoChange, value, valueRef, locale, lastChangedValueRef, onValueCommitted } = useNumberFieldRootContext();
	const disabled = disabledProp || contextDisabled || value != null && value <= minWithDefault;
	const props = useNumberFieldButton({
		isIncrement: false,
		inputRef,
		startAutoChange,
		stopAutoChange,
		inputValue,
		disabled,
		readOnly,
		id,
		setValue,
		getStepAmount,
		incrementValue,
		allowInputSyncRef,
		formatOptionsRef,
		valueRef,
		isPressedRef,
		intentionalTouchCheckTimeout,
		movesAfterTouchRef,
		locale,
		lastChangedValueRef,
		onValueCommitted
	});
	const { getButtonProps, buttonRef } = useButton({
		disabled,
		native: nativeButton,
		focusableWhenDisabled: true
	});
	const buttonState = import_react.useMemo(() => ({
		...state,
		disabled
	}), [state, disabled]);
	return useRenderElement("button", componentProps, {
		ref: [forwardedRef, buttonRef],
		state: buttonState,
		props: [
			props,
			elementProps,
			getButtonProps
		],
		stateAttributesMapping: stateAttributesMapping$9
	});
});
NumberFieldDecrement.displayName = "NumberFieldDecrement";
//#endregion
//#region node_modules/@base-ui/react/esm/number-field/input/NumberFieldInput.js
var stateAttributesMapping$8 = {
	...fieldValidityMapping,
	...stateAttributesMapping$9
};
var NAVIGATE_KEYS = new Set([
	"Backspace",
	"Delete",
	"ArrowLeft",
	"ArrowRight",
	"Tab",
	"Enter",
	"Escape"
]);
/**
* The native input control in the number field.
* Renders an `<input>` element.
*
* Documentation: [Base UI Number Field](https://base-ui.com/react/components/number-field)
*/
var NumberFieldInput = /* @__PURE__ */ import_react.forwardRef(function NumberFieldInput(componentProps, forwardedRef) {
	const { render, className, ...elementProps } = componentProps;
	const { allowInputSyncRef, disabled, formatOptionsRef, getAllowedNonNumericKeys, getStepAmount, id, incrementValue, inputMode, inputValue, max, min, name, readOnly, required, setValue, state, setInputValue, locale, inputRef, value, onValueCommitted, lastChangedValueRef, hasPendingCommitRef, valueRef } = useNumberFieldRootContext();
	const { clearErrors } = useFormContext();
	const { validationMode, setTouched, setFocused, invalid, shouldValidateOnChange, validation } = useFieldRootContext();
	const { labelId } = useLabelableContext();
	const hasTouchedInputRef = import_react.useRef(false);
	const blockRevalidationRef = import_react.useRef(false);
	useField({
		id,
		commit: validation.commit,
		value,
		controlRef: inputRef,
		name,
		getValue: () => value ?? null
	});
	useValueChanged(value, (previousValue) => {
		const validateOnChange = shouldValidateOnChange();
		clearErrors(name);
		if (validateOnChange) validation.commit(value);
		if (previousValue === value || validateOnChange) return;
		if (blockRevalidationRef.current) {
			blockRevalidationRef.current = false;
			return;
		}
		validation.commit(value, true);
	});
	return useRenderElement("input", componentProps, {
		ref: [forwardedRef, inputRef],
		state,
		props: [
			{
				id,
				required,
				disabled,
				readOnly,
				inputMode,
				value: inputValue,
				type: "text",
				autoComplete: "off",
				autoCorrect: "off",
				spellCheck: "false",
				"aria-roledescription": "Number field",
				"aria-invalid": invalid || void 0,
				"aria-labelledby": labelId,
				suppressHydrationWarning: true,
				onFocus(event) {
					if (event.defaultPrevented || readOnly || disabled) return;
					setFocused(true);
					if (hasTouchedInputRef.current) return;
					hasTouchedInputRef.current = true;
					const target = event.currentTarget;
					const length = target.value.length;
					target.setSelectionRange(length, length);
				},
				onBlur(event) {
					if (event.defaultPrevented || readOnly || disabled) return;
					setTouched(true);
					setFocused(false);
					const hadManualInput = !allowInputSyncRef.current;
					const hadPendingProgrammaticChange = hasPendingCommitRef.current;
					allowInputSyncRef.current = true;
					if (inputValue.trim() === "") {
						setValue(null, createChangeEventDetails(inputClear, event.nativeEvent));
						if (validationMode === "onBlur") validation.commit(null);
						onValueCommitted(null, createGenericEventDetails(inputClear, event.nativeEvent));
						return;
					}
					const formatOptions = formatOptionsRef.current;
					const parsedValue = parseNumber(inputValue, locale, formatOptions);
					if (parsedValue === null) return;
					const hasExplicitPrecision = formatOptions?.maximumFractionDigits != null || formatOptions?.minimumFractionDigits != null;
					const maxFrac = formatOptions?.maximumFractionDigits;
					const committed = hasExplicitPrecision && typeof maxFrac === "number" ? Number(parsedValue.toFixed(maxFrac)) : parsedValue;
					const nextEventDetails = createGenericEventDetails(inputBlur, event.nativeEvent);
					const shouldUpdateValue = value !== committed;
					const shouldCommit = hadManualInput || shouldUpdateValue || hadPendingProgrammaticChange;
					if (validationMode === "onBlur") validation.commit(committed);
					if (shouldUpdateValue) {
						blockRevalidationRef.current = true;
						setValue(committed, createChangeEventDetails(inputBlur, event.nativeEvent));
					}
					if (shouldCommit) onValueCommitted(committed, nextEventDetails);
					const canonicalText = formatNumber(committed, locale, formatOptions);
					const maxPrecisionText = formatNumberMaxPrecision(parsedValue, locale, formatOptions);
					if (!(!hasExplicitPrecision && parsedValue === value && inputValue === maxPrecisionText) && inputValue !== canonicalText) setInputValue(canonicalText);
				},
				onChange(event) {
					if (event.nativeEvent.defaultPrevented) return;
					allowInputSyncRef.current = false;
					const targetValue = event.target.value;
					if (targetValue.trim() === "") {
						setInputValue(targetValue);
						setValue(null, createChangeEventDetails(inputClear, event.nativeEvent));
						return;
					}
					const allowedNonNumericKeys = getAllowedNonNumericKeys();
					if (!Array.from(targetValue).every((ch) => {
						const isAsciiDigit = ch >= "0" && ch <= "9";
						const isArabicNumeral = ARABIC_DETECT_RE.test(ch);
						const isHanNumeral = HAN_DETECT_RE.test(ch);
						const isPersianNumeral = PERSIAN_DETECT_RE.test(ch);
						const isFullwidthNumeral = FULLWIDTH_DETECT_RE.test(ch);
						const isMinus = ANY_MINUS_DETECT_RE.test(ch);
						return isAsciiDigit || isArabicNumeral || isHanNumeral || isPersianNumeral || isFullwidthNumeral || isMinus || allowedNonNumericKeys.has(ch);
					})) return;
					const parsedValue = parseNumber(targetValue, locale, formatOptionsRef.current);
					setInputValue(targetValue);
					if (parsedValue !== null) setValue(parsedValue, createChangeEventDetails(inputChange, event.nativeEvent));
				},
				onKeyDown(event) {
					if (event.defaultPrevented || readOnly || disabled) return;
					const nativeEvent = event.nativeEvent;
					allowInputSyncRef.current = true;
					const allowedNonNumericKeys = getAllowedNonNumericKeys();
					let isAllowedNonNumericKey = allowedNonNumericKeys.has(event.key);
					const { decimal, currency, percentSign } = getNumberLocaleDetails(locale, formatOptionsRef.current);
					const selectionStart = event.currentTarget.selectionStart;
					const selectionEnd = event.currentTarget.selectionEnd;
					const isAllSelected = selectionStart === 0 && selectionEnd === inputValue.length;
					const selectionContainsIndex = (index) => selectionStart != null && selectionEnd != null && index >= selectionStart && index < selectionEnd;
					if (ANY_MINUS_DETECT_RE.test(event.key) && Array.from(allowedNonNumericKeys).some((k) => ANY_MINUS_DETECT_RE.test(k || ""))) {
						const existingIndex = inputValue.search(ANY_MINUS_RE);
						const isReplacingExisting = existingIndex != null && existingIndex !== -1 && selectionContainsIndex(existingIndex);
						isAllowedNonNumericKey = !(ANY_MINUS_DETECT_RE.test(inputValue) || ANY_PLUS_DETECT_RE.test(inputValue)) || isAllSelected || isReplacingExisting;
					}
					if (ANY_PLUS_DETECT_RE.test(event.key) && Array.from(allowedNonNumericKeys).some((k) => ANY_PLUS_DETECT_RE.test(k || ""))) {
						const existingIndex = inputValue.search(ANY_PLUS_RE);
						const isReplacingExisting = existingIndex != null && existingIndex !== -1 && selectionContainsIndex(existingIndex);
						isAllowedNonNumericKey = !(ANY_MINUS_DETECT_RE.test(inputValue) || ANY_PLUS_DETECT_RE.test(inputValue)) || isAllSelected || isReplacingExisting;
					}
					[
						decimal,
						currency,
						percentSign
					].forEach((symbol) => {
						if (event.key === symbol) {
							const isSymbolHighlighted = selectionContainsIndex(inputValue.indexOf(symbol));
							isAllowedNonNumericKey = !inputValue.includes(symbol) || isAllSelected || isSymbolHighlighted;
						}
					});
					const isAsciiDigit = event.key >= "0" && event.key <= "9";
					const isArabicNumeral = ARABIC_DETECT_RE.test(event.key);
					const isHanNumeral = HAN_DETECT_RE.test(event.key);
					const isFullwidthNumeral = FULLWIDTH_DETECT_RE.test(event.key);
					const isNavigateKey = NAVIGATE_KEYS.has(event.key);
					if (event.which === 229 || event.altKey || event.ctrlKey || event.metaKey || isAllowedNonNumericKey || isAsciiDigit || isArabicNumeral || isFullwidthNumeral || isHanNumeral || isNavigateKey) return;
					const parsedValue = parseNumber(inputValue, locale, formatOptionsRef.current);
					const amount = getStepAmount(event) ?? 1;
					stopEvent(event);
					const commitDetails = createGenericEventDetails(keyboard, nativeEvent);
					if (event.key === "ArrowUp") {
						incrementValue(amount, {
							direction: 1,
							currentValue: parsedValue,
							event: nativeEvent,
							reason: keyboard
						});
						onValueCommitted(lastChangedValueRef.current ?? valueRef.current, commitDetails);
					} else if (event.key === "ArrowDown") {
						incrementValue(amount, {
							direction: -1,
							currentValue: parsedValue,
							event: nativeEvent,
							reason: keyboard
						});
						onValueCommitted(lastChangedValueRef.current ?? valueRef.current, commitDetails);
					} else if (event.key === "Home" && min != null) {
						setValue(min, createChangeEventDetails(keyboard, nativeEvent));
						onValueCommitted(lastChangedValueRef.current ?? valueRef.current, commitDetails);
					} else if (event.key === "End" && max != null) {
						setValue(max, createChangeEventDetails(keyboard, nativeEvent));
						onValueCommitted(lastChangedValueRef.current ?? valueRef.current, commitDetails);
					}
				},
				onPaste(event) {
					if (event.defaultPrevented || readOnly || disabled) return;
					event.preventDefault();
					const pastedData = (event.clipboardData || window.Clipboard).getData("text/plain");
					const parsedValue = parseNumber(pastedData, locale, formatOptionsRef.current);
					if (parsedValue !== null) {
						allowInputSyncRef.current = false;
						setValue(parsedValue, createChangeEventDetails(inputPaste, event.nativeEvent));
						setInputValue(pastedData);
					}
				}
			},
			validation.getValidationProps(),
			elementProps
		],
		stateAttributesMapping: stateAttributesMapping$8
	});
});
NumberFieldInput.displayName = "NumberFieldInput";
//#endregion
//#region node_modules/@base-ui/react/esm/number-field/scrub-area/NumberFieldScrubAreaContext.js
var NumberFieldScrubAreaContext = /* @__PURE__ */ import_react.createContext(void 0);
NumberFieldScrubAreaContext.displayName = "NumberFieldScrubAreaContext";
function useNumberFieldScrubAreaContext() {
	const context = import_react.useContext(NumberFieldScrubAreaContext);
	if (context === void 0) throw new Error("Base UI: NumberFieldScrubAreaContext is missing. NumberFieldScrubArea parts must be placed within <NumberField.ScrubArea>.");
	return context;
}
//#endregion
//#region node_modules/@base-ui/react/esm/number-field/utils/getViewportRect.js
function getViewportRect(teleportDistance, scrubAreaEl) {
	const win = getWindow(scrubAreaEl);
	const rect = scrubAreaEl.getBoundingClientRect();
	if (rect && teleportDistance != null) return {
		x: rect.left - teleportDistance / 2,
		y: rect.top - teleportDistance / 2,
		width: rect.right + teleportDistance / 2,
		height: rect.bottom + teleportDistance / 2
	};
	const vV = win.visualViewport;
	if (vV) return {
		x: vV.offsetLeft,
		y: vV.offsetTop,
		width: vV.offsetLeft + vV.width,
		height: vV.offsetTop + vV.height
	};
	return {
		x: 0,
		y: 0,
		width: win.document.documentElement.clientWidth,
		height: win.document.documentElement.clientHeight
	};
}
//#endregion
//#region node_modules/@base-ui/react/esm/number-field/utils/subscribeToVisualViewportResize.js
function subscribeToVisualViewportResize(element, visualScaleRef) {
	const vV = getWindow(element).visualViewport;
	if (!vV) return () => {};
	function handleVisualResize() {
		if (vV) visualScaleRef.current = vV.scale;
	}
	handleVisualResize();
	vV.addEventListener("resize", handleVisualResize);
	return () => {
		vV.removeEventListener("resize", handleVisualResize);
	};
}
//#endregion
//#region node_modules/@base-ui/react/esm/number-field/scrub-area/NumberFieldScrubArea.js
/**
* An interactive area where the user can click and drag to change the field value.
* Renders a `<span>` element.
*
* Documentation: [Base UI Number Field](https://base-ui.com/react/components/number-field)
*/
var NumberFieldScrubArea = /* @__PURE__ */ import_react.forwardRef(function NumberFieldScrubArea(componentProps, forwardedRef) {
	const { render, className, direction = "horizontal", pixelSensitivity = 2, teleportDistance, ...elementProps } = componentProps;
	const { state, setIsScrubbing: setRootScrubbing, disabled, readOnly, inputRef, incrementValue, getStepAmount, onValueCommitted, lastChangedValueRef, valueRef } = useNumberFieldRootContext();
	const scrubAreaRef = import_react.useRef(null);
	const isScrubbingRef = import_react.useRef(false);
	const didMoveRef = import_react.useRef(false);
	const pointerDownTargetRef = import_react.useRef(null);
	const scrubAreaCursorRef = import_react.useRef(null);
	const virtualCursorCoords = import_react.useRef({
		x: 0,
		y: 0
	});
	const visualScaleRef = import_react.useRef(1);
	const exitPointerLockTimeout = useTimeout();
	const [isTouchInput, setIsTouchInput] = import_react.useState(false);
	const [isPointerLockDenied, setIsPointerLockDenied] = import_react.useState(false);
	const [isScrubbing, setIsScrubbing] = import_react.useState(false);
	import_react.useEffect(() => {
		if (!isScrubbing || !scrubAreaCursorRef.current) return;
		return subscribeToVisualViewportResize(scrubAreaCursorRef.current, visualScaleRef);
	}, [isScrubbing]);
	function updateCursorTransform(x, y) {
		if (scrubAreaCursorRef.current) scrubAreaCursorRef.current.style.transform = `translate3d(${x}px,${y}px,0) scale(${1 / visualScaleRef.current})`;
	}
	const onScrub = useStableCallback(({ movementX, movementY }) => {
		const virtualCursor = scrubAreaCursorRef.current;
		const scrubAreaEl = scrubAreaRef.current;
		if (!virtualCursor || !scrubAreaEl) return;
		const rect = getViewportRect(teleportDistance, scrubAreaEl);
		const coords = virtualCursorCoords.current;
		const newCoords = {
			x: Math.round(coords.x + movementX),
			y: Math.round(coords.y + movementY)
		};
		const cursorWidth = virtualCursor.offsetWidth;
		const cursorHeight = virtualCursor.offsetHeight;
		if (newCoords.x + cursorWidth / 2 < rect.x) newCoords.x = rect.width - cursorWidth / 2;
		else if (newCoords.x + cursorWidth / 2 > rect.width) newCoords.x = rect.x - cursorWidth / 2;
		if (newCoords.y + cursorHeight / 2 < rect.y) newCoords.y = rect.height - cursorHeight / 2;
		else if (newCoords.y + cursorHeight / 2 > rect.height) newCoords.y = rect.y - cursorHeight / 2;
		virtualCursorCoords.current = newCoords;
		updateCursorTransform(newCoords.x, newCoords.y);
	});
	const onScrubbingChange = useStableCallback((scrubbingValue, { clientX, clientY }) => {
		import_react_dom.flushSync(() => {
			setIsScrubbing(scrubbingValue);
			setRootScrubbing(scrubbingValue);
		});
		const virtualCursor = scrubAreaCursorRef.current;
		if (!virtualCursor || !scrubbingValue) return;
		const initialCoords = {
			x: clientX - virtualCursor.offsetWidth / 2,
			y: clientY - virtualCursor.offsetHeight / 2
		};
		virtualCursorCoords.current = initialCoords;
		updateCursorTransform(initialCoords.x, initialCoords.y);
	});
	import_react.useEffect(function registerGlobalScrubbingEventListeners() {
		if (!inputRef.current || disabled || readOnly || !isScrubbing) return;
		let cumulativeDelta = 0;
		function handleScrubPointerUp(event) {
			function handler() {
				try {
					ownerDocument(scrubAreaRef.current).exitPointerLock();
				} catch {} finally {
					isScrubbingRef.current = false;
					onScrubbingChange(false, event);
					onValueCommitted(lastChangedValueRef.current ?? valueRef.current, createGenericEventDetails(scrub, event));
					if (!didMoveRef.current && pointerDownTargetRef.current != null) pointerDownTargetRef.current.dispatchEvent(new MouseEvent("click", {
						bubbles: true,
						cancelable: true
					}));
					didMoveRef.current = false;
					pointerDownTargetRef.current = null;
				}
			}
			if (isFirefox) exitPointerLockTimeout.start(20, handler);
			else handler();
		}
		function handleScrubPointerMove(event) {
			if (!isScrubbingRef.current) return;
			event.preventDefault();
			onScrub(event);
			const { movementX, movementY } = event;
			cumulativeDelta += direction === "vertical" ? movementY : movementX;
			if (Math.abs(cumulativeDelta) >= pixelSensitivity) {
				cumulativeDelta = 0;
				didMoveRef.current = true;
				const rawAmount = (direction === "vertical" ? -movementY : movementX) * (getStepAmount(event) ?? 1);
				if (rawAmount !== 0) incrementValue(Math.abs(rawAmount), {
					direction: rawAmount >= 0 ? 1 : -1,
					event,
					reason: scrub
				});
			}
		}
		const win = getWindow(inputRef.current);
		win.addEventListener("pointerup", handleScrubPointerUp, true);
		win.addEventListener("pointermove", handleScrubPointerMove, true);
		return () => {
			exitPointerLockTimeout.clear();
			win.removeEventListener("pointerup", handleScrubPointerUp, true);
			win.removeEventListener("pointermove", handleScrubPointerMove, true);
		};
	}, [
		disabled,
		readOnly,
		incrementValue,
		isScrubbing,
		getStepAmount,
		inputRef,
		onScrubbingChange,
		onScrub,
		direction,
		pixelSensitivity,
		lastChangedValueRef,
		onValueCommitted,
		valueRef,
		exitPointerLockTimeout
	]);
	import_react.useEffect(function registerScrubberTouchPreventListener() {
		const element = scrubAreaRef.current;
		if (!element || disabled || readOnly) return;
		function handleTouchStart(event) {
			if (event.touches.length === 1) event.preventDefault();
		}
		element.addEventListener("touchstart", handleTouchStart);
		return () => {
			element.removeEventListener("touchstart", handleTouchStart);
		};
	}, [disabled, readOnly]);
	const element = useRenderElement("span", componentProps, {
		ref: [forwardedRef, scrubAreaRef],
		state,
		props: [{
			role: "presentation",
			style: {
				touchAction: "none",
				WebkitUserSelect: "none",
				userSelect: "none"
			},
			async onPointerDown(event) {
				const isMainButton = !event.button || event.button === 0;
				if (event.defaultPrevented || readOnly || !isMainButton || disabled) return;
				const isTouch = event.pointerType === "touch";
				setIsTouchInput(isTouch);
				if (event.pointerType === "mouse") {
					event.preventDefault();
					inputRef.current?.focus();
				}
				isScrubbingRef.current = true;
				didMoveRef.current = false;
				pointerDownTargetRef.current = event.target;
				onScrubbingChange(true, event.nativeEvent);
				if (!isTouch && !isWebKit) try {
					await ownerDocument(scrubAreaRef.current).body.requestPointerLock();
					setIsPointerLockDenied(false);
				} catch (error) {
					setIsPointerLockDenied(true);
				} finally {
					if (isScrubbingRef.current) import_react_dom.flushSync(() => {
						onScrubbingChange(true, event.nativeEvent);
					});
				}
			}
		}, elementProps],
		stateAttributesMapping: stateAttributesMapping$9
	});
	const contextValue = import_react.useMemo(() => ({
		isScrubbing,
		isTouchInput,
		isPointerLockDenied,
		scrubAreaCursorRef,
		scrubAreaRef,
		direction,
		pixelSensitivity,
		teleportDistance
	}), [
		isScrubbing,
		isTouchInput,
		isPointerLockDenied,
		direction,
		pixelSensitivity,
		teleportDistance
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(NumberFieldScrubAreaContext.Provider, {
		value: contextValue,
		children: element
	});
});
NumberFieldScrubArea.displayName = "NumberFieldScrubArea";
//#endregion
//#region node_modules/@base-ui/react/esm/number-field/scrub-area-cursor/NumberFieldScrubAreaCursor.js
/**
* A custom element to display instead of the native cursor while using the scrub area.
* Renders a `<span>` element.
*
* This component uses the [Pointer Lock API](https://developer.mozilla.org/en-US/docs/Web/API/Pointer_Lock_API), which may prompt the browser to display a related notification. It is disabled
* in Safari to avoid a layout shift that this notification causes there.
*
* Documentation: [Base UI Number Field](https://base-ui.com/react/components/number-field)
*/
var NumberFieldScrubAreaCursor = /* @__PURE__ */ import_react.forwardRef(function NumberFieldScrubAreaCursor(componentProps, forwardedRef) {
	const { render, className, ...elementProps } = componentProps;
	const { state } = useNumberFieldRootContext();
	const { isScrubbing, isTouchInput, isPointerLockDenied, scrubAreaCursorRef } = useNumberFieldScrubAreaContext();
	const [domElement, setDomElement] = import_react.useState(null);
	const element = useRenderElement("span", componentProps, {
		enabled: isScrubbing && !isWebKit && !isTouchInput && !isPointerLockDenied,
		ref: [
			forwardedRef,
			scrubAreaCursorRef,
			setDomElement
		],
		state,
		props: [{
			role: "presentation",
			style: {
				position: "fixed",
				top: 0,
				left: 0,
				pointerEvents: "none"
			}
		}, elementProps],
		stateAttributesMapping: stateAttributesMapping$9
	});
	return element && /* @__PURE__ */ import_react_dom.createPortal(element, ownerDocument(domElement).body);
});
NumberFieldScrubAreaCursor.displayName = "NumberFieldScrubAreaCursor";
//#endregion
//#region node_modules/@base-ui/react/esm/number-field/index.parts.js
var index_parts_exports$15 = /* @__PURE__ */ __exportAll({
	Decrement: () => NumberFieldDecrement,
	Group: () => NumberFieldGroup,
	Increment: () => NumberFieldIncrement,
	Input: () => NumberFieldInput,
	Root: () => NumberFieldRoot,
	ScrubArea: () => NumberFieldScrubArea,
	ScrubAreaCursor: () => NumberFieldScrubAreaCursor
});
//#endregion
//#region node_modules/@base-ui/react/esm/progress/root/ProgressRootContext.js
/**
* @internal
*/
var ProgressRootContext = /* @__PURE__ */ import_react.createContext(void 0);
ProgressRootContext.displayName = "ProgressRootContext";
function useProgressRootContext() {
	const context = import_react.useContext(ProgressRootContext);
	if (context === void 0) throw new Error("Base UI: ProgressRootContext is missing. Progress parts must be placed within <Progress.Root>.");
	return context;
}
//#endregion
//#region node_modules/@base-ui/react/esm/progress/root/ProgressRootDataAttributes.js
var ProgressRootDataAttributes = /* @__PURE__ */ function(ProgressRootDataAttributes) {
	/**
	* Present when the progress has completed.
	*/
	ProgressRootDataAttributes["complete"] = "data-complete";
	/**
	* Present when the progress is in interminate state.
	*/
	ProgressRootDataAttributes["indeterminate"] = "data-indeterminate";
	/**
	* Present while the progress is progressing.
	*/
	ProgressRootDataAttributes["progressing"] = "data-progressing";
	return ProgressRootDataAttributes;
}({});
//#endregion
//#region node_modules/@base-ui/react/esm/progress/root/stateAttributesMapping.js
var progressStateAttributesMapping = { status(value) {
	if (value === "progressing") return { [ProgressRootDataAttributes.progressing]: "" };
	if (value === "complete") return { [ProgressRootDataAttributes.complete]: "" };
	if (value === "indeterminate") return { [ProgressRootDataAttributes.indeterminate]: "" };
	return null;
} };
//#endregion
//#region node_modules/@base-ui/react/esm/progress/root/ProgressRoot.js
function getDefaultAriaValueText(formattedValue, value) {
	if (value == null) return "indeterminate progress";
	return formattedValue || `${value}%`;
}
/**
* Groups all parts of the progress bar and provides the task completion status to screen readers.
* Renders a `<div>` element.
*
* Documentation: [Base UI Progress](https://base-ui.com/react/components/progress)
*/
var ProgressRoot = /* @__PURE__ */ import_react.forwardRef(function ProgressRoot(componentProps, forwardedRef) {
	const { format, getAriaValueText = getDefaultAriaValueText, locale, max = 100, min = 0, value, render, className, children, ...elementProps } = componentProps;
	const [labelId, setLabelId] = import_react.useState();
	const formatOptionsRef = useValueAsRef(format);
	let status = "indeterminate";
	if (Number.isFinite(value)) status = value === max ? "complete" : "progressing";
	const formattedValue = formatNumberValue(value, locale, formatOptionsRef.current);
	const state = import_react.useMemo(() => ({ status }), [status]);
	const defaultProps = {
		"aria-labelledby": labelId,
		"aria-valuemax": max,
		"aria-valuemin": min,
		"aria-valuenow": value ?? void 0,
		"aria-valuetext": getAriaValueText(formattedValue, value),
		role: "progressbar",
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [children, /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
			role: "presentation",
			style: visuallyHidden,
			children: "x"
		})] })
	};
	const contextValue = import_react.useMemo(() => ({
		formattedValue,
		max,
		min,
		setLabelId,
		state,
		status,
		value
	}), [
		formattedValue,
		max,
		min,
		setLabelId,
		state,
		status,
		value
	]);
	const element = useRenderElement("div", componentProps, {
		state,
		ref: forwardedRef,
		props: [defaultProps, elementProps],
		stateAttributesMapping: progressStateAttributesMapping
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ProgressRootContext.Provider, {
		value: contextValue,
		children: element
	});
});
ProgressRoot.displayName = "ProgressRoot";
//#endregion
//#region node_modules/@base-ui/react/esm/progress/track/ProgressTrack.js
/**
* Contains the progress bar indicator.
* Renders a `<div>` element.
*
* Documentation: [Base UI Progress](https://base-ui.com/react/components/progress)
*/
var ProgressTrack = /* @__PURE__ */ import_react.forwardRef(function ProgressTrack(componentProps, forwardedRef) {
	const { render, className, ...elementProps } = componentProps;
	const { state } = useProgressRootContext();
	return useRenderElement("div", componentProps, {
		state,
		ref: forwardedRef,
		props: elementProps,
		stateAttributesMapping: progressStateAttributesMapping
	});
});
ProgressTrack.displayName = "ProgressTrack";
//#endregion
//#region node_modules/@base-ui/react/esm/progress/indicator/ProgressIndicator.js
/**
* Visualizes the completion status of the task.
* Renders a `<div>` element.
*
* Documentation: [Base UI Progress](https://base-ui.com/react/components/progress)
*/
var ProgressIndicator = /* @__PURE__ */ import_react.forwardRef(function ProgressIndicator(componentProps, forwardedRef) {
	const { render, className, ...elementProps } = componentProps;
	const { max, min, value, state } = useProgressRootContext();
	const percentageValue = Number.isFinite(value) && value !== null ? valueToPercent(value, min, max) : null;
	return useRenderElement("div", componentProps, {
		state,
		ref: forwardedRef,
		props: [{ style: import_react.useCallback(() => {
			if (percentageValue == null) return {};
			return {
				insetInlineStart: 0,
				height: "inherit",
				width: `${percentageValue}%`
			};
		}, [percentageValue])() }, elementProps],
		stateAttributesMapping: progressStateAttributesMapping
	});
});
ProgressIndicator.displayName = "ProgressIndicator";
//#endregion
//#region node_modules/@base-ui/react/esm/progress/value/ProgressValue.js
/**
* A text label displaying the current value.
* Renders a `<span>` element.
*
* Documentation: [Base UI Progress](https://base-ui.com/react/components/progress)
*/
var ProgressValue = /* @__PURE__ */ import_react.forwardRef(function ProgressValue(componentProps, forwardedRef) {
	const { className, render, children, ...elementProps } = componentProps;
	const { value, formattedValue, state } = useProgressRootContext();
	const formattedValueArg = value == null ? "indeterminate" : formattedValue;
	const formattedValueDisplay = value == null ? null : formattedValue;
	return useRenderElement("span", componentProps, {
		state,
		ref: forwardedRef,
		props: [{
			"aria-hidden": true,
			children: typeof children === "function" ? children(formattedValueArg, value) : formattedValueDisplay
		}, elementProps],
		stateAttributesMapping: progressStateAttributesMapping
	});
});
ProgressValue.displayName = "ProgressValue";
//#endregion
//#region node_modules/@base-ui/react/esm/progress/label/ProgressLabel.js
/**
* An accessible label for the progress bar.
* Renders a `<span>` element.
*
* Documentation: [Base UI Progress](https://base-ui.com/react/components/progress)
*/
var ProgressLabel = /* @__PURE__ */ import_react.forwardRef(function ProgressLabel(componentProps, forwardedRef) {
	const { render, className, id: idProp, ...elementProps } = componentProps;
	const { setLabelId, state } = useProgressRootContext();
	return useRenderElement("span", componentProps, {
		state,
		ref: forwardedRef,
		props: [{
			id: useRegisteredLabelId(idProp, setLabelId),
			role: "presentation"
		}, elementProps],
		stateAttributesMapping: progressStateAttributesMapping
	});
});
ProgressLabel.displayName = "ProgressLabel";
//#endregion
//#region node_modules/@base-ui/react/esm/progress/index.parts.js
var index_parts_exports$18 = /* @__PURE__ */ __exportAll({
	Indicator: () => ProgressIndicator,
	Label: () => ProgressLabel,
	Root: () => ProgressRoot,
	Track: () => ProgressTrack,
	Value: () => ProgressValue
});
//#endregion
//#region node_modules/@base-ui/react/esm/radio/root/RadioRootDataAttributes.js
var RadioRootDataAttributes = /* @__PURE__ */ function(RadioRootDataAttributes) {
	/**
	* Present when the radio is checked.
	*/
	RadioRootDataAttributes["checked"] = "data-checked";
	/**
	* Present when the radio is not checked.
	*/
	RadioRootDataAttributes["unchecked"] = "data-unchecked";
	/**
	* Present when the radio is disabled.
	*/
	RadioRootDataAttributes["disabled"] = "data-disabled";
	/**
	* Present when the radio is readonly.
	*/
	RadioRootDataAttributes["readonly"] = "data-readonly";
	/**
	* Present when the radio is required.
	*/
	RadioRootDataAttributes["required"] = "data-required";
	/**
	* Present when the radio is in valid state (when wrapped in Field.Root).
	*/
	RadioRootDataAttributes["valid"] = "data-valid";
	/**
	* Present when the radio is in invalid state (when wrapped in Field.Root).
	*/
	RadioRootDataAttributes["invalid"] = "data-invalid";
	/**
	* Present when the radio has been touched (when wrapped in Field.Root).
	*/
	RadioRootDataAttributes["touched"] = "data-touched";
	/**
	* Present when the radio's value has changed (when wrapped in Field.Root).
	*/
	RadioRootDataAttributes["dirty"] = "data-dirty";
	/**
	* Present when the radio is checked (when wrapped in Field.Root).
	*/
	RadioRootDataAttributes["filled"] = "data-filled";
	/**
	* Present when the radio is focused (when wrapped in Field.Root).
	*/
	RadioRootDataAttributes["focused"] = "data-focused";
	return RadioRootDataAttributes;
}({});
//#endregion
//#region node_modules/@base-ui/react/esm/radio/utils/stateAttributesMapping.js
var stateAttributesMapping$7 = {
	checked(value) {
		if (value) return { [RadioRootDataAttributes.checked]: "" };
		return { [RadioRootDataAttributes.unchecked]: "" };
	},
	...transitionStatusMapping,
	...fieldValidityMapping
};
//#endregion
//#region node_modules/@base-ui/react/esm/radio-group/RadioGroupContext.js
var RadioGroupContext = /* @__PURE__ */ import_react.createContext(void 0);
RadioGroupContext.displayName = "RadioGroupContext";
function useRadioGroupContext() {
	return import_react.useContext(RadioGroupContext);
}
//#endregion
//#region node_modules/@base-ui/react/esm/radio/root/RadioRootContext.js
var RadioRootContext = /* @__PURE__ */ import_react.createContext(void 0);
RadioRootContext.displayName = "RadioRootContext";
function useRadioRootContext() {
	const value = import_react.useContext(RadioRootContext);
	if (value === void 0) throw new Error("Base UI: RadioRootContext is missing. Radio parts must be placed within <Radio.Root>.");
	return value;
}
//#endregion
//#region node_modules/@base-ui/react/esm/radio/root/RadioRoot.js
/**
* Represents the radio button itself.
* Renders a `<span>` element and a hidden `<input>` beside.
*
* Documentation: [Base UI Radio](https://base-ui.com/react/components/radio)
*/
var RadioRoot = /* @__PURE__ */ import_react.forwardRef(function RadioRoot(componentProps, forwardedRef) {
	const { render, className, disabled: disabledProp = false, readOnly: readOnlyProp = false, required: requiredProp = false, "aria-labelledby": ariaLabelledByProp, value, inputRef: inputRefProp, nativeButton = false, id: idProp, ...elementProps } = componentProps;
	const groupContext = useRadioGroupContext();
	const { disabled: disabledGroup, readOnly: readOnlyGroup, required: requiredGroup, checkedValue, touched = false, validation, name } = groupContext ?? {};
	const setCheckedValue = groupContext?.setCheckedValue ?? NOOP;
	const setTouched = groupContext?.setTouched ?? NOOP;
	const registerControlRef = groupContext?.registerControlRef ?? NOOP;
	const registerInputRef = groupContext?.registerInputRef ?? NOOP;
	const { setDirty, validityData, setTouched: setFieldTouched, setFilled, state: fieldState, disabled: fieldDisabled } = useFieldRootContext();
	const fieldItemContext = useFieldItemContext();
	const { labelId, getDescriptionProps } = useLabelableContext();
	const disabled = fieldDisabled || fieldItemContext.disabled || disabledGroup || disabledProp;
	const readOnly = readOnlyGroup || readOnlyProp;
	const required = requiredGroup || requiredProp;
	const checked = groupContext ? checkedValue === value : value === "";
	const serializedValue = import_react.useMemo(() => serializeValue(value), [value]);
	const radioRef = import_react.useRef(null);
	const inputRef = import_react.useRef(null);
	const handleControlRef = useStableCallback((element) => {
		if (!element) return;
		registerControlRef(element, disabled);
	});
	const mergedInputRef = useMergedRefs(inputRefProp, inputRef, registerInputRef);
	useIsoLayoutEffect(() => {
		if (inputRef.current?.checked) setFilled(true);
	}, [setFilled]);
	useIsoLayoutEffect(() => {
		if (!inputRef.current) return;
		if (disabled && checked) {
			registerInputRef(null);
			return;
		}
		if (radioRef.current) registerControlRef(radioRef.current, disabled);
		registerInputRef(inputRef.current);
	}, [
		checked,
		disabled,
		registerControlRef,
		registerInputRef
	]);
	const id = useBaseUiId();
	const inputId = useLabelableId({
		id: idProp,
		implicit: false,
		controlRef: radioRef
	});
	const hiddenInputId = nativeButton ? void 0 : inputId;
	const ariaLabelledBy = useAriaLabelledBy(ariaLabelledByProp, labelId, inputRef, !nativeButton, hiddenInputId);
	const rootProps = {
		role: "radio",
		"aria-checked": checked,
		"aria-required": required || void 0,
		"aria-readonly": readOnly || void 0,
		"aria-labelledby": ariaLabelledBy,
		[ACTIVE_COMPOSITE_ITEM]: checked ? "" : void 0,
		id: nativeButton ? inputId : id,
		onKeyDown(event) {
			if (event.key === "Enter") event.preventDefault();
		},
		onClick(event) {
			if (event.defaultPrevented || disabled || readOnly) return;
			event.preventDefault();
			inputRef.current?.dispatchEvent(new PointerEvent("click", {
				bubbles: true,
				shiftKey: event.shiftKey,
				ctrlKey: event.ctrlKey,
				altKey: event.altKey,
				metaKey: event.metaKey
			}));
		},
		onFocus(event) {
			if (event.defaultPrevented || disabled || readOnly || !touched) return;
			inputRef.current?.click();
			setTouched(false);
		}
	};
	const { getButtonProps, buttonRef } = useButton({
		disabled,
		native: nativeButton
	});
	const inputProps = {
		type: "radio",
		ref: mergedInputRef,
		id: hiddenInputId,
		name,
		tabIndex: -1,
		style: name ? visuallyHiddenInput : visuallyHidden,
		"aria-hidden": true,
		...value !== void 0 ? { value: serializedValue } : EMPTY_OBJECT,
		disabled,
		checked,
		required,
		readOnly,
		onChange(event) {
			if (event.nativeEvent.defaultPrevented) return;
			if (disabled || readOnly || value === void 0) return;
			const details = createChangeEventDetails(none, event.nativeEvent);
			if (details.isCanceled) return;
			setFieldTouched(true);
			setDirty(value !== validityData.initialValue);
			setFilled(true);
			setCheckedValue(value, details);
		},
		onFocus() {
			radioRef.current?.focus();
		}
	};
	const state = import_react.useMemo(() => ({
		...fieldState,
		required,
		disabled,
		readOnly,
		checked
	}), [
		fieldState,
		disabled,
		readOnly,
		checked,
		required
	]);
	const contextValue = state;
	const isRadioGroup = groupContext !== void 0;
	const refs = [
		forwardedRef,
		radioRef,
		buttonRef,
		handleControlRef
	];
	const props = [
		rootProps,
		getDescriptionProps,
		validation?.getValidationProps ?? EMPTY_OBJECT,
		elementProps,
		getButtonProps
	];
	const element = useRenderElement("span", componentProps, {
		enabled: !isRadioGroup,
		state,
		ref: refs,
		props,
		stateAttributesMapping: stateAttributesMapping$7
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(RadioRootContext.Provider, {
		value: contextValue,
		children: [isRadioGroup ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CompositeItem, {
			tag: "span",
			render,
			className,
			state,
			refs,
			props,
			stateAttributesMapping: stateAttributesMapping$7
		}) : element, /* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", { ...inputProps })]
	});
});
RadioRoot.displayName = "RadioRoot";
//#endregion
//#region node_modules/@base-ui/react/esm/radio/indicator/RadioIndicator.js
/**
* Indicates whether the radio button is selected.
* Renders a `<span>` element.
*
* Documentation: [Base UI Radio](https://base-ui.com/react/components/radio)
*/
var RadioIndicator = /* @__PURE__ */ import_react.forwardRef(function RadioIndicator(componentProps, forwardedRef) {
	const { render, className, keepMounted = false, ...elementProps } = componentProps;
	const rootState = useRadioRootContext();
	const rendered = rootState.checked;
	const { mounted, transitionStatus, setMounted } = useTransitionStatus(rendered);
	const state = {
		...rootState,
		transitionStatus
	};
	const indicatorRef = import_react.useRef(null);
	const shouldRender = keepMounted || mounted;
	const element = useRenderElement("span", componentProps, {
		ref: [forwardedRef, indicatorRef],
		state,
		props: elementProps,
		stateAttributesMapping: stateAttributesMapping$7
	});
	useOpenChangeComplete({
		open: rendered,
		ref: indicatorRef,
		onComplete() {
			if (!rendered) setMounted(false);
		}
	});
	if (!shouldRender) return null;
	return element;
});
RadioIndicator.displayName = "RadioIndicator";
//#endregion
//#region node_modules/@base-ui/react/esm/radio/index.parts.js
var index_parts_exports$19 = /* @__PURE__ */ __exportAll({
	Indicator: () => RadioIndicator,
	Root: () => RadioRoot
});
//#endregion
//#region node_modules/@base-ui/react/esm/radio-group/RadioGroup.js
var MODIFIER_KEYS = [SHIFT];
/**
* Provides a shared state to a series of radio buttons.
* Renders a `<div>` element.
*
* Documentation: [Base UI Radio Group](https://base-ui.com/react/components/radio)
*/
var RadioGroup = /* @__PURE__ */ import_react.forwardRef(function RadioGroup(componentProps, forwardedRef) {
	const { render, className, disabled: disabledProp, readOnly, required, onValueChange: onValueChangeProp, value: externalValue, defaultValue, name: nameProp, inputRef: inputRefProp, id: idProp, ...elementProps } = componentProps;
	const { setTouched: setFieldTouched, setFocused, shouldValidateOnChange, validationMode, name: fieldName, disabled: fieldDisabled, state: fieldState, validation, setDirty, setFilled, validityData } = useFieldRootContext();
	const { labelId } = useLabelableContext();
	const { clearErrors } = useFormContext();
	const fieldsetContext = useFieldsetRootContext(true);
	const disabled = fieldDisabled || disabledProp;
	const name = fieldName ?? nameProp;
	const id = useBaseUiId(idProp);
	const [checkedValue, setCheckedValueUnwrapped] = useControlled({
		controlled: externalValue,
		default: defaultValue,
		name: "RadioGroup",
		state: "value"
	});
	const onValueChange = useStableCallback(onValueChangeProp);
	const setCheckedValue = useStableCallback((value, eventDetails) => {
		onValueChange(value, eventDetails);
		if (eventDetails.isCanceled) return;
		setCheckedValueUnwrapped(value);
	});
	const controlRef = import_react.useRef(null);
	const groupInputRef = import_react.useRef(null);
	const firstEnabledInputRef = import_react.useRef(null);
	function setInputRef(hiddenInput) {
		let cleanup = void 0;
		if (inputRefProp) if (typeof inputRefProp === "function") cleanup = inputRefProp(hiddenInput);
		else inputRefProp.current = hiddenInput;
		groupInputRef.current = hiddenInput;
		validation.inputRef.current = hiddenInput;
		return cleanup;
	}
	const registerControlRef = useStableCallback((element, isDisabled = false) => {
		if (!element) return;
		if (isDisabled) {
			if (controlRef.current === element) controlRef.current = null;
			return;
		}
		if (controlRef.current == null) controlRef.current = element;
	});
	const registerInputRef = useStableCallback((input) => {
		if (!input || input.disabled) return;
		if (!firstEnabledInputRef.current) firstEnabledInputRef.current = input;
		const currentInput = groupInputRef.current;
		if (input.checked || currentInput == null || currentInput.disabled) return setInputRef(input);
	});
	useField({
		id,
		commit: validation.commit,
		value: checkedValue,
		controlRef,
		name,
		getValue: () => checkedValue ?? null
	});
	useValueChanged(checkedValue, () => {
		clearErrors(name);
		setDirty(checkedValue !== validityData.initialValue);
		setFilled(checkedValue != null);
		if (shouldValidateOnChange()) validation.commit(checkedValue);
		else validation.commit(checkedValue, true);
		const fallbackInput = firstEnabledInputRef.current;
		if (checkedValue == null && fallbackInput && !fallbackInput.disabled) setInputRef(fallbackInput);
	});
	const [touched, setTouched] = import_react.useState(false);
	const ariaLabelledby = elementProps["aria-labelledby"] ?? labelId ?? fieldsetContext?.legendId;
	const state = {
		...fieldState,
		disabled: disabled ?? false,
		required: required ?? false,
		readOnly: readOnly ?? false
	};
	const contextValue = import_react.useMemo(() => ({
		...fieldState,
		checkedValue,
		disabled,
		validation,
		name,
		onValueChange,
		readOnly,
		registerControlRef,
		registerInputRef,
		required,
		setCheckedValue,
		setTouched,
		touched
	}), [
		checkedValue,
		disabled,
		validation,
		fieldState,
		name,
		onValueChange,
		readOnly,
		registerControlRef,
		registerInputRef,
		required,
		setCheckedValue,
		setTouched,
		touched
	]);
	const defaultProps = {
		role: "radiogroup",
		"aria-required": required || void 0,
		"aria-disabled": disabled || void 0,
		"aria-readonly": readOnly || void 0,
		"aria-labelledby": ariaLabelledby,
		onFocus() {
			setFocused(true);
		},
		onBlur(event) {
			if (!contains(event.currentTarget, event.relatedTarget)) {
				setFieldTouched(true);
				setFocused(false);
				if (validationMode === "onBlur") validation.commit(checkedValue);
			}
		},
		onKeyDownCapture(event) {
			if (event.key.startsWith("Arrow")) {
				setFieldTouched(true);
				setTouched(true);
				setFocused(true);
			}
		}
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(RadioGroupContext.Provider, {
		value: contextValue,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CompositeRoot, {
			render,
			className,
			state,
			props: [
				defaultProps,
				validation.getValidationProps,
				elementProps
			],
			refs: [forwardedRef],
			stateAttributesMapping: fieldValidityMapping,
			enableHomeAndEndKeys: false,
			modifierKeys: MODIFIER_KEYS
		})
	});
});
RadioGroup.displayName = "RadioGroup";
//#endregion
//#region node_modules/@base-ui/react/esm/select/root/SelectRootContext.js
var SelectRootContext = /* @__PURE__ */ import_react.createContext(null);
SelectRootContext.displayName = "SelectRootContext";
var SelectFloatingContext = /* @__PURE__ */ import_react.createContext(null);
SelectFloatingContext.displayName = "SelectFloatingContext";
function useSelectRootContext() {
	const context = import_react.useContext(SelectRootContext);
	if (context === null) throw new Error("Base UI: SelectRootContext is missing. Select parts must be placed within <Select.Root>.");
	return context;
}
function useSelectFloatingContext() {
	const context = import_react.useContext(SelectFloatingContext);
	if (context === null) throw new Error("Base UI: SelectFloatingContext is missing. Select parts must be placed within <Select.Root>.");
	return context;
}
//#endregion
//#region node_modules/@base-ui/react/esm/select/store.js
var selectors$1 = {
	id: createSelector((state) => state.id),
	labelId: createSelector((state) => state.labelId),
	modal: createSelector((state) => state.modal),
	multiple: createSelector((state) => state.multiple),
	items: createSelector((state) => state.items),
	itemToStringLabel: createSelector((state) => state.itemToStringLabel),
	itemToStringValue: createSelector((state) => state.itemToStringValue),
	isItemEqualToValue: createSelector((state) => state.isItemEqualToValue),
	value: createSelector((state) => state.value),
	hasSelectedValue: createSelector((state) => {
		const { value, multiple, itemToStringValue } = state;
		if (value == null) return false;
		if (multiple && Array.isArray(value)) return value.length > 0;
		return stringifyAsValue(value, itemToStringValue) !== "";
	}),
	hasNullItemLabel: createSelector((state, enabled) => {
		return enabled ? hasNullItemLabel(state.items) : false;
	}),
	open: createSelector((state) => state.open),
	mounted: createSelector((state) => state.mounted),
	forceMount: createSelector((state) => state.forceMount),
	transitionStatus: createSelector((state) => state.transitionStatus),
	openMethod: createSelector((state) => state.openMethod),
	activeIndex: createSelector((state) => state.activeIndex),
	selectedIndex: createSelector((state) => state.selectedIndex),
	isActive: createSelector((state, index) => state.activeIndex === index),
	isSelected: createSelector((state, index, itemValue) => {
		const comparer = state.isItemEqualToValue;
		const storeValue = state.value;
		if (state.multiple) return Array.isArray(storeValue) && storeValue.some((selectedItem) => compareItemEquality(itemValue, selectedItem, comparer));
		if (state.selectedIndex === index && state.selectedIndex !== null) return true;
		return compareItemEquality(itemValue, storeValue, comparer);
	}),
	isSelectedByFocus: createSelector((state, index) => {
		return state.selectedIndex === index;
	}),
	popupProps: createSelector((state) => state.popupProps),
	triggerProps: createSelector((state) => state.triggerProps),
	triggerElement: createSelector((state) => state.triggerElement),
	positionerElement: createSelector((state) => state.positionerElement),
	listElement: createSelector((state) => state.listElement),
	scrollUpArrowVisible: createSelector((state) => state.scrollUpArrowVisible),
	scrollDownArrowVisible: createSelector((state) => state.scrollDownArrowVisible),
	hasScrollArrows: createSelector((state) => state.hasScrollArrows)
};
//#endregion
//#region node_modules/@base-ui/react/esm/select/root/SelectRoot.js
/**
* Groups all parts of the select.
* Doesn’t render its own HTML element.
*
* Documentation: [Base UI Select](https://base-ui.com/react/components/select)
*/
function SelectRoot(props) {
	const { id, value: valueProp, defaultValue = null, onValueChange, open: openProp, defaultOpen = false, onOpenChange, name: nameProp, autoComplete, disabled: disabledProp = false, readOnly = false, required = false, modal = true, actionsRef, inputRef, onOpenChangeComplete, items, multiple = false, itemToStringLabel, itemToStringValue, isItemEqualToValue = defaultItemEquality, highlightItemOnHover = true, children } = props;
	const { clearErrors } = useFormContext();
	const { setDirty, setTouched, setFocused, shouldValidateOnChange, validityData, setFilled, name: fieldName, disabled: fieldDisabled, validation, validationMode } = useFieldRootContext();
	const generatedId = useLabelableId({ id });
	const disabled = fieldDisabled || disabledProp;
	const name = fieldName ?? nameProp;
	const [value, setValueUnwrapped] = useControlled({
		controlled: valueProp,
		default: multiple ? defaultValue ?? EMPTY_ARRAY : defaultValue,
		name: "Select",
		state: "value"
	});
	const [open, setOpenUnwrapped] = useControlled({
		controlled: openProp,
		default: defaultOpen,
		name: "Select",
		state: "open"
	});
	const listRef = import_react.useRef([]);
	const labelsRef = import_react.useRef([]);
	const popupRef = import_react.useRef(null);
	const scrollHandlerRef = import_react.useRef(null);
	const scrollArrowsMountedCountRef = import_react.useRef(0);
	const valueRef = import_react.useRef(null);
	const valuesRef = import_react.useRef([]);
	const typingRef = import_react.useRef(false);
	const keyboardActiveRef = import_react.useRef(false);
	const selectedItemTextRef = import_react.useRef(null);
	const selectionRef = import_react.useRef({
		allowSelectedMouseUp: false,
		allowUnselectedMouseUp: false
	});
	const alignItemWithTriggerActiveRef = import_react.useRef(false);
	const { mounted, setMounted, transitionStatus } = useTransitionStatus(open);
	const { openMethod, triggerProps: interactionTypeProps } = useOpenInteractionType(open);
	const store = useRefWithInit(() => new Store({
		id: generatedId,
		labelId: void 0,
		modal,
		multiple,
		itemToStringLabel,
		itemToStringValue,
		isItemEqualToValue,
		value,
		open,
		mounted,
		transitionStatus,
		items,
		forceMount: false,
		openMethod: null,
		activeIndex: null,
		selectedIndex: null,
		popupProps: {},
		triggerProps: {},
		triggerElement: null,
		positionerElement: null,
		listElement: null,
		scrollUpArrowVisible: false,
		scrollDownArrowVisible: false,
		hasScrollArrows: false
	})).current;
	const activeIndex = useStore(store, selectors$1.activeIndex);
	const selectedIndex = useStore(store, selectors$1.selectedIndex);
	const triggerElement = useStore(store, selectors$1.triggerElement);
	const positionerElement = useStore(store, selectors$1.positionerElement);
	const serializedValue = import_react.useMemo(() => {
		if (multiple && Array.isArray(value) && value.length === 0) return "";
		return stringifyAsValue(value, itemToStringValue);
	}, [
		multiple,
		value,
		itemToStringValue
	]);
	const fieldStringValue = import_react.useMemo(() => {
		if (multiple && Array.isArray(value)) return value.map((currentValue) => stringifyAsValue(currentValue, itemToStringValue));
		return stringifyAsValue(value, itemToStringValue);
	}, [
		multiple,
		value,
		itemToStringValue
	]);
	const controlRef = useValueAsRef(store.state.triggerElement);
	useField({
		id: generatedId,
		commit: validation.commit,
		value,
		controlRef,
		name,
		getValue: () => fieldStringValue
	});
	const initialValueRef = import_react.useRef(value);
	useIsoLayoutEffect(() => {
		if (value !== initialValueRef.current) store.set("forceMount", true);
	}, [store, value]);
	useIsoLayoutEffect(() => {
		setFilled(multiple ? Array.isArray(value) && value.length > 0 : value != null);
	}, [
		multiple,
		value,
		setFilled
	]);
	useIsoLayoutEffect(function syncSelectedIndex() {
		if (open) return;
		const registry = valuesRef.current;
		if (multiple) {
			const currentValue = Array.isArray(value) ? value : [];
			if (currentValue.length === 0) {
				store.set("selectedIndex", null);
				return;
			}
			const lastValue = currentValue[currentValue.length - 1];
			const lastIndex = findItemIndex(registry, lastValue, isItemEqualToValue);
			store.set("selectedIndex", lastIndex === -1 ? null : lastIndex);
			return;
		}
		const index = findItemIndex(registry, value, isItemEqualToValue);
		store.set("selectedIndex", index === -1 ? null : index);
	}, [
		multiple,
		open,
		value,
		valuesRef,
		isItemEqualToValue,
		store
	]);
	useValueChanged(value, () => {
		clearErrors(name);
		setDirty(value !== validityData.initialValue);
		if (shouldValidateOnChange()) validation.commit(value);
		else validation.commit(value, true);
	});
	const setOpen = useStableCallback((nextOpen, eventDetails) => {
		onOpenChange?.(nextOpen, eventDetails);
		if (eventDetails.isCanceled) return;
		setOpenUnwrapped(nextOpen);
		if (!nextOpen && (eventDetails.reason === "focus-out" || eventDetails.reason === "outside-press")) {
			setTouched(true);
			setFocused(false);
			if (validationMode === "onBlur") validation.commit(value);
		}
		if (!nextOpen && store.state.activeIndex !== null) {
			const activeOption = listRef.current[store.state.activeIndex];
			queueMicrotask(() => {
				activeOption?.setAttribute("tabindex", "-1");
			});
		}
	});
	const handleUnmount = useStableCallback(() => {
		setMounted(false);
		store.set("activeIndex", null);
		onOpenChangeComplete?.(false);
	});
	useOpenChangeComplete({
		enabled: !actionsRef,
		open,
		ref: popupRef,
		onComplete() {
			if (!open) handleUnmount();
		}
	});
	import_react.useImperativeHandle(actionsRef, () => ({ unmount: handleUnmount }), [handleUnmount]);
	const setValue = useStableCallback((nextValue, eventDetails) => {
		onValueChange?.(nextValue, eventDetails);
		if (eventDetails.isCanceled) return;
		setValueUnwrapped(nextValue);
	});
	const handleScrollArrowVisibility = useStableCallback(() => {
		const scroller = store.state.listElement || popupRef.current;
		if (!scroller) return;
		const viewportTop = scroller.scrollTop;
		const viewportBottom = scroller.scrollTop + scroller.clientHeight;
		const shouldShowUp = viewportTop > 1;
		const shouldShowDown = viewportBottom < scroller.scrollHeight - 1;
		if (store.state.scrollUpArrowVisible !== shouldShowUp) store.set("scrollUpArrowVisible", shouldShowUp);
		if (store.state.scrollDownArrowVisible !== shouldShowDown) store.set("scrollDownArrowVisible", shouldShowDown);
	});
	const floatingContext = useFloatingRootContext({
		open,
		onOpenChange: setOpen,
		elements: {
			reference: triggerElement,
			floating: positionerElement
		}
	});
	const { getReferenceProps, getFloatingProps, getItemProps } = useInteractions([
		useClick(floatingContext, {
			enabled: !readOnly && !disabled,
			event: "mousedown"
		}),
		useDismiss(floatingContext, { bubbles: false }),
		useListNavigation(floatingContext, {
			enabled: !readOnly && !disabled,
			listRef,
			activeIndex,
			selectedIndex,
			disabledIndices: EMPTY_ARRAY,
			onNavigate(nextActiveIndex) {
				if (nextActiveIndex === null && !open) return;
				store.set("activeIndex", nextActiveIndex);
			},
			focusItemOnHover: false
		}),
		useTypeahead(floatingContext, {
			enabled: !readOnly && !disabled && (open || !multiple),
			listRef: labelsRef,
			activeIndex,
			selectedIndex,
			onMatch(index) {
				if (open) store.set("activeIndex", index);
				else setValue(valuesRef.current[index], createChangeEventDetails("none"));
			},
			onTypingChange(typing) {
				typingRef.current = typing;
			}
		})
	]);
	const mergedTriggerProps = import_react.useMemo(() => {
		return mergeProps(getReferenceProps(), interactionTypeProps, generatedId ? { id: generatedId } : EMPTY_OBJECT);
	}, [
		getReferenceProps,
		interactionTypeProps,
		generatedId
	]);
	useOnFirstRender(() => {
		store.update({
			popupProps: getFloatingProps(),
			triggerProps: mergedTriggerProps
		});
	});
	useIsoLayoutEffect(() => {
		store.update({
			id: generatedId,
			modal,
			multiple,
			value,
			open,
			mounted,
			transitionStatus,
			popupProps: getFloatingProps(),
			triggerProps: mergedTriggerProps,
			items,
			itemToStringLabel,
			itemToStringValue,
			isItemEqualToValue,
			openMethod
		});
	}, [
		store,
		generatedId,
		modal,
		multiple,
		value,
		open,
		mounted,
		transitionStatus,
		getFloatingProps,
		mergedTriggerProps,
		items,
		itemToStringLabel,
		itemToStringValue,
		isItemEqualToValue,
		openMethod
	]);
	const contextValue = import_react.useMemo(() => ({
		store,
		name,
		required,
		disabled,
		readOnly,
		multiple,
		itemToStringLabel,
		itemToStringValue,
		highlightItemOnHover,
		setValue,
		setOpen,
		listRef,
		popupRef,
		scrollHandlerRef,
		handleScrollArrowVisibility,
		scrollArrowsMountedCountRef,
		getItemProps,
		events: floatingContext.context.events,
		valueRef,
		valuesRef,
		labelsRef,
		typingRef,
		selectionRef,
		selectedItemTextRef,
		validation,
		onOpenChangeComplete,
		keyboardActiveRef,
		alignItemWithTriggerActiveRef,
		initialValueRef
	}), [
		store,
		name,
		required,
		disabled,
		readOnly,
		multiple,
		itemToStringLabel,
		itemToStringValue,
		highlightItemOnHover,
		setValue,
		setOpen,
		getItemProps,
		floatingContext.context.events,
		validation,
		onOpenChangeComplete,
		handleScrollArrowVisibility
	]);
	const ref = useMergedRefs(inputRef, validation.inputRef);
	const hasMultipleSelection = multiple && Array.isArray(value) && value.length > 0;
	const hiddenInputName = multiple ? void 0 : name;
	const hiddenInputs = import_react.useMemo(() => {
		if (!multiple || !Array.isArray(value) || !name) return null;
		return value.map((v) => {
			const currentSerializedValue = stringifyAsValue(v, itemToStringValue);
			return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", {
				type: "hidden",
				name,
				value: currentSerializedValue
			}, currentSerializedValue);
		});
	}, [
		multiple,
		value,
		name,
		itemToStringValue
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectRootContext.Provider, {
		value: contextValue,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(SelectFloatingContext.Provider, {
			value: floatingContext,
			children: [
				children,
				/* @__PURE__ */ (0, import_jsx_runtime.jsx)("input", {
					...validation.getInputValidationProps({
						onFocus() {
							store.state.triggerElement?.focus({ focusVisible: true });
						},
						onChange(event) {
							if (event.nativeEvent.defaultPrevented) return;
							const nextValue = event.target.value;
							const details = createChangeEventDetails(none, event.nativeEvent);
							function handleChange() {
								if (multiple) return;
								const matchingValue = valuesRef.current.find((v) => {
									if (stringifyAsValue(v, itemToStringValue).toLowerCase() === nextValue.toLowerCase()) return true;
									return false;
								});
								if (matchingValue != null) {
									setDirty(matchingValue !== validityData.initialValue);
									setValue(matchingValue, details);
									if (shouldValidateOnChange()) validation.commit(matchingValue);
								}
							}
							store.set("forceMount", true);
							queueMicrotask(handleChange);
						}
					}),
					id: generatedId && hiddenInputName == null ? `${generatedId}-hidden-input` : void 0,
					name: hiddenInputName,
					autoComplete,
					value: serializedValue,
					disabled,
					required: required && !hasMultipleSelection,
					readOnly,
					ref,
					style: name ? visuallyHiddenInput : visuallyHidden,
					tabIndex: -1,
					"aria-hidden": true
				}),
				hiddenInputs
			]
		})
	});
}
//#endregion
//#region node_modules/@base-ui/react/esm/select/label/SelectLabel.js
/**
* An accessible label that is automatically associated with the select trigger.
* Renders a `<div>` element.
*
* Documentation: [Base UI Select](https://base-ui.com/react/components/select)
*/
var SelectLabel = /* @__PURE__ */ import_react.forwardRef(function SelectLabel(componentProps, forwardedRef) {
	const { render, className, ...elementProps } = componentProps;
	const elementPropsWithoutId = elementProps;
	delete elementPropsWithoutId.id;
	const fieldRootContext = useFieldRootContext();
	const { store } = useSelectRootContext();
	const triggerElement = useStore(store, selectors$1.triggerElement);
	const rootId = useStore(store, selectors$1.id);
	const labelProps = useLabel({
		id: getDefaultLabelId(rootId),
		fallbackControlId: triggerElement?.id ?? rootId,
		setLabelId(nextLabelId) {
			store.set("labelId", nextLabelId);
		}
	});
	return useRenderElement("div", componentProps, {
		ref: forwardedRef,
		state: fieldRootContext.state,
		props: [labelProps, elementProps],
		stateAttributesMapping: fieldValidityMapping
	});
});
SelectLabel.displayName = "SelectLabel";
//#endregion
//#region node_modules/@base-ui/react/esm/select/trigger/SelectTrigger.js
var BOUNDARY_OFFSET = 2;
var SELECTED_DELAY = 400;
var UNSELECTED_DELAY = 200;
var stateAttributesMapping$6 = {
	...pressableTriggerOpenStateMapping,
	...fieldValidityMapping,
	value: () => null
};
/**
* A button that opens the select popup.
* Renders a `<button>` element.
*
* Documentation: [Base UI Select](https://base-ui.com/react/components/select)
*/
var SelectTrigger = /* @__PURE__ */ import_react.forwardRef(function SelectTrigger(componentProps, forwardedRef) {
	const { render, className, id: idProp, disabled: disabledProp = false, nativeButton = true, ...elementProps } = componentProps;
	const { setTouched, setFocused, validationMode, state: fieldState, disabled: fieldDisabled } = useFieldRootContext();
	const { labelId: fieldLabelId } = useLabelableContext();
	const { store, setOpen, selectionRef, validation, readOnly, required, alignItemWithTriggerActiveRef, disabled: selectDisabled, keyboardActiveRef } = useSelectRootContext();
	const disabled = fieldDisabled || selectDisabled || disabledProp;
	const open = useStore(store, selectors$1.open);
	const value = useStore(store, selectors$1.value);
	const triggerProps = useStore(store, selectors$1.triggerProps);
	const positionerElement = useStore(store, selectors$1.positionerElement);
	const listElement = useStore(store, selectors$1.listElement);
	const rootId = useStore(store, selectors$1.id);
	const selectLabelId = useStore(store, selectors$1.labelId);
	const hasSelectedValue = useStore(store, selectors$1.hasSelectedValue);
	const shouldCheckNullItemLabel = !hasSelectedValue && open;
	const hasNullItemLabel = useStore(store, selectors$1.hasNullItemLabel, shouldCheckNullItemLabel);
	const id = idProp ?? rootId;
	const ariaLabelledBy = resolveAriaLabelledBy(fieldLabelId, selectLabelId);
	useLabelableId({ id });
	const positionerRef = useValueAsRef(positionerElement);
	const triggerRef = import_react.useRef(null);
	const { getButtonProps, buttonRef } = useButton({
		disabled,
		native: nativeButton
	});
	const mergedRef = useMergedRefs(forwardedRef, triggerRef, buttonRef, useStableCallback((element) => {
		store.set("triggerElement", element);
	}));
	const timeoutFocus = useTimeout();
	const timeoutMouseDown = useTimeout();
	const selectedDelayTimeout = useTimeout();
	const unselectedDelayTimeout = useTimeout();
	import_react.useEffect(() => {
		if (open) {
			if (!(hasSelectedValue || hasNullItemLabel)) selectedDelayTimeout.start(SELECTED_DELAY, () => {
				selectionRef.current.allowUnselectedMouseUp = true;
				selectionRef.current.allowSelectedMouseUp = true;
			});
			else unselectedDelayTimeout.start(UNSELECTED_DELAY, () => {
				selectionRef.current.allowUnselectedMouseUp = true;
				selectedDelayTimeout.start(UNSELECTED_DELAY, () => {
					selectionRef.current.allowSelectedMouseUp = true;
				});
			});
			return () => {
				selectedDelayTimeout.clear();
				unselectedDelayTimeout.clear();
			};
		}
		selectionRef.current = {
			allowSelectedMouseUp: false,
			allowUnselectedMouseUp: false
		};
		timeoutMouseDown.clear();
	}, [
		open,
		hasSelectedValue,
		hasNullItemLabel,
		selectionRef,
		timeoutMouseDown,
		selectedDelayTimeout,
		unselectedDelayTimeout
	]);
	const ariaControlsId = import_react.useMemo(() => {
		return listElement?.id ?? getFloatingFocusElement(positionerElement)?.id;
	}, [listElement, positionerElement]);
	const props = mergeProps(triggerProps, {
		id,
		role: "combobox",
		"aria-expanded": open ? "true" : "false",
		"aria-haspopup": "listbox",
		"aria-controls": open ? ariaControlsId : void 0,
		"aria-labelledby": ariaLabelledBy,
		"aria-readonly": readOnly || void 0,
		"aria-required": required || void 0,
		tabIndex: disabled ? -1 : 0,
		ref: mergedRef,
		onFocus(event) {
			setFocused(true);
			if (open && alignItemWithTriggerActiveRef.current) setOpen(false, createChangeEventDetails(none, event.nativeEvent));
			timeoutFocus.start(0, () => {
				store.set("forceMount", true);
			});
		},
		onBlur(event) {
			if (contains(positionerElement, event.relatedTarget)) return;
			setTouched(true);
			setFocused(false);
			if (validationMode === "onBlur") validation.commit(value);
		},
		onPointerMove() {
			keyboardActiveRef.current = false;
		},
		onKeyDown() {
			keyboardActiveRef.current = true;
		},
		onMouseDown(event) {
			if (open) return;
			const doc = ownerDocument(event.currentTarget);
			function handleMouseUp(mouseEvent) {
				if (!triggerRef.current) return;
				const mouseUpTarget = mouseEvent.target;
				if (contains(triggerRef.current, mouseUpTarget) || contains(positionerRef.current, mouseUpTarget) || mouseUpTarget === triggerRef.current) return;
				const bounds = getPseudoElementBounds(triggerRef.current);
				if (mouseEvent.clientX >= bounds.left - BOUNDARY_OFFSET && mouseEvent.clientX <= bounds.right + BOUNDARY_OFFSET && mouseEvent.clientY >= bounds.top - BOUNDARY_OFFSET && mouseEvent.clientY <= bounds.bottom + BOUNDARY_OFFSET) return;
				setOpen(false, createChangeEventDetails(cancelOpen, mouseEvent));
			}
			timeoutMouseDown.start(0, () => {
				doc.addEventListener("mouseup", handleMouseUp, { once: true });
			});
		}
	}, validation.getValidationProps, elementProps, getButtonProps);
	props.role = "combobox";
	const state = {
		...fieldState,
		open,
		disabled,
		value,
		readOnly,
		placeholder: !hasSelectedValue
	};
	return useRenderElement("button", componentProps, {
		ref: [forwardedRef, triggerRef],
		state,
		stateAttributesMapping: stateAttributesMapping$6,
		props
	});
});
SelectTrigger.displayName = "SelectTrigger";
//#endregion
//#region node_modules/@base-ui/react/esm/select/value/SelectValue.js
var stateAttributesMapping$5 = { value: () => null };
/**
* A text label of the currently selected item.
* Renders a `<span>` element.
*
* Documentation: [Base UI Select](https://base-ui.com/react/components/select)
*/
var SelectValue = /* @__PURE__ */ import_react.forwardRef(function SelectValue(componentProps, forwardedRef) {
	const { className, render, children: childrenProp, placeholder, ...elementProps } = componentProps;
	const { store, valueRef } = useSelectRootContext();
	const value = useStore(store, selectors$1.value);
	const items = useStore(store, selectors$1.items);
	const itemToStringLabel = useStore(store, selectors$1.itemToStringLabel);
	const hasSelectedValue = useStore(store, selectors$1.hasSelectedValue);
	const shouldCheckNullItemLabel = !hasSelectedValue && placeholder != null && childrenProp == null;
	const hasNullLabel = useStore(store, selectors$1.hasNullItemLabel, shouldCheckNullItemLabel);
	const state = {
		value,
		placeholder: !hasSelectedValue
	};
	let children = null;
	if (typeof childrenProp === "function") children = childrenProp(value);
	else if (childrenProp != null) children = childrenProp;
	else if (!hasSelectedValue && placeholder != null && !hasNullLabel) children = placeholder;
	else if (Array.isArray(value)) children = resolveMultipleLabels(value, items, itemToStringLabel);
	else children = resolveSelectedLabel(value, items, itemToStringLabel);
	return useRenderElement("span", componentProps, {
		state,
		ref: [forwardedRef, valueRef],
		props: [{ children }, elementProps],
		stateAttributesMapping: stateAttributesMapping$5
	});
});
SelectValue.displayName = "SelectValue";
//#endregion
//#region node_modules/@base-ui/react/esm/select/icon/SelectIcon.js
/**
* An icon that indicates that the trigger button opens a select popup.
* Renders a `<span>` element.
*
* Documentation: [Base UI Select](https://base-ui.com/react/components/select)
*/
var SelectIcon = /* @__PURE__ */ import_react.forwardRef(function SelectIcon(componentProps, forwardedRef) {
	const { className, render, ...elementProps } = componentProps;
	const { store } = useSelectRootContext();
	return useRenderElement("span", componentProps, {
		state: { open: useStore(store, selectors$1.open) },
		ref: forwardedRef,
		props: [{
			"aria-hidden": true,
			children: "▼"
		}, elementProps],
		stateAttributesMapping: triggerOpenStateMapping$1
	});
});
SelectIcon.displayName = "SelectIcon";
//#endregion
//#region node_modules/@base-ui/react/esm/select/portal/SelectPortalContext.js
var SelectPortalContext = /* @__PURE__ */ import_react.createContext(void 0);
SelectPortalContext.displayName = "SelectPortalContext";
//#endregion
//#region node_modules/@base-ui/react/esm/select/portal/SelectPortal.js
/**
* A portal element that moves the popup to a different part of the DOM.
* By default, the portal element is appended to `<body>`.
* Renders a `<div>` element.
*
* Documentation: [Base UI Select](https://base-ui.com/react/components/select)
*/
var SelectPortal = /* @__PURE__ */ import_react.forwardRef(function SelectPortal(portalProps, forwardedRef) {
	const { store } = useSelectRootContext();
	const mounted = useStore(store, selectors$1.mounted);
	const forceMount = useStore(store, selectors$1.forceMount);
	if (!(mounted || forceMount)) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectPortalContext.Provider, {
		value: true,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FloatingPortal, {
			ref: forwardedRef,
			...portalProps
		})
	});
});
SelectPortal.displayName = "SelectPortal";
//#endregion
//#region node_modules/@base-ui/react/esm/select/backdrop/SelectBackdrop.js
var stateAttributesMapping$4 = {
	...popupStateMapping,
	...transitionStatusMapping
};
/**
* An overlay displayed beneath the menu popup.
* Renders a `<div>` element.
*
* Documentation: [Base UI Select](https://base-ui.com/react/components/select)
*/
var SelectBackdrop = /* @__PURE__ */ import_react.forwardRef(function SelectBackdrop(componentProps, forwardedRef) {
	const { className, render, ...elementProps } = componentProps;
	const { store } = useSelectRootContext();
	const open = useStore(store, selectors$1.open);
	const mounted = useStore(store, selectors$1.mounted);
	return useRenderElement("div", componentProps, {
		state: {
			open,
			transitionStatus: useStore(store, selectors$1.transitionStatus)
		},
		ref: forwardedRef,
		props: [{
			role: "presentation",
			hidden: !mounted,
			style: {
				userSelect: "none",
				WebkitUserSelect: "none"
			}
		}, elementProps],
		stateAttributesMapping: stateAttributesMapping$4
	});
});
SelectBackdrop.displayName = "SelectBackdrop";
//#endregion
//#region node_modules/@base-ui/react/esm/select/positioner/SelectPositionerContext.js
var SelectPositionerContext = /* @__PURE__ */ import_react.createContext(void 0);
SelectPositionerContext.displayName = "SelectPositionerContext";
function useSelectPositionerContext() {
	const context = import_react.useContext(SelectPositionerContext);
	if (!context) throw new Error("Base UI: SelectPositionerContext is missing. SelectPositioner parts must be placed within <Select.Positioner>.");
	return context;
}
//#endregion
//#region node_modules/@base-ui/react/esm/select/popup/utils.js
function clearStyles(element, originalStyles) {
	if (element) Object.assign(element.style, originalStyles);
}
var LIST_FUNCTIONAL_STYLES = {
	position: "relative",
	maxHeight: "100%",
	overflowX: "hidden",
	overflowY: "auto"
};
//#endregion
//#region node_modules/@base-ui/react/esm/select/positioner/SelectPositioner.js
var FIXED = { position: "fixed" };
/**
* Positions the select popup.
* Renders a `<div>` element.
*
* Documentation: [Base UI Select](https://base-ui.com/react/components/select)
*/
var SelectPositioner = /* @__PURE__ */ import_react.forwardRef(function SelectPositioner(componentProps, forwardedRef) {
	const { anchor, positionMethod = "absolute", className, render, side = "bottom", align = "center", sideOffset = 0, alignOffset = 0, collisionBoundary = "clipping-ancestors", collisionPadding, arrowPadding = 5, sticky = false, disableAnchorTracking, alignItemWithTrigger = true, collisionAvoidance = DROPDOWN_COLLISION_AVOIDANCE, ...elementProps } = componentProps;
	const { store, listRef, labelsRef, alignItemWithTriggerActiveRef, selectedItemTextRef, valuesRef, initialValueRef, popupRef, setValue } = useSelectRootContext();
	const floatingRootContext = useSelectFloatingContext();
	const open = useStore(store, selectors$1.open);
	const mounted = useStore(store, selectors$1.mounted);
	const modal = useStore(store, selectors$1.modal);
	const value = useStore(store, selectors$1.value);
	const openMethod = useStore(store, selectors$1.openMethod);
	const positionerElement = useStore(store, selectors$1.positionerElement);
	const triggerElement = useStore(store, selectors$1.triggerElement);
	const isItemEqualToValue = useStore(store, selectors$1.isItemEqualToValue);
	const transitionStatus = useStore(store, selectors$1.transitionStatus);
	const scrollUpArrowRef = import_react.useRef(null);
	const scrollDownArrowRef = import_react.useRef(null);
	const [controlledAlignItemWithTrigger, setControlledAlignItemWithTrigger] = import_react.useState(alignItemWithTrigger);
	const alignItemWithTriggerActive = mounted && controlledAlignItemWithTrigger && openMethod !== "touch";
	if (!mounted && controlledAlignItemWithTrigger !== alignItemWithTrigger) setControlledAlignItemWithTrigger(alignItemWithTrigger);
	useIsoLayoutEffect(() => {
		if (!mounted) {
			if (selectors$1.scrollUpArrowVisible(store.state)) store.set("scrollUpArrowVisible", false);
			if (selectors$1.scrollDownArrowVisible(store.state)) store.set("scrollDownArrowVisible", false);
		}
	}, [store, mounted]);
	import_react.useImperativeHandle(alignItemWithTriggerActiveRef, () => alignItemWithTriggerActive);
	useScrollLock((alignItemWithTriggerActive || modal) && open && openMethod !== "touch", triggerElement);
	const positioning = useAnchorPositioning({
		anchor,
		floatingRootContext,
		positionMethod,
		mounted,
		side,
		sideOffset,
		align,
		alignOffset,
		arrowPadding,
		collisionBoundary,
		collisionPadding,
		sticky,
		disableAnchorTracking: disableAnchorTracking ?? alignItemWithTriggerActive,
		collisionAvoidance,
		keepMounted: true
	});
	const renderedSide = alignItemWithTriggerActive ? "none" : positioning.side;
	const positionerStyles = alignItemWithTriggerActive ? FIXED : positioning.positionerStyles;
	const defaultProps = import_react.useMemo(() => {
		const hiddenStyles = {};
		if (!open) hiddenStyles.pointerEvents = "none";
		return {
			role: "presentation",
			hidden: !mounted,
			style: {
				...positionerStyles,
				...hiddenStyles
			}
		};
	}, [
		open,
		mounted,
		positionerStyles
	]);
	const state = {
		open,
		side: renderedSide,
		align: positioning.align,
		anchorHidden: positioning.anchorHidden
	};
	const element = useRenderElement("div", componentProps, {
		ref: [forwardedRef, useStableCallback((element) => {
			store.set("positionerElement", element);
		})],
		state,
		stateAttributesMapping: popupStateMapping,
		props: [
			defaultProps,
			getDisabledMountTransitionStyles(transitionStatus),
			elementProps
		]
	});
	const prevMapSizeRef = import_react.useRef(0);
	const onMapChange = useStableCallback((map) => {
		if (map.size === 0 && prevMapSizeRef.current === 0) return;
		if (valuesRef.current.length === 0) return;
		const prevSize = prevMapSizeRef.current;
		prevMapSizeRef.current = map.size;
		if (map.size === prevSize) return;
		const eventDetails = createChangeEventDetails(none);
		if (prevSize !== 0 && !store.state.multiple && value !== null) {
			if (findItemIndex(valuesRef.current, value, isItemEqualToValue) === -1) {
				const initialSelectedValue = initialValueRef.current;
				const nextValue = initialSelectedValue != null && findItemIndex(valuesRef.current, initialSelectedValue, isItemEqualToValue) !== -1 ? initialSelectedValue : null;
				setValue(nextValue, eventDetails);
				if (nextValue === null) {
					store.set("selectedIndex", null);
					selectedItemTextRef.current = null;
				}
			}
		}
		if (prevSize !== 0 && store.state.multiple && Array.isArray(value)) {
			const hasVisibleItem = (selectedItemValue) => findItemIndex(valuesRef.current, selectedItemValue, isItemEqualToValue) !== -1;
			const nextValue = value.filter((selectedItemValue) => hasVisibleItem(selectedItemValue));
			if (nextValue.length !== value.length || nextValue.some((selectedItemValue) => !selectedValueIncludes(value, selectedItemValue, isItemEqualToValue))) {
				setValue(nextValue, eventDetails);
				if (nextValue.length === 0) {
					store.set("selectedIndex", null);
					selectedItemTextRef.current = null;
				}
			}
		}
		if (open && alignItemWithTriggerActive) {
			store.update({
				scrollUpArrowVisible: false,
				scrollDownArrowVisible: false
			});
			const stylesToClear = { height: "" };
			clearStyles(positionerElement, stylesToClear);
			clearStyles(popupRef.current, stylesToClear);
		}
	});
	const contextValue = import_react.useMemo(() => ({
		...positioning,
		side: renderedSide,
		alignItemWithTriggerActive,
		setControlledAlignItemWithTrigger,
		scrollUpArrowRef,
		scrollDownArrowRef
	}), [
		positioning,
		renderedSide,
		alignItemWithTriggerActive,
		setControlledAlignItemWithTrigger
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CompositeList, {
		elementsRef: listRef,
		labelsRef,
		onMapChange,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(SelectPositionerContext.Provider, {
			value: contextValue,
			children: [mounted && modal && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(InternalBackdrop, {
				inert: inertValue(!open),
				cutout: triggerElement
			}), element]
		})
	});
});
SelectPositioner.displayName = "SelectPositioner";
//#endregion
//#region node_modules/@base-ui/utils/esm/isMouseWithinBounds.js
function isMouseWithinBounds(event) {
	const targetRect = event.currentTarget.getBoundingClientRect();
	return targetRect.top + 1 <= event.clientY && event.clientY <= targetRect.bottom - 1 && targetRect.left + 1 <= event.clientX && event.clientX <= targetRect.right - 1;
}
//#endregion
//#region node_modules/@base-ui/react/esm/select/popup/SelectPopup.js
var SCROLL_EPS_PX = 1;
var stateAttributesMapping$3 = {
	...popupStateMapping,
	...transitionStatusMapping
};
/**
* A container for the select list.
* Renders a `<div>` element.
*
* Documentation: [Base UI Select](https://base-ui.com/react/components/select)
*/
var SelectPopup = /* @__PURE__ */ import_react.forwardRef(function SelectPopup(componentProps, forwardedRef) {
	const { render, className, finalFocus, ...elementProps } = componentProps;
	const { store, popupRef, onOpenChangeComplete, setOpen, valueRef, selectedItemTextRef, keyboardActiveRef, multiple, handleScrollArrowVisibility, scrollHandlerRef, highlightItemOnHover } = useSelectRootContext();
	const { side, align, alignItemWithTriggerActive, setControlledAlignItemWithTrigger, scrollDownArrowRef, scrollUpArrowRef } = useSelectPositionerContext();
	const insideToolbar = useToolbarRootContext(true) != null;
	const floatingRootContext = useSelectFloatingContext();
	const { nonce, disableStyleElements } = useCSPContext();
	const highlightTimeout = useTimeout();
	const id = useStore(store, selectors$1.id);
	const open = useStore(store, selectors$1.open);
	const mounted = useStore(store, selectors$1.mounted);
	const popupProps = useStore(store, selectors$1.popupProps);
	const transitionStatus = useStore(store, selectors$1.transitionStatus);
	const triggerElement = useStore(store, selectors$1.triggerElement);
	const positionerElement = useStore(store, selectors$1.positionerElement);
	const listElement = useStore(store, selectors$1.listElement);
	const initialHeightRef = import_react.useRef(0);
	const reachedMaxHeightRef = import_react.useRef(false);
	const maxHeightRef = import_react.useRef(0);
	const initialPlacedRef = import_react.useRef(false);
	const originalPositionerStylesRef = import_react.useRef({});
	const scrollArrowFrame = useAnimationFrame();
	const handleScroll = useStableCallback((scroller) => {
		if (!positionerElement || !popupRef.current || !initialPlacedRef.current) return;
		if (reachedMaxHeightRef.current || !alignItemWithTriggerActive) {
			handleScrollArrowVisibility();
			return;
		}
		const isTopPositioned = positionerElement.style.top === "0px";
		const isBottomPositioned = positionerElement.style.bottom === "0px";
		const currentHeight = positionerElement.getBoundingClientRect().height;
		const doc = ownerDocument(positionerElement);
		const positionerStyles = getComputedStyle(positionerElement);
		const marginTop = parseFloat(positionerStyles.marginTop);
		const marginBottom = parseFloat(positionerStyles.marginBottom);
		const maxPopupHeight = getMaxPopupHeight(getComputedStyle(popupRef.current));
		const maxAvailableHeight = Math.min(doc.documentElement.clientHeight - marginTop - marginBottom, maxPopupHeight);
		const scrollTop = scroller.scrollTop;
		const maxScrollTop = getMaxScrollTop(scroller);
		let nextPositionerHeight = 0;
		let nextScrollTop = null;
		let setReachedMax = false;
		let scrollToMax = false;
		const setHeight = (height) => {
			positionerElement.style.height = `${height}px`;
		};
		const handleSmallDiff = (diff, targetScrollTop) => {
			const heightDelta = clamp(diff, 0, maxAvailableHeight - currentHeight);
			if (heightDelta > 0) setHeight(currentHeight + heightDelta);
			scroller.scrollTop = targetScrollTop;
			if (maxAvailableHeight - (currentHeight + heightDelta) <= SCROLL_EPS_PX) reachedMaxHeightRef.current = true;
			handleScrollArrowVisibility();
		};
		if (isTopPositioned) {
			const diff = maxScrollTop - scrollTop;
			const idealHeight = currentHeight + diff;
			const nextHeight = Math.min(idealHeight, maxAvailableHeight);
			nextPositionerHeight = nextHeight;
			if (diff <= SCROLL_EPS_PX) {
				handleSmallDiff(diff, maxScrollTop);
				return;
			}
			if (maxAvailableHeight - nextHeight > SCROLL_EPS_PX) scrollToMax = true;
			else setReachedMax = true;
		} else if (isBottomPositioned) {
			const diff = scrollTop;
			const idealHeight = currentHeight + diff;
			const nextHeight = Math.min(idealHeight, maxAvailableHeight);
			const overshoot = idealHeight - maxAvailableHeight;
			nextPositionerHeight = nextHeight;
			if (diff <= SCROLL_EPS_PX) {
				handleSmallDiff(diff, 0);
				return;
			}
			if (maxAvailableHeight - nextHeight > SCROLL_EPS_PX) nextScrollTop = 0;
			else {
				setReachedMax = true;
				if (scrollTop < maxScrollTop) nextScrollTop = scrollTop - (diff - overshoot);
			}
		}
		nextPositionerHeight = Math.ceil(nextPositionerHeight);
		if (nextPositionerHeight !== 0) setHeight(nextPositionerHeight);
		if (scrollToMax || nextScrollTop != null) {
			const nextMaxScrollTop = getMaxScrollTop(scroller);
			const target = scrollToMax ? nextMaxScrollTop : clamp(nextScrollTop, 0, nextMaxScrollTop);
			if (Math.abs(scroller.scrollTop - target) > SCROLL_EPS_PX) scroller.scrollTop = target;
		}
		if (setReachedMax || nextPositionerHeight >= maxAvailableHeight - SCROLL_EPS_PX) reachedMaxHeightRef.current = true;
		handleScrollArrowVisibility();
	});
	import_react.useImperativeHandle(scrollHandlerRef, () => handleScroll, [handleScroll]);
	useOpenChangeComplete({
		open,
		ref: popupRef,
		onComplete() {
			if (open) onOpenChangeComplete?.(true);
		}
	});
	const state = {
		open,
		transitionStatus,
		side,
		align
	};
	useIsoLayoutEffect(() => {
		if (!positionerElement || !popupRef.current || Object.keys(originalPositionerStylesRef.current).length) return;
		originalPositionerStylesRef.current = {
			top: positionerElement.style.top || "0",
			left: positionerElement.style.left || "0",
			right: positionerElement.style.right,
			height: positionerElement.style.height,
			bottom: positionerElement.style.bottom,
			minHeight: positionerElement.style.minHeight,
			maxHeight: positionerElement.style.maxHeight,
			marginTop: positionerElement.style.marginTop,
			marginBottom: positionerElement.style.marginBottom
		};
	}, [popupRef, positionerElement]);
	useIsoLayoutEffect(() => {
		if (open || alignItemWithTriggerActive) return;
		initialPlacedRef.current = false;
		reachedMaxHeightRef.current = false;
		initialHeightRef.current = 0;
		maxHeightRef.current = 0;
		clearStyles(positionerElement, originalPositionerStylesRef.current);
	}, [
		open,
		alignItemWithTriggerActive,
		positionerElement,
		popupRef
	]);
	useIsoLayoutEffect(() => {
		const popupElement = popupRef.current;
		if (!open || !triggerElement || !positionerElement || !popupElement || store.state.transitionStatus === "ending") return;
		if (!alignItemWithTriggerActive) {
			initialPlacedRef.current = true;
			scrollArrowFrame.request(handleScrollArrowVisibility);
			popupElement.style.removeProperty("--transform-origin");
			return;
		}
		queueMicrotask(() => {
			const restoreTransformStyles = unsetTransformStyles(popupElement);
			popupElement.style.removeProperty("--transform-origin");
			try {
				const positionerStyles = getComputedStyle(positionerElement);
				const popupStyles = getComputedStyle(popupElement);
				const doc = ownerDocument(triggerElement);
				const win = getWindow(positionerElement);
				const scale = getScale(triggerElement);
				const triggerRect = normalizeRect(triggerElement.getBoundingClientRect(), scale);
				const positionerRect = normalizeRect(positionerElement.getBoundingClientRect(), scale);
				const triggerX = triggerRect.left;
				const triggerHeight = triggerRect.height;
				const scroller = listElement || popupElement;
				const scrollHeight = scroller.scrollHeight;
				const borderBottom = parseFloat(popupStyles.borderBottomWidth);
				const marginTop = parseFloat(positionerStyles.marginTop) || 10;
				const marginBottom = parseFloat(positionerStyles.marginBottom) || 10;
				const minHeight = parseFloat(positionerStyles.minHeight) || 100;
				const maxPopupHeight = getMaxPopupHeight(popupStyles);
				const paddingLeft = 5;
				const paddingRight = 5;
				const triggerCollisionThreshold = 20;
				const viewportHeight = doc.documentElement.clientHeight - marginTop - marginBottom;
				const viewportWidth = doc.documentElement.clientWidth;
				const availableSpaceBeneathTrigger = viewportHeight - triggerRect.bottom + triggerHeight;
				const textElement = selectedItemTextRef.current;
				const valueElement = valueRef.current;
				let textRect;
				let offsetX = 0;
				let offsetY = 0;
				if (textElement && valueElement) {
					const valueRect = normalizeRect(valueElement.getBoundingClientRect(), scale);
					textRect = normalizeRect(textElement.getBoundingClientRect(), scale);
					const valueLeftFromTriggerLeft = valueRect.left - triggerX;
					const textLeftFromPositionerLeft = textRect.left - positionerRect.left;
					const valueCenterFromPositionerTop = valueRect.top - triggerRect.top + valueRect.height / 2;
					const textCenterFromTriggerTop = textRect.top - positionerRect.top + textRect.height / 2;
					offsetX = valueLeftFromTriggerLeft - textLeftFromPositionerLeft;
					offsetY = textCenterFromTriggerTop - valueCenterFromPositionerTop;
				}
				const idealHeight = availableSpaceBeneathTrigger + offsetY + marginBottom + borderBottom;
				let height = Math.min(viewportHeight, idealHeight);
				const maxHeight = viewportHeight - marginTop - marginBottom;
				const scrollTop = idealHeight - height;
				const left = Math.max(paddingLeft, triggerX + offsetX);
				const maxRight = viewportWidth - paddingRight;
				const rightOverflow = Math.max(0, left + positionerRect.width - maxRight);
				positionerElement.style.left = `${left - rightOverflow}px`;
				positionerElement.style.height = `${height}px`;
				positionerElement.style.maxHeight = "auto";
				positionerElement.style.marginTop = `${marginTop}px`;
				positionerElement.style.marginBottom = `${marginBottom}px`;
				popupElement.style.height = "100%";
				const maxScrollTop = getMaxScrollTop(scroller);
				const isTopPositioned = scrollTop >= maxScrollTop - SCROLL_EPS_PX;
				if (isTopPositioned) height = Math.min(viewportHeight, positionerRect.height) - (scrollTop - maxScrollTop);
				const fallbackToAlignPopupToTrigger = triggerRect.top < triggerCollisionThreshold || triggerRect.bottom > viewportHeight - triggerCollisionThreshold || Math.ceil(height) + SCROLL_EPS_PX < Math.min(scrollHeight, minHeight);
				const isPinchZoomed = (win.visualViewport?.scale ?? 1) !== 1 && isWebKit;
				if (fallbackToAlignPopupToTrigger || isPinchZoomed) {
					initialPlacedRef.current = true;
					clearStyles(positionerElement, originalPositionerStylesRef.current);
					import_react_dom.flushSync(() => setControlledAlignItemWithTrigger(false));
					return;
				}
				if (isTopPositioned) {
					const topOffset = Math.max(0, viewportHeight - idealHeight);
					positionerElement.style.top = positionerRect.height >= maxHeight ? "0" : `${topOffset}px`;
					positionerElement.style.height = `${height}px`;
					scroller.scrollTop = getMaxScrollTop(scroller);
					initialHeightRef.current = Math.max(minHeight, height);
				} else {
					positionerElement.style.bottom = "0";
					initialHeightRef.current = Math.max(minHeight, height);
					scroller.scrollTop = scrollTop;
				}
				if (textRect) {
					const popupTop = positionerRect.top;
					const popupHeight = positionerRect.height;
					const textCenterY = textRect.top + textRect.height / 2;
					const clampedY = clamp(popupHeight > 0 ? (textCenterY - popupTop) / popupHeight * 100 : 50, 0, 100);
					popupElement.style.setProperty("--transform-origin", `50% ${clampedY}%`);
				}
				if (initialHeightRef.current === viewportHeight || height >= maxPopupHeight) reachedMaxHeightRef.current = true;
				handleScrollArrowVisibility();
				setTimeout(() => {
					initialPlacedRef.current = true;
				});
			} finally {
				restoreTransformStyles();
			}
		});
	}, [
		store,
		open,
		positionerElement,
		triggerElement,
		valueRef,
		selectedItemTextRef,
		popupRef,
		handleScrollArrowVisibility,
		alignItemWithTriggerActive,
		setControlledAlignItemWithTrigger,
		scrollArrowFrame,
		scrollDownArrowRef,
		scrollUpArrowRef,
		listElement
	]);
	import_react.useEffect(() => {
		if (!alignItemWithTriggerActive || !positionerElement || !open) return;
		const win = getWindow(positionerElement);
		function handleResize(event) {
			setOpen(false, createChangeEventDetails(windowResize, event));
		}
		win.addEventListener("resize", handleResize);
		return () => {
			win.removeEventListener("resize", handleResize);
		};
	}, [
		setOpen,
		alignItemWithTriggerActive,
		positionerElement,
		open
	]);
	const defaultProps = {
		...listElement ? {
			role: "presentation",
			"aria-orientation": void 0
		} : {
			role: "listbox",
			"aria-multiselectable": multiple || void 0,
			id: `${id}-list`
		},
		onKeyDown(event) {
			keyboardActiveRef.current = true;
			if (insideToolbar && COMPOSITE_KEYS.has(event.key)) event.stopPropagation();
		},
		onMouseMove() {
			keyboardActiveRef.current = false;
		},
		onPointerLeave(event) {
			if (!highlightItemOnHover || isMouseWithinBounds(event) || event.pointerType === "touch") return;
			const popup = event.currentTarget;
			highlightTimeout.start(0, () => {
				store.set("activeIndex", null);
				popup.focus({ preventScroll: true });
			});
		},
		onScroll(event) {
			if (listElement) return;
			handleScroll(event.currentTarget);
		},
		...alignItemWithTriggerActive && { style: listElement ? { height: "100%" } : LIST_FUNCTIONAL_STYLES }
	};
	const element = useRenderElement("div", componentProps, {
		ref: [forwardedRef, popupRef],
		state,
		stateAttributesMapping: stateAttributesMapping$3,
		props: [
			popupProps,
			defaultProps,
			getDisabledMountTransitionStyles(transitionStatus),
			{ className: !listElement && alignItemWithTriggerActive ? styleDisableScrollbar.className : void 0 },
			elementProps
		]
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [!disableStyleElements && styleDisableScrollbar.getElement(nonce), /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FloatingFocusManager, {
		context: floatingRootContext,
		modal: false,
		disabled: !mounted,
		returnFocus: finalFocus,
		restoreFocus: true,
		children: element
	})] });
});
SelectPopup.displayName = "SelectPopup";
function getMaxPopupHeight(popupStyles) {
	const maxHeightStyle = popupStyles.maxHeight || "";
	return maxHeightStyle.endsWith("px") ? parseFloat(maxHeightStyle) || Infinity : Infinity;
}
function getMaxScrollTop(scroller) {
	return Math.max(0, scroller.scrollHeight - scroller.clientHeight);
}
function getScale(element) {
	return platform.getScale(element);
}
function normalizeRect(rect, scale) {
	return rectToClientRect({
		x: rect.x / scale.x,
		y: rect.y / scale.y,
		width: rect.width / scale.x,
		height: rect.height / scale.y
	});
}
var TRANSFORM_STYLE_RESETS = [
	["transform", "none"],
	["scale", "1"],
	["translate", "0 0"]
];
function unsetTransformStyles(popupElement) {
	const { style } = popupElement;
	const originalStyles = {};
	for (const [property, value] of TRANSFORM_STYLE_RESETS) {
		originalStyles[property] = style.getPropertyValue(property);
		style.setProperty(property, value, "important");
	}
	return () => {
		for (const [property] of TRANSFORM_STYLE_RESETS) {
			const originalValue = originalStyles[property];
			if (originalValue) style.setProperty(property, originalValue);
			else style.removeProperty(property);
		}
	};
}
//#endregion
//#region node_modules/@base-ui/react/esm/select/list/SelectList.js
/**
* A container for the select items.
* Renders a `<div>` element.
*
* Documentation: [Base UI Select](https://base-ui.com/react/components/select)
*/
var SelectList = /* @__PURE__ */ import_react.forwardRef(function SelectList(componentProps, forwardedRef) {
	const { className, render, ...elementProps } = componentProps;
	const { store, scrollHandlerRef } = useSelectRootContext();
	const { alignItemWithTriggerActive } = useSelectPositionerContext();
	const hasScrollArrows = useStore(store, selectors$1.hasScrollArrows);
	const openMethod = useStore(store, selectors$1.openMethod);
	const multiple = useStore(store, selectors$1.multiple);
	const defaultProps = {
		id: `${useStore(store, selectors$1.id)}-list`,
		role: "listbox",
		"aria-multiselectable": multiple || void 0,
		onScroll(event) {
			scrollHandlerRef.current?.(event.currentTarget);
		},
		...alignItemWithTriggerActive && { style: LIST_FUNCTIONAL_STYLES },
		className: hasScrollArrows && openMethod !== "touch" ? styleDisableScrollbar.className : void 0
	};
	return useRenderElement("div", componentProps, {
		ref: [forwardedRef, useStableCallback((element) => {
			store.set("listElement", element);
		})],
		props: [defaultProps, elementProps]
	});
});
SelectList.displayName = "SelectList";
//#endregion
//#region node_modules/@base-ui/react/esm/select/item/SelectItemContext.js
var SelectItemContext = /* @__PURE__ */ import_react.createContext(void 0);
SelectItemContext.displayName = "SelectItemContext";
function useSelectItemContext() {
	const context = import_react.useContext(SelectItemContext);
	if (!context) throw new Error("Base UI: SelectItemContext is missing. SelectItem parts must be placed within <Select.Item>.");
	return context;
}
//#endregion
//#region node_modules/@base-ui/react/esm/select/item/SelectItem.js
/**
* An individual option in the select popup.
* Renders a `<div>` element.
*
* Documentation: [Base UI Select](https://base-ui.com/react/components/select)
*/
var SelectItem = /* @__PURE__ */ import_react.memo(/* @__PURE__ */ import_react.forwardRef(function SelectItem(componentProps, forwardedRef) {
	const { render, className, value: itemValue = null, label, disabled = false, nativeButton = false, ...elementProps } = componentProps;
	const textRef = import_react.useRef(null);
	const listItem = useCompositeListItem({
		label,
		textRef,
		indexGuessBehavior: IndexGuessBehavior.GuessFromOrder
	});
	const { store, getItemProps, setOpen, setValue, selectionRef, typingRef, valuesRef, keyboardActiveRef, multiple, highlightItemOnHover } = useSelectRootContext();
	const highlightTimeout = useTimeout();
	const highlighted = useStore(store, selectors$1.isActive, listItem.index);
	const selected = useStore(store, selectors$1.isSelected, listItem.index, itemValue);
	const selectedByFocus = useStore(store, selectors$1.isSelectedByFocus, listItem.index);
	const isItemEqualToValue = useStore(store, selectors$1.isItemEqualToValue);
	const index = listItem.index;
	const hasRegistered = index !== -1;
	const itemRef = import_react.useRef(null);
	const indexRef = useValueAsRef(index);
	useIsoLayoutEffect(() => {
		if (!hasRegistered) return;
		const values = valuesRef.current;
		values[index] = itemValue;
		return () => {
			delete values[index];
		};
	}, [
		hasRegistered,
		index,
		itemValue,
		valuesRef
	]);
	useIsoLayoutEffect(() => {
		if (!hasRegistered) return;
		const selectedValue = store.state.value;
		let selectedCandidate = selectedValue;
		if (multiple && Array.isArray(selectedValue) && selectedValue.length > 0) selectedCandidate = selectedValue[selectedValue.length - 1];
		if (selectedCandidate !== void 0 && compareItemEquality(itemValue, selectedCandidate, isItemEqualToValue)) store.set("selectedIndex", index);
	}, [
		hasRegistered,
		index,
		multiple,
		isItemEqualToValue,
		store,
		itemValue
	]);
	const state = {
		disabled,
		selected,
		highlighted
	};
	const rootProps = getItemProps({
		active: highlighted,
		selected
	});
	rootProps.onFocus = void 0;
	rootProps.id = void 0;
	const lastKeyRef = import_react.useRef(null);
	const pointerTypeRef = import_react.useRef("mouse");
	const didPointerDownRef = import_react.useRef(false);
	const { getButtonProps, buttonRef } = useButton({
		disabled,
		focusableWhenDisabled: true,
		native: nativeButton,
		composite: true
	});
	function commitSelection(event) {
		const selectedValue = store.state.value;
		if (multiple) {
			const currentValue = Array.isArray(selectedValue) ? selectedValue : [];
			setValue(selected ? removeItem(currentValue, itemValue, isItemEqualToValue) : [...currentValue, itemValue], createChangeEventDetails(itemPress, event));
		} else {
			setValue(itemValue, createChangeEventDetails(itemPress, event));
			setOpen(false, createChangeEventDetails(itemPress, event));
		}
	}
	const defaultProps = {
		role: "option",
		"aria-selected": selected,
		tabIndex: highlighted ? 0 : -1,
		onFocus() {
			store.set("activeIndex", index);
		},
		onMouseEnter() {
			if (!keyboardActiveRef.current && store.state.selectedIndex === null && highlightItemOnHover) store.set("activeIndex", index);
		},
		onMouseMove() {
			if (highlightItemOnHover) store.set("activeIndex", index);
		},
		onMouseLeave(event) {
			if (!highlightItemOnHover || keyboardActiveRef.current || isMouseWithinBounds(event)) return;
			highlightTimeout.start(0, () => {
				if (store.state.activeIndex === index) store.set("activeIndex", null);
			});
		},
		onTouchStart() {
			selectionRef.current = {
				allowSelectedMouseUp: false,
				allowUnselectedMouseUp: false
			};
		},
		onKeyDown(event) {
			lastKeyRef.current = event.key;
			store.set("activeIndex", index);
			if (event.key === " " && typingRef.current) event.preventDefault();
		},
		onClick(event) {
			didPointerDownRef.current = false;
			if (event.type === "keydown" && lastKeyRef.current === null) return;
			if (disabled || event.type === "keydown" && lastKeyRef.current === " " && typingRef.current || pointerTypeRef.current !== "touch" && !highlighted) return;
			lastKeyRef.current = null;
			commitSelection(event.nativeEvent);
		},
		onPointerEnter(event) {
			pointerTypeRef.current = event.pointerType;
		},
		onPointerDown(event) {
			pointerTypeRef.current = event.pointerType;
			didPointerDownRef.current = true;
		},
		onMouseUp() {
			if (disabled) return;
			if (didPointerDownRef.current) {
				didPointerDownRef.current = false;
				return;
			}
			const disallowSelectedMouseUp = !selectionRef.current.allowSelectedMouseUp && selected;
			const disallowUnselectedMouseUp = !selectionRef.current.allowUnselectedMouseUp && !selected;
			if (disallowSelectedMouseUp || disallowUnselectedMouseUp || pointerTypeRef.current !== "touch" && !highlighted) return;
			itemRef.current?.click();
		}
	};
	const element = useRenderElement("div", componentProps, {
		ref: [
			buttonRef,
			forwardedRef,
			listItem.ref,
			itemRef
		],
		state,
		props: [
			rootProps,
			defaultProps,
			elementProps,
			getButtonProps
		]
	});
	const contextValue = import_react.useMemo(() => ({
		selected,
		indexRef,
		textRef,
		selectedByFocus,
		hasRegistered
	}), [
		selected,
		indexRef,
		textRef,
		selectedByFocus,
		hasRegistered
	]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectItemContext.Provider, {
		value: contextValue,
		children: element
	});
}));
SelectItem.displayName = "SelectItem";
//#endregion
//#region node_modules/@base-ui/react/esm/select/item-indicator/SelectItemIndicator.js
/**
* Indicates whether the select item is selected.
* Renders a `<span>` element.
*
* Documentation: [Base UI Select](https://base-ui.com/react/components/select)
*/
var SelectItemIndicator = /* @__PURE__ */ import_react.forwardRef(function SelectItemIndicator(componentProps, forwardedRef) {
	const keepMounted = componentProps.keepMounted ?? false;
	const { selected } = useSelectItemContext();
	if (!(keepMounted || selected)) return null;
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Inner, {
		...componentProps,
		ref: forwardedRef
	});
});
SelectItemIndicator.displayName = "SelectItemIndicator";
var Inner = /* @__PURE__ */ import_react.memo(/* @__PURE__ */ import_react.forwardRef((componentProps, forwardedRef) => {
	const { render, className, keepMounted, ...elementProps } = componentProps;
	const { selected } = useSelectItemContext();
	const indicatorRef = import_react.useRef(null);
	const { transitionStatus, setMounted } = useTransitionStatus(selected);
	const element = useRenderElement("span", componentProps, {
		ref: [forwardedRef, indicatorRef],
		state: {
			selected,
			transitionStatus
		},
		props: [{
			"aria-hidden": true,
			children: "✔️"
		}, elementProps],
		stateAttributesMapping: transitionStatusMapping
	});
	useOpenChangeComplete({
		open: selected,
		ref: indicatorRef,
		onComplete() {
			if (!selected) setMounted(false);
		}
	});
	return element;
}));
Inner.displayName = "Inner";
//#endregion
//#region node_modules/@base-ui/react/esm/select/item-text/SelectItemText.js
/**
* A text label of the select item.
* Renders a `<div>` element.
*
* Documentation: [Base UI Select](https://base-ui.com/react/components/select)
*/
var SelectItemText = /* @__PURE__ */ import_react.memo(/* @__PURE__ */ import_react.forwardRef(function SelectItemText(componentProps, forwardedRef) {
	const { indexRef, textRef, selectedByFocus, hasRegistered } = useSelectItemContext();
	const { selectedItemTextRef } = useSelectRootContext();
	const { className, render, ...elementProps } = componentProps;
	return useRenderElement("div", componentProps, {
		ref: [
			import_react.useCallback((node) => {
				if (!node || !hasRegistered) return;
				const hasNoSelectedItemText = selectedItemTextRef.current === null || !selectedItemTextRef.current.isConnected;
				if (selectedByFocus || hasNoSelectedItemText && indexRef.current === 0) selectedItemTextRef.current = node;
			}, [
				selectedItemTextRef,
				indexRef,
				selectedByFocus,
				hasRegistered
			]),
			forwardedRef,
			textRef
		],
		props: elementProps
	});
}));
SelectItemText.displayName = "SelectItemText";
//#endregion
//#region node_modules/@base-ui/react/esm/select/arrow/SelectArrow.js
var stateAttributesMapping$2 = {
	...popupStateMapping,
	...transitionStatusMapping
};
/**
* Displays an element positioned against the select popup anchor.
* Renders a `<div>` element.
*
* Documentation: [Base UI Select](https://base-ui.com/react/components/select)
*/
var SelectArrow = /* @__PURE__ */ import_react.forwardRef(function SelectArrow(componentProps, forwardedRef) {
	const { className, render, ...elementProps } = componentProps;
	const { store } = useSelectRootContext();
	const { side, align, arrowRef, arrowStyles, arrowUncentered, alignItemWithTriggerActive } = useSelectPositionerContext();
	const element = useRenderElement("div", componentProps, {
		state: {
			open: useStore(store, selectors$1.open, true),
			side,
			align,
			uncentered: arrowUncentered
		},
		ref: [arrowRef, forwardedRef],
		props: [{
			style: arrowStyles,
			"aria-hidden": true
		}, elementProps],
		stateAttributesMapping: stateAttributesMapping$2
	});
	if (alignItemWithTriggerActive) return null;
	return element;
});
SelectArrow.displayName = "SelectArrow";
//#endregion
//#region node_modules/@base-ui/react/esm/select/scroll-arrow/SelectScrollArrow.js
/**
* @internal
*/
var SelectScrollArrow = /* @__PURE__ */ import_react.forwardRef(function SelectScrollArrow(componentProps, forwardedRef) {
	const { render, className, direction, keepMounted = false, ...elementProps } = componentProps;
	const { store, popupRef, listRef, handleScrollArrowVisibility, scrollArrowsMountedCountRef } = useSelectRootContext();
	const { side, scrollDownArrowRef, scrollUpArrowRef } = useSelectPositionerContext();
	const stateVisible = useStore(store, direction === "up" ? selectors$1.scrollUpArrowVisible : selectors$1.scrollDownArrowVisible);
	const openMethod = useStore(store, selectors$1.openMethod);
	const visible = stateVisible && openMethod !== "touch";
	const timeout = useTimeout();
	const scrollArrowRef = direction === "up" ? scrollUpArrowRef : scrollDownArrowRef;
	const { transitionStatus, setMounted } = useTransitionStatus(visible);
	useIsoLayoutEffect(() => {
		scrollArrowsMountedCountRef.current += 1;
		if (!store.state.hasScrollArrows) store.set("hasScrollArrows", true);
		return () => {
			scrollArrowsMountedCountRef.current = Math.max(0, scrollArrowsMountedCountRef.current - 1);
			if (scrollArrowsMountedCountRef.current === 0 && store.state.hasScrollArrows) store.set("hasScrollArrows", false);
		};
	}, [store, scrollArrowsMountedCountRef]);
	useOpenChangeComplete({
		open: visible,
		ref: scrollArrowRef,
		onComplete() {
			if (!visible) setMounted(false);
		}
	});
	const element = useRenderElement("div", componentProps, {
		ref: [forwardedRef, scrollArrowRef],
		state: {
			direction,
			visible,
			side,
			transitionStatus
		},
		props: [{
			"aria-hidden": true,
			children: direction === "up" ? "▲" : "▼",
			style: { position: "absolute" },
			onMouseMove(event) {
				if (event.movementX === 0 && event.movementY === 0 || timeout.isStarted()) return;
				store.set("activeIndex", null);
				function scrollNextItem() {
					const scroller = store.state.listElement ?? popupRef.current;
					if (!scroller) return;
					store.set("activeIndex", null);
					handleScrollArrowVisibility();
					const isScrolledToTop = scroller.scrollTop === 0;
					const isScrolledToBottom = Math.round(scroller.scrollTop + scroller.clientHeight) >= scroller.scrollHeight;
					if (listRef.current.length === 0) if (direction === "up") store.set("scrollUpArrowVisible", !isScrolledToTop);
					else store.set("scrollDownArrowVisible", !isScrolledToBottom);
					if (direction === "up" && isScrolledToTop || direction === "down" && isScrolledToBottom) {
						timeout.clear();
						return;
					}
					if ((store.state.listElement || popupRef.current) && listRef.current && listRef.current.length > 0) {
						const items = listRef.current;
						const scrollArrowHeight = scrollArrowRef.current?.offsetHeight || 0;
						if (direction === "up") {
							let firstVisibleIndex = 0;
							const scrollTop = scroller.scrollTop + scrollArrowHeight;
							for (let i = 0; i < items.length; i += 1) {
								const item = items[i];
								if (item) {
									if (item.offsetTop >= scrollTop) {
										firstVisibleIndex = i;
										break;
									}
								}
							}
							const targetIndex = Math.max(0, firstVisibleIndex - 1);
							if (targetIndex < firstVisibleIndex) {
								const targetItem = items[targetIndex];
								if (targetItem) scroller.scrollTop = Math.max(0, targetItem.offsetTop - scrollArrowHeight);
							} else scroller.scrollTop = 0;
						} else {
							let lastVisibleIndex = items.length - 1;
							const scrollBottom = scroller.scrollTop + scroller.clientHeight - scrollArrowHeight;
							for (let i = 0; i < items.length; i += 1) {
								const item = items[i];
								if (item) {
									if (item.offsetTop + item.offsetHeight > scrollBottom) {
										lastVisibleIndex = Math.max(0, i - 1);
										break;
									}
								}
							}
							const targetIndex = Math.min(items.length - 1, lastVisibleIndex + 1);
							if (targetIndex > lastVisibleIndex) {
								const targetItem = items[targetIndex];
								if (targetItem) scroller.scrollTop = targetItem.offsetTop + targetItem.offsetHeight - scroller.clientHeight + scrollArrowHeight;
							} else scroller.scrollTop = scroller.scrollHeight - scroller.clientHeight;
						}
					}
					timeout.start(40, scrollNextItem);
				}
				timeout.start(40, scrollNextItem);
			},
			onMouseLeave() {
				timeout.clear();
			}
		}, elementProps]
	});
	if (!(visible || keepMounted)) return null;
	return element;
});
SelectScrollArrow.displayName = "SelectScrollArrow";
//#endregion
//#region node_modules/@base-ui/react/esm/select/scroll-down-arrow/SelectScrollDownArrow.js
/**
* An element that scrolls the select popup down when hovered. Does not render when using touch input.
* Renders a `<div>` element.
*
* Documentation: [Base UI Select](https://base-ui.com/react/components/select)
*/
var SelectScrollDownArrow = /* @__PURE__ */ import_react.forwardRef(function SelectScrollDownArrow(props, forwardedRef) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectScrollArrow, {
		...props,
		ref: forwardedRef,
		direction: "down"
	});
});
SelectScrollDownArrow.displayName = "SelectScrollDownArrow";
//#endregion
//#region node_modules/@base-ui/react/esm/select/scroll-up-arrow/SelectScrollUpArrow.js
/**
* An element that scrolls the select popup up when hovered. Does not render when using touch input.
* Renders a `<div>` element.
*
* Documentation: [Base UI Select](https://base-ui.com/react/components/select)
*/
var SelectScrollUpArrow = /* @__PURE__ */ import_react.forwardRef(function SelectScrollUpArrow(props, forwardedRef) {
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectScrollArrow, {
		...props,
		ref: forwardedRef,
		direction: "up"
	});
});
SelectScrollUpArrow.displayName = "SelectScrollUpArrow";
//#endregion
//#region node_modules/@base-ui/react/esm/select/group/SelectGroupContext.js
var SelectGroupContext = /* @__PURE__ */ import_react.createContext(void 0);
SelectGroupContext.displayName = "SelectGroupContext";
function useSelectGroupContext() {
	const context = import_react.useContext(SelectGroupContext);
	if (context === void 0) throw new Error("Base UI: SelectGroupContext is missing. SelectGroup parts must be placed within <Select.Group>.");
	return context;
}
//#endregion
//#region node_modules/@base-ui/react/esm/select/group/SelectGroup.js
/**
* Groups related select items with the corresponding label.
* Renders a `<div>` element.
*
* Documentation: [Base UI Select](https://base-ui.com/react/components/select)
*/
var SelectGroup = /* @__PURE__ */ import_react.forwardRef(function SelectGroup(componentProps, forwardedRef) {
	const { className, render, ...elementProps } = componentProps;
	const [labelId, setLabelId] = import_react.useState();
	const contextValue = import_react.useMemo(() => ({
		labelId,
		setLabelId
	}), [labelId, setLabelId]);
	const element = useRenderElement("div", componentProps, {
		ref: forwardedRef,
		props: [{
			role: "group",
			"aria-labelledby": labelId
		}, elementProps]
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(SelectGroupContext.Provider, {
		value: contextValue,
		children: element
	});
});
SelectGroup.displayName = "SelectGroup";
//#endregion
//#region node_modules/@base-ui/react/esm/select/group-label/SelectGroupLabel.js
/**
* An accessible label that is automatically associated with its parent group.
* Renders a `<div>` element.
*
* Documentation: [Base UI Select](https://base-ui.com/react/components/select)
*/
var SelectGroupLabel = /* @__PURE__ */ import_react.forwardRef(function SelectGroupLabel(componentProps, forwardedRef) {
	const { className, render, id: idProp, ...elementProps } = componentProps;
	const { setLabelId } = useSelectGroupContext();
	const id = useBaseUiId(idProp);
	useIsoLayoutEffect(() => {
		setLabelId(id);
	}, [id, setLabelId]);
	return useRenderElement("div", componentProps, {
		ref: forwardedRef,
		props: [{ id }, elementProps]
	});
});
SelectGroupLabel.displayName = "SelectGroupLabel";
//#endregion
//#region node_modules/@base-ui/react/esm/select/index.parts.js
var index_parts_exports$21 = /* @__PURE__ */ __exportAll({
	Arrow: () => SelectArrow,
	Backdrop: () => SelectBackdrop,
	Group: () => SelectGroup,
	GroupLabel: () => SelectGroupLabel,
	Icon: () => SelectIcon,
	Item: () => SelectItem,
	ItemIndicator: () => SelectItemIndicator,
	ItemText: () => SelectItemText,
	Label: () => SelectLabel,
	List: () => SelectList,
	Popup: () => SelectPopup,
	Portal: () => SelectPortal,
	Positioner: () => SelectPositioner,
	Root: () => SelectRoot,
	ScrollDownArrow: () => SelectScrollDownArrow,
	ScrollUpArrow: () => SelectScrollUpArrow,
	Separator: () => Separator,
	Trigger: () => SelectTrigger,
	Value: () => SelectValue
});
//#endregion
//#region node_modules/@base-ui/react/esm/toast/provider/ToastProviderContext.js
var ToastContext = /* @__PURE__ */ import_react.createContext(void 0);
ToastContext.displayName = "ToastContext";
function useToastProviderContext() {
	const context = import_react.useContext(ToastContext);
	if (!context) throw new Error("Base UI: useToastManager must be used within <Toast.Provider>.");
	return context;
}
//#endregion
//#region node_modules/@base-ui/utils/esm/generateId.js
var counter = 0;
function generateId(prefix) {
	counter += 1;
	return `${prefix}-${Math.random().toString(36).slice(2, 6)}-${counter}`;
}
//#endregion
//#region node_modules/@base-ui/react/esm/toast/utils/resolvePromiseOptions.js
function resolvePromiseOptions(options, result) {
	if (typeof options === "string") return { description: options };
	if (typeof options === "function") {
		const resolvedOptions = options(result);
		return typeof resolvedOptions === "string" ? { description: resolvedOptions } : resolvedOptions;
	}
	return options;
}
//#endregion
//#region node_modules/@base-ui/react/esm/toast/store.js
var toastMapSelector = createSelectorMemoized((state) => state.toasts, (toasts) => {
	const map = /* @__PURE__ */ new Map();
	let visibleIndex = 0;
	let offsetY = 0;
	toasts.forEach((toast, toastIndex) => {
		const isEnding = toast.transitionStatus === "ending";
		map.set(toast.id, {
			value: toast,
			domIndex: toastIndex,
			visibleIndex: isEnding ? -1 : visibleIndex,
			offsetY
		});
		offsetY += toast.height || 0;
		if (!isEnding) visibleIndex += 1;
	});
	return map;
});
var selectors = {
	toasts: createSelector((state) => state.toasts),
	isEmpty: createSelector((state) => state.toasts.length === 0),
	toast: createSelector(toastMapSelector, (toastMap, id) => toastMap.get(id)?.value),
	toastIndex: createSelector(toastMapSelector, (toastMap, id) => toastMap.get(id)?.domIndex ?? -1),
	toastOffsetY: createSelector(toastMapSelector, (toastMap, id) => toastMap.get(id)?.offsetY ?? 0),
	toastVisibleIndex: createSelector(toastMapSelector, (toastMap, id) => toastMap.get(id)?.visibleIndex ?? -1),
	hovering: createSelector((state) => state.hovering),
	focused: createSelector((state) => state.focused),
	expanded: createSelector((state) => state.hovering || state.focused),
	expandedOrOutOfFocus: createSelector((state) => state.hovering || state.focused || !state.isWindowFocused),
	prevFocusElement: createSelector((state) => state.prevFocusElement)
};
var ToastStore = class extends ReactStore {
	timers = /* @__PURE__ */ new Map();
	areTimersPaused = false;
	constructor(initialState) {
		super(initialState, {}, selectors);
	}
	setFocused(focused) {
		this.set("focused", focused);
	}
	setHovering(hovering) {
		this.set("hovering", hovering);
	}
	setIsWindowFocused(isWindowFocused) {
		this.set("isWindowFocused", isWindowFocused);
	}
	setPrevFocusElement(prevFocusElement) {
		this.set("prevFocusElement", prevFocusElement);
	}
	setViewport = (viewport) => {
		this.set("viewport", viewport);
	};
	disposeEffect = () => {
		return () => {
			this.timers.forEach((timer) => {
				timer.timeout?.clear();
			});
			this.timers.clear();
		};
	};
	removeToast(toastId) {
		const index = selectors.toastIndex(this.state, toastId);
		if (index === -1) return;
		this.state.toasts[index]?.onRemove?.();
		const newToasts = [...this.state.toasts];
		newToasts.splice(index, 1);
		this.setToasts(newToasts);
	}
	addToast = (toast) => {
		const { toasts, timeout, limit } = this.state;
		const id = toast.id || generateId("toast");
		const toastToAdd = {
			...toast,
			id,
			transitionStatus: "starting"
		};
		const updatedToasts = [toastToAdd, ...toasts];
		const activeToasts = updatedToasts.filter((t) => t.transitionStatus !== "ending");
		if (activeToasts.length > limit) {
			const excessCount = activeToasts.length - limit;
			const oldestActiveToasts = activeToasts.slice(-excessCount);
			const limitedIds = new Set(oldestActiveToasts.map((t) => t.id));
			this.setToasts(updatedToasts.map((t) => {
				const limited = limitedIds.has(t.id);
				if (t.limited !== limited) return {
					...t,
					limited
				};
				return t;
			}));
		} else this.setToasts(updatedToasts.map((t) => t.limited ? {
			...t,
			limited: false
		} : t));
		const duration = toastToAdd.timeout ?? timeout;
		if (toastToAdd.type !== "loading" && duration > 0) this.scheduleTimer(id, duration, () => this.closeToast(id));
		if (selectors.expandedOrOutOfFocus(this.state)) this.pauseTimers();
		return id;
	};
	updateToast = (id, updates) => {
		this.updateToastInternal(id, updates);
	};
	updateToastInternal = (id, updates) => {
		const { timeout, toasts } = this.state;
		const prevToast = selectors.toast(this.state, id) ?? null;
		if (!prevToast) return;
		if (prevToast.transitionStatus === "ending") return;
		const nextToast = {
			...prevToast,
			...updates
		};
		this.setToasts(toasts.map((toast) => toast.id === id ? {
			...toast,
			...updates
		} : toast));
		const nextTimeout = nextToast.timeout ?? timeout;
		const prevTimeout = prevToast?.timeout ?? timeout;
		const timeoutUpdated = Object.hasOwn(updates, "timeout");
		const shouldHaveTimer = nextToast.transitionStatus !== "ending" && nextToast.type !== "loading" && nextTimeout > 0;
		const hasTimer = this.timers.has(id);
		const timeoutChanged = prevTimeout !== nextTimeout;
		const wasLoading = prevToast?.type === "loading";
		if (!shouldHaveTimer && hasTimer) {
			this.timers.get(id)?.timeout?.clear();
			this.timers.delete(id);
			return;
		}
		if (shouldHaveTimer && (!hasTimer || timeoutChanged || timeoutUpdated || wasLoading)) {
			const timer = this.timers.get(id);
			if (timer) {
				timer.timeout?.clear();
				this.timers.delete(id);
			}
			this.scheduleTimer(id, nextTimeout, () => this.closeToast(id));
			if (selectors.expandedOrOutOfFocus(this.state)) this.pauseTimers();
		}
	};
	closeToast = (toastId) => {
		const closeAll = toastId === void 0;
		const { limit, toasts } = this.state;
		let toastsToClose;
		if (closeAll) {
			toastsToClose = toasts;
			this.timers.forEach((timer) => {
				timer.timeout?.clear();
			});
			this.timers.clear();
		} else {
			const toast = selectors.toast(this.state, toastId);
			if (!toast) return;
			toastsToClose = [toast];
			const timer = this.timers.get(toastId);
			if (timer?.timeout) {
				timer.timeout.clear();
				this.timers.delete(toastId);
			}
		}
		let activeIndex = 0;
		const updates = { toasts: toasts.map((item) => {
			if (closeAll || item.id === toastId) return {
				...item,
				transitionStatus: "ending",
				height: 0
			};
			if (item.transitionStatus === "ending") return item;
			const isLimited = activeIndex >= limit;
			activeIndex += 1;
			return item.limited !== isLimited ? {
				...item,
				limited: isLimited
			} : item;
		}) };
		if (closeAll || toasts.length === 1) {
			updates.hovering = false;
			updates.focused = false;
		}
		this.update(updates);
		toastsToClose.forEach((toast) => {
			if (toast.transitionStatus !== "ending") toast.onClose?.();
		});
		this.handleFocusManagement(toastId);
	};
	promiseToast = (promiseValue, options) => {
		const loadingOptions = resolvePromiseOptions(options.loading);
		const id = this.addToast({
			...loadingOptions,
			type: "loading"
		});
		const handledPromise = promiseValue.then((result) => {
			const successOptions = resolvePromiseOptions(options.success, result);
			this.updateToast(id, {
				...successOptions,
				type: "success",
				timeout: successOptions.timeout
			});
			return result;
		}).catch((error) => {
			const errorOptions = resolvePromiseOptions(options.error, error);
			this.updateToast(id, {
				...errorOptions,
				type: "error",
				timeout: errorOptions.timeout
			});
			return Promise.reject(error);
		});
		if ({}.hasOwnProperty.call(options, "setPromise")) options.setPromise(handledPromise);
		return handledPromise;
	};
	pauseTimers() {
		if (this.areTimersPaused) return;
		this.areTimersPaused = true;
		this.timers.forEach((timer) => {
			if (timer.timeout) {
				timer.timeout.clear();
				const elapsed = Date.now() - timer.start;
				const remaining = timer.delay - elapsed;
				timer.remaining = remaining > 0 ? remaining : 0;
			}
		});
	}
	resumeTimers() {
		if (!this.areTimersPaused) return;
		this.areTimersPaused = false;
		this.timers.forEach((timer, id) => {
			timer.remaining = timer.remaining > 0 ? timer.remaining : timer.delay;
			timer.timeout ??= Timeout.create();
			timer.timeout.start(timer.remaining, () => {
				this.timers.delete(id);
				timer.callback();
			});
			timer.start = Date.now();
		});
	}
	restoreFocusToPrevElement() {
		this.state.prevFocusElement?.focus({ preventScroll: true });
	}
	handleDocumentPointerDown = (event) => {
		if (event.pointerType !== "touch") return;
		const target = getTarget(event);
		if (contains(this.state.viewport, target)) return;
		this.resumeTimers();
		this.update({
			hovering: false,
			focused: false
		});
	};
	scheduleTimer(id, delay, callback) {
		const start = Date.now();
		const shouldStartActive = !selectors.expandedOrOutOfFocus(this.state);
		const currentTimeout = shouldStartActive ? Timeout.create() : void 0;
		currentTimeout?.start(delay, () => {
			this.timers.delete(id);
			callback();
		});
		this.timers.set(id, {
			timeout: currentTimeout,
			start: shouldStartActive ? start : 0,
			delay,
			remaining: delay,
			callback
		});
	}
	setToasts(newToasts) {
		const updates = { toasts: newToasts };
		if (newToasts.length === 0) {
			updates.hovering = false;
			updates.focused = false;
		}
		this.update(updates);
	}
	handleFocusManagement(toastId) {
		const activeEl = activeElement(ownerDocument(this.state.viewport));
		if (!this.state.viewport || !contains(this.state.viewport, activeEl) || !matchesFocusVisible(activeEl)) return;
		if (toastId === void 0) {
			this.restoreFocusToPrevElement();
			return;
		}
		const toasts = selectors.toasts(this.state);
		const currentIndex = selectors.toastIndex(this.state, toastId);
		let nextToast = null;
		let index = currentIndex + 1;
		while (index < toasts.length) {
			if (toasts[index].transitionStatus !== "ending") {
				nextToast = toasts[index];
				break;
			}
			index += 1;
		}
		if (!nextToast) {
			index = currentIndex - 1;
			while (index >= 0) {
				if (toasts[index].transitionStatus !== "ending") {
					nextToast = toasts[index];
					break;
				}
				index -= 1;
			}
		}
		if (nextToast) nextToast.ref?.current?.focus();
		else this.restoreFocusToPrevElement();
	}
};
//#endregion
//#region node_modules/@base-ui/react/esm/toast/provider/ToastProvider.js
/**
* Provides a context for creating and managing toasts.
*
* Documentation: [Base UI Toast](https://base-ui.com/react/components/toast)
*/
var ToastProvider = function ToastProvider(props) {
	const { children, timeout = 5e3, limit = 3, toastManager } = props;
	const store = useRefWithInit(() => new ToastStore({
		timeout,
		limit,
		viewport: null,
		toasts: [],
		hovering: false,
		focused: false,
		isWindowFocused: true,
		prevFocusElement: null
	})).current;
	useOnMount(store.disposeEffect);
	import_react.useEffect(function subscribeToToastManager() {
		if (!toastManager) return;
		return toastManager[" subscribe"](({ action, options }) => {
			const id = options.id;
			if (action === "promise" && options.promise) store.promiseToast(options.promise, options);
			else if (action === "update" && id) store.updateToast(id, options);
			else if (action === "close") store.closeToast(id);
			else store.addToast(options);
		});
	}, [
		store,
		timeout,
		toastManager
	]);
	store.useSyncedValues({
		timeout,
		limit
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ToastContext.Provider, {
		value: store,
		children
	});
};
ToastProvider.displayName = "ToastProvider";
//#endregion
//#region node_modules/@base-ui/react/esm/toast/viewport/ToastViewportCssVars.js
var ToastViewportCssVars = /* @__PURE__ */ function(ToastViewportCssVars) {
	/**
	* Indicates the height of the frontmost toast.
	* @type {number}
	*/
	ToastViewportCssVars["frontmostHeight"] = "--toast-frontmost-height";
	return ToastViewportCssVars;
}({});
//#endregion
//#region node_modules/@base-ui/react/esm/toast/viewport/ToastViewport.js
/**
* A container viewport for toasts.
* Renders a `<div>` element.
*
* Documentation: [Base UI Toast](https://base-ui.com/react/components/toast)
*/
var ToastViewport = /* @__PURE__ */ import_react.forwardRef(function ToastViewport(componentProps, forwardedRef) {
	const { render, className, children, ...elementProps } = componentProps;
	const store = useToastProviderContext();
	const windowFocusTimeout = useTimeout();
	const handlingFocusGuardRef = import_react.useRef(false);
	const markedReadyForMouseLeaveRef = import_react.useRef(false);
	const isEmpty = store.useState("isEmpty");
	const toasts = store.useState("toasts");
	const focused = store.useState("focused");
	const expanded = store.useState("expanded");
	const prevFocusElement = store.useState("prevFocusElement");
	const frontmostHeight = toasts[0]?.height ?? 0;
	const hasTransitioningToasts = import_react.useMemo(() => toasts.some((toast) => toast.transitionStatus === "ending"), [toasts]);
	import_react.useEffect(() => {
		const viewport = store.state.viewport;
		if (!viewport) return;
		function handleGlobalKeyDown(event) {
			if (isEmpty) return;
			if (event.key === "F6" && event.target !== viewport) {
				event.preventDefault();
				store.setPrevFocusElement(activeElement(ownerDocument(viewport)));
				viewport?.focus({ preventScroll: true });
				store.pauseTimers();
				store.setFocused(true);
			}
		}
		const win = getWindow(viewport);
		win.addEventListener("keydown", handleGlobalKeyDown);
		return () => {
			win.removeEventListener("keydown", handleGlobalKeyDown);
		};
	}, [store, isEmpty]);
	import_react.useEffect(() => {
		const viewport = store.state.viewport;
		if (!viewport || isEmpty) return;
		const win = getWindow(viewport);
		function handleWindowBlur(event) {
			if (event.target !== win) return;
			store.setIsWindowFocused(false);
			store.pauseTimers();
		}
		function handleWindowFocus(event) {
			if (event.relatedTarget || event.target === win) return;
			const target = getTarget(event);
			const activeEl = activeElement(ownerDocument(viewport));
			if (!contains(viewport, target) || !matchesFocusVisible(activeEl)) store.resumeTimers();
			windowFocusTimeout.start(0, () => store.setIsWindowFocused(true));
		}
		win.addEventListener("blur", handleWindowBlur, true);
		win.addEventListener("focus", handleWindowFocus, true);
		return () => {
			win.removeEventListener("blur", handleWindowBlur, true);
			win.removeEventListener("focus", handleWindowFocus, true);
		};
	}, [
		store,
		windowFocusTimeout,
		isEmpty
	]);
	import_react.useEffect(() => {
		const viewport = store.state.viewport;
		if (!viewport || isEmpty) return;
		const doc = ownerDocument(viewport);
		doc.addEventListener("pointerdown", store.handleDocumentPointerDown, true);
		return () => {
			doc.removeEventListener("pointerdown", store.handleDocumentPointerDown, true);
		};
	}, [isEmpty, store]);
	function handleFocusGuard(event) {
		const viewport = store.state.viewport;
		if (!viewport) return;
		handlingFocusGuardRef.current = true;
		if (event.relatedTarget === viewport) toasts[0]?.ref?.current?.focus();
		else store.restoreFocusToPrevElement();
	}
	function handleKeyDown(event) {
		if (event.key === "Tab" && event.shiftKey && event.target === store.state.viewport) {
			event.preventDefault();
			store.restoreFocusToPrevElement();
			store.resumeTimers();
		}
	}
	import_react.useEffect(() => {
		if (!store.state.isWindowFocused || hasTransitioningToasts || !markedReadyForMouseLeaveRef.current) return;
		store.resumeTimers();
		store.setHovering(false);
		markedReadyForMouseLeaveRef.current = false;
	}, [hasTransitioningToasts, store]);
	function handleMouseEnter() {
		store.pauseTimers();
		store.setHovering(true);
		markedReadyForMouseLeaveRef.current = false;
	}
	function handleMouseLeave() {
		if (hasTransitioningToasts) markedReadyForMouseLeaveRef.current = true;
		else {
			store.resumeTimers();
			store.setHovering(false);
		}
	}
	function handleFocus() {
		if (handlingFocusGuardRef.current) {
			handlingFocusGuardRef.current = false;
			return;
		}
		if (focused) return;
		if (matchesFocusVisible(ownerDocument(store.state.viewport).activeElement)) {
			store.setFocused(true);
			store.pauseTimers();
		}
	}
	function handleBlur(event) {
		if (!focused || contains(store.state.viewport, event.relatedTarget)) return;
		store.setFocused(false);
		store.resumeTimers();
	}
	const defaultProps = {
		tabIndex: -1,
		role: "region",
		"aria-live": "polite",
		"aria-atomic": false,
		"aria-relevant": "additions text",
		"aria-label": "Notifications",
		onMouseEnter: handleMouseEnter,
		onMouseMove: handleMouseEnter,
		onMouseLeave: handleMouseLeave,
		onFocus: handleFocus,
		onBlur: handleBlur,
		onKeyDown: handleKeyDown,
		onClick: handleFocus
	};
	const state = { expanded };
	const element = useRenderElement("div", componentProps, {
		ref: [forwardedRef, store.setViewport],
		state,
		props: [
			defaultProps,
			{ style: { [ToastViewportCssVars.frontmostHeight]: frontmostHeight ? `${frontmostHeight}px` : void 0 } },
			elementProps,
			{ children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [
				!isEmpty && prevFocusElement && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FocusGuard, { onFocus: handleFocusGuard }),
				children,
				!isEmpty && prevFocusElement && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FocusGuard, { onFocus: handleFocusGuard })
			] }) }
		]
	});
	const highPriorityToasts = import_react.useMemo(() => toasts.filter((toast) => toast.priority === "high"), [toasts]);
	return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_react.Fragment, { children: [
		!isEmpty && prevFocusElement && /* @__PURE__ */ (0, import_jsx_runtime.jsx)(FocusGuard, { onFocus: handleFocusGuard }),
		element,
		!focused && highPriorityToasts.length > 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			style: visuallyHidden,
			children: highPriorityToasts.map((toast) => /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", {
				role: "alert",
				"aria-atomic": true,
				children: [/* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: toast.title }), /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: toast.description })]
			}, toast.id))
		})
	] });
});
ToastViewport.displayName = "ToastViewport";
//#endregion
//#region node_modules/@base-ui/react/esm/toast/root/ToastRootContext.js
var ToastRootContext = /* @__PURE__ */ import_react.createContext(void 0);
ToastRootContext.displayName = "ToastRootContext";
function useToastRootContext() {
	const context = import_react.useContext(ToastRootContext);
	if (!context) throw new Error("Base UI: ToastRootContext is missing. Toast parts must be used within <Toast.Root>.");
	return context;
}
//#endregion
//#region node_modules/@base-ui/react/esm/toast/root/ToastRootCssVars.js
var ToastRootCssVars = /* @__PURE__ */ function(ToastRootCssVars) {
	/**
	* Indicates the index of the toast in the list.
	* @type {number}
	*/
	ToastRootCssVars["index"] = "--toast-index";
	/**
	* Indicates the vertical pixels offset of the toast in the list when expanded.
	* @type {number}
	*/
	ToastRootCssVars["offsetY"] = "--toast-offset-y";
	/**
	* Indicates the measured natural height of the toast in pixels.
	* @type {number}
	*/
	ToastRootCssVars["height"] = "--toast-height";
	/**
	* Indicates the horizontal swipe movement of the toast.
	* @type {number}
	*/
	ToastRootCssVars["swipeMovementX"] = "--toast-swipe-movement-x";
	/**
	* Indicates the vertical swipe movement of the toast.
	* @type {number}
	*/
	ToastRootCssVars["swipeMovementY"] = "--toast-swipe-movement-y";
	return ToastRootCssVars;
}({});
//#endregion
//#region node_modules/@base-ui/react/esm/toast/root/ToastRoot.js
var stateAttributesMapping$1 = {
	...transitionStatusMapping,
	swipeDirection(value) {
		return value ? { "data-swipe-direction": value } : null;
	}
};
var SWIPE_THRESHOLD = 40;
var REVERSE_CANCEL_THRESHOLD = 10;
var OPPOSITE_DIRECTION_DAMPING_FACTOR = .5;
var MIN_DRAG_THRESHOLD = 1;
var TOAST_SWIPE_IGNORE_SELECTOR = `${BASE_UI_SWIPE_IGNORE_SELECTOR},${LEGACY_SWIPE_IGNORE_SELECTOR}`;
function getDisplacement(direction, deltaX, deltaY) {
	switch (direction) {
		case "up": return -deltaY;
		case "down": return deltaY;
		case "left": return -deltaX;
		case "right": return deltaX;
		default: return 0;
	}
}
function getElementTransform(element) {
	const transform = window.getComputedStyle(element).transform;
	let translateX = 0;
	let translateY = 0;
	let scale = 1;
	if (transform && transform !== "none") {
		const matrix = transform.match(/matrix(?:3d)?\(([^)]+)\)/);
		if (matrix) {
			const values = matrix[1].split(", ").map(parseFloat);
			if (values.length === 6) {
				translateX = values[4];
				translateY = values[5];
				scale = Math.sqrt(values[0] * values[0] + values[1] * values[1]);
			} else if (values.length === 16) {
				translateX = values[12];
				translateY = values[13];
				scale = values[0];
			}
		}
	}
	return {
		x: translateX,
		y: translateY,
		scale
	};
}
/**
* Groups all parts of an individual toast.
* Renders a `<div>` element.
*
* Documentation: [Base UI Toast](https://base-ui.com/react/components/toast)
*/
var ToastRoot = /* @__PURE__ */ import_react.forwardRef(function ToastRoot(componentProps, forwardedRef) {
	const { toast, render, className, swipeDirection = ["down", "right"], ...elementProps } = componentProps;
	const isAnchored = toast.positionerProps?.anchor !== void 0;
	let swipeDirections = [];
	if (!isAnchored) swipeDirections = Array.isArray(swipeDirection) ? swipeDirection : [swipeDirection];
	const swipeEnabled = swipeDirections.length > 0;
	const store = useToastProviderContext();
	const [currentSwipeDirection, setCurrentSwipeDirection] = import_react.useState(void 0);
	const [isSwiping, setIsSwiping] = import_react.useState(false);
	const [isRealSwipe, setIsRealSwipe] = import_react.useState(false);
	const [dragDismissed, setDragDismissed] = import_react.useState(false);
	const [dragOffset, setDragOffset] = import_react.useState({
		x: 0,
		y: 0
	});
	const [initialTransform, setInitialTransform] = import_react.useState({
		x: 0,
		y: 0,
		scale: 1
	});
	const [titleId, setTitleId] = import_react.useState();
	const [descriptionId, setDescriptionId] = import_react.useState();
	const [lockedDirection, setLockedDirection] = import_react.useState(null);
	const rootRef = import_react.useRef(null);
	const dragStartPosRef = import_react.useRef({
		x: 0,
		y: 0
	});
	const initialTransformRef = import_react.useRef({
		x: 0,
		y: 0,
		scale: 1
	});
	const intendedSwipeDirectionRef = import_react.useRef(void 0);
	const maxSwipeDisplacementRef = import_react.useRef(0);
	const cancelledSwipeRef = import_react.useRef(false);
	const swipeCancelBaselineRef = import_react.useRef({
		x: 0,
		y: 0
	});
	const isFirstPointerMoveRef = import_react.useRef(false);
	const domIndex = store.useState("toastIndex", toast.id);
	const visibleIndex = store.useState("toastVisibleIndex", toast.id);
	const offsetY = store.useState("toastOffsetY", toast.id);
	const focused = store.useState("focused");
	const expanded = store.useState("expanded");
	useOpenChangeComplete({
		open: toast.transitionStatus !== "ending",
		ref: rootRef,
		onComplete() {
			if (toast.transitionStatus === "ending") store.removeToast(toast.id);
		}
	});
	/**
	* Recalculates the natural height of the toast and updates it in the toast manager.
	* @param flushSync Whether to flush the update synchronously. Use in observer
	* callbacks to avoid visual flickers.
	*/
	const recalculateHeight = useStableCallback((flushSync = false) => {
		const element = rootRef.current;
		if (!element) return;
		const previousHeight = element.style.height;
		element.style.height = "auto";
		const height = element.offsetHeight;
		element.style.height = previousHeight;
		function update() {
			store.updateToastInternal(toast.id, {
				ref: rootRef,
				height,
				...toast.transitionStatus === "starting" ? { transitionStatus: void 0 } : {}
			});
		}
		if (flushSync) import_react_dom.flushSync(update);
		else update();
	});
	useIsoLayoutEffect(recalculateHeight, [recalculateHeight]);
	function applyDirectionalDamping(deltaX, deltaY) {
		let newDeltaX = deltaX;
		let newDeltaY = deltaY;
		if (!swipeDirections.includes("left") && !swipeDirections.includes("right")) newDeltaX = deltaX > 0 ? deltaX ** OPPOSITE_DIRECTION_DAMPING_FACTOR : -(Math.abs(deltaX) ** OPPOSITE_DIRECTION_DAMPING_FACTOR);
		else {
			if (!swipeDirections.includes("right") && deltaX > 0) newDeltaX = deltaX ** OPPOSITE_DIRECTION_DAMPING_FACTOR;
			if (!swipeDirections.includes("left") && deltaX < 0) newDeltaX = -(Math.abs(deltaX) ** OPPOSITE_DIRECTION_DAMPING_FACTOR);
		}
		if (!swipeDirections.includes("up") && !swipeDirections.includes("down")) newDeltaY = deltaY > 0 ? deltaY ** OPPOSITE_DIRECTION_DAMPING_FACTOR : -(Math.abs(deltaY) ** OPPOSITE_DIRECTION_DAMPING_FACTOR);
		else {
			if (!swipeDirections.includes("down") && deltaY > 0) newDeltaY = deltaY ** OPPOSITE_DIRECTION_DAMPING_FACTOR;
			if (!swipeDirections.includes("up") && deltaY < 0) newDeltaY = -(Math.abs(deltaY) ** OPPOSITE_DIRECTION_DAMPING_FACTOR);
		}
		return {
			x: newDeltaX,
			y: newDeltaY
		};
	}
	function handlePointerDown(event) {
		if (event.button !== 0) return;
		if (event.pointerType === "touch") store.pauseTimers();
		const target = getTarget(event.nativeEvent);
		if (target ? target.closest(`button,a,input,textarea,[role="button"],${TOAST_SWIPE_IGNORE_SELECTOR}`) : false) return;
		cancelledSwipeRef.current = false;
		intendedSwipeDirectionRef.current = void 0;
		maxSwipeDisplacementRef.current = 0;
		dragStartPosRef.current = {
			x: event.clientX,
			y: event.clientY
		};
		swipeCancelBaselineRef.current = dragStartPosRef.current;
		if (rootRef.current) {
			const transform = getElementTransform(rootRef.current);
			initialTransformRef.current = transform;
			setInitialTransform(transform);
			setDragOffset({
				x: transform.x,
				y: transform.y
			});
		}
		store.setHovering(true);
		setIsSwiping(true);
		setIsRealSwipe(false);
		setLockedDirection(null);
		isFirstPointerMoveRef.current = true;
		rootRef.current?.setPointerCapture(event.pointerId);
	}
	function handlePointerMove(event) {
		if (!isSwiping) return;
		event.preventDefault();
		if (isFirstPointerMoveRef.current) {
			dragStartPosRef.current = {
				x: event.clientX,
				y: event.clientY
			};
			isFirstPointerMoveRef.current = false;
		}
		const { clientY, clientX, movementX, movementY } = event;
		if (movementY < 0 && clientY > swipeCancelBaselineRef.current.y || movementY > 0 && clientY < swipeCancelBaselineRef.current.y) swipeCancelBaselineRef.current = {
			x: swipeCancelBaselineRef.current.x,
			y: clientY
		};
		if (movementX < 0 && clientX > swipeCancelBaselineRef.current.x || movementX > 0 && clientX < swipeCancelBaselineRef.current.x) swipeCancelBaselineRef.current = {
			x: clientX,
			y: swipeCancelBaselineRef.current.y
		};
		const deltaX = clientX - dragStartPosRef.current.x;
		const deltaY = clientY - dragStartPosRef.current.y;
		const cancelDeltaY = clientY - swipeCancelBaselineRef.current.y;
		const cancelDeltaX = clientX - swipeCancelBaselineRef.current.x;
		if (!isRealSwipe) {
			if (Math.sqrt(deltaX * deltaX + deltaY * deltaY) >= MIN_DRAG_THRESHOLD) {
				setIsRealSwipe(true);
				if (lockedDirection === null) {
					const hasHorizontal = swipeDirections.includes("left") || swipeDirections.includes("right");
					const hasVertical = swipeDirections.includes("up") || swipeDirections.includes("down");
					if (hasHorizontal && hasVertical) setLockedDirection(Math.abs(deltaX) > Math.abs(deltaY) ? "horizontal" : "vertical");
				}
			}
		}
		let candidate;
		if (!intendedSwipeDirectionRef.current) {
			if (lockedDirection === "vertical") {
				if (deltaY > 0) candidate = "down";
				else if (deltaY < 0) candidate = "up";
			} else if (lockedDirection === "horizontal") {
				if (deltaX > 0) candidate = "right";
				else if (deltaX < 0) candidate = "left";
			} else if (Math.abs(deltaX) >= Math.abs(deltaY)) candidate = deltaX > 0 ? "right" : "left";
			else candidate = deltaY > 0 ? "down" : "up";
			if (candidate && swipeDirections.includes(candidate)) {
				intendedSwipeDirectionRef.current = candidate;
				maxSwipeDisplacementRef.current = getDisplacement(candidate, deltaX, deltaY);
				setCurrentSwipeDirection(candidate);
			}
		} else {
			const direction = intendedSwipeDirectionRef.current;
			const currentDisplacement = getDisplacement(direction, cancelDeltaX, cancelDeltaY);
			if (currentDisplacement > SWIPE_THRESHOLD) {
				cancelledSwipeRef.current = false;
				setCurrentSwipeDirection(direction);
			} else if (!(swipeDirections.includes("left") && swipeDirections.includes("right")) && !(swipeDirections.includes("up") && swipeDirections.includes("down")) && maxSwipeDisplacementRef.current - currentDisplacement >= REVERSE_CANCEL_THRESHOLD) cancelledSwipeRef.current = true;
		}
		const dampedDelta = applyDirectionalDamping(deltaX, deltaY);
		let newOffsetX = initialTransformRef.current.x;
		let newOffsetY = initialTransformRef.current.y;
		if (lockedDirection === "horizontal") {
			if (swipeDirections.includes("left") || swipeDirections.includes("right")) newOffsetX += dampedDelta.x;
		} else if (lockedDirection === "vertical") {
			if (swipeDirections.includes("up") || swipeDirections.includes("down")) newOffsetY += dampedDelta.y;
		} else {
			if (swipeDirections.includes("left") || swipeDirections.includes("right")) newOffsetX += dampedDelta.x;
			if (swipeDirections.includes("up") || swipeDirections.includes("down")) newOffsetY += dampedDelta.y;
		}
		setDragOffset({
			x: newOffsetX,
			y: newOffsetY
		});
	}
	function handlePointerUp(event) {
		if (!isSwiping) return;
		setIsSwiping(false);
		setIsRealSwipe(false);
		setLockedDirection(null);
		rootRef.current?.releasePointerCapture(event.pointerId);
		if (cancelledSwipeRef.current) {
			setDragOffset({
				x: initialTransform.x,
				y: initialTransform.y
			});
			setCurrentSwipeDirection(void 0);
			return;
		}
		let shouldClose = false;
		const deltaX = dragOffset.x - initialTransform.x;
		const deltaY = dragOffset.y - initialTransform.y;
		let dismissDirection;
		for (const direction of swipeDirections) {
			switch (direction) {
				case "right":
					if (deltaX > SWIPE_THRESHOLD) {
						shouldClose = true;
						dismissDirection = "right";
					}
					break;
				case "left":
					if (deltaX < -SWIPE_THRESHOLD) {
						shouldClose = true;
						dismissDirection = "left";
					}
					break;
				case "down":
					if (deltaY > SWIPE_THRESHOLD) {
						shouldClose = true;
						dismissDirection = "down";
					}
					break;
				case "up":
					if (deltaY < -SWIPE_THRESHOLD) {
						shouldClose = true;
						dismissDirection = "up";
					}
					break;
				default: break;
			}
			if (shouldClose) break;
		}
		if (shouldClose) {
			setCurrentSwipeDirection(dismissDirection);
			setDragDismissed(true);
			store.closeToast(toast.id);
		} else {
			setDragOffset({
				x: initialTransform.x,
				y: initialTransform.y
			});
			setCurrentSwipeDirection(void 0);
		}
	}
	function handleKeyDown(event) {
		if (event.key === "Escape") {
			if (!rootRef.current || !contains(rootRef.current, activeElement(ownerDocument(rootRef.current)))) return;
			store.closeToast(toast.id);
		}
	}
	import_react.useEffect(() => {
		if (!swipeEnabled) return;
		const element = rootRef.current;
		if (!element) return;
		function preventDefaultTouchStart(event) {
			if (contains(element, event.target)) event.preventDefault();
		}
		element.addEventListener("touchmove", preventDefaultTouchStart, { passive: false });
		return () => {
			element.removeEventListener("touchmove", preventDefaultTouchStart);
		};
	}, [swipeEnabled]);
	function getDragStyles() {
		if (!isSwiping && dragOffset.x === initialTransform.x && dragOffset.y === initialTransform.y && !dragDismissed) return {
			[ToastRootCssVars.swipeMovementX]: "0px",
			[ToastRootCssVars.swipeMovementY]: "0px"
		};
		const deltaX = dragOffset.x - initialTransform.x;
		const deltaY = dragOffset.y - initialTransform.y;
		return {
			transition: isSwiping ? "none" : void 0,
			transform: isSwiping ? `translateX(${dragOffset.x}px) translateY(${dragOffset.y}px) scale(${initialTransform.scale})` : void 0,
			[ToastRootCssVars.swipeMovementX]: `${deltaX}px`,
			[ToastRootCssVars.swipeMovementY]: `${deltaY}px`
		};
	}
	const isHighPriority = toast.priority === "high";
	const defaultProps = {
		role: isHighPriority ? "alertdialog" : "dialog",
		tabIndex: 0,
		"aria-modal": false,
		"aria-labelledby": titleId,
		"aria-describedby": descriptionId,
		"aria-hidden": isHighPriority && !focused ? true : void 0,
		onPointerDown: swipeEnabled ? handlePointerDown : void 0,
		onPointerMove: swipeEnabled ? handlePointerMove : void 0,
		onPointerUp: swipeEnabled ? handlePointerUp : void 0,
		onKeyDown: handleKeyDown,
		inert: inertValue(toast.limited),
		style: {
			...getDragStyles(),
			[ToastRootCssVars.index]: toast.transitionStatus === "ending" ? domIndex : visibleIndex,
			[ToastRootCssVars.offsetY]: `${offsetY}px`,
			[ToastRootCssVars.height]: toast.height ? `${toast.height}px` : void 0
		}
	};
	const toastRoot = import_react.useMemo(() => ({
		rootRef,
		toast,
		titleId,
		setTitleId,
		descriptionId,
		setDescriptionId,
		swiping: isSwiping,
		swipeDirection: currentSwipeDirection,
		recalculateHeight,
		index: domIndex,
		visibleIndex,
		expanded
	}), [
		toast,
		titleId,
		descriptionId,
		isSwiping,
		currentSwipeDirection,
		recalculateHeight,
		domIndex,
		visibleIndex,
		expanded
	]);
	const state = {
		transitionStatus: toast.transitionStatus,
		expanded,
		limited: toast.limited || false,
		type: toast.type,
		swiping: toastRoot.swiping,
		swipeDirection: toastRoot.swipeDirection
	};
	const element = useRenderElement("div", componentProps, {
		ref: [forwardedRef, toastRoot.rootRef],
		state,
		stateAttributesMapping: stateAttributesMapping$1,
		props: [defaultProps, elementProps]
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ToastRootContext.Provider, {
		value: toastRoot,
		children: element
	});
});
ToastRoot.displayName = "ToastRoot";
//#endregion
//#region node_modules/@base-ui/react/esm/toast/content/ToastContent.js
/**
* A container for the contents of a toast.
* Renders a `<div>` element.
*
* Documentation: [Base UI Toast](https://base-ui.com/react/components/toast)
*/
var ToastContent = /* @__PURE__ */ import_react.forwardRef(function ToastContent(componentProps, forwardedRef) {
	const { render, className, ...elementProps } = componentProps;
	const { visibleIndex, expanded, recalculateHeight } = useToastRootContext();
	const contentRef = import_react.useRef(null);
	useIsoLayoutEffect(() => {
		const node = contentRef.current;
		if (!node) return;
		recalculateHeight();
		if (typeof ResizeObserver !== "function" || typeof MutationObserver !== "function") return;
		const resizeObserver = new ResizeObserver(() => recalculateHeight(true));
		const mutationObserver = new MutationObserver(() => recalculateHeight(true));
		resizeObserver.observe(node);
		mutationObserver.observe(node, {
			childList: true,
			subtree: true,
			characterData: true
		});
		return () => {
			resizeObserver.disconnect();
			mutationObserver.disconnect();
		};
	}, [recalculateHeight]);
	return useRenderElement("div", componentProps, {
		ref: [forwardedRef, contentRef],
		state: {
			expanded,
			behind: visibleIndex > 0
		},
		props: elementProps
	});
});
ToastContent.displayName = "ToastContent";
//#endregion
//#region node_modules/@base-ui/react/esm/toast/description/ToastDescription.js
/**
* A description that describes the toast.
* Can be used as the default message for the toast when no title is provided.
* Renders a `<p>` element.
*
* Documentation: [Base UI Toast](https://base-ui.com/react/components/toast)
*/
var ToastDescription = /* @__PURE__ */ import_react.forwardRef(function ToastDescription(componentProps, forwardedRef) {
	const { render, className, id: idProp, children: childrenProp, ...elementProps } = componentProps;
	const { toast, setDescriptionId } = useToastRootContext();
	const children = childrenProp ?? toast.description;
	const shouldRender = Boolean(children);
	const id = useId(idProp);
	useIsoLayoutEffect(() => {
		if (!shouldRender) return;
		setDescriptionId(id);
		return () => {
			setDescriptionId(void 0);
		};
	}, [
		shouldRender,
		id,
		setDescriptionId
	]);
	const element = useRenderElement("p", componentProps, {
		ref: forwardedRef,
		state: { type: toast.type },
		props: {
			...elementProps,
			id,
			children
		}
	});
	if (!shouldRender) return null;
	return element;
});
ToastDescription.displayName = "ToastDescription";
//#endregion
//#region node_modules/@base-ui/react/esm/toast/title/ToastTitle.js
/**
* A title that labels the toast.
* Renders an `<h2>` element.
*
* Documentation: [Base UI Toast](https://base-ui.com/react/components/toast)
*/
var ToastTitle = /* @__PURE__ */ import_react.forwardRef(function ToastTitle(componentProps, forwardedRef) {
	const { render, className, id: idProp, children: childrenProp, ...elementProps } = componentProps;
	const { toast, setTitleId } = useToastRootContext();
	const children = childrenProp ?? toast.title;
	const shouldRender = Boolean(children);
	const id = useId(idProp);
	useIsoLayoutEffect(() => {
		if (!shouldRender) return;
		setTitleId(id);
		return () => {
			setTitleId(void 0);
		};
	}, [
		shouldRender,
		id,
		setTitleId
	]);
	const element = useRenderElement("h2", componentProps, {
		ref: forwardedRef,
		state: { type: toast.type },
		props: {
			...elementProps,
			id,
			children
		}
	});
	if (!shouldRender) return null;
	return element;
});
ToastTitle.displayName = "ToastTitle";
//#endregion
//#region node_modules/@base-ui/react/esm/toast/close/ToastClose.js
/**
* Closes the toast when clicked.
* Renders a `<button>` element.
*
* Documentation: [Base UI Toast](https://base-ui.com/react/components/toast)
*/
var ToastClose = /* @__PURE__ */ import_react.forwardRef(function ToastClose(componentProps, forwardedRef) {
	const { render, className, disabled, nativeButton = true, ...elementProps } = componentProps;
	const store = useToastProviderContext();
	const { toast } = useToastRootContext();
	const expanded = store.useState("expanded");
	const [hasFocus, setHasFocus] = import_react.useState(false);
	const { getButtonProps, buttonRef } = useButton({
		disabled,
		native: nativeButton
	});
	const state = { type: toast.type };
	return useRenderElement("button", componentProps, {
		ref: [forwardedRef, buttonRef],
		state,
		props: [
			{
				"aria-hidden": !expanded && !hasFocus,
				onClick() {
					store.closeToast(toast.id);
				},
				onFocus() {
					setHasFocus(true);
				},
				onBlur() {
					setHasFocus(false);
				}
			},
			elementProps,
			getButtonProps
		]
	});
});
ToastClose.displayName = "ToastClose";
//#endregion
//#region node_modules/@base-ui/react/esm/toast/action/ToastAction.js
/**
* Performs an action when clicked.
* Renders a `<button>` element.
*
* Documentation: [Base UI Toast](https://base-ui.com/react/components/toast)
*/
var ToastAction = /* @__PURE__ */ import_react.forwardRef(function ToastAction(componentProps, forwardedRef) {
	const { render, className, disabled, nativeButton = true, ...elementProps } = componentProps;
	const { toast } = useToastRootContext();
	const computedChildren = toast.actionProps?.children ?? elementProps.children;
	const shouldRender = Boolean(computedChildren);
	const { getButtonProps, buttonRef } = useButton({
		disabled,
		native: nativeButton
	});
	const state = { type: toast.type };
	const element = useRenderElement("button", componentProps, {
		ref: [forwardedRef, buttonRef],
		state,
		props: [
			elementProps,
			toast.actionProps,
			getButtonProps,
			{ children: computedChildren }
		]
	});
	if (!shouldRender) return null;
	return element;
});
ToastAction.displayName = "ToastAction";
//#endregion
//#region node_modules/@base-ui/react/esm/toast/portal/ToastPortal.js
/**
* A portal element that moves the viewport to a different part of the DOM.
* By default, the portal element is appended to `<body>`.
* Renders a `<div>` element.
*
* Documentation: [Base UI Toast](https://base-ui.com/react/components/toast)
*/
var ToastPortal = FloatingPortalLite;
//#endregion
//#region node_modules/@base-ui/react/esm/toast/positioner/ToastPositionerContext.js
var ToastPositionerContext = /* @__PURE__ */ import_react.createContext(void 0);
ToastPositionerContext.displayName = "ToastPositionerContext";
function useToastPositionerContext() {
	const context = import_react.useContext(ToastPositionerContext);
	if (context === void 0) throw new Error("Base UI: ToastPositionerContext is missing. ToastPositioner parts must be placed within <Toast.Positioner>.");
	return context;
}
//#endregion
//#region node_modules/@base-ui/react/esm/toast/positioner/ToastPositioner.js
/**
* Positions the toast against the anchor.
* Renders a `<div>` element.
*
* Documentation: [Base UI Toast](https://base-ui.com/react/components/toast)
*/
var ToastPositioner = /* @__PURE__ */ import_react.forwardRef(function ToastPositioner(componentProps, forwardedRef) {
	const { toast, ...props } = componentProps;
	const store = useToastProviderContext();
	const positionerProps = toast.positionerProps ?? EMPTY_OBJECT;
	const { render, className, anchor: anchorProp = positionerProps.anchor, positionMethod = positionerProps.positionMethod ?? "absolute", side = positionerProps.side ?? "top", align = positionerProps.align ?? "center", sideOffset = positionerProps.sideOffset ?? 0, alignOffset = positionerProps.alignOffset ?? 0, collisionBoundary = positionerProps.collisionBoundary ?? "clipping-ancestors", collisionPadding = positionerProps.collisionPadding ?? 5, arrowPadding = positionerProps.arrowPadding ?? 5, sticky = positionerProps.sticky ?? false, disableAnchorTracking = positionerProps.disableAnchorTracking ?? false, collisionAvoidance = positionerProps.collisionAvoidance ?? POPUP_COLLISION_AVOIDANCE, ...elementProps } = props;
	const [positionerElement, setPositionerElement] = import_react.useState(null);
	const domIndex = store.useState("toastIndex", toast.id);
	const visibleIndex = store.useState("toastVisibleIndex", toast.id);
	const anchor = isElement(anchorProp) ? anchorProp : null;
	const positioning = useAnchorPositioning({
		anchor,
		positionMethod,
		floatingRootContext: useFloatingRootContext({
			open: true,
			onOpenChange: NOOP,
			elements: {
				floating: positionerElement,
				reference: anchor
			}
		}),
		mounted: true,
		side,
		sideOffset,
		align,
		alignOffset,
		collisionBoundary,
		collisionPadding,
		sticky,
		arrowPadding,
		disableAnchorTracking,
		keepMounted: true,
		collisionAvoidance
	});
	const defaultProps = import_react.useMemo(() => {
		const hiddenStyles = {};
		return {
			role: "presentation",
			style: {
				...positioning.positionerStyles,
				...hiddenStyles,
				[ToastRootCssVars.index]: toast.transitionStatus === "ending" ? domIndex : visibleIndex
			}
		};
	}, [
		positioning.positionerStyles,
		toast.transitionStatus,
		domIndex,
		visibleIndex
	]);
	const state = import_react.useMemo(() => ({
		side: positioning.side,
		align: positioning.align,
		anchorHidden: positioning.anchorHidden
	}), [
		positioning.side,
		positioning.align,
		positioning.anchorHidden
	]);
	const contextValue = import_react.useMemo(() => ({
		...state,
		arrowRef: positioning.arrowRef,
		arrowStyles: positioning.arrowStyles,
		arrowUncentered: positioning.arrowUncentered
	}), [
		state,
		positioning.arrowRef,
		positioning.arrowStyles,
		positioning.arrowUncentered
	]);
	const element = useRenderElement("div", componentProps, {
		state,
		props: [
			defaultProps,
			getDisabledMountTransitionStyles(toast.transitionStatus),
			elementProps
		],
		ref: [forwardedRef, setPositionerElement],
		stateAttributesMapping: popupStateMapping
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ToastPositionerContext.Provider, {
		value: contextValue,
		children: element
	});
});
ToastPositioner.displayName = "ToastPositioner";
//#endregion
//#region node_modules/@base-ui/react/esm/toast/arrow/ToastArrow.js
/**
* Displays an element positioned against the toast anchor.
* Renders a `<div>` element.
*
* Documentation: [Base UI Toast](https://base-ui.com/react/components/toast)
*/
var ToastArrow = /* @__PURE__ */ import_react.forwardRef(function ToastArrow(componentProps, forwardedRef) {
	const { className, render, ...elementProps } = componentProps;
	const { arrowRef, side, align, arrowUncentered, arrowStyles } = useToastPositionerContext();
	return useRenderElement("div", componentProps, {
		state: {
			side,
			align,
			uncentered: arrowUncentered
		},
		ref: [forwardedRef, arrowRef],
		props: [{
			style: arrowStyles,
			"aria-hidden": true
		}, elementProps]
	});
});
ToastArrow.displayName = "ToastArrow";
//#endregion
//#region node_modules/@base-ui/react/esm/toast/useToastManager.js
/**
* Returns the array of toasts and methods to manage them.
*/
function useToastManager() {
	const store = import_react.useContext(ToastContext);
	if (!store) throw new Error("Base UI: useToastManager must be used within <Toast.Provider>.");
	const toasts = store.useState("toasts");
	return import_react.useMemo(() => ({
		toasts,
		add: store.addToast,
		close: store.closeToast,
		update: store.updateToast,
		promise: store.promiseToast
	}), [toasts, store]);
}
//#endregion
//#region node_modules/@base-ui/react/esm/toast/createToastManager.js
/**
* Creates a new toast manager.
*/
function createToastManager() {
	const listeners = /* @__PURE__ */ new Set();
	function emit(data) {
		listeners.forEach((listener) => listener(data));
	}
	return {
		" subscribe": function subscribe(listener) {
			listeners.add(listener);
			return () => {
				listeners.delete(listener);
			};
		},
		add(options) {
			const id = options.id || generateId("toast");
			emit({
				action: "add",
				options: {
					...options,
					id,
					transitionStatus: "starting"
				}
			});
			return id;
		},
		close(id) {
			emit({
				action: "close",
				options: { id }
			});
		},
		update(id, updates) {
			emit({
				action: "update",
				options: {
					...updates,
					id
				}
			});
		},
		promise(promiseValue, options) {
			let handledPromise = promiseValue;
			emit({
				action: "promise",
				options: {
					...options,
					promise: promiseValue,
					setPromise(promise) {
						handledPromise = promise;
					}
				}
			});
			return handledPromise;
		}
	};
}
//#endregion
//#region node_modules/@base-ui/react/esm/toast/index.parts.js
var index_parts_exports$25 = /* @__PURE__ */ __exportAll({
	Action: () => ToastAction,
	Arrow: () => ToastArrow,
	Close: () => ToastClose,
	Content: () => ToastContent,
	Description: () => ToastDescription,
	Portal: () => ToastPortal,
	Positioner: () => ToastPositioner,
	Provider: () => ToastProvider,
	Root: () => ToastRoot,
	Title: () => ToastTitle,
	Viewport: () => ToastViewport,
	createToastManager: () => createToastManager,
	useToastManager: () => useToastManager
});
//#endregion
//#region node_modules/@base-ui/react/esm/toggle-group/ToggleGroupDataAttributes.js
var ToggleGroupDataAttributes = /* @__PURE__ */ function(ToggleGroupDataAttributes) {
	/**
	* Present when the toggle group is disabled.
	*/
	ToggleGroupDataAttributes["disabled"] = "data-disabled";
	/**
	* Indicates the orientation of the toggle group.
	* @type {'horizontal' | 'vertical'}
	*/
	ToggleGroupDataAttributes["orientation"] = "data-orientation";
	/**
	* Present when the toggle group allows multiple buttons to be in the pressed state at the same time.
	*/
	ToggleGroupDataAttributes["multiple"] = "data-multiple";
	return ToggleGroupDataAttributes;
}({});
//#endregion
//#region node_modules/@base-ui/react/esm/toggle-group/ToggleGroup.js
var stateAttributesMapping = { multiple(value) {
	if (value) return { [ToggleGroupDataAttributes.multiple]: "" };
	return null;
} };
/**
* Provides a shared state to a series of toggle buttons.
*
* Documentation: [Base UI Toggle Group](https://base-ui.com/react/components/toggle-group)
*/
var ToggleGroup = /* @__PURE__ */ import_react.forwardRef(function ToggleGroup(componentProps, forwardedRef) {
	const { defaultValue: defaultValueProp, disabled: disabledProp = false, loopFocus = true, onValueChange, orientation = "horizontal", multiple = false, value: valueProp, className, render, ...elementProps } = componentProps;
	const toolbarContext = useToolbarRootContext(true);
	const defaultValue = import_react.useMemo(() => {
		if (valueProp === void 0) return defaultValueProp ?? [];
	}, [valueProp, defaultValueProp]);
	const isValueInitialized = import_react.useMemo(() => valueProp !== void 0 || defaultValueProp !== void 0, [valueProp, defaultValueProp]);
	const disabled = (toolbarContext?.disabled ?? false) || disabledProp;
	const [groupValue, setValueState] = useControlled({
		controlled: valueProp,
		default: defaultValue,
		name: "ToggleGroup",
		state: "value"
	});
	const setGroupValue = useStableCallback((newValue, nextPressed, eventDetails) => {
		let newGroupValue;
		if (multiple) {
			newGroupValue = groupValue.slice();
			if (nextPressed) newGroupValue.push(newValue);
			else newGroupValue.splice(groupValue.indexOf(newValue), 1);
		} else newGroupValue = nextPressed ? [newValue] : [];
		if (Array.isArray(newGroupValue)) {
			onValueChange?.(newGroupValue, eventDetails);
			if (eventDetails.isCanceled) return;
			setValueState(newGroupValue);
		}
	});
	const state = {
		disabled,
		multiple,
		orientation
	};
	const contextValue = import_react.useMemo(() => ({
		disabled,
		orientation,
		setGroupValue,
		value: groupValue,
		isValueInitialized
	}), [
		disabled,
		orientation,
		setGroupValue,
		groupValue,
		isValueInitialized
	]);
	const defaultProps = { role: "group" };
	const element = useRenderElement("div", componentProps, {
		enabled: Boolean(toolbarContext),
		state,
		ref: forwardedRef,
		props: [defaultProps, elementProps],
		stateAttributesMapping
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ToggleGroupContext.Provider, {
		value: contextValue,
		children: toolbarContext ? element : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CompositeRoot, {
			render,
			className,
			state,
			refs: [forwardedRef],
			props: [defaultProps, elementProps],
			stateAttributesMapping,
			loopFocus,
			enableHomeAndEndKeys: true
		})
	});
});
ToggleGroup.displayName = "ToggleGroup";
//#endregion
//#region node_modules/@base-ui/react/esm/toolbar/separator/ToolbarSeparator.js
/**
* A separator element accessible to screen readers.
* Renders a `<div>` element.
*
* Documentation: [Base UI Toolbar](https://base-ui.com/react/components/toolbar)
*/
var ToolbarSeparator = /* @__PURE__ */ import_react.forwardRef(function ToolbarSeparator(props, forwardedRef) {
	const orientation = {
		vertical: "horizontal",
		horizontal: "vertical"
	}[useToolbarRootContext().orientation];
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Separator, {
		orientation,
		...props,
		ref: forwardedRef
	});
});
ToolbarSeparator.displayName = "ToolbarSeparator";
//#endregion
//#region node_modules/@base-ui/react/esm/toolbar/root/ToolbarRoot.js
/**
* A container for grouping a set of controls, such as buttons, toggle groups, or menus.
* Renders a `<div>` element.
*
* Documentation: [Base UI Toolbar](https://base-ui.com/react/components/toolbar)
*/
var ToolbarRoot = /* @__PURE__ */ import_react.forwardRef(function ToolbarRoot(componentProps, forwardedRef) {
	const { disabled = false, loopFocus = true, orientation = "horizontal", className, render, ...elementProps } = componentProps;
	const [itemMap, setItemMap] = import_react.useState(() => /* @__PURE__ */ new Map());
	const disabledIndices = import_react.useMemo(() => {
		const output = [];
		for (const itemMetadata of itemMap.values()) if (itemMetadata?.index && !itemMetadata.focusableWhenDisabled) output.push(itemMetadata.index);
		return output;
	}, [itemMap]);
	const toolbarRootContext = import_react.useMemo(() => ({
		disabled,
		orientation,
		setItemMap
	}), [
		disabled,
		orientation,
		setItemMap
	]);
	const state = {
		disabled,
		orientation
	};
	const defaultProps = {
		"aria-orientation": orientation,
		role: "toolbar"
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ToolbarRootContext.Provider, {
		value: toolbarRootContext,
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CompositeRoot, {
			render,
			className,
			state,
			refs: [forwardedRef],
			props: [defaultProps, elementProps],
			disabledIndices,
			loopFocus,
			onMapChange: setItemMap,
			orientation
		})
	});
});
ToolbarRoot.displayName = "ToolbarRoot";
//#endregion
//#region node_modules/@base-ui/react/esm/toolbar/group/ToolbarGroupContext.js
var ToolbarGroupContext = /* @__PURE__ */ import_react.createContext(void 0);
ToolbarGroupContext.displayName = "ToolbarGroupContext";
function useToolbarGroupContext(optional) {
	const context = import_react.useContext(ToolbarGroupContext);
	if (context === void 0 && !optional) throw new Error("Base UI: ToolbarGroupContext is missing. ToolbarGroup parts must be placed within <Toolbar.Group>.");
	return context;
}
//#endregion
//#region node_modules/@base-ui/react/esm/toolbar/group/ToolbarGroup.js
/**
* Groups several toolbar items or toggles.
* Renders a `<div>` element.
*
* Documentation: [Base UI Toolbar](https://base-ui.com/react/components/toolbar)
*/
var ToolbarGroup = /* @__PURE__ */ import_react.forwardRef(function ToolbarGroup(componentProps, forwardedRef) {
	const { className, disabled: disabledProp = false, render, ...elementProps } = componentProps;
	const { orientation, disabled: toolbarDisabled } = useToolbarRootContext();
	const disabled = toolbarDisabled || disabledProp;
	const contextValue = import_react.useMemo(() => ({ disabled }), [disabled]);
	const element = useRenderElement("div", componentProps, {
		state: {
			disabled,
			orientation
		},
		ref: forwardedRef,
		props: [{ role: "group" }, elementProps]
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ToolbarGroupContext.Provider, {
		value: contextValue,
		children: element
	});
});
ToolbarGroup.displayName = "ToolbarGroup";
//#endregion
//#region node_modules/@base-ui/react/esm/toolbar/button/ToolbarButton.js
/**
* A button that can be used as-is or as a trigger for other components.
* Renders a `<button>` element.
*
* Documentation: [Base UI Toolbar](https://base-ui.com/react/components/toolbar)
*/
var ToolbarButton = /* @__PURE__ */ import_react.forwardRef(function ToolbarButton(componentProps, forwardedRef) {
	const { className, disabled: disabledProp = false, focusableWhenDisabled = true, render, nativeButton = true, ...elementProps } = componentProps;
	const itemMetadata = import_react.useMemo(() => ({ focusableWhenDisabled }), [focusableWhenDisabled]);
	const { disabled: toolbarDisabled, orientation } = useToolbarRootContext();
	const groupContext = useToolbarGroupContext(true);
	const disabled = toolbarDisabled || (groupContext?.disabled ?? false) || disabledProp;
	const { getButtonProps, buttonRef } = useButton({
		disabled,
		focusableWhenDisabled,
		native: nativeButton
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CompositeItem, {
		tag: "button",
		render,
		className,
		metadata: itemMetadata,
		state: {
			disabled,
			orientation,
			focusable: focusableWhenDisabled
		},
		refs: [forwardedRef, buttonRef],
		props: [
			elementProps,
			{ disabled },
			getButtonProps
		]
	});
});
ToolbarButton.displayName = "ToolbarButton";
//#endregion
//#region node_modules/@base-ui/react/esm/toolbar/link/ToolbarLink.js
var TOOLBAR_LINK_METADATA = { focusableWhenDisabled: true };
/**
* A link component.
* Renders an `<a>` element.
*
* Documentation: [Base UI Toolbar](https://base-ui.com/react/components/toolbar)
*/
var ToolbarLink = /* @__PURE__ */ import_react.forwardRef(function ToolbarLink(componentProps, forwardedRef) {
	const { className, render, ...elementProps } = componentProps;
	const { orientation } = useToolbarRootContext();
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CompositeItem, {
		tag: "a",
		render,
		className,
		metadata: TOOLBAR_LINK_METADATA,
		state: { orientation },
		refs: [forwardedRef],
		props: [elementProps]
	});
});
ToolbarLink.displayName = "ToolbarLink";
//#endregion
//#region node_modules/@base-ui/react/esm/toolbar/input/ToolbarInput.js
/**
* A native input element that integrates with Toolbar keyboard navigation.
* Renders an `<input>` element.
*
* Documentation: [Base UI Toolbar](https://base-ui.com/react/components/toolbar)
*/
var ToolbarInput = /* @__PURE__ */ import_react.forwardRef(function ToolbarInput(componentProps, forwardedRef) {
	const { className, focusableWhenDisabled = true, render, disabled: disabledProp = false, ...elementProps } = componentProps;
	const itemMetadata = import_react.useMemo(() => ({ focusableWhenDisabled }), [focusableWhenDisabled]);
	const { disabled: toolbarDisabled, orientation } = useToolbarRootContext();
	const groupContext = useToolbarGroupContext(true);
	const disabled = toolbarDisabled || (groupContext?.disabled ?? false) || disabledProp;
	const { props: focusableWhenDisabledProps } = useFocusableWhenDisabled({
		composite: true,
		disabled,
		focusableWhenDisabled,
		isNativeButton: false
	});
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(CompositeItem, {
		tag: "input",
		render,
		className,
		metadata: itemMetadata,
		state: {
			disabled,
			orientation,
			focusable: focusableWhenDisabled
		},
		refs: [forwardedRef],
		props: [
			{
				onClick(event) {
					if (disabled) event.preventDefault();
				},
				onKeyDown(event) {
					if (event.key !== "ArrowLeft" && event.key !== "ArrowRight" && disabled) stopEvent(event);
				},
				onPointerDown(event) {
					if (disabled) event.preventDefault();
				}
			},
			elementProps,
			focusableWhenDisabledProps
		]
	});
});
ToolbarInput.displayName = "ToolbarInput";
//#endregion
//#region node_modules/@base-ui/react/esm/toolbar/index.parts.js
var index_parts_exports$26 = /* @__PURE__ */ __exportAll({
	Button: () => ToolbarButton,
	Group: () => ToolbarGroup,
	Input: () => ToolbarInput,
	Link: () => ToolbarLink,
	Root: () => ToolbarRoot,
	Separator: () => ToolbarSeparator
});
//#endregion
export { index_parts_exports as Accordion, index_parts_exports$1 as AlertDialog, index_parts_exports$2 as Autocomplete, index_parts_exports$3 as Avatar, Button, CSPProvider, index_parts_exports$4 as Checkbox, CheckboxGroup, index_parts_exports$5 as Collapsible, index_parts_exports$6 as Combobox, index_parts_exports$7 as ContextMenu, index_parts_exports$8 as Dialog, DirectionProvider, index_parts_exports$9 as Drawer, index_parts_exports$10 as Field, index_parts_exports$11 as Fieldset, Form, Input, index_parts_exports$12 as Menu, Menubar, index_parts_exports$13 as Meter, index_parts_exports$14 as NavigationMenu, index_parts_exports$15 as NumberField, index_parts_exports$16 as Popover, index_parts_exports$17 as PreviewCard, index_parts_exports$18 as Progress, index_parts_exports$19 as Radio, RadioGroup, index_parts_exports$20 as ScrollArea, index_parts_exports$21 as Select, Separator, index_parts_exports$22 as Slider, index_parts_exports$23 as Switch, index_parts_exports$24 as Tabs, index_parts_exports$25 as Toast, Toggle, ToggleGroup, index_parts_exports$26 as Toolbar, index_parts_exports$27 as Tooltip, makeEventPreventable, mergeClassNames, mergeProps, mergePropsN, useDirection, useRender };

//# sourceMappingURL=@base-ui_react.js.map