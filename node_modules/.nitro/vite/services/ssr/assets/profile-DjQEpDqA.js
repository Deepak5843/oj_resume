import { createFileRoute, lazyRouteComponent } from "@tanstack/react-router";
import z$1 from "zod";
//#region src/routes/dashboard/settings/profile.tsx
var $$splitComponentImporter = () => import("./profile-BgkCbvam.js");
var Route = createFileRoute("/dashboard/settings/profile")({ component: lazyRouteComponent($$splitComponentImporter, "component") });
z$1.object({
	name: z$1.string().trim().min(1).max(64),
	username: z$1.string().trim().min(1).max(64).regex(/^[a-z0-9._-]+$/, { message: "Username can only contain lowercase letters, numbers, dots, hyphens and underscores." }),
	email: z$1.email().trim()
});
//#endregion
export { Route as t };
