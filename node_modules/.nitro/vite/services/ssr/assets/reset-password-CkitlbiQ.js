import "./style-Dwb9U8QV.js";
import { t as Button } from "./button-C8loFr-r.js";
import "./error-codes-HOUJ9XOX.js";
import { t as Input } from "./input-BL08BOA0.js";
import "./label-B2IowpIJ.js";
import { a as FormItem, i as FormField, n as FormControl, o as FormLabel, s as FormMessage, t as Form } from "./form-BEU_kK8D.js";
import { t as authClient } from "./client-xNbkeDAk.js";
import { t as Route } from "./reset-password-DETk9CKZ.js";
import { useNavigate } from "@tanstack/react-router";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { Trans } from "@lingui/react";
import { EyeIcon, EyeSlashIcon } from "@phosphor-icons/react";
import z$1 from "zod";
import { i18n } from "@lingui/core";
import { toast } from "sonner";
import { zodResolver } from "@hookform/resolvers/zod";
import { useForm } from "react-hook-form";
import { useToggle } from "usehooks-ts";
//#region src/routes/auth/reset-password.tsx?tsr-split=component
var formSchema = z$1.object({ password: z$1.string().min(6).max(64) });
function RouteComponent() {
	const navigate = useNavigate();
	const { token } = Route.useSearch();
	const [showPassword, toggleShowPassword] = useToggle(false);
	const form = useForm({
		resolver: zodResolver(formSchema),
		defaultValues: { password: "" }
	});
	const onSubmit = async (data) => {
		const toastId = toast.loading(i18n._({ id: "qzR3YP" }));
		const { error } = await authClient.resetPassword({
			token,
			newPassword: data.password
		});
		if (error) {
			toast.error(error.message, { id: toastId });
			return;
		}
		toast.success(i18n._({ id: "X2cmDC" }), { id: toastId });
		navigate({ to: "/auth/login" });
	};
	return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsxs("div", {
		className: "space-y-1 text-center",
		children: [/* @__PURE__ */ jsx("h1", {
			className: "font-bold text-2xl tracking-tight",
			children: /* @__PURE__ */ jsx(Trans, { id: "slOprG" })
		}), /* @__PURE__ */ jsx("div", {
			className: "text-muted-foreground",
			children: /* @__PURE__ */ jsx(Trans, { id: "15fhbV" })
		})]
	}), /* @__PURE__ */ jsx(Form, {
		...form,
		children: /* @__PURE__ */ jsxs("form", {
			className: "space-y-6",
			onSubmit: form.handleSubmit(onSubmit),
			children: [/* @__PURE__ */ jsx(FormField, {
				control: form.control,
				name: "password",
				render: ({ field }) => /* @__PURE__ */ jsxs(FormItem, { children: [
					/* @__PURE__ */ jsx(FormLabel, { children: /* @__PURE__ */ jsx(Trans, { id: "7vhWI8" }) }),
					/* @__PURE__ */ jsxs("div", {
						className: "flex items-center gap-x-1.5",
						children: [/* @__PURE__ */ jsx(FormControl, { render: /* @__PURE__ */ jsx(Input, {
							min: 6,
							max: 64,
							type: showPassword ? "text" : "password",
							autoComplete: "new-password",
							...field
						}) }), /* @__PURE__ */ jsx(Button, {
							size: "icon",
							variant: "ghost",
							onClick: toggleShowPassword,
							children: showPassword ? /* @__PURE__ */ jsx(EyeIcon, {}) : /* @__PURE__ */ jsx(EyeSlashIcon, {})
						})]
					}),
					/* @__PURE__ */ jsx(FormMessage, {})
				] })
			}), /* @__PURE__ */ jsx(Button, {
				type: "submit",
				className: "w-full",
				children: /* @__PURE__ */ jsx(Trans, { id: "KbS2K9" })
			})]
		})
	})] });
}
//#endregion
export { RouteComponent as component };
