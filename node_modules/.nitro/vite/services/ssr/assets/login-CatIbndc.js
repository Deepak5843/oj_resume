import { createFileRoute, lazyRouteComponent, redirect } from "@tanstack/react-router";
import z$1 from "zod";
//#region src/routes/auth/login.tsx
var $$splitComponentImporter = () => import("./login-5L3ydwJZ.js");
var Route = createFileRoute("/auth/login")({
	component: lazyRouteComponent($$splitComponentImporter, "component"),
	beforeLoad: async ({ context }) => {
		if (context.session) throw redirect({
			to: "/dashboard",
			replace: true
		});
		return { session: null };
	}
});
z$1.object({
	identifier: z$1.string().trim().toLowerCase(),
	password: z$1.string().trim().min(6).max(64)
});
//#endregion
export { Route as t };
