import { r as orpc } from "./client-BWxMxJtI.js";
import { t as cn } from "./style-Dwb9U8QV.js";
import { t as Button } from "./button-C8loFr-r.js";
import { t as authClient } from "./client-xNbkeDAk.js";
import { useRouter } from "@tanstack/react-router";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { Trans } from "@lingui/react";
import { GithubLogoIcon, GoogleLogoIcon, VaultIcon } from "@phosphor-icons/react";
import { i18n } from "@lingui/core";
import { useQuery } from "@tanstack/react-query";
import { toast } from "sonner";
//#region src/routes/auth/-components/social-auth.tsx
function SocialAuth() {
	const router = useRouter();
	const { data: authProviders = {} } = useQuery(orpc.auth.providers.list.queryOptions());
	const handleSocialLogin = async (provider) => {
		const toastId = toast.loading(i18n._({ id: "XOxZT4" }));
		const { error } = await authClient.signIn.social({
			provider,
			callbackURL: "/dashboard"
		});
		if (error) {
			toast.error(error.message, { id: toastId });
			return;
		}
		toast.dismiss(toastId);
		router.invalidate();
	};
	const handleOAuthLogin = async () => {
		const toastId = toast.loading(i18n._({ id: "XOxZT4" }));
		const { error } = await authClient.signIn.oauth2({
			providerId: "custom",
			callbackURL: "/dashboard"
		});
		if (error) {
			toast.error(error.message, { id: toastId });
			return;
		}
		toast.dismiss(toastId);
		router.invalidate();
	};
	return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsxs("div", {
		className: "flex items-center gap-x-2",
		children: [
			/* @__PURE__ */ jsx("hr", { className: "flex-1" }),
			/* @__PURE__ */ jsx("span", {
				className: "font-medium text-xs tracking-wide",
				children: /* @__PURE__ */ jsx(Trans, { id: "3BEoB6" })
			}),
			/* @__PURE__ */ jsx("hr", { className: "flex-1" })
		]
	}), /* @__PURE__ */ jsx("div", { children: /* @__PURE__ */ jsxs("div", {
		className: "grid grid-cols-2 gap-4",
		children: [
			/* @__PURE__ */ jsxs(Button, {
				variant: "secondary",
				onClick: handleOAuthLogin,
				className: cn("hidden", "custom" in authProviders && "inline-flex"),
				children: [/* @__PURE__ */ jsx(VaultIcon, {}), authProviders.custom]
			}),
			/* @__PURE__ */ jsxs(Button, {
				onClick: () => handleSocialLogin("google"),
				className: cn("hidden flex-1 bg-[#4285F4] text-white hover:bg-[#4285F4]/80", "google" in authProviders && "inline-flex"),
				children: [/* @__PURE__ */ jsx(GoogleLogoIcon, {}), "Google"]
			}),
			/* @__PURE__ */ jsxs(Button, {
				onClick: () => handleSocialLogin("github"),
				className: cn("hidden flex-1 bg-[#2b3137] text-white hover:bg-[#2b3137]/80", "github" in authProviders && "inline-flex"),
				children: [/* @__PURE__ */ jsx(GithubLogoIcon, {}), "GitHub"]
			})
		]
	}) })] });
}
//#endregion
export { SocialAuth as t };
